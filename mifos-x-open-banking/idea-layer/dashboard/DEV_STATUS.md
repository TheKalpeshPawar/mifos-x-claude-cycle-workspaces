# Development Status — mifos-x-open-banking

> Generated by `/idea-sync` STEP 4d (`_shared/idea-sync-artifacts.md:59`)
> Sources: `FEATURES.md` · `REQUIREMENTS.md` · `idea-plan.yaml` §requirements, §release_phases
> Generated: 2026-07-30

**20 of 23 screens ship. P0–P2 implemented, P3 planned.**

---

## Phase status

| Phase | Label | Features | Status |
|---|---|---|:---:|
| **P0** | Consent + core account information | consent-onboarding · consent-dashboard · accounts · balances · transactions · settings · licences | ✅ implemented |
| **P1** | Payments-context read surfaces | beneficiaries · standing-orders · direct-debits · scheduled-payments | ✅ implemented |
| **P2** | Statements, product & account holder | statements · product · account-holder | ✅ implemented |
| **P3** | PISP consumer domestic payment initiation | send-money · payment-consent · payment-status | 🕐 planned |

---

## Requirements coverage

| FR | Description | Status |
|---|---|:---:|
| FR-001 | DCR once per brand using the signed SSA | ✅ |
| FR-002 | FAPI consent journey — client_credentials → consent → authorize → token | ✅ |
| FR-003 | List authorised accounts + account detail | ✅ |
| FR-004 | Balances per account | ✅ |
| FR-005 | Transactions with filtering + detail | ✅ |
| FR-006 | Beneficiaries, standing orders, direct debits, scheduled payments (read-only) | ✅ |
| FR-007 | Statements, product, account-holder | ✅ |
| FR-008 | Consent dashboard — permissions, expiry, revoke, 90-day reconfirm | ✅ |
| FR-011 | Client-side PFM (categorisation, spending, budgets, subscriptions) | ⚠️ **partial** |
| FR-012 | Stage + submit a domestic payment | 🕐 spec only |
| FR-013 | PISP authorisation return leg | 🕐 spec only |
| FR-014 | Payment settlement tracking | 🕐 spec only |
| FR-015 | Consumer `Risk` context, no merchant fields | 🕐 spec only |
| FR-016 | Detached JWS on every PISP write | 🕐 spec only |
| FR-017 | Idempotency key persisted across retries | 🕐 spec only |

**FR-011 is partial and it matters.** The primitives ship — `SpendingCalculator`,
`SpendingSnapshot`, the 7-value `TransactionCategory` — but the dashboard, spending-by-category,
budgets and recurring-subscription screens were never built and were removed from scope on
2026-07-28. Only the per-transaction category tag remains. `SpendingCalculator` retains its tests
and has **no production caller**.

> FR-009 and FR-010 are absent from `§requirements`. FR-010 was the ATM/branch locator, dropped in
> the 2026-07-28 reverse sync. The gap in numbering is expected, not a missing record.

---

## Screen inventory — 23

| Status | Count | Screens |
|---|:---:|---|
| **Implemented** | 20 | account-detail · account-holder · accounts · beneficiaries · consent-callback · consent-detail · consent-list · direct-debits · home · licences · login · product · scheduled-payments · settings · standing-orders · statement-detail · statements · transaction-detail · transactions · user-onboarding |
| **Specification only** | 3 | send-money · payment-consent · payment-status |

Two screens have no module of their own: `licences` lives in `feature/settings`,
`user-onboarding` in `feature/login`.

---

## Artifact coverage

| Artifact | Coverage |
|---|---|
| Screen specs | 23/23 |
| Exports (SPEC.md + API.md + `.meta.yaml`) | 23/23 |
| Mockups (MOCKUP.md + PROMPTS_FIGMA.md) | 23/23 |
| Per-state prompts | 95/95 states |
| Doc set | 10/10 |
| i18n | 545 declared · 517 referenced · 0 missing |
| Rendered previews | **0 — deferred by decision** |
| Stitch visuals | **0 — deferred by decision** |

---

## Blockers

### P3 — four cross-cutting prerequisites, none per-feature

| # | Prerequisite | State |
|---|---|---|
| 1 | `core/network/api/Pisp.kt` | absent — only `Aisp.kt`, `OAuth.kt` ship |
| 2 | Detached JWS (PS256, `b64:false` + `crit`) | absent — **every write returns `400 U019` without it** |
| 3 | `x-idempotency-key` persisted across retries | absent |
| 4 | Payments-scope token caller | `ConsentCreationScope.PAYMENTS` exists at `OAuth.kt:103`; no caller |

Nothing in P3 can be device-verified until #2 lands. The 130 PISP DTO files already ship, so the
remaining work is the API surface, two headers and one caller — not a DTO layer.

### Open defects

| Defect | Severity |
|---|---|
| `scheduled-payments` — `unsupported` declared in idea-layer but **not in source**; a `U000` still routes to the retryable error path, so a credit-card visitor sees a Retry that cannot succeed | error |
| `U000` modelled in no `api.yaml` for any of the three gated features — the state exists, the condition producing it does not | error |
| PISP regulatory ADR unwritten (`adr_required: true` on 3 features) | warn |
| `flow_ref` on 9 of 23 `docs.yaml` — per-flow views cover 3 of 9 flows | warn |
| `account-detail` — 14 test scenarios with empty descriptions | warn |
| 2 components lack `accessibility_label` (`settings/app_version_row`, `transactions/tx_category_tag`) | warn |

---

## Sync gate

`/idea-sync` STEP 4.VERIFY — **7 of 8** artifact groups present.

Outstanding: `prototype/index.html`. Its validation requires per-screen `screens/{feature}/{feature}.html`
files (`idea-sync-artifacts.md:182`), which are rendered-HTML output and **deferred by decision**.
Generating the shell alone would produce 23 dead iframes, so it is deliberately left absent.
