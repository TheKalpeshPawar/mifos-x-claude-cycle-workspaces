schema_version: "1.0"
last_run: "2026-07-28T00:00:00Z"
last_run_by: "claude"
command: "/gap-analysis-project (reverse sync from source)"
project: "mifos-x/mifos-x-open-banking"
features_evaluated: 20
overall: "pass"

# ─── RESET 2026-07-28 ───────────────────────────────────────────────────
# The prior file carried 44 findings (5 open) against a 25-feature idea-layer
# that no longer exists. Every open finding was either resolved by the reverse
# sync or removed with its feature:
#
#   pfm-dashboard / sub-action-leaf-missing   -> feature deleted (never built)
#   4 x stitch_coverage (statements, login,   -> stitch artifacts deleted;
#     accounts prompt_sha, PROJECTS_INDEX)       generation deferred
#
# Findings are recomputed by /idea-sync and /idea-verify against the current
# 20-feature roster. Nothing is carried forward.

summary:
  total_findings: 0
  open_findings: 0
  closed_findings: 0
  by_gap_class:
    fail: 0
    warn: 0
    advisory: 0
  dto_findings: 0
  stitch_findings: 0
  capability_completeness_findings: 0

findings: []

deferred_by_design:
  # Not findings — deliberately absent artifacts, recorded so a future run does
  # not raise them as regressions.
  - class: stitch_coverage
    reason: "Stitch mockup generation deferred; may not be run at all (open decision)."
    features: all
  - class: preview_render
    reason: "HTML preview generation deferred."
    features: all
  - class: export_artifacts
    reason: "SPEC.md / API.md removed with the pre-sync idea-layer; regenerate via /idea-feature-export --all."
    features: all
