schema_version: "1.0"
last_run: "2026-08-02T18:05:00Z"
last_run_by: "claude"
command: "/idea-verify — 12 checks; 9 parse-blocking errors found and healed, 2 warns carried"

# ═══════════════════════════════════════════════════════════════════════════
# 2026-08-02T18:05 /idea-verify — collapsed-key corruption found and healed.
# ═══════════════════════════════════════════════════════════════════════════
run_2026_08_02_verify_collapsed_key:
  status: success
  errors_found: 9
  errors_healed: 9
  errors_open: 0
  warnings: 2
  cache_mode: cold

  finding_E1:
    id: YAML_PARSE_FAILURE
    severity: error
    healed: true
    count: 9
    features: [account-detail, account-holder, consent-callback, consent-detail,
               consent-list, payment-consent, product, send-money]
    summary: >-
      8 of 23 ui.yaml could not be parsed at all. In each, a newline between two mapping
      keys had been lost on the first component under `components:` — always a
      progress_circular loading indicator. SIBLING_LOADER would have raised CORRUPT_STATE
      for all eight features, so every downstream consumer would have skipped them.
    evidence: >-
      Both corrupted forms were reproduced as isolated snippets and parsed, rather than
      asserted from the YAML spec. Unquoted form -> ScannerError "mapping values are not
      allowed here". Quoted form -> "expected <block end>, but found '<scalar>'".
    fix: newline restored at each of the 9 sites; no other bytes touched.
    verified: 225/225 idea-layer YAML files parse post-heal.

  warnings_carried:
    W1_i18n_orphans:
      severity: advisory
      count: 4
      keys: [error.send_money.consent_mismatch, error.send_money.invalid_field,
             error.send_money.rate_limited, send_money.review_reference_empty]
      note: >-
        Declared in _strings/strings.yaml, referenced by no ui.yaml binding. Plausibly
        consumed by ViewModel error mapping rather than a spec binding, so NOT swept —
        deleting a key a ViewModel resolves at runtime would break the surface it labels.
        Needs a source check before disposition.
    W2_consent_callback_unreferenced:
      severity: advisory
      note: >-
        consent-callback is the only screen that is never an in-app nav target. Correct by
        design — it is the OAuth redirect landing screen, reached by deep link. Recorded so
        a future orphan-route sweep does not delete it.

  checks_executed:
    yaml_parse: "225 files, 9 failures found -> healed -> 225/225 pass"
    i18n: "560 references, 0 unresolved; 4 declared-but-unreferenced (W1)"
    nav_targets: "0 dead — bare `atm-locator` and `party` occur only inside historical comments"
    action_contract: "23/23 features, every on_click/on_change/on_dismiss/on_select carries a contract"
    state_binding_coverage: "23/23 — every declared state has >=1 bound component"
    flow_refs: "23/23 resolve against flows/"
    journey_screen_ids: "0 dangling — the 2 apparent hits are `removed_in_sync:` records, not sequence entries"
    sibling_completeness: "23/23 features x 7 siblings = 161 files, none missing"
    schema_version: "ui/api/flow/docs/tests all 4.0; demo-data/data-flow all 2.1.0 (consistent)"
    demo_data: "23/23 substantive"
    tabs_and_duplicate_keys: "0 tab-indented files, 0 duplicate top-level keys"
    orphan_screens: "1 advisory (W2)"

  correction_to_prior_record: >-
    The 16:40 block below records `yaml_parse: 319 files, 0 failures`. That corpus already
    contained all 9 corruptions, so the line is not usable as evidence. Left in place rather
    than edited — the false green is itself the useful signal.

# ═══════════════════════════════════════════════════════════════════════════
# 2026-08-02T16:40 /idea-verify (latest) — 16 checks, 0 errors, 2 warns.
# ═══════════════════════════════════════════════════════════════════════════
run_2026_08_02_verify_post_sync:
  status: success
  errors: 0
  warnings: 2
  cache_mode: cold
  why_cold: >-
    The 16:10 gate sweep belongs to capabilities.idea-sync, not to idea-verify, and the
    last_verify cache has never been seeded for this project. Nothing to short-circuit on, so
    all 16 checks re-ran against the live corpus instead of being copied from the sync record.

  checks_executed:
    yaml_parse: "319 files, 0 failures"
    schema_closure: "G-IDEA-SCHEMA-CLOSURE — all 9 sub-checks PASS"
    action_contract: "23 ui.yaml, 431 blocks, 0 violations, 0 unevaluated"
    i18n: "535 references, 0 unresolved"
    demo_data: "23/23, 0 DD1 violations"
    state_coherence: "G-ILC fail=0 warn=0"
    aco: "0 orphans"
    data_flow_coverage: "G-DATAFLOW-COVERAGE PASS across 23 features"
    sibling_headers: "23/23 x 7 sibling kinds, all 3 header keys"
    states_present: "23/23"
    nav_targets: "42 edges, 0 dead"
    flow_refs: "8 distinct, 0 dead"
    journey_screen_ids: "23 distinct across 9 journeys, 0 dangling"
    scenario_count: "23/23 declared == actual"
    test_states: "225 references, 0 undeclared"
    untested_declared_states: "0"

  st1_confirmed_closed: >-
    Verified by re-derivation, not by reading the sync's record: 0 declared states lack a
    scenario, all 225 test-state references resolve, scenario_count matches actual on 23/23,
    and there are no duplicate scenario ids. The 7 scenarios the sync added are intact.

  orphan_sweep_cleared:
    screen: consent-callback
    verdict: not-an-orphan
    detail: >-
      Flagged by the mechanical sweep (neither a nav target nor a flow entry_screen) and cleared
      on reading — flow.yaml:8-9 declares entry_points[0].source hsbc_oauth_redirect, docs.yaml:20
      records it as reachable ONLY via the external redirect return. Same disposition as 15:00.

  open_warns:
    - id: SH-1
      severity: warn
      status: planned-step0-complete-awaiting-decision
      detail: >-
        Sibling bodies still use the screen_id/endpoints/scenarios/on_mount dialect. A 3-tier
        migration toward mifos-x-group-banking (a working 30-feature v4.0 implementation) is
        APPROVED and its Step 0 consumer audit is done. Execution is gated on one decision.
      step0_result: >-
        Exactly ONE consumer works today and stops working after Tier 3:
        gap-detection/m1-static-yaml-scan.ts matches top-level `on_mount:` — 23/23 on this
        project, 0/30 on the v4 exemplar — so its async-coverage-missing detection goes silent.
        Framework file, not fixable here. Everything else is already inert on this project
        (framework-verify-api-contract-orphan.sh, verify-roundtrip.sh), unaffected
        (flow-completeness-scan.ts, framework-verify-data-flow-coverage.sh), needs a re-check
        before Tier 2 (maestro-flow-gen.ts), or is a regenerable derived artifact
        (_cache/data-flow-views 44 files, exports 47 files, TRAINING_MASTER, LAYER_STATUS).
      correction_logged: >-
        During the audit I claimed framework-verify-data-flow-coverage.sh was blind on this
        project's dialect. Measurement disproved it — 101 `trigger:` lines here vs 88 on the
        exemplar; the gate reads both shapes. Claim withdrawn.
      open_decision: "all 3 tiers accepting m1 goes silent · stop after Tier 2 · or proceed and log m1 upstream"
    - id: FW-1
      severity: error
      scope: framework
      detail: >-
        Reproduced side by side again this run: action-contract-walk.ts reports 431 component
        blocks, framework-verify-action-contract.sh reports 15, over the same 23 files. Excluded
        from this project's warn count — it is a framework defect and no framework file is edited.

  last_verify_cache:
    written: false
    why: "STEP V.6 gates on overall PASS; SH-1 and FW-1 remain open warns and no rule_versions baseline exists."

# ═══════════════════════════════════════════════════════════════════════════
# 2026-08-02T16:10 /idea-sync "fix the warnings" (latest) — ST-1 CLOSED.
# ═══════════════════════════════════════════════════════════════════════════
run_2026_08_02_fix_warnings:
  status: success
  errors: 0
  warns_closed: 1
  warns_remaining: 2
  detail_ref: state/SYNC_STEPS.yaml#run_2026_08_02_fix_warnings

  ST-1: CLOSED
  st1_detail: >-
    7 declared states had no tests.yaml scenario and could never be exercised. All 7 now covered
    across 5 features — direct-debits/scheduled-payments/standing-orders `unsupported`,
    payment-status `loading`, payment-consent `validating`+`exchanging`+`checking`. Project
    scenario total 218 -> 225, exactly +7. Verified mechanically: 0 untested declared states,
    23/23 scenario_count == actual, 0 duplicate scenario ids, 225 test-state refs 0 undeclared.

  not_fixed_and_why:
    - id: SH-1
      severity: warn
      status: open-by-decision
      why: >-
        The header half is done (161/161). Closing the rest means restructuring every sibling
        body to the v4.0 schemas — endpoints->api, scenarios->testing, on_mount->entries — which
        the user was shown the cost of and explicitly declined one turn earlier. "Fix the
        warnings" was not treated as authority to reverse that; it needs a fresh call.
    - id: FW-1
      severity: error
      scope: framework
      status: open-not-fixable-here
      why: >-
        A defect in the shipped framework-verify-action-contract.sh (15 component blocks reported
        vs the .ts walker's 431 over the same 23 files). No project-side change can correct it and
        the standing directive is to leave framework files alone. Mitigation unchanged: invoke
        action-contract-walk.ts for this gate.

  interruption_recovery: >-
    A usage limit killed 3 of the 5 enrich writers mid-flight. On resume the instruction was
    "restart all tasks", but 3 features had already finished and re-running them would have
    appended duplicate scenarios. Ground truth was re-derived from the files before acting:
    direct-debits + scheduled-payments + standing-orders complete, payment-status half-applied
    (TC-PSTAT-009 in tests.yaml but docs.yaml still at 8), payment-consent untouched. Only the
    drift was repaired and only payment-consent re-dispatched.

  gates_after:
    yaml_parse: "319 files, 0 failures"
    schema_closure: "G-IDEA-SCHEMA-CLOSURE all 9 sub-checks PASS"
    action_contract: "23 ui.yaml, 431 blocks, 0 violations, 0 unevaluated"
    i18n: "535 references, 0 unresolved — no invented {strings.*} token, which is the failure mode adding scenarios invites"
    demo_data: "23/23, 0 DD1 violations"
    state_coherence: "G-ILC fail=0 warn=0"
    aco: "0 orphans"
    scenario_count_integrity: "23/23 declared == actual"
    test_state_refs: "225 references, 0 undeclared"
    untested_declared_states: "0"

# ═══════════════════════════════════════════════════════════════════════════
# 2026-08-02T15:00 /idea-verify (latest) — cold re-run, 16 checks, 0 errors.
# ═══════════════════════════════════════════════════════════════════════════
run_2026_08_02_verify_post_drain:
  status: success
  errors: 0
  warnings: 3
  cache_mode: cold
  why_cold: >-
    The 14:20 run deliberately did not advance last_verify, so there was no rule_versions
    baseline to short-circuit against. Every check below was RE-EXECUTED against the live
    corpus — nothing copied from the drain's own closing sweep.

  checks_executed:
    yaml_parse: "319 files, 0 failures"
    schema_closure: "G-IDEA-SCHEMA-CLOSURE — WRITABLE, DFR, PC, WR, PAG, CAPS, KM, CT, CSS all PASS (exit 0)"
    action_contract: "23 ui.yaml, 431 component blocks, 0 violations, 0 unevaluated"
    i18n: "535 references, 0 unresolved"
    demo_data: "23/23 data-layer features carry demo-data.yaml, 0 DD1 violations"
    state_coherence: "G-ILC SC-1..SC-5 fail=0 warn=0"
    aco: "0 orphans"
    sibling_headers: "23/23 x 7 siblings carry all 3 header keys; 161/161 feature_id == directory slug"
    states_present: "23/23 declare a non-empty states[]"
    nav_targets: "42 edges, 0 dead — 20 distinct real targets + 1 documented placeholder"
    flow_refs: "8 distinct, 8/8 resolve to idea-layer/flows/"
    journey_screen_ids: "23 distinct screen_id values across 9 journeys, 0 dangling"
    cross_sibling_scenario_count: "23/23 docs.yaml scenario_count == len(tests.yaml scenarios)"
    cross_sibling_test_states: "218 test state references, 0 target an undeclared state"
    orphan_screens: "0 — see consent_callback_cleared below"
    declared_states_untested: "7 (ST-1, advisory)"

  corrections_to_prior_records:
    - field: nav_targets
      was: "39 edges"
      now: "42 edges"
      why: >-
        The 14:20 record's 39 was a hand tally of a `uniq -c` table rather than a machine
        count. `rg -cN` over `^\s+target: ` returns 42, and the 2026-07-31 record independently
        recorded 42. The 0-dead verdict never changed — only the denominator was wrong.
    - field: ST-1 count
      was: 6
      now: 7
      why: >-
        The instance list was correct (3 `unsupported` + 3 payment-consent + 1 payment-status)
        but the total was mis-added. Re-derived mechanically this run.

  consent_callback_cleared:
    verdict: not-an-orphan
    detail: >-
      A mechanical orphan sweep (screen is neither a nav target nor a flow entry_screen) flags
      consent-callback. Reading the feature clears it: flow.yaml:8-9 declares
      `entry_points[0].source: hsbc_oauth_redirect`, and docs.yaml:20 states the screen is
      "reachable ONLY via the flow (an external redirect return), never by a nav" with
      system_capabilities[deep_link] declared. Correct by design, not a gap.

  open_warns: [SH-1-SIBLING-BODY-DIALECT, ST-1-DECLARED-STATES-UNTESTED, FW-1-ACTION-CONTRACT-SH-UNDERCOUNTS]

  fw1_still_reproducible: >-
    Both entry points were run side by side this pass. action-contract-walk.ts reports 431
    component blocks; framework-verify-action-contract.sh reports 15 over the same 23 files.
    Framework-scope, no framework file edited, mitigation is to invoke the .ts walker.

  last_verify_cache:
    written: false
    why: >-
      STEP V.6 gates the cache on overall PASS. Error-severity is zero, but SH-1 and ST-1
      remain open warns and no per-feature rule_versions baseline has ever been seeded for
      this project, so there is nothing to advance without fabricating it.

# ═══════════════════════════════════════════════════════════════════════════
# 2026-08-02 DRAIN (latest) — /idea-migrate-schema, SH-1, FW-2, /idea-heal,
# re-verify. All 3 error classes from the 13:05 verify are closed.
# ═══════════════════════════════════════════════════════════════════════════
run_2026_08_02_drain:
  status: success
  errors_before: 28
  errors_after: 0
  sequence: "/idea-migrate-schema -> SH-1 hand-fix -> FW-2 project-side fix -> /idea-heal (19 clusters) -> /idea-verify"

  step_1_migrate_schema:
    outcome: already-current
    engine_invoked: false
    detail: >-
      No-op on both axes, as predicted before running it. schema-contract version-gated to
      ALREADY_CURRENT (23/23 docs.yaml declare contract_version "2.0.0" == framework current);
      schema-split found zero monoliths. Recorded in state/MIGRATION_LOG.yaml, including WHY
      this command could not have closed SH-1: the header triple is not in the schema-contract
      engine's authorised write set (engine_writes = [contract_version, status, approve_hash,
      last_verify]), and schema-split only emits it when splitting a monolith.

  step_2_SH1:
    verdict: DOWNGRADED-to-warn
    files_changed: 161
    detail: >-
      Prepended `sibling:` / `schema_version:` / `feature_id:` to all 23 features x 7 siblings.
      ui/api/flow/docs/tests carry schema_version "4.0"; data-flow/demo-data carry "2.1.0",
      matching the shipped shape in mifos-x-group-banking (210/210 files). Every feature_id
      equals its screens/{slug}/ directory name — verified across all 161, zero mismatches.
      Purely additive: no existing key renamed, moved or deleted; `screen_id:` still sits
      alongside the new `feature_id:`.
    why_not_closed: >-
      The header is now present but the files STILL do not validate against
      core/schemas/contract/siblings/*.json, and saying otherwise would be false bookkeeping.
      Measured body divergence, unchanged by this run: docs.id 0/23, docs.name 0/23,
      docs.review_package 0/23, flow.navigates_to 0/23, tests.testing 0/23,
      data-flow.entries 0/23, demo-data.entries 0/23, ui.screens 7/23. This project uses a
      screen_id / endpoints / scenarios / on_mount dialect that is internally consistent
      across all 23 features and that every shipped gate passes against. Full conformance
      would restructure all 161 files and break every consumer reading the dialect; the user
      chose the additive header instead, with this note kept on the books.
    known_pattern_conflict: >-
      schema.contract.json#/$defs/FeatureId is ^[a-z][a-zA-Z0-9]*$, which rejects hyphens.
      Every feature_id here is kebab (account-detail, send-money, ...) because that is the
      directory name SIBLING_LOADER resolves and what the reference project ships
      (feature_id: admin-dashboard violates the same pattern). Matched the shipped convention
      over the pattern deliberately rather than inventing a camelCase id no path resolves to.
    residual_severity: warn

  step_3_FW2:
    verdict: CLOSED-project-side
    detail: >-
      Renamed `path:` -> `source_ref:` on the 5 existing_source_surface entries in
      screens/payment-consent/api.yaml. They are Kotlin file references, never endpoints, but
      ACO-1 greps `- path:` at any indent regardless of parent block. Verified safe first:
      existing_source_surface has zero consumers anywhere outside the three api.yaml files
      that declare it. After: `orphans: 0`.
    framework_gate_untouched: true

  step_4_heal:
    mode: fanout
    clusters: 19
    rounds: 1
    halt_reason: success
    findings_resolved: 23        # 19 WR-1 feature-clusters (24 instances) + 4 KM-1
    escalated_gaps: 0
    detail: >-
      One sonnet-router writer per feature, disjoint write-sets, each applying
      /idea-feature-enrich semantics scoped to its own feature. Every writer derived its
      values from that feature's own api.yaml#endpoints[] and data-flow.yaml rather than
      inventing them, and reported the sibling each value came from. Converged in ONE round —
      no cluster hit the per_gap_cap, nothing escalated.
    km1_landings:
      home: "aggregate -> composite (3 parallel AIS calls + 2 derivations)"
      send-money: "form -> composite (validate + stage consent + submit, two PISP writes)"
      payment-consent: "auth -> composite (OAuth redirect + token exchange + status poll)"
      payment-status: >-
        detail -> crud. The writer chose crud over the composite predicted in the seeded
        fix_hint, reasoning that the poll loop repeats ONE GET rather than combining distinct
        operation classes. Its reading of the feature body is taken over the hint's guess.

  step_5_reverify:
    gates_all_green:
      yaml_parse: "319 files, 0 failures"
      schema_closure: "G-IDEA-SCHEMA-CLOSURE all 9 sub-checks PASS (WR + KM now green)"
      action_contract: "23 ui.yaml, 431 blocks, 0 violations, 0 unevaluated (action-contract-walk.ts)"
      i18n: "535 references, 0 unresolved"
      demo_data: "23/23"
      state_coherence: "G-ILC fail=0 warn=0"
      aco: "0 orphans"
      sibling_headers: "23/23 on all 7 sibling kinds, all 3 keys"
      nav_targets: >-
        42 edges, 0 dead — 20 distinct real targets + the documented _placeholder:atm-locator.
        CORRECTED 2026-08-02T15:00 by the following /idea-verify: this line first read "39
        edges", which was a hand tally of a uniq -c table, not a machine count. `rg -cN` over
        `^\s+target: ` gives 42, and the 2026-07-31 record independently says 42. 39 was the
        outlier; the 0-dead verdict was unaffected.
      flow_refs: "8/8 resolve"
      journey_screen_ids: >-
        0 dangling. `atm-locator` and `profile` appear ONLY as the first entry under a
        removed_in_sync: block (developer-full-surface.yaml:78, settings-and-profile.yaml:56) —
        re-read to confirm, not inferred from a grep hit.
      cross_sibling_scenario_count: "23/23 match"
      cross_sibling_test_states: "0 tests target an undeclared state"
    method: >-
      Every gate above was RE-EXECUTED at the orchestrator level after the fan-out. The
      cluster writers' own pass reports were not taken as the verdict.

  still_open:
    - id: SH-1
      severity: warn
      what: "sibling bodies diverge from the v4.0 schemas (header present, shape not conformant)"
    - id: ST-1
      severity: warn
      count: 7
      what: >-
        7 declared states no scenario targets. The 3 `unsupported` states (direct-debits,
        scheduled-payments, standing-orders) are the real gap; payment-consent's
        validating/exchanging/checking (3) and payment-status' loading (1) are
        spec-ahead-of-source.
      count_corrected: >-
        First recorded as 6. The instance LIST was right (3+3+1) but the total was mis-added;
        re-derived mechanically by the following /idea-verify at 15:00 and confirmed 7.
    - id: FW-1
      severity: error
      scope: framework
      what: >-
        framework-verify-action-contract.sh still carries its own bash+grep walk instead of
        calling action-contract-walk.ts — 15 blocks vs 431. Not fixable project-side and no
        framework file was edited. Mitigation: invoke the .ts walker directly for this gate.

# ═══════════════════════════════════════════════════════════════════════════
# 2026-08-02 /idea-verify — FULL PASS (latest). 28 idea-layer errors across 3
# classes + 2 framework-side gate defects. Cross-sibling class from 07-31 is CLOSED.
# ═══════════════════════════════════════════════════════════════════════════
run_2026_08_02_verify:
  status: partial
  errors: 28
  warnings: 6
  method: >-
    Every gate below was EXECUTED this run against the live corpus, not copied from the
    07-31/08-02 records. The 11 cross-sibling findings the 07-31 fourth run raised were
    re-checked by reading the edited files themselves (scenario_count vs actual scenarios,
    vm_actions blocks, test state targets) rather than trusting the heal log — all 11 confirm
    CLOSED.
  re_executed_and_passing:
    yaml_parse: "318 idea-layer YAMLs parse clean (yaml-parse-check.ts, exit 0)"
    action_contract: "G-ACTION-CONTRACT via action-contract-walk.ts — 23 ui.yaml / 431 blocks / 0 violations / 0 unevaluated"
    i18n: "G-I18N 535 references, all resolve (exit 0)"
    demo_data: "23/23 data-layer features carry demo-data.yaml (exit 0)"
    state_coherence: "G-ILC SC-1..SC-5 fail=0 warn=0"
    states_present: "23/23 declare a non-empty states[]"
    nav_targets: >-
      42 edges checked, 0 dead — 41 resolve, the 42nd is the documented _placeholder:atm-locator.
      CORRECTED at 15:00 from "39 edges": that figure was a hand tally of a uniq -c table, not
      a machine count. The 0-dead verdict was never affected.
    flow_refs: "8 distinct flow_ref values, 8/8 resolve to idea-layer/flows/"
    journey_screen_ids: >-
      0 dangling across 9 journeys. `profile` and `atm-locator` appear only inside
      removed_in_sync: provenance blocks, never in a live screen_sequence — re-read to confirm.
    cross_sibling_scenario_count: "23/23 docs.yaml#test_fixtures.scenario_count == len(tests.yaml#scenarios)"
    cross_sibling_test_states: "218 test state references, 0 target an undeclared state"
    schema_closure_passing: "G-IDEA-WRITABLE, -DFR, -PC, -PAG, -CAPS, -CT, -CSS all PASS"

  new_findings:
    SH-1_sibling_header_missing:
      severity: error
      count: 161
      scope: "every sibling YAML in the project — 23 features x 7 siblings"
      detail: >-
        core/schemas/contract/siblings/ui.sibling.schema.json declares required:
        [sibling, schema_version, feature_id, screens, components, states, state_model].
        NOT ONE of this project's 161 sibling files carries the v4.0 header triple
        `sibling:` / `schema_version:` / `feature_id:`. ui.yaml uses `screen_id:` where the
        schema names `feature_id`, and no file declares a schema_version at all.
      evidence: >-
        Measured per sibling kind: ui/api/flow/docs/tests/data-flow/demo-data — 23 files each,
        schema_version=0 and sibling=0 for all seven. The sibling project
        mifos-x/mifos-x-group-banking carries the header on 30/30 ui.yaml
        (`sibling: ui` / `schema_version: "4.0"` / `feature_id: admin-dashboard`), so this is a
        per-project defect, not a framework-wide convention change.
      why_no_gate_caught_this: >-
        SIBLING_LOADER.md step 104 STRIPS header keys before validating, so the loader never
        misses them, and no shipped evaluator validates a sibling against its own JSON schema.
        The capsule's Phase 1 step 7 schema-version check is specified but has no evaluator.
      impact: >-
        Any consumer that validates against the sibling schema rejects all 161 files, and
        /idea-migrate-schema cannot detect which version to migrate FROM.
      fix: "/idea-migrate-schema (adds the header triple), or /idea-feature-enrich per feature"
    WR-1_call_api_missing_data_edges:
      severity: error
      count: 24
      gate: G-IDEA-WR (framework-verify-idea-schema-writes-reads.sh)
      detail: >-
        24 action_contract blocks declare `effect: call_api` with neither `writes_to[]` nor
        `reads_from[]`. The rule went HARD on 2026-07-24, so these are FAIL not WARN.
      spread: >-
        19 of 23 features carry at least one; home, send-money, payment-status, transactions
        and consent-detail carry two each. Clean: licences, login, settings, user-onboarding.
      sample: "screens/home/ui.yaml:263 select_account — effect call_api, external_library_refs present, no reads_from"
      fix: "/idea-feature-enrich per feature, or /idea-heal to fan out"
    KM-1_business_logic_kind_off_enum:
      severity: error
      count: 4
      gate: G-IDEA-KM (framework-verify-idea-schema-kind-modules.sh)
      detail: >-
        business_logic.kind must be in the closed enum
        {audio_processing|text_processing|image_processing|ml_inference|calculation|streaming|crud|nav_only|composite}.
      instances:
        - "home ui.yaml:19 kind: aggregate"
        - "send-money ui.yaml:38 kind: form"
        - "payment-status ui.yaml:32 kind: detail"
        - "payment-consent ui.yaml:51 kind: auth"
      note: >-
        All four read as reasonable archetype words, which is why they survived — they are
        archetype vocabulary leaking into a slot that drives /implement STEP 1.CONTEXT module
        branching. The nearest in-enum values are composite (home, send-money, payment-consent)
        and crud (payment-status), but picking them is a design call, not a mechanical rename.
    ST-1_declared_states_untested:
      severity: warn
      count: 7      # CORRECTED at 15:00 from 6 — the instances below always listed 3+3+1
      detail: "States declared in ui.yaml#states[] that no tests.yaml scenario targets."
      instances:
        - "direct-debits, scheduled-payments, standing-orders: `unsupported` (3)"
        - "payment-consent: `validating`, `exchanging`, `checking` (3 of its 5 states)"
        - "payment-status: `loading`"
      note: >-
        The payment-consent and payment-status gaps are expected — both are spec-ahead-of-source
        P3 features. The three `unsupported` states are shipped features and are the real gap.

  framework_gate_defects:
    # Not idea-layer findings. Defects in the shipped evaluators, surfaced by running them.
    FW-1_action_contract_sh_undercounts:
      severity: error
      scope: framework
      file: .claude-runtime/scripts/framework-verify-action-contract.sh
      detail: >-
        The shell gate never delegates to action-contract-walk.ts — it carries its own
        bash+grep component walk (lines 92-249). On this project it reports "23 ui.yaml,
        15 component blocks" where the canonical parse-and-walk evaluator reports 431.
        It also pins the PRE-2026-07-31 INTERACTIVE_TYPES list (line 57), so filled_button,
        text_button, select and the other types the .ts widened are still skipped by the .sh.
      impact: >-
        ~96.5% of components are unevaluated by the shell entry point. Both report 0 violations
        here so no false-negative surfaced THIS run, but the gate cannot vouch for what it did
        not walk. The 07-31 record of "428 blocks" came from the .ts path; anything invoking
        the .sh gets 15.
      fix: "make framework-verify-action-contract.sh exec the .ts walker, deleting the bash walk"
    FW-2_aco_matches_non_endpoint_paths:
      severity: warn
      scope: framework
      file: .claude-runtime/scripts/framework-verify-api-contract-orphan.sh
      detail: >-
        ACO-1 greps every `path:` key in api.yaml regardless of parent. All 5 "orphans" it
        reports for this project are
        screens/payment-consent/api.yaml#existing_source_surface.{reusable,must_not_reuse}[].path
        — a Kotlin-file reuse map, not HTTP endpoints. The real endpoints: block (3 OAuth/PISP
        paths, lines 6-66) was not the thing flagged.
      fix: "scope the ACO-1 endpoint scan to the `endpoints:` parent block"

  last_verify_cache:
    written: false
    why: >-
      STEP V.6 — update last_verify only when overall is pass. 28 error-severity findings are
      open, so the cache stays at its last known-good value for diffing.

  recommended_next: >-
    SH-1 is mechanical and project-wide — route to /idea-migrate-schema. WR-1 and KM-1 are
    content decisions across 22 and 4 features — route to /idea-heal or /idea-feature-enrich.
    FW-1 and FW-2 are framework-side and belong to a framework fix, not this project.

# ═══════════════════════════════════════════════════════════════════════════
# 2026-08-02 /idea-sync — SOURCE-TRUTH REVERSE PASS. 8 closed, 1 new warn.
# ═══════════════════════════════════════════════════════════════════════════
run_2026_08_02_source_truth:
  status: success
  direction: source-to-idea
  direction_note: >-
    RULE-GAP-IDEA-FIRST-001 makes the idea-layer the SoT by default. The user directed the
    opposite for this run ("source is the truth here"), which is a legitimate and precedented
    mode here — /gap-analysis-project ran the same direction on 2026-07-28. This pass did NOT
    re-derive features from source; it closed 8 named divergences.
  trigger: "/idea-verify-e2e 2026-08-02 — roster mean 77/100, range 54-87, 0 features >= 90"

  closed:
    - id: F1-SCHEDULED-PAYMENTS-UNSUPPORTED
      how: reclassified-not-deleted
      detail: >-
        Kept and declared as spec_ahead_of_source rather than pruned to match source. See the
        finding below, now status: source-owed with idea_layer_obligation: CLOSED.
    - id: F2-PISP-NO-SOURCE
      how: marked-spec-ahead-of-source
      detail: >-
        3 docs.yaml gained a spec_ahead_of_source block recording MEASURED source state — 130
        PISP wire DTOs and the SendMoneyRoute / PayTab placeholders are present; service, store,
        repository and feature module are absent. Drift and source-test checks now score N/A.
    - id: F3-TEST-TAG-TRACEABILITY
      how: backfilled-from-source
      detail: >-
        Was 7 of 23 features declaring any test_tag while source carried 282 TestTags constants
        across 18 modules. Now 259 bindings across 20 features, and 23/23 carry a test_tags
        reconciliation block. Audited: every FLAT const in all 18 modules is either bound to a
        component or recorded under test_tags.unbound with the reason it has no anchor.
      conventions_preserved: >-
        Three naming forms coexist in source (feature:camelCase, feature_snake_case, bare) and
        two modules mix forms internally. All copied verbatim — the value is what onNodeWithTag
        asserts on, so normalising would break shipped tests.
    - id: F4-ACCOUNTS-MISSING-SIBLINGS
      how: authored-from-source
      detail: >-
        accounts/docs.yaml gained nav_callbacks, vm_actions and test_fixtures. Its impl_notes
        was corrected in the same edit: it declared an AccountsEvent generic that does not
        exist (source uses Nothing), a 9-field AccountsState against source's 1, a
        ConsentExpiring state pruned on 07-28, a consent_expiry_check and a parallelism block
        that are not in the ViewModel, and three nav edges against source's one.
      why_invisible: "no shipped gate compares impl_notes against source"
    - id: F5-EXPORT-MATRIX-STALE-LEDGER
      how: corrected-against-disk
      detail: >-
        pending_generation declared mockup / prompts_figma / preview_html `absent, deferred`
        while 23 / 23 / 95 files sat on disk. All three flipped to COMPLETE with disk-verified
        counts; 4 __all__ cells registered so those groups are watched from now on; summary
        recounted (58 ledger rows, 436 tracked artifacts). Only mockup_stitch remains deferred,
        which is the settled 2026-07-30 user decision.
    - id: F6-STITCH-PROMPTS-STALE
      how: rebuilt
      detail: >-
        95 prompts rebuilt via stitch-prompt-build.ts --force (BUILD stage only, no SDK call).
        The two content-confirmed stale cases are fixed: send-money prompts now carry
        form_step_indicator / no_saved_payees / the four review_* rows, and transactions prompts
        carry search_bar.
    - id: F7-TRAINING-MASTER-BEHIND
      how: re-measured
      detail: >-
        component_inventory 414 -> 430 components / 431 walker blocks, with the reconciliation
        between those two measures recorded (they were previously asserted equal and are not);
        by_type refreshed; action_contract.coverage_holes closed (the walker's vocabulary was
        widened framework-side on 07-31); a new test_tags section added.
    - id: F8-FLOW-METADATA
      how: backfilled-and-reshaped
      detail: >-
        flow_ref backfilled into 14 docs.yaml (23/23 now declare one, derived from
        flows/*.yaml#screens[] membership, not guessed); flows/send-money-payment.yaml reshaped
        out of the journey container into the flow container its 8 siblings use;
        journeys/send-money-payment.yaml authored (it was the only happy-path flow with no
        journey); journeys/INDEX.md refreshed from a stale 20/20 to 23/23.
      not_a_defect: >-
        consent-expired still has no journey ON PURPOSE — it is type: error, and journeys model
        happy-path narratives. The flows' feature_ref values consent-onboarding and
        consent-dashboard are legitimate plan-level features (idea-plan.yaml:412,417), not
        dangling screen refs; an earlier read of them as dangling was wrong.

  incidental_repair:
    - id: ACCOUNT-DESCRIPTION-CARD-MISSING
      severity: error
      feature: account-detail
      detail: >-
        components/AccountDescriptionCard.kt ships and was listed in
        ui.yaml#implementation.components, but the idea-layer modelled NO component for it —
        which is why accountDetail:descriptionCard had no anchor. The surface was missing, not
        just the tag. Added account_description_card (+ label and value children) plus the two
        strings, mirroring source: a surfaceContainer card between the identity card and the
        balances, omitted entirely when description is blank.
      why_it_matters: >-
        HSBC reports AccountTypeCode CACC for a Global Money wallet exactly as for an ordinary
        current account, so the free-text description is the sole distinguishing signal — the
        same one HsbcProductType.resolve matches on to gate which Explore chips appear.

  gates_after:
    yaml_parse: "295 files, 0 failures"
    action_contract: "23 ui.yaml, 431 blocks, 0 violations, 0 unevaluated (--report-untyped empty)"
    i18n: "564 declared, 535 referenced, 0 unresolved"
    nav_targets: "20 distinct, 0 dead"
    initial_state: "23/23 are declared states"
    flow_ref_resolution: "23/23 resolve to a flows/*.yaml"
    journey_screen_ids: "0 dangling across 9 journeys"

  new_finding_open:
    id: PROMPT-BUILDER-CHILDREN-ONLY
    severity: warn
    owner: framework
    status: OPEN
    instances: 11
    features: [accounts, direct-debits, user-onboarding, transactions, beneficiaries]
    summary: >-
      stitch-prompt-build.ts walks only `children:` when rendering the component tree, so
      components declared under any other container key never reach the prompt.
    evidence: >-
      After a FORCED rebuild, 11 component ids still do not appear in their prompts: accounts'
      4 filter chips and direct-debits' 2 count chips and user-onboarding's 2 trust chips (all
      under `chips:`), transactions' 2 period totals (under `stats:`), and
      beneficiaries#beneficiary_avatar (under `leading:`). Surviving --force is what proves this
      is a builder limitation rather than staleness.
    why_not_fixed_here: >-
      The project-side "fix" — renaming those keys to `children:` — would be wrong: `leading:`
      is a legitimate M3 list-item slot and `stats:` entries are data rows, not components.
      Teaching the builder the other container keys is a framework change with cross-project
      reach and wants its own RED/GREEN canary.

  not_addressed_this_run:
    - id: XS-3-WIDER-VM-ACTIONS-RECLASSIFY
      why: >-
        Still OPEN and deliberately untouched. The 2026-08-01 sync held it for a disposition
        decision because the information-preserving remedy is a docs-sibling SCHEMA change with
        cross-project reach, and this run's scope was the 8 e2e findings. Unchanged, not missed.

# NOTE ON ORDERING: this run's timestamp (18:27:46Z, machine-read) is EARLIER than the
# 22:45:00Z / 23:20:00Z stamps on the heal rounds recorded below. Those two were not
# machine-read — the same clock defect the 18:07 verify record already flagged. This run
# is the LATEST; read the heal blocks below as prior, not subsequent.

# ═══════════════════════════════════════════════════════════════════════════
# 2026-07-31 /idea-verify — FOURTH RUN (latest). 11 new errors, cross-sibling.
# ═══════════════════════════════════════════════════════════════════════════
run_2026_07_31_fourth:
  status: partial
  mechanical_gates: "ALL PASS, re-executed not copied — yaml-parse 188/0, G-ACTION-CONTRACT 428 blocks/0 violations, G-I18N 533 refs/0 unresolved, 23/23 states, 42 nav edges/0 dead, 0 bad flow entries, 0 missing demo-data"
  heal_round_2_reconfirmed: "all four capability fixes verified by READING the edited files — search_bar, send-money validators[], system_capabilities on 5 features, settings preferences[]"
  d_action_contract_vocab: "CLOSED framework-side — the walker's INTERACTIVE set was widened 2026-07-31 to include text_button + select; both previously-unevaluated components now evaluate and pass; --report-untyped is empty"

  new_findings_count: 11
  new_findings_class: half-applied-reverse-sync
  new_findings_scope: "9 features — user-onboarding, settings, accounts, consent-callback, home, send-money, consent-detail, login, account-holder"
  why_invisible_until_now: >-
    No shipped gate compares docs.yaml or tests.yaml against their own ui.yaml. The gates
    validate ui.yaml internally and strings globally; a sibling that contradicts ui.yaml is
    outside every one of them. That is exactly how the 07-28 and 07-30 syncs corrected
    ui.yaml and left its siblings describing the deleted design.

  findings:
    - id: XS-3-VM-ACTIONS-GHOST-STATE
      rule_id: RULE-IDEA-GAP-DISCOVERY-001
      gap_class: sibling-declares-removed-surface
      severity: error
      instances: 4
      features: [user-onboarding, settings]
      evidence_refs:
        - "file:idea-layer/screens/user-onboarding/docs.yaml:46-52 -> vm_action StepNext maps currentStep to permissions_overview + consent_explainer"
        - "file:idea-layer/screens/user-onboarding/docs.yaml:78    -> NavigateToLogin 'Only callable from consent_explainer state (step 3)'"
        - "file:idea-layer/screens/user-onboarding/ui.yaml:66      -> states: [intro]"
        - "file:idea-layer/screens/user-onboarding/ui.yaml:36-39   -> removed_in_sync lists those exact states and actions as never built"
        - "file:idea-layer/screens/settings/docs.yaml:128-133      -> clearLocalDataConfirm sets state = clear_confirm"
        - "file:idea-layer/screens/settings/ui.yaml:31             -> removed_in_sync lists clear_confirm AND clearLocalDataConfirm as removed"
      summary: "docs.yaml#vm_actions declares actions driving states the feature no longer has, which its own ui.yaml records as never built."
      detail: >-
        user-onboarding is the sharpest case. Its docs.yaml description was CORRECTED on
        2026-07-30 to read "Stateless: no ViewModel, no I/O", and ui.yaml sets state_model: {}
        with viewmodel: null — yet five vm_actions survive directly beneath that corrected
        description, three of them naming states deleted in the same sync. A codegen consumer
        reading vm_actions would emit a ViewModel for a screen the spec says has none.
      delegated_to: "/idea-feature-enrich"
      confidence: 100

    - id: XS-2B-TEST-TARGETS-UNDECLARED-STATE
      rule_id: RULE-IDEA-GAP-DISCOVERY-001
      gap_class: test-asserts-nonexistent-state
      severity: error
      instances: 2
      features: [accounts, consent-callback]
      evidence_refs:
        - "file:idea-layer/screens/accounts/tests.yaml:60-66        -> TC-ACCTS-007 state: consent_expiring"
        - "file:idea-layer/screens/accounts/ui.yaml:109             -> states: [loading, content, empty, error]"
        - "file:idea-layer/_strings/strings.yaml:58                 -> 'Pruned 2026-07-28: consent_expiring.* (no such state)'"
        - "file:idea-layer/screens/consent-callback/tests.yaml:29-33 -> TC-CALLBACK-003 state: empty"
        - "file:idea-layer/screens/consent-callback/ui.yaml:30      -> states: [loading, awaiting, content, access_denied, security_error, error]"
      summary: "Two test scenarios assert against states their feature does not declare, so they can never be satisfied."
      detail: >-
        accounts is the clearest evidence of the sweep gap: the 07-28 prune removed the
        consent_expiring STATE and swept its STRINGS in the same edit — and left the test
        that exercises it. consent-callback's awaiting surface is named `awaiting`, but the
        test (and docs.yaml's PollConsentStatus, which says "Transition to empty (awaiting)
        state") both still call it `empty`.
      delegated_to: "/idea-feature-enrich"
      confidence: 100

    - id: XS-1-SCENARIO-COUNT-DRIFT
      rule_id: RULE-IDEA-GAP-DISCOVERY-001
      gap_class: stale-sibling-bookkeeping
      severity: error
      instances: 7
      features: [user-onboarding, settings, home, send-money, consent-detail, login, account-holder]
      evidence: "docs.yaml#test_fixtures.scenario_count vs len(tests.yaml#scenarios)"
      table:
        user-onboarding: { declared: 12, actual: 4 }
        settings:        { declared: 17, actual: 9 }
        home:            { declared: 17, actual: 13 }
        send-money:      { declared: 12, actual: 14 }
        consent-detail:  { declared: 9,  actual: 11 }
        login:           { declared: 7,  actual: 8 }
        account-holder:  { declared: 6,  actual: 7 }
      summary: "7 features declare a test scenario_count that disagrees with their actual tests.yaml."
      detail: >-
        Drifts in BOTH directions, so this is stale bookkeeping rather than a systematic
        undercount. priority_distribution is stale alongside it: user-onboarding declares
        high:7 / medium:5 while its 4 real scenarios are critical x2 and normal x2 — different
        buckets, not merely different totals. home's ids are non-contiguous (001,002,003,006,
        007,010-015,017,018), confirming scenarios were deleted without updating the count.
      delegated_to: "/idea-feature-enrich"
      confidence: 100

  not_auto_applied_because: >-
    These are content edits across 9 features and /idea-verify is read-only by contract.
    Route to /idea-feature-enrich per feature (it owns vm_actions and test_fixtures) or
    /idea-heal to fan out. Fix the SIBLINGS to match ui.yaml — ui.yaml is the side that was
    reverse-synced from source and is the correct one.

  promotion_candidate:
    pattern: "sibling docs.yaml/tests.yaml contradicting their own ui.yaml after a reverse sync"
    proposed_check: >-
      Assert per feature that (a) every docs.yaml#vm_actions state reference resolves to a
      declared ui.yaml#states member, (b) every tests.yaml#scenarios[].state resolves likewise,
      and (c) docs.yaml#test_fixtures.scenario_count == len(tests.yaml#scenarios). All three are
      deterministic and would have caught all 11 findings the moment the reverse sync landed.
    recurrence: 1

# ═══════════════════════════════════════════════════════════════════════════
# 2026-07-31 /idea-heal — ROUND 1
# ═══════════════════════════════════════════════════════════════════════════
heal_round_1:
  target: "G-CAP-001 CAPABILITIES_UNDECLARED (23 features)"
  delegated_to: "/idea-feature-enrich Phase 2 step 21 (populate capabilities[] from the closed enum)"
  delegation_note: >-
    The engine's D7 delegation table has no CAPABILITIES_UNDECLARED row. Routed to enrich
    because its declared contract owns this field; scoped to that sub-step rather than a
    full 23-feature re-enrich.
  status: RESOLVED
  result: "23/23 features now declare capabilities[]; 0 undeclared"

  declared:
    has_ui: 23
    has_flow: 23
    has_api: 20              # not licences, settings, user-onboarding — zero operations
    has_system_integration: 5 # login, consent-callback, payment-consent, send-money, user-onboarding
    has_search: 2            # beneficiaries, transactions
    has_static_content: 2    # licences, user-onboarding
    has_form_input: 1        # send-money
    has_local_preferences: 1 # settings

  deliberately_not_declared:
    - capability: has_payments
      why: >-
        In-app-purchase compliance (payment_plans[], iap_identifiers[], receipt verification,
        restore-purchases, App Store 5.1.1). This app does bank payment initiation over OBIE
        rails. Declaring it raises eight inapplicable errors and makes /kmp-client-gen emit
        StoreKit boilerplate.
    - capability: is_auth_feature
      why: >-
        Means the feature IS signin/signup/OTP; demands auth_screens[], OTP code TTL, resend
        rate limit and account-lockout policy. Under the OBIE redirect model the PSU
        authenticates AT their bank — there is no password, OTP or lockout here to declare.
    - capability: has_offline_cache
      why: >-
        Means OFFLINE read with conflict resolution; demands stale-data indicators, eviction
        policy and a conflict strategy. The three caches present are memory / memory_lru —
        ephemeral, not offline persistence.
    - capability: requires_auth
      why: >-
        Arguable and left undecided rather than guessed. Every AIS screen does require an
        authorised consent, so a case exists — but the textual signal was unreliable
        (consent/token appear in nearly every screen's prose) and RULE-SEED-AUTH-001's
        obligations were not evaluated against this app. Flagged, not assumed.

  eligibility_delta:
    before: "0 of 148 capability-gated rule files could run"
    after: "127 of 148 now eligible"
    by_capability: { has_ui: 93, has_api: 26, has_flow: 3, has_system_integration: 1,
                     has_local_preferences: 1, has_static_content: 1, has_search: 1, has_form_input: 1 }
    still_dormant: 21
    dormant_breakdown: >-
      Most are correctly inapplicable capability rules (has_payments, is_auth_feature,
      has_offline_cache, has_social, has_gamification, has_media_playback, has_file_operation,
      has_content_creation, has_data_migration, has_realtime, has_journey). Five are not
      feature capabilities at all but command/pipeline gates (kmp_implement ×3, ci, secrets,
      framework, has_export_matrix). Two are artifacts of THIS measurement rather than real
      dormancy: RULE-DEMO-DATA-001 uses disjunction syntax (`has_api OR has_offline_cache`)
      which the eligibility probe read literally and so under-counted — the real iterator
      resolves disjunctions and it IS eligible via has_api; and one row matched a trailing
      comment in RULE_VERSION_POLICY.md.

  structural_matrix_after: "23/23 PASS on all 7 checks · 0 findings · G-CAP-001 closed"

  # ─────────────────────────────────────────────────────────────────────────
  # NEWLY SURFACED — these were structurally invisible before this round
  # ─────────────────────────────────────────────────────────────────────────
  newly_surfaced:
    note: >-
      Declaring a capability activates its rule's sub-checks. These four classes are the
      direct consequence, and finding them is the POINT of the fix — they were unreachable
      while capabilities[] was empty. They are content changes across 9 features, not
      mechanical edits, so round 2 was not auto-applied.
    findings:
      - id: G-CAP-SEARCH-001
        rule: RULE-CAP-HAS-SEARCH-001 V1
        severity: error
        feature: transactions
        detail: >-
          V1 requires a dedicated search-type input, not a repurposed text input.
          transactions#search_field is a `text_field`; beneficiaries#search_field uses the
          dedicated `search_bar` and passes. This was predicted before declaring the
          capability and declared anyway — hiding it by omitting has_search would have been
          the wrong trade.
        fix_hint: "Retype transactions#search_field to search_bar, or waive V1 with justification."
      - id: G-CAP-FORM-001
        rule: RULE-CAP-HAS-FORM-INPUT-001 V2 + V5
        severity: error
        feature: send-money
        detail: >-
          V2 requires every input to declare validators[] (type + pattern + length): zero are
          declared across four text_fields, including the sort-code and account-number fields
          that have hard 8-digit formats and max_length already set. V5 requires required
          fields to be marked: none are. V3 passes — amount_field carries error_text.
        fix_hint: "Add validators[] to manual_sort_code, manual_account_number, amount_field, reference_field; mark required fields."
      - id: G-CAP-SYSINT-001
        rule: RULE-CAP-HAS-SYSTEM-INTEGRATION-001 V1
        severity: error
        features: [login, consent-callback, payment-consent, send-money, user-onboarding]
        detail: >-
          V1 requires system_capabilities[] enumerating the specific integrations
          (deep_link / notifications / widget / permission). None of the five declares it,
          though all five perform an app-to-app or browser redirect.
        fix_hint: "Declare system_capabilities: [deep_link] on each; add permission rationale + denied-state path if any permission is requested."
      - id: G-CAP-PREFS-001
        rule: RULE-CAP-HAS-LOCAL-PREFERENCES-001 V1
        severity: error
        feature: settings
        detail: >-
          V1 requires a preferences[] list with key, type, default and scope. settings has no
          such block — the only "preferences" matches are prose in the description. The theme
          preference is real (DataStore-backed, with a defaults path) but undeclared.
        fix_hint: "Declare preferences[] with the theme key: {key, type: string, default: System, scope: device}."

  round_2_applied:
    at: "2026-07-31T23:20:00Z"
    command: "/idea-sync — issue fixing first, then artifact sync"
    all_four: CLOSED
    fixes:
      - id: G-CAP-SEARCH-001
        fix: "transactions#search_field retyped text_field -> search_bar"
        also: >-
          Switched on_click -> on_change. A search surface filters as the PSU types; the
          component's own test_context already declared interaction_type: input, so the
          on_click was the anomaly. Now matches beneficiaries#beneficiary_search exactly.
      - id: G-CAP-FORM-001
        fix: "All 4 send-money inputs gained validators[] + an explicit required decision + per-field error_text"
        detail: >-
          sort code ^[0-9]{6}$ with hyphen strip — SIX digits, not the 8 that max_length
          implies, because max_length counts the two display hyphens and validating the raw
          8 characters would reject every correctly-typed code. account number ^[0-9]{8}$.
          amount required + min 1 minor unit + max_dynamic against the selected account's
          balance (advisory — funds-confirmation stays authoritative). reference is the only
          optional field, capped at the OBIE 35-char RemittanceInformation limit.
        state_model_extended: >-
          Content gained fieldErrors: FieldErrors and availableBalanceMinorUnits: Long?.
          fieldErrors is a MAP, not a reuse of the single validationError, because two fields
          can be wrong at once — a short sort code AND a short account number — which one
          nullable error cannot represent. Without this the error_text bindings added here
          would have been dangling references.
        v4_note: >-
          Submit-guard satisfied by state transition, not a `disabled:` flag: confirm_button
          binds to content only, so confirming unmounts it and renders submitting_indicator.
          Recorded in docs.yaml#form_submit_guard so a reader looking for `disabled:` does
          not read its absence as a failure.
      - id: G-CAP-SYSINT-001
        fix: "system_capabilities: [deep_link] declared on all 5 redirect features"
        v2_v3_na: >-
          Permission rationale and denied-state UX are genuinely N/A: the app requests ZERO
          device permissions. No api.yaml declares requires_permission, and every
          'permission' reference in the idea-layer is an OBIE consent SCOPE granted at the
          bank, not an OS grant. Recorded once in login/docs.yaml and cross-referenced.
      - id: G-CAP-PREFS-001
        fix: "settings gained preferences[] with dark_theme_config {type, default: System, scope: device, allowed[]}"
        also: >-
          preferences_migrations: [] declared explicitly so a future rename records a
          migration rather than silently orphaning a stored value, plus a V4 security note
          that no tokens or PII live in DataStore — OBIE tokens go to secure storage.

  artifact_sync:
    measured_wrong_first: >-
      A first pass compared every artifact against max(ui.yaml, docs.yaml) and reported all
      23 features stale — 192 artifacts. That was the wrong denominator. Re-measured against
      each artifact's DECLARED source (SPEC <- ui.yaml, API <- api.yaml, MOCKUP/PROMPTS <-
      ui+docs, preview <- ui+tokens), only 2 features were genuinely stale.
    why_most_were_not: >-
      Adding capabilities[] to 23 docs.yaml bumped every mtime, but NO export renders that
      field — verified: the only 'capabilit' matches in exports/ are AccountCapabilityRegistry
      (a domain class) and a PIPELINE_STATE path. Content was already correct everywhere.
    content_updated: [send-money, transactions]
    content_update_detail: >-
      send-money SPEC gained a Field validation contract section (the table codegen reads for
      validators, the fieldErrors state backing, and the submit-guard note). transactions SPEC
      corrected two component-inventory references from text_field to search_bar.
    provenance_restamped: 151
    restamp_scope: "ONLY `Source hash`, `> Generated:` and `<!-- render-hash: -->` lines — no rendered body touched"
    stale_after: 0

  next_round:
    would_address: []
    applied_in_round_2: "all four — see round_2_applied above"
    also_open: "requires_auth — decide whether AIS screens should declare it (see deliberately_not_declared)"

# ═══════════════════════════════════════════════════════════════════════════
# CURRENT OPEN STATE
# ═══════════════════════════════════════════════════════════════════════════
# Was 0, set by the heal rounds below. Now 11 — see run_2026_07_31_fourth at the head of
# this file. The heal rounds' 0 was correct for the dimension they measured (capability-gated
# rules); the 11 live in the cross-sibling dimension nothing had checked until this run.

# ═══════════════════════════════════════════════════════════════════════════
# 2026-08-01 /idea-sync — the 11 verify findings FIXED; one new class surfaced
# ═══════════════════════════════════════════════════════════════════════════
run_2026_08_01_sync:
  command: "/idea-sync — issue fixing first, then artifact measurement"
  preflight: "PASS ×3 — source-of-truth-sync (A=B≠C, 0 divergent tokens), mockup-theme-consistency (0 findings), pf2-5 idea-migration (no drift)"
  fixed: 11
  fix_detail:
    XS-2b:
      - "consent-callback TC-CALLBACK-003: state empty -> awaiting. RENAMED not rewritten — every assertion already targeted awaiting_state and the consent_callback_awaiting_* strings, so only the label was wrong. docs.yaml PollConsentStatus wording corrected in the same edit."
      - "accounts TC-ACCTS-007: REMOVED. Asserted state consent_expiring plus a banner and Reconfirm button that do not exist; strings.yaml:58 records the matching 07-28 prune. Unsatisfiable rather than mislabelled, and with no surviving surface to retarget — unlike TC-CALLBACK-003. Ids left non-contiguous deliberately."
    XS-3:
      - "user-onboarding docs.yaml: vm_actions -> [] (was 5 actions driving a pager and sheet that ui.yaml:34-40 records as never built). nav_callbacks added for the one real outward edge. Empty is correct, not placeholder: ui.yaml sets state_model {} and viewmodel null, and TC-ONB-003 asserts no ViewModel resolves."
      - "settings docs.yaml: vm_actions rewritten 12 -> 4, mirroring ui.yaml#state_model.actions. Verify had flagged only clearLocalDataConfirm; reading the block showed 8 of 12 drove surfaces removed on 07-28 (Security, Notifications, profile, cache-erasure). The 3 nav-host callbacks were MOVED to nav_callbacks, not deleted. error_cases for the two removed surfaces swept in the same edit."
    XS-1:
      - "7 scenario_count corrections: user-onboarding 12->4, settings 17->9, home 17->13, send-money 12->14, consent-detail 9->11, login 7->8, account-holder 6->7"
      - "home priority_distribution recorded AS MEASURED (medium:2, unset:11) — only 2 of 13 scenarios carry a priority key. Reconstructing high/medium/low buckets would have fabricated data the suite does not contain."
      - "settings declared_sum was 14 against a declared 17 — the block did not agree with itself."
    incidental_repairs:
      - "account-holder test_fixtures.source pointed at screens/party/tests.yaml — a DANGLING path (party was renamed to account-holder on 07-28; the directory does not exist). Repaired."
      - "send-money test_fixtures.source pointed at demo-data.yaml, which holds fixture DATA and zero scenarios; all 14 TC-SEND-* live in tests.yaml. Retargeted, with demo-data kept as a fixture_data pointer."
  post_fix_gates: "ALL PASS — yaml-parse 209/0 (incl. maestro), xsib-probe NO FINDINGS, G-ACTION-CONTRACT 428 blocks/0 violations, G-I18N 533 refs/0 unresolved"
  artifact_staleness: >-
    Measured against each artifact's DECLARED source rather than file mtime, per the lesson
    recorded in this file's own artifact_sync block. No export renders vm_actions,
    test_fixtures or scenario_count — the single grep hit, exports/home/SPEC.md:156, is a
    reverse-sync CHANGELOG TABLE naming the field, not a rendering of its contents. So these
    edits made ZERO artifacts stale and no regeneration was required.

  new_finding_open:
    id: XS-3-WIDER-VM-ACTIONS-RECLASSIFY
    severity: warn
    status: OPEN — awaiting disposition decision, deliberately NOT auto-applied
    instances: 24
    features: [account-detail, beneficiaries, consent-detail, consent-list, direct-debits, home,
               login, scheduled-payments, standing-orders, statement-detail, statements,
               transaction-detail, transactions]
    summary: >-
      24 further docs.yaml#vm_actions entries name actions that their own
      ui.yaml#implementation.removed_in_sync records as removed.
    why_not_auto_fixed: >-
      These are NOT the same as the settings/user-onboarding cases already fixed, and treating
      them identically would be wrong. There the surface does not exist AT ALL (no Security
      section, no three-step pager). Here the behaviour DOES happen — the screen really does
      load its data and really does navigate back — just via init{} stream collection and
      nav-host callbacks rather than a dispatched Action member. Deleting `LoadAccountDetail`
      would delete the documentation that the screen loads account detail, which is true.
      The information-preserving fix is RECLASSIFICATION (marking each entry as init-stream or
      nav-callback) rather than deletion, but that adds a field to the docs sibling schema and
      is a cross-project contract change — not something a project-scope sync run should invent
      unilaterally. Surfaced for a decision instead.
    measurement_note: >-
      A first heuristic compared vm_actions names against ui.yaml#state_model.actions and
      reported 18 features divergent. That was the WRONG DENOMINATOR — the same error this
      file's artifact_sync block records making with mtimes — because vm_actions documents
      ViewModel METHODS while state_model.actions documents Action sealed-class MEMBERS; they
      are different vocabularies and disagreeing is normal. Re-measured against
      removed_in_sync, which is source-derived ground truth. 26 raw hits, 2 discarded as
      tokenizer false positives (direct-debits and standing-orders RetryLoad matched because
      their removal note says RetryLoad SURVIVES). 24 genuine.

# Updated 2026-08-02 (/idea-sync source-truth pass).
#   errors open: 0
#   warns  open: 2 — XS-3-WIDER-VM-ACTIONS-RECLASSIFY (project, awaiting a disposition
#                    decision) and PROMPT-BUILDER-CHILDREN-ONLY (framework, recorded not fixed)
# The 8 findings from the 2026-08-02 /idea-verify-e2e run are all closed; see
# run_2026_08_02_source_truth above. Two of them were closed by DECLARING the divergence
# rather than removing it (the scheduled-payments unsupported state and the 3 PISP features),
# with the user's explicit agreement — those are now source obligations, not idea-layer gaps.
open_findings_now: 3          # ST-1 closed 2026-08-02T16:10; 0 error-severity remain
open_findings_breakdown:
  errors: 0
  warns: 3
  warn_ids: [SH-1-SIBLING-BODY-DIALECT, XS-3-WIDER-VM-ACTIONS-RECLASSIFY, PROMPT-BUILDER-CHILDREN-ONLY]
  st1_closed_at: "2026-08-02T16:10:00Z"
  closed_this_run:
    - id: WR-1-CALL-API-MISSING-DATA-EDGES
      was: "error, 24 instances across 19 features"
      how: "/idea-heal fan-out; G-IDEA-WR now PASS"
    - id: KM-1-BUSINESS-LOGIC-KIND-OFF-ENUM
      was: "error, 4 features"
      how: "/idea-heal fan-out; G-IDEA-KM now PASS"
    - id: FW-2-ACO-MATCHES-NON-ENDPOINT-PATHS
      was: "warn, 5 false-positive orphans"
      how: "project-side key rename path -> source_ref; ACO now reports 0 orphans"
  downgraded_this_run:
    - id: SH-1-SIBLING-HEADER-MISSING
      from: error
      to: warn
      new_id: SH-1-SIBLING-BODY-DIALECT
      why: >-
        The 161 missing headers are fixed. What remains is the body-shape divergence from
        core/schemas/contract/siblings/*.json, which the header fix does not address and which
        the user chose not to restructure. Kept on the books as a warn rather than closed.
  note: >-
    Counted as finding CLASSES, not instances. FW-1 remains an open ERROR but is framework-scope
    (a defect in framework-verify-action-contract.sh) and is deliberately excluded from this
    project's count — no framework file was edited.
source_obligations_open:
  # Not idea-layer findings. Things SOURCE owes, tracked here so they are not forgotten.
  - id: scheduled-payments-U000-branch
    owed_by: /implement
    detail_ref: "screens/scheduled-payments/ui.yaml#spec_ahead_of_source"
  - id: pisp-feature-modules
    owed_by: /kmp-implement
    features: [send-money, payment-consent, payment-status]
    detail_ref: "screens/{feature}/docs.yaml#spec_ahead_of_source"
open_decisions:
  - id: D-REQUIRES-AUTH
    kind: decision
    detail: >-
      Whether the AIS screens should declare requires_auth. Every one does require an
      authorised consent to call its endpoints, so a case exists — but the textual signal
      was unreliable (consent/token appear in nearly every screen's prose) and
      RULE-SEED-AUTH-001's obligations were never evaluated against this app. Left undecided
      rather than guessed; declaring it would activate one more rule.
  - id: D-BANKING-RAILS-CAPABILITY
    kind: framework-gap
    detail: >-
      Nothing in the 20-capability vocabulary means "moves real money over banking rails".
      has_payments is store billing only, so PISP features sit at the same medium tier as any
      form screen. Raising a new capability is the fix; borrowing has_payments' tier by
      mis-declaring it is not.
  - id: D-ACTION-CONTRACT-VOCAB
    kind: framework-gap
    status: RESOLVED
    resolved_at: "2026-07-31"
    resolved_by: "framework-side widening of action-contract-walk.ts, confirmed by /idea-verify fourth run"
    detail: >-
      WAS: 2 on_click components remain outside the action-contract interactive vocabulary
      (home#view_all_transactions_link type=text_button, settings#theme_row type=select) and
      go unevaluated. Widening the vocabulary is cross-project.
    resolution: >-
      The walker's INTERACTIVE set was widened on 2026-07-31 to include text_button, select and
      the other types registered in layers/idea/templates/registry/actions.yaml. The widening
      was MEASURED before being made rather than assumed on blast-radius grounds: across the
      whole repo it newly evaluates exactly these 2 components, fails 0, and affects no canary
      fixture. Both now evaluate and PASS, and --report-untyped returns empty. Verified this run:
      G-ACTION-CONTRACT scans 428 blocks with 0 violations and 0 unevaluated.
project: "mifos-x/mifos-x-open-banking"
roster_size: 23                 # canonical count from idea-layer/_list.json
features_evaluated: 23

# ═══════════════════════════════════════════════════════════════════════════
# 2026-07-31 /idea-verify — full pass, all 23 features
# ═══════════════════════════════════════════════════════════════════════════
run_2026_07_31:
  matrix: "23/23 PASS on parse · contract_version · states · nav targets · flow entry · i18n · action_contract"
  measurements:
    yaml_parsed: "161 sibling files across 23 features, 0 parse failures"
    contract_version: "23/23 at 2.0.0 == VERSION_MANIFEST#contract_versions.current"
    dead_nav_targets: 0
    dead_flow_entries: 0
    i18n: "554 declared keys, 0 unresolved {strings.*} references"
    action_contract: "420 component blocks, 0 violations"
  findings_open: 1

  findings:
    - id: G-CAP-001
      kind: CAPABILITIES_UNDECLARED
      severity: error
      scope: "all 23 features"
      detail: >-
        No feature declares a top-level `capabilities[]`. docs.yaml carries flat `has_ui` /
        `has_server` booleans instead. capabilities.registry.json states plainly: "Features
        MUST declare a non-empty subset" of the 20 capabilities.
      consequence: >-
        This is not cosmetic. The capability-rule iterator gates on
        `requires_capability ⊆ feature.capabilities`, so an empty set means NO
        capability-gated rule fires. Measured against the live rule tree: 147 of 568 rule
        files declare `requires_capability` and therefore never ran on this project —
        98 gated on has_ui (against 23 screens that ARE all UI), 27 on has_api, plus
        has_flow, requires_auth, is_auth_feature and the rest. Every /idea-verify result
        this project has ever recorded covers only the ungated remainder.
      blast_radius: "project-wide and pre-existing — the 20 already-approved features share the same shape"
      blocks:
        - "/idea-approve C1 — iterates capabilities[] to resolve required_sections; an empty set passes vacuously"
        - "/idea-approve C5 — effective detail tier is computed from capabilities[]; empty collapses it to the weakest setting"

  last_verify_cache:
    written: false
    why: >-
      The runtime writes docs.yaml#last_verify only when a feature passes, and all 23 pass
      the mechanical checks — so a cache COULD be written here. It deliberately was not.
      Writing it would give /idea-approve C4 a cache to read, converting an honest
      "unassertable" into a PASS founded on an iteration where 147 applicable rules never
      fired. That is the same false-green this session already refused once at the approve
      gate; producing it from the verify side instead would be the same error wearing a
      different hat. Declare capabilities[] first, re-run, then the cache means something.

  correction:
    what: "Earlier guidance in this session recommended declaring `has_payments` on the 3 PISP features. That was WRONG."
    why: >-
      RULE-CAP-HAS-PAYMENTS-001 is IN-APP-PURCHASE compliance, not money movement. Its eight
      error sub-checks are payment_plans[], refund_policy, iap_identifiers[], server-side
      receipt verification, a restore-purchases flow, tax inclusivity, and an App Store
      5.1.1 cancel-subscription path. The registry gloss "IAP, subscriptions, transactions"
      reads ambiguously; the rule body does not. Declaring it on a bank-transfer feature
      would raise eight inapplicable errors, invite someone to fabricate SKUs to clear them,
      and make /kmp-client-gen emit StoreKit / BillingClient boilerplate into an Open
      Banking client.
    correct_sets:
      send-money:      [has_ui, has_api, has_form_input]
      payment-consent: [has_ui, has_api]
      payment-status:  [has_ui, has_api]
    resulting_tier: "medium (has_ui and has_form_input are both medium) — NOT maximum"
    residual_gap: >-
      This framework has no capability meaning "moves real money over banking rails".
      has_payments covers store billing only. So PISP features land at the same medium tier
      as any ordinary form screen. That is a genuine framework gap worth raising on its own
      terms — but the fix is a new capability, never mis-declaring an existing one to borrow
      its tier.

  next_required:
    - "DECIDE: declare capabilities[] across the roster. This activates 147 dormant rules and will surface real findings — that is the point, but it is a deliberate step, not a side effect of a verify run."
    - "THEN re-run /idea-verify so last_verify reflects a full iteration"
    - "THEN /idea-approve — C1/C4/C5 become assertable at medium tier"

# ─── superseded 2026-07-30 record retained below ───────────────────────────
superseded_2026_07_30:
  note: "Prior /idea-heal record. Its findings_stale flag is resolved: findings ARE recomputed above."
checks_run:
  # /idea-verify Phase 1-2, partial pass. Recorded per-check so a reader knows exactly which
  # gates are evidence and which are still owed — an unqualified "features_evaluated: 23"
  # would overstate this.
  - { check: "P1.6 yaml-parse",        scope: "290 files", result: PASS, note: "269 clean; 21 maestro FAILs are FALSE POSITIVES — multi-doc YAML the checker cannot read" }
  - { check: "P2.9 nav-target",        scope: "22 targets", result: PASS, note: "0 dead; atm-locator/party matches were comment text, not live targets" }
  - { check: "P2.11 i18n-coverage",    scope: "519 tokens", result: PASS, note: "547 declared leaf keys, 0 missing (real YAML parse, not grep)" }
  - { check: "P2.13 action-contract",  scope: "411 blocks", result: FAIL, note: "1 violation + 6 unevaluated — see findings" }
  - { check: "P2.10 flow-transitions", scope: "35 nav targets", result: PASS, note: "PASS after 2 fixes this run — was 2 findings, both stale idea->source nav in home" }
  - { check: "P2.12 demo-data",        scope: "23 features", result: PASS }
  - { check: "P2.14 flow-entry",       scope: "23 features", result: PASS, note: "no entry_point references an undeclared state" }
  - { check: "P2.15 orphan-screen",    scope: "23 screens", result: PASS, note: "home/settings reachable via app-shell bottom nav (:29,:65); consent-callback via flows/onboarding-consent.yaml — OR-1 counts nav OR flow" }

fixes_applied_this_run:
  # /idea-heal applying check-10 findings inline. Both were the SAME class: home's ui.yaml
  # declaring navigation that source does not have, left behind by the 2026-07-28 reverse sync.
  - at: "2026-07-30"
    file: "screens/home/ui.yaml"
    was: "hero balance card on_click: navigate_account_detail -> target: account-detail (effect: navigate)"
    now: "on_click: open_account_selector (effect: transform_state)"
    evidence: "HomeContent.kt:63 onClick = onOpenAccountSelector · HomeScreen.kt:50 HomeAction.OpenAccountSelector · zero onNavigateToAccountDetail in feature/home"
    note: >-
      The file already contradicted itself: its own account_selector_sheet comment recorded
      that HomeContent.kt:63 wires the hero card to the selector, but the hero card's on_click
      still said navigate. The 07-30 stale-artifact pass reshaped the switcher and documented
      the trigger without updating the trigger — a half-applied fix.
  - at: "2026-07-30"
    file: "screens/home/ui.yaml + _strings/strings.yaml"
    was: "connect_bank_button -> navigate_login -> target: login"
    now: "component removed; home.empty.body retargeted to Settings -> Consents"
    evidence: "HomeEmpty.kt:37 'Points the PSU to Settings rather than offering its own connect button'; file has no Button and no onClick; homeGraph exposes only 2 callbacks, neither to login"
    note: >-
      Strings home.empty.cta + home.empty.cta.accessibility swept in the SAME edit. i18n
      re-verified after: 545 declared / 517 referenced / 0 missing (was 547/519 — exactly the
      2 removed). Swept together deliberately: the 07-30 quick_actions_card removal orphaned
      9 keys by not sweeping, and the next sync had to catch them.
overall: "pass"

# ─── 2026-07-30 /idea-heal run scope (read this before trusting the counts) ───
# This run settled the deferred_by_design decision and repaired stale state; it
# did NOT re-seed findings. The last actual feature evaluation was the 07-28
# reverse sync against a 20-feature roster — three PISP screens (send-money,
# payment-consent, payment-status) have been added since and have NEVER been
# evaluated by /idea-verify. `findings: []` therefore means "not recomputed",
# NOT "verified clean". A full pass (23 features x 7 siblings) is still owed.
findings_stale: true
findings_stale_reason: >-
  Roster grew 20 -> 23 after the last evaluation; the three PISP features are
  unevaluated. Re-seed with /idea-verify before treating findings[] as truth.

# ─── RESET 2026-07-28 ───────────────────────────────────────────────────
# The prior file carried 44 findings (5 open) against a 25-feature idea-layer
# that no longer exists. Every open finding was either resolved by the reverse
# sync or removed with its feature:
#
#   pfm-dashboard / sub-action-leaf-missing   -> feature deleted (never built)
#   4 x stitch_coverage (statements, login,   -> stitch artifacts deleted;
#     accounts prompt_sha, PROJECTS_INDEX)       generation deferred
#
# Findings are recomputed by /idea-sync and /idea-verify against the current
# 20-feature roster. Nothing is carried forward.

summary:
  total_findings: 3
  open_findings: 3
  closed_findings: 0
  by_gap_class:
    fail: 2
    warn: 1
    advisory: 0
  dto_findings: 0
  stitch_findings: 0
  capability_completeness_findings: 0

findings:
  # ═══════════════════════════════════════════════════════════════════════════
  # 2026-08-02 /idea-heal — the 23 WR-1 + KM-1 rows seeded here for the drain are
  # ALL CLOSED and have been removed from this list rather than left as open
  # findings. Detail: run_2026_08_02_heal above. Re-verified after the drain by
  # running the gates, not by trusting the cluster reports:
  #   G-IDEA-WR PASS · G-IDEA-KM PASS · G-IDEA-SCHEMA-CLOSURE all 9 sub-checks green.
  # Every value written came from the feature's own api.yaml#endpoints[] +
  # data-flow.yaml, never invented — each writer reported its source per finding.
  # ═══════════════════════════════════════════════════════════════════════════

  # ─────────────────────────────────────────────────────────────────────────
  # Seeded 2026-07-30 by /idea-heal --feature scheduled-payments via the engine's
  # verify-stage discovery producer (_shared/idea-gap-discovery.md). Discovery ran
  # INLINE per STEP D0's fallback (Read tool only, no sub-agent dispatch available
  # this session). Passed the STEP D4 evidence gate: every cited path resolves and
  # each anchor was re-read with the Read tool to confirm the claim is grounded.
  #
  # NOT found by /idea-verify, and worth recording why: verify's states check is only
  # "ui.yaml#states[] non-empty" (idea-verify capsule Phase 1 step 8). scheduled-
  # payments declares four states, so it PASSES. No deterministic check compares
  # declared states across a feature cluster — which is exactly the class the
  # discovery producer exists to catch, and why the 07-28 back-fill could skip one
  # feature silently. See promotion_candidates below.
  - rule_id: RULE-IDEA-GAP-DISCOVERY-001
    gap_class: discovered-missing-state-vs-cluster-siblings
    severity: error
    evidence: "file:idea-layer/screens/scheduled-payments/ui.yaml#states"
    evidence_refs:
      - "file:idea-layer/screens/scheduled-payments/ui.yaml:15  -> states: [loading, content, empty, error]"
      - "file:idea-layer/screens/direct-debits/ui.yaml:15        -> states: [loading, content, empty, unsupported, error]"
      - "file:idea-layer/screens/standing-orders/ui.yaml:15      -> states: [loading, content, empty, unsupported, error]"
      - "file:idea-layer/screens/direct-debits/ui.yaml:252-263   -> unsupported_direct_debits component block to mirror"
      - "file:idea-layer/screens/direct-debits/ui.yaml:46        -> records that direct-debits' unsupported state was BACK-FILLED from source; 'the idea-layer never declared it'"
    feature: scheduled-payments
    screen: scheduled-payments
    source: gap-discovery
    scope: cross-feature
    discovery_id: "DISCOVERY:scheduled-payments:missing-unsupported-state"
    confidence: 95
    removal_tier: high
    delegated_to: "/idea-feature-enrich"
    recurrence: 1
    # RECLASSIFIED 2026-08-02 by /idea-sync (source-truth reverse pass), from
    # `partially-resolved` (an OPEN idea-layer defect) to `source-owed` (a CLOSED idea-layer
    # obligation with an open SOURCE obligation). The distinction matters: nothing further is
    # owed on the idea side — the state, its component block, strings, preview and test all
    # exist and are correct. What remains is a source change, and counting it as an open
    # idea-layer finding made every verify run report a defect against a file that is right.
    #
    # The user's 2026-08-02 direction was source-is-truth. Applied literally this state would
    # have been PRUNED to match source. It is deliberately kept instead, because source itself
    # already records the refusal (BankingStores.kt:377 recordIfUnsupported) and only fails to
    # render it — so the idea-layer is documenting a half-built product gate, not an invention.
    # Deleting it would erase the specification of a live user-visible defect.
    status: source-owed
    owed_by: /implement
    idea_layer_obligation: CLOSED
    source_obligation: OPEN
    declared_in: "screens/scheduled-payments/ui.yaml#spec_ahead_of_source"
    source_evidence_reverified_2026_08_02:
      - "core/data/.../banking/store/BankingStores.kt:377 — scheduledPaymentsStore calls result.error.recordIfUnsupported(endpoint = AccountEndpoint.ScheduledPayments), same as directDebitsStore:234 and standingOrdersStore:344"
      - "feature/scheduled-payments/.../ui/ScheduledPaymentsViewModel.kt:64 — routes EVERY ScreenState.Error to classifyScheduledPaymentsError(); no isUnsupportedForProduct() pre-check, no Unsupported member on ScheduledPaymentsUiState"
      - "feature/direct-debits/.../ui/DirectDebitsViewModel.kt:64 — the shape to mirror, guarded ahead of classification"
      - "core/data/.../util/ObieErrorCodes.kt:35,39 — isUnsupportedForProduct() helpers already exist; nothing new is needed below the ViewModel"
    resolution_log:
      - at: "2026-07-30"
        by: "/idea-feature-enrich-loop --feature scheduled-payments"
        step: 1
        done: >-
          idea-layer state declared. ui.yaml#states now [loading, content, empty,
          unsupported, error]; unsupported_scheduled_payments empty_state block added
          between the empty and error blocks (mirrors direct-debits/ui.yaml:251-268,
          adapted to this feature's flat sp_* string convention); strings
          sp_unsupported_title + sp_unsupported_a11y added. All three touched files
          parse; both new keys resolve 1:1 so i18n parity holds.
        delegate_note: >-
          The engine's declared delegate /idea-feature-enrich was NOT invocable this
          session (harness "Unknown skill" — root-caused to its SKILL.md `arguments:`
          map, since removed; registration only refreshes in a NEW session). Ran the
          in-family /idea-feature-enrich-loop instead, whose runtime IS the Steps C-G
          sub-file of idea-feature-enrich. Its 882-line detail body was NOT loaded —
          remaining context could not hold it AND complete the mutation safely, so the
          change was confined to the shape this finding already specifies.
      - step: 2
        status: OPEN
        what: "Declare the U000 refusal in api.yaml for ALL THREE gated features (scheduled-payments, direct-debits, standing-orders). U000 is still modelled nowhere in the idea-layer — the state exists, the condition producing it does not."
      - step: 3
        status: OPEN
        what: "Source, via /implement (never hand-patched first): Unsupported(message) on ScheduledPaymentsUiState + the error.isUnsupportedForProduct() branch ahead of classification, mirroring DirectDebitsViewModel.kt:64, + ScheduledPaymentsUnsupported.kt."
    summary: >-
      scheduled-payments omits the `unsupported` state that both its capability-gated
      cluster siblings declare, so a product-refusal (OBIE U000) has no state to land in.
    detail: >-
      All three of direct-debits, standing-orders and scheduled-payments are product-gated
      — each one's store fetcher calls recordIfUnsupported() on a U000 refusal before
      rethrowing (source BankingStores.kt:234, :344, :377). Only the first two declare an
      `unsupported` state, and only the first two implement one.

      The 2026-07-28 reverse sync back-filled `unsupported` into direct-debits and
      standing-orders FROM source (their ui.yaml:46 says so explicitly) and MISSED
      scheduled-payments. It is therefore the one feature where the state is absent from
      BOTH layers: no `unsupported` in ui.yaml#states, and zero Unsupported files in
      feature/scheduled-payments/.

      User-visible consequence: source classifyScheduledPaymentsError() has no U000 branch,
      so the refusal falls to `else -> ScheduledPaymentsError.NetworkError`, which that
      function's own KDoc documents as retriable. A PSU reaching this screen on a credit
      card (the product that cannot serve it) gets a Retry button that can never succeed.

      Root cause is UPSTREAM of both layers: `U000` is modelled NOWHERE in the idea-layer —
      not in scheduled-payments/api.yaml, and not even in direct-debits/api.yaml. The
      idea-layer carries the STATE but has never declared the CONDITION that produces it,
      which is what let a back-fill cover two features and skip the third undetected.
    fix_order:
      - "1. idea-layer (this finding, delegated): add `unsupported` to scheduled-payments/ui.yaml#states + the unsupported_scheduled_payments component block, state_binding, test_tag, accessibility_label and strings, mirroring direct-debits/ui.yaml:252-263."
      - "2. idea-layer (recommended, wider): declare the U000 refusal in api.yaml for all THREE gated features so the condition is specified, not just the state."
      - "3. source (follows via /implement, NOT hand-patched first): Unsupported(message) on the sealed interface + the error.isUnsupportedForProduct() branch ahead of classification, mirroring DirectDebitsViewModel.kt:64, + ScheduledPaymentsUnsupported.kt. Shared helpers isUnsupportedForProduct()/obieMessage() already exist in core/data/util."
    fix_order_rationale: >-
      Idea-layer first per RULE-GAP-IDEA-FIRST-001. Patching source alone would leave
      ui.yaml declaring four states — the idea<->source drift the framework exists to
      prevent — and the next /kmp-implement could regenerate the defect.

  # ── Seeded 2026-07-30 by /idea-verify Phase 2 check 13 (action-contract completeness) ──
  # Produced by the SHIPPED walker .claude-runtime/scripts/action-contract-walk.ts, run from
  # the project root — not by grep. Contradicts PIPELINE_STATE's 07-16 idea-verify record,
  # which claimed "G-ACTION-CONTRACT 0 violations (611 component blocks)". The walker now
  # scans 23 ui.yaml / 411 component blocks and reports 1 violation + 6 unevaluated.
  - rule_id: RULE-IDEA-ACTION-CONTRACT-001
    gap_class: action-contract-coverage-hole
    severity: error
    evidence: "file:idea-layer/screens/accounts/ui.yaml#components[filter_all]"
    evidence_refs:
      - "file:idea-layer/screens/accounts/ui.yaml -> filter_all, filter_current, filter_savings, filter_credit — all type=<none>"
      - "file:idea-layer/screens/home/ui.yaml -> view_all_transactions_link type=text_button"
      - "file:idea-layer/screens/settings/ui.yaml -> theme_row type=select"
    feature: accounts
    source: idea-verify-check-13
    scope: cross-feature
    discovery_id: "VERIFY:action-contract:unevaluated-types"
    confidence: 100
    removal_tier: low
    delegated_to: "/idea-feature-enrich"
    recurrence: 1
    status: resolved
    resolution:
      at: "2026-07-30"
      by: "/idea-heal (inline)"
      outcome: >-
        RESOLVED for the 4 real ones; the other 2 are BY DESIGN, not a defect — my original
        framing of them as "a framework fix" was wrong and is corrected here.
        (a) accounts' filter_all/current/savings/credit now declare `type: chip`, matching the
        convention account-detail's chips already use. Making them visible immediately exposed
        4 GENUINE AC3 violations that the coverage hole had been hiding — `effect:
        transform_state` with no library_refs — now fixed with external_library_refs:
        [kotlinx-coroutines]. That is the concrete proof the hole was masking real defects,
        not merely under-reporting.
        (b) home/view_all_transactions_link (text_button) and settings/theme_row (select) remain
        unevaluated DELIBERATELY. action-contract-walk.ts:32-37 documents the decision verbatim:
        the vocabulary omits text_button/segmented_button/etc. because "widening it is a
        rule-semantics change with cross-project blast radius, so it is deliberately NOT done
        here — see the gate's --report-untyped output". The untyped report IS the intended
        surfacing mechanism; it is working as designed. Retyping those two components to fit
        the tool would distort the spec to satisfy a gate, which is backwards.
      verified: "walker re-run: 414 blocks, AC3 violations 4 -> 0; unevaluated 6 -> 2 (both by design)"
    summary: >-
      6 on_click components fall outside the action-contract rule's interactive type
      vocabulary and are therefore NEVER evaluated — the walker labels this "a coverage
      hole, not a pass".
    detail: >-
      Four of the six (accounts' filter chips) declare NO `type:` at all. A component with an
      on_click but no type escapes RULE-IDEA-ACTION-CONTRACT-001 silently: it is neither
      passed nor failed, it is skipped. That is how a feature can report a clean
      action-contract gate while shipping unverified handlers. The other two declare types
      (`text_button`, `select`) that are real but absent from the rule's vocabulary.
      Two fixes are possible and they are NOT equivalent: (a) give the four accounts filters
      a real type, which is a project fix; (b) extend the rule's vocabulary to cover
      text_button and select, which is a FRAMEWORK fix affecting every project.
  - rule_id: RULE-IDEA-ACTION-CONTRACT-001
    gap_class: dc2-forbidden-token
    severity: warn
    evidence: "file:idea-layer/screens/account-detail/ui.yaml#components[chip_atm_locator]"
    feature: account-detail
    source: idea-verify-check-13
    scope: cross-file
    discovery_id: "VERIFY:account-detail:dc2-placeholder-token"
    confidence: 55
    removal_tier: low
    delegated_to: null
    recurrence: 1
    summary: >-
      AC5: chip_atm_locator's description carries the DC-2 forbidden token 'placeholder'.
    detail: >-
      LIKELY FALSE POSITIVE, recorded rather than actioned. The description legitimately
      names the SOURCE class PlaceholderScreen("ATM locator") — a real Compose destination in
      cmp-navigation — and the chip's target was deliberately retargeted to
      `_placeholder:atm-locator` when the atm-locator screen was deleted on 2026-07-28. The
      handover records this same substring class misfiring twice before ('TBD' matched inside
      the OBIE balance type 'ITBD'; 'placeholder' matched inside PlaceholderScreen). Confidence
      is 55 — below the RULE-AUTO-FIX-001 floor of 80 — so it carries NO delegated_to and must
      NOT be auto-fixed. The real fix is probably a framework one: DC-2 should match a token,
      not a substring, so a legitimate identifier containing 'placeholder' stops tripping it.

promotion_candidates:
  # STEP D6 — proposal only; discovery never edits the registry itself (human gate).
  - pattern: "capability-gated cluster siblings declaring divergent state sets"
    sample_evidence:
      - "file:idea-layer/screens/scheduled-payments/ui.yaml:15"
      - "file:idea-layer/screens/direct-debits/ui.yaml:15"
    recurrence: 1
    proposed_check: >-
      For features sharing a gating mechanism (a store fetcher calling
      recordIfUnsupported, or an AccountEndpoint capability row), assert their
      ui.yaml#states sets match, or that a divergence carries an explicit waiver.
      Would have caught this deterministically instead of relying on a discovery pass.

deferred_by_design:
  # Not findings — deliberately absent artifacts, recorded so a future run does
  # not raise them as regressions.
  #
  # DECISION 2026-07-30 (user, via /idea-heal): the visual tier is deferred to a
  # future cycle. This closes the "open decision" the 07-28 reset left on
  # stitch_coverage. Both classes below are SETTLED deferrals, not pending
  # questions — a verify/sync run must count them as satisfied, not absent.
  - class: stitch_coverage
    reason: >-
      Stitch MOCKUP generation deferred to a future cycle (user decision
      2026-07-30). Scope is mockups/{feature}/ — the Stitch-SDK visual output
      (stitch/{state}/code.html + screen.png) and STITCH_STATUS.yaml. NOT an
      open question; do not re-raise as a gap. Revisit only on an explicit
      request to run the visual tier.
    features: all
    decided_at: "2026-07-30"
    decided_by: user
    status: settled
    # SCOPE CORRECTED 2026-07-30 (user). An earlier revision of this entry claimed the
    # deferral also covered screens/*/prompts/{state}.md. That was the agent's inference,
    # NOT the user's decision — the user deferred "previews and stitch MOCKUP generation".
    # Per-state prompts are NOT deferred: they are local markdown design directives built
    # by .claude-runtime/scripts/stitch-prompt-build.ts with zero Stitch-SDK involvement.
    excludes:
      - "screens/*/prompts/{state}.md — NOT deferred; see open_no_command_path below"
    RESOLVED_2026_07_30:
      what: "95 per-state prompts built across 23/23 features — screens/{f}/prompts/{state}.md"
      how: >-
        deno run .claude-runtime/scripts/stitch-prompt-build.ts --workspace
        mifos-x/mifos-x-open-banking --all-features --all-states --force
      why_force: >-
        Every state failed STN7 ("inputs changed since last build — regenerate"), which is the
        cache-freshness gate doing its job: ui.yaml had changed since the last build. --force is
        the sanctioned way past a stale cache, not a gate bypass. With it, 95 written / 0 failed.
      no_sdk_involved: >-
        This is the BUILD stage only (capsule IFS-3). The Stitch SDK GENERATE stage was NOT run,
        so the deferred mockups were not produced — prompts and mockups stayed correctly separated.
      verified: "23/23 prompts/ dirs; 95 .md files; delimited '↓↓↓ MOCKUP PROMPT ↑↑↑' block present"
    superseded_open_no_command_path:
      note: "Kept for provenance — the gap below is what RESOLVED_2026_07_30 above closed."
      artifact: "idea-layer/screens/{feature}/prompts/{state}.md"
      problem: >-
        No command generates prompts alone. /idea-feature-stitch is a single 6-step
        pipeline (BUILD -> POLISH -> VALIDATE -> GENERATE -> VERIFY -> PREFLIGHT) whose
        own capsule states "One command, no flag toggles"; its --build-prompts flag is
        deprecated and IGNORED, so invoking it runs Stage 4 GENERATE against the Stitch
        SDK — i.e. it produces the very mockups that ARE deferred. /idea-feature-export
        --all excludes prompts by design (idea-feature-export.md:47). /idea-feature-render
        builds them only as a self-heal side effect of the preview run, which is deferred.
      only_local_path: >-
        .claude-runtime/scripts/stitch-prompt-build.ts — the script /idea-feature-stitch
        itself consumes at its BUILD stage (capsule IFS-3), so running it is that stage,
        not a hand-rolled substitute.
      recommendation: >-
        Sequence AFTER the /idea-verify re-seed and the enrich pass. Those edit multiple
        ui.yaml files (scheduled-payments already changed this session), and prompts are
        derived from ui.yaml — generating 23 x ~4 files first means regenerating them.
        Timing only; this is NOT a deferral.
  - class: preview_render
    reason: >-
      HTML preview generation (screens/*/preview/{state}.html) deferred to a
      future cycle (user decision 2026-07-30). Scope is the rendered HTML ONLY.
      The renderer's input, screens/*/prompts/{state}.md, is NOT deferred —
      corrected 2026-07-30; see stitch_coverage#excludes above.
    features: all
    decided_at: "2026-07-30"
    decided_by: user
    status: settled

# RESOLVED 2026-07-30 — the export_artifacts deferral recorded on 2026-07-28 is
# removed, not carried forward: /idea-feature-export --all regenerated SPEC.md +
# API.md for 23/23 features (69 files incl. .meta.yaml sidecars). The artifacts it
# described as absent now exist, so retaining the entry would suppress a real
# regression if they went missing again.
