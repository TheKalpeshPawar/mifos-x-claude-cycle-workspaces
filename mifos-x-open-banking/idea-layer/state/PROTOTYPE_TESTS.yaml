# PROTOTYPE_TESTS.yaml — Navigation & Interaction Audit Results
# Written by /idea sync STEP 1 (verify) on 2026-06-12T00:00:00Z
# Project: mifos-x/mifos-x-open-banking
# Screens tested: 39 (post-HSBC migration + consent/redirect enrichment — consumer Open Banking only)

version: 1
ran_at: "2026-06-12T00:00:00Z"

passes:
  navigation:
    total: 39
    passed: 39
    failed: []
    note: "All on_click nav targets resolve to valid screen IDs (34 distinct screen-shaped targets, 0 dead destinations cross-checked against the 39-screen roster). Non-screen targets (account_picker_sheet, ddd_cancel_dialog, soc_* form fields, chart periods, view_consent_detail, etc.) are local UI dialogs/state actions — not broken nav. External URL targets in about/ are browser-opens, not screen nav."

  interaction:
    total: 39
    passed: 36
    failed: []
    advisory:
      - beneficiaries: "weak-typed target:\"\" on read-only AIS rows (add/edit not supported under Open Banking) — advisory, not a defect."
      - products: "weak-typed target:\"\" on product-compare chips — advisory."
      - send-money-confirm: "weak-typed target:\"\" on review-row taps — advisory (shipped/approved)."
    note: "0 draft screens remain — all 8 HSBC consent/redirect screens enriched this session (draft→enriched). 3 advisory weak-typed clicks unchanged (known-advisory, not blocking)."

  states:
    total: 39
    passed: 39
    failed: []
    note: "Every screen declares states matching idea-plan §10. The 8 enriched consent/redirect screens now carry their state models (consent-request idle/submitting/error/content/empty; bank-authorize-handoff preparing/redirecting/error; auth-callback exchanging/success/error; consent-intro/declined/expired content; payment-authorize-handoff preparing/redirecting/error; payment-declined content)."

  api:
    total: 39
    passed: 39
    failed: []
    note: "SKIP-by-policy — backend.owned=false (external HSBC sandbox). Server connectivity + owned-DB checks are expected to be absent and are not failures per project policy. All api.yaml entries are client-side OBIE contracts (mTLS + detached JWS), not owned-backend RPC. 30 screens carry api.yaml; 9 are static/gate screens (about, settings, splash + the 6 newly-enriched have api.yaml now except the 4 pure-redirect screens whose api is the OBIE token/consent contract)."

  reachability:
    total: 39
    passed: 35
    failed: []
    advisory:
      - consent-expired: "No screen's connections include consent-expired as a 'to'. Reached programmatically per §9 FR-005 (prior consent EXPD/RJCT/revoked → re-consent) + app-main.yaml splash→consent-expired. WARN — advisory, intentional (no fake incoming edge)."
      - notifications: "No flow connection lists notifications as 'to'. Reachable via app-shell top-bar bell + §9 FR-023 event feed (verify-checker blind spot for app-shell chrome). WARN — advisory."
      - standing-order-edit: "PISP standing-order create/edit wiring (§9 FR-044, ties to standing-order-create). consumer-banking.yaml DOES list standing-order-detail→standing-order-edit, so it IS reachable in the flow graph; flagged advisory pending the PISP edit-flow decision. WARN — advisory/deferred."
      - "5 unbound OBIE endpoint groups": "VRP / International-Scheduled / International-Standing-Order / File / Multi-Bill have screens:[] in api_manifest — NEW-capability candidates (step-G goal track), not reachability defects. WARN — advisory."
    note: "consent-manager reachability RESOLVED this session — settings→consent-manager edge (FR-022 'Data & Consent' row) added to consumer-banking.yaml + settings/ui.yaml. Remaining advisory warnings are intentional/deferred, none blocking. Entry point: splash."

summary:
  total_passes: 5
  total_checks: 195
  total_failures: 0
  total_warnings: 7
  blocking: false
  note: "Zero blocking failures. 0 draft screens (all enriched). 7 advisory warnings (3 weak-typed clicks + 3 reachability + 1 unbound-OBIE-groups class). consent-manager UI-orphan resolved. All known-advisory per the HSBC migration reconcile."
