schema_version: "1.0.0"
generated_at: "2026-07-28T00:00:00Z"
command: "/gap-analysis-project (reverse sync — validation state reset)"
actor: claude
scope:
  workspace: mifos-x
  project: mifos-x-open-banking
  screens_count: 20
  categories_requested: [all]

# ─── RESET 2026-07-28 ───────────────────────────────────────────────────
# The prior record scored a 25-screen corpus (REG-001 component types, WCAG AA
# contrast pairs, STATE-001 state composition, RENDER-STALE). All four inputs
# changed in the reverse sync: 6 screens deleted, every ui.yaml contract rewritten,
# and the rendered previews it checked against were removed entirely.
#
# It also recorded that 14 of ~20 declared rules could not execute at all
# (validation-rules.json v2.0.0 checks fields that exist in no schema and no
# ui.yaml). That limitation is unchanged and is NOT resolved by this sync —
# it is a framework-side gap, carried forward here so it is not lost.

validated: false
reason: "Awaiting re-run — the screen corpus and every ui.yaml contract were rewritten on 2026-07-28."

carried_forward_framework_gap:
  finding: >-
    validation-rules.json is v2.0.0 / lastUpdated 2026-02-10 and checks fields
    (keyboard.focusable, label_source, supported_states, motion.*, breakpoints)
    that exist neither in core/schemas/ui-yaml.schema.json nor in any project
    ui.yaml. Corpus size is self-inconsistent: runtime reports 102 rules,
    _metadata says 147, the categories block holds 67.
  consequence: "14 of ~20 declared rules cannot execute. Any score is partial-corpus."
  owner: framework
  first_recorded: "2026-07-16"

results: []
