# /idea verify --rule RULE-DTO-REGISTRY-001
**Project:** mifos-x/mifos-x-open-banking
**Run at:** 2026-05-30T00:00:00Z
**Rule:** RULE-DTO-REGISTRY-001 v1.0.0
**Overall:** FAIL

---

## Summary

| Sub-check | Scope | Status | Count |
|-----------|-------|--------|-------|
| D1-REGISTRY-PRESENT | project | ✓ PASS | 45 DTOs registered |
| D2-EVERY-CONSUMER-REF-RESOLVES | feature | ✗ FAIL | 17 unregistered type refs across 12 features |
| D3-NO-DUPLICATE-NAMES | project | ✓ PASS | no duplicates |
| D4-NO-SHADOW-OF-SHARED | project | ✓ PASS | no shadows |
| D5-USED-BY-FRESH | feature | ⚠ WARN | all 45 DTOs have empty used_by[] |
| D6-ORIGIN-MATCHES-SERVER | feature | ✓ PASS | no table/rpc/rest origin DTOs declared |
| D7-REPLACED-BY-RESOLVES | feature | ✓ PASS | no replaced_by fields present |

**Checks run:** 7 · **Pass:** 4 · **Fail:** 2 (D2 + D5-warn) · **Warn:** 1

---

## D1 — REGISTRY-PRESENT: PASS

`idea-layer/dtos/` exists with 45 DTO files. Registry last updated 2026-05-28.

---

## D2 — EVERY-CONSUMER-REF-RESOLVES: FAIL

17 PascalCase `type:` references in `api.yaml` files have no matching `idea-layer/dtos/{Name}.yaml` and are not in `templates/shared/common-dtos.yaml`.

One explicit `dto:` ref is also unresolved:
- `standing-order-edit/api.yaml` → `dto: StandingOrderUpdateRequest` — **no file at `idea-layer/dtos/StandingOrderUpdateRequest.yaml`**

### Violations

| # | Unregistered Type | Feature(s) | File | Error Code |
|---|-------------------|-----------|------|-----------|
| 1 | `AccountInfo` | pfm-dashboard | api.yaml `/api[2].response.fields[2].type` | `DTO_REF_UNKNOWN` |
| 2 | `Amount` | pfm-dashboard, customer-detail, send-money, fo-dashboard | api.yaml | `DTO_REF_UNKNOWN` |
| 3 | `AmountOfMoney` | pfm-dashboard | api.yaml `/api[2].response.fields[4].type` | `DTO_REF_UNKNOWN` |
| 4 | `CardReplacement` | card-detail | api.yaml `/api[0].response.fields[14].type` | `DTO_REF_UNKNOWN` |
| 5 | `Challenge` | send-money, send-money-confirm | api.yaml | `DTO_REF_UNKNOWN` |
| 6 | `Charge` | send-money-confirm, send-money | api.yaml | `DTO_REF_UNKNOWN` |
| 7 | `Comment` | transaction-detail | api.yaml `/api[0].response.fields[6].children[1].type` | `DTO_REF_UNKNOWN` |
| 8 | `ConsentRedirect` | consent-manager | api.yaml `/api[0].response.fields[6].type` | `DTO_REF_UNKNOWN` |
| 9 | `CustomerAccountLink` | customer-detail | api.yaml `/api[2].response.fields[0].type` | `DTO_REF_UNKNOWN` |
| 10 | `DirectDebitCounterparty` | direct-debits | api.yaml `/api[1].request.fields[2].type` | `DTO_REF_UNKNOWN` |
| 11 | `Payment` | direct-debit-detail | api.yaml `/api[0].response.fields[12].type` | `DTO_REF_UNKNOWN` |
| 12 | `ProductDetails` | products | api.yaml `/api[0].response.fields[8].type` | `DTO_REF_UNKNOWN` |
| 13 | `ProductMeta` | products | api.yaml `/api[0].response.fields[9].type` | `DTO_REF_UNKNOWN` |
| 14 | `StandingOrderSchedule` | standing-orders | api.yaml `/api[0].response.fields[9].type` | `DTO_REF_UNKNOWN` |
| 15 | `StandingOrderUpdateRequest` | standing-order-edit | api.yaml `request.dto` | `DTO_REF_UNKNOWN` |
| 16 | `Tag` | transaction-tags, transaction-detail | api.yaml | `DTO_REF_UNKNOWN` |
| 17 | `User` | pfm-dashboard, transaction-tags, forgot-password, change-password, profile | api.yaml | `DTO_REF_UNKNOWN` |

**Fix options per violation:**
- **Create** the missing `idea-layer/dtos/{Name}.yaml` (most cases — these are legitimate domain types)
- **Remap** the consumer ref to an existing registered DTO if it is a rename/alias
- Run `/idea generate-dtos {Name}` to bootstrap a new DTO file

**Priority order** (most consumers first):
1. `Amount` (4 features) — likely alias of `MoneyAmount`; confirm and remap or create
2. `User` (5 features) — confirm vs `UserProfile`; likely remap
3. `Challenge` / `Charge` (2 features each) — payment flow types; create new DTOs
4. `StandingOrderUpdateRequest` — explicit `dto:` ref; blocking for standing-order-edit `/implement`
5. Remaining single-feature types

---

## D3 — NO-DUPLICATE-NAMES: PASS

No duplicate `name:` declarations across the 45 DTO files.

---

## D4 — NO-SHADOW-OF-SHARED: PASS

None of `ErrorEnvelope`, `PaginationCursor`, `PaginatedEnvelope`, `AuthSession` shadowed by project DTOs.

---

## D5 — USED-BY-FRESH: WARN

All 45 DTO files have `used_by: []` (empty). Back-references are not populated. This is WARN-severity (auto-fixed, non-blocking).

**Fix:** `/idea generate-dtos --refresh-back-refs`

---

## D6 — ORIGIN-MATCHES-SERVER: PASS

No DTOs declare `source.origin` of `table`, `rpc`, or `rest`. All project DTOs use `origin: project`. No server-layer cross-check required.

---

## D7 — REPLACED-BY-RESOLVES: PASS

No `replaced_by:` declarations in any DTO file.

---

## Fix Commands

```bash
# Auto-fixable (non-blocking):
/idea generate-dtos --refresh-back-refs          # fixes D5 used_by drift

# Requires judgment (D2 violations):
# Option A — remap likely aliases:
#   Amount       → MoneyAmount  (check field shape; update all consumers)
#   User         → UserProfile  (check field shape; update all consumers)

# Option B — create missing DTOs (for net-new types):
/idea generate-dtos AccountInfo
/idea generate-dtos CardReplacement
/idea generate-dtos Challenge
/idea generate-dtos Charge
/idea generate-dtos Comment
/idea generate-dtos ConsentRedirect
/idea generate-dtos CustomerAccountLink
/idea generate-dtos DirectDebitCounterparty
/idea generate-dtos Payment
/idea generate-dtos ProductDetails
/idea generate-dtos ProductMeta
/idea generate-dtos StandingOrderSchedule
/idea generate-dtos StandingOrderUpdateRequest   # critical — explicit dto: ref, blocks /implement
/idea generate-dtos Tag
/idea generate-dtos AmountOfMoney

# After fixes, re-verify:
/idea verify --rule RULE-DTO-REGISTRY-001
```
