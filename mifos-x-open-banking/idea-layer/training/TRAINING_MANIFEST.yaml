# TRAINING_MANIFEST.yaml — index for mifos-x-open-banking project training context.
#
# Written alongside TRAINING_MASTER.yaml by /idea-sync STEP 0.TRAIN.GEN (2026-07-31).
# Read this to find a section; read TRAINING_MASTER.yaml to consume it.

schema_version: "1.0.0"
project: mifos-x-open-banking
generated_at: "2026-07-31T19:50:00Z"
generated_by: "/idea-sync STEP 0.TRAIN.GEN"

master: TRAINING_MASTER.yaml

sections:
  - id: component_inventory
    covers: "414 typed components across 23 ui.yaml; per-type frequency; 3-tier registry rules; action_contract obligations + 2 known coverage holes"
    read_before: "authoring or editing any screens/*/ui.yaml component block"
  - id: state_patterns
    covers: "canonical loading/content/error/empty baseline + the OBIE consent and payment journey states"
    read_before: "adding a screen state or a preview render"
  - id: api_patterns
    covers: "external-consumer posture (no owned backend/DB), 28 GET / 5 POST / 1 DELETE, AISP+PISP role split"
    read_before: "editing screens/*/api.yaml or idea-layer/server/apis/*"
  - id: design_tokens
    covers: "M3 Trust Blue, seed #266489, 29 roles per mode, Roboto + Roboto Mono; DESIGN.md is a SUBSET of design-tokens.yaml"
    read_before: "any mockup, preview render, or theme comparison"
  - id: navigation_graph
    covers: "23 nodes / 80 edges / 9 flows; bottom-nav and flow-only reachability conventions"
    read_before: "adding navigation or running an orphan-screen check"
  - id: tech_stack
    covers: "KMP + Compose Multiplatform, stream-first (BaseViewModel + Store5), Koin, Ktorfit, Room; template-derived module policy"
    read_before: "any /implement or /kmp-implement dispatch"

framework_companion:
  path: training-layer/TRAINING_MASTER.yaml
  note: >-
    RULE-TRAINING-001 requires BOTH reads before a project-layer Write/Edit: this file
    for project intelligence, the framework file for generic patterns. Project wins on
    conflict for project-specific facts; framework wins on generic architecture.

regeneration:
  trigger: "/idea-sync STEP 0.TRAIN.GEN — regenerates when absent or under 500 bytes"
  note: >-
    Counts here are a measurement, not a contract. They go stale as the idea-layer
    changes; re-run /idea-sync after any material roster or component change.
