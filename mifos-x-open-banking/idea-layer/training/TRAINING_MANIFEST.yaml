# TRAINING_MANIFEST.yaml — source pointer for project training intelligence
# Companion to TRAINING_MASTER.yaml per RULE-TRAINING-MASTER-001.
# Generated 2026-05-22 (empty-slate scaffold).

schema_version: "1.0"
generated_at: "2026-05-22"
project: mifos-x-open-banking

# ─── Source pointers ──────────────────────────────────────────────────────────
sources:
  - id: source_scan
    type: kotlin_source
    path: "source/mifos-x-open-banking/"
    scanned_at: "2026-05-20"
    scanner: "/idea-plan --from-source (4-agent deep import)"
    artifacts_produced:
      - idea-plan.yaml
      - screens/home/ui.yaml
      - screens/profile/ui.yaml
      - screens/settings/ui.yaml
      - screens/splash/ui.yaml

  - id: design_system_scaffold
    type: manual_scaffold
    path: "idea-layer/design-system/"
    created_at: "2026-05-22"
    artifacts_produced:
      - design-system/design-tokens.yaml
      - design-system/DESIGN.md
      - design-system/COMPONENTS.md
      - design-system/app-shell.yaml

# ─── Training master file ─────────────────────────────────────────────────────
master_file: training/TRAINING_MASTER.yaml

# ─── Growth policy (per RULE-TRAINING-EVOLVE-001) ────────────────────────────
growth:
  auto_grow: true
  triggers:
    - /idea-enrich (STEP FINAL.TRAIN)
    - /implement (new pattern detected)
    - /idea sync (STEP FINAL.TRAIN)
  policy: "Append new patterns; never overwrite existing intelligence."
