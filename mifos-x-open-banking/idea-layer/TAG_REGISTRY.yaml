# TAG_REGISTRY.yaml — mifos-x-open-banking analytics tag catalog
# Generated by /idea sync STEP 4 (2026-06-30).
# Source: screens/*/ui.yaml on_click actions + screens/*/docs.yaml vm_actions + flows/*.yaml connections.
# Consumer: analytics event tracking layer (Firebase Analytics / Mixpanel wrapper).
# Schema: tags[]{name, screen, trigger, description}

schema_version: "1.0"
project: "mifos-x-open-banking"
generated: "2026-06-30"
total_tags: 68

tags:

  # ── Screen View Events (one per screen, always fired on enter) ──────────────

  - name: screen_view_user_onboarding
    screen: user-onboarding
    trigger: screen_enter
    description: "PSU lands on the 3-step AISP onboarding pager."

  - name: screen_view_login
    screen: login
    trigger: screen_enter
    description: "PSU reaches the OAuth/FAPI consent-staging screen showing 11 requested permissions."

  - name: screen_view_consent_callback
    screen: consent-callback
    trigger: screen_enter
    description: "App receives FAPI deep-link callback from HSBC after SCA."

  - name: screen_view_accounts
    screen: accounts
    trigger: screen_enter
    description: "PSU views the authorised accounts balance list (primary bottom-nav tab 0)."

  - name: screen_view_account_detail
    screen: account-detail
    trigger: screen_enter
    description: "PSU opens a single account detail with balances and action chips."

  - name: screen_view_product
    screen: product
    trigger: screen_enter
    description: "PSU views product terms (credit interest AER, overdraft EAR) for an account."

  - name: screen_view_party
    screen: party
    trigger: screen_enter
    description: "PSU views account holder / PSU details from OBIE /party + /parties endpoints."

  - name: screen_view_atm_locator
    screen: atm-locator
    trigger: screen_enter
    description: "PSU opens the ATM locator (HSBC Open Data, no AIS consent required)."

  - name: screen_view_transactions
    screen: transactions
    trigger: screen_enter
    description: "PSU views transaction history for an account (bottom-nav tab 1 or from account-detail)."

  - name: screen_view_transaction_detail
    screen: transaction-detail
    trigger: screen_enter
    description: "PSU opens a single transaction detail card."

  - name: screen_view_consent_list
    screen: consent-list
    trigger: screen_enter
    description: "PSU opens the consent management dashboard (bottom-nav tab 2)."

  - name: screen_view_consent_detail
    screen: consent-detail
    trigger: screen_enter
    description: "PSU opens a specific consent — permissions, expiry, revoke action."

  - name: screen_view_standing_orders
    screen: standing-orders
    trigger: screen_enter
    description: "PSU views standing orders list for an account."

  - name: screen_view_direct_debits
    screen: direct-debits
    trigger: screen_enter
    description: "PSU views direct debit mandate list for an account."

  - name: screen_view_scheduled_payments
    screen: scheduled-payments
    trigger: screen_enter
    description: "PSU views future-dated scheduled payments for an account."

  - name: screen_view_beneficiaries
    screen: beneficiaries
    trigger: screen_enter
    description: "PSU views saved payees (beneficiaries) list for an account."

  - name: screen_view_statements
    screen: statements
    trigger: screen_enter
    description: "PSU views the chronological statement list for an account."

  - name: screen_view_statement_detail
    screen: statement-detail
    trigger: screen_enter
    description: "PSU opens a single statement period with balances, fees, and transactions."

  - name: screen_view_pfm_dashboard
    screen: pfm-dashboard
    trigger: screen_enter
    description: "PSU views PFM overview: net worth, period spending vs income, top categories."

  - name: screen_view_spending_by_category
    screen: spending-by-category
    trigger: screen_enter
    description: "PSU views spending breakdown by category for a selected period."

  - name: screen_view_budgets
    screen: budgets
    trigger: screen_enter
    description: "PSU views and manages monthly category budgets with progress bars."

  - name: screen_view_recurring_subscriptions
    screen: recurring-subscriptions
    trigger: screen_enter
    description: "PSU views auto-detected recurring subscriptions and payments."

  - name: screen_view_settings
    screen: settings
    trigger: screen_enter
    description: "PSU opens app settings (bottom-nav tab 3 / More)."

  - name: screen_view_profile
    screen: profile
    trigger: screen_enter
    description: "PSU views connected identity from OBIE /party endpoint plus consent summary."

  # ── Onboarding / OAuth Flow Actions ────────────────────────────────────────

  - name: onboarding_step_next
    screen: user-onboarding
    trigger: on_click
    description: "PSU taps Next on onboarding pager (step 1→2 or step 2→3)."

  - name: onboarding_step_back
    screen: user-onboarding
    trigger: on_click
    description: "PSU taps Back on onboarding pager (step 2→1 or step 3→2)."

  - name: onboarding_ob_explainer_open
    screen: user-onboarding
    trigger: on_click
    description: "PSU opens the FAPI journey plain-English explainer bottom sheet."

  - name: start_oauth
    screen: login
    trigger: on_click
    description: "PSU taps 'Continue to HSBC' — triggers POST /account-access-consents + FAPI authorize redirect."

  - name: consent_authorised
    screen: consent-callback
    trigger: auto
    description: "Token exchange succeeded and /account-access-consents status = Authorised."

  - name: consent_rejected
    screen: consent-callback
    trigger: auto
    description: "PSU rejected consent at HSBC or token exchange failed (OAuth error redirect)."

  - name: consent_state_mismatch
    screen: consent-callback
    trigger: auto
    description: "FAPI state parameter mismatch detected on callback — security error surfaced."

  # ── Accounts Screen Actions ─────────────────────────────────────────────────

  - name: tap_account_card
    screen: accounts
    trigger: on_click
    description: "PSU taps an account card to open account-detail."

  - name: consent_expiry_warning_shown
    screen: accounts
    trigger: auto
    description: "Consent expiry banner shown because days_remaining < 30."

  - name: tap_reconfirm_consent
    screen: accounts
    trigger: on_click
    description: "PSU taps the reconfirm CTA on the expiry warning banner → navigates to consent-detail."

  # ── Account Detail Actions ──────────────────────────────────────────────────

  - name: tap_transactions_chip
    screen: account-detail
    trigger: on_click
    description: "PSU taps the Transactions action chip → navigates to transactions."

  - name: tap_statements_chip
    screen: account-detail
    trigger: on_click
    description: "PSU taps the Statements action chip → navigates to statements."

  - name: tap_standing_orders_chip
    screen: account-detail
    trigger: on_click
    description: "PSU taps the Standing Orders chip → navigates to standing-orders."

  - name: tap_direct_debits_chip
    screen: account-detail
    trigger: on_click
    description: "PSU taps the Direct Debits chip → navigates to direct-debits."

  - name: tap_scheduled_payments_chip
    screen: account-detail
    trigger: on_click
    description: "PSU taps the Scheduled chip → navigates to scheduled-payments."

  - name: tap_beneficiaries_chip
    screen: account-detail
    trigger: on_click
    description: "PSU taps the Beneficiaries chip → navigates to beneficiaries."

  - name: tap_product_chip
    screen: account-detail
    trigger: on_click
    description: "PSU taps the Product chip → navigates to product terms screen."

  - name: tap_party_chip
    screen: account-detail
    trigger: on_click
    description: "PSU taps the Party chip → navigates to account holder detail screen."

  - name: tap_atm_locator
    screen: account-detail
    trigger: on_click
    description: "PSU taps ATM Locator → navigates to atm-locator screen."

  - name: retry_load
    screen: account-detail
    trigger: on_click
    description: "PSU taps Retry on a recoverable error state."

  # ── Transactions Actions ─────────────────────────────────────────────────────

  - name: filter_transactions_credit
    screen: transactions
    trigger: on_click
    description: "PSU taps the Credit filter chip to show only credit transactions."

  - name: filter_transactions_debit
    screen: transactions
    trigger: on_click
    description: "PSU taps the Debit filter chip to show only debit transactions."

  - name: open_date_range_picker
    screen: transactions
    trigger: on_click
    description: "PSU opens the date-range chip to set fromBookingDateTime and toBookingDateTime."

  - name: search_transactions
    screen: transactions
    trigger: text_input
    description: "PSU enters search query to filter transactions by merchant name or reference."

  - name: load_more_transactions
    screen: transactions
    trigger: on_click
    description: "PSU taps 'Load more' to fetch next page via OBIE Links.Next pagination."

  - name: copy_transaction_reference
    screen: transaction-detail
    trigger: on_click
    description: "PSU taps the copy icon on transaction reference — writes to clipboard."

  # ── Consent Management Actions ───────────────────────────────────────────────

  - name: tap_consent_card
    screen: consent-list
    trigger: on_click
    description: "PSU taps a consent card → navigates to consent-detail."

  - name: connect_hsbc
    screen: consent-list
    trigger: on_click
    description: "PSU taps 'Connect HSBC' on empty-state → navigates to user-onboarding."

  - name: revoke_consent_initiated
    screen: consent-detail
    trigger: on_click
    description: "PSU taps 'Revoke access' → shows confirmation dialog (two-step destructive action)."

  - name: revoke_consent_confirmed
    screen: consent-detail
    trigger: on_click
    description: "PSU confirms revoke in dialog → DELETE /account-access-consents/{id} fired."

  - name: revoke_consent_success
    screen: consent-detail
    trigger: auto
    description: "DELETE consent-revoke 204 returned; local PSU token cleared; navigate to consent-list."

  - name: reconfirm_consent_tap
    screen: consent-detail
    trigger: on_click
    description: "PSU taps 'Reconfirm before expiry' → fresh consent staging via login screen."

  # ── ATM Locator Actions ──────────────────────────────────────────────────────

  - name: filter_atm_24h
    screen: atm-locator
    trigger: on_click
    description: "PSU toggles 24h filter chip — client-side filter applied to allAtms list."

  - name: filter_atm_wheelchair
    screen: atm-locator
    trigger: on_click
    description: "PSU toggles Wheelchair chip — client-side filter applied to allAtms list."

  - name: filter_atm_deposit
    screen: atm-locator
    trigger: on_click
    description: "PSU toggles Cash Deposit chip — client-side filter applied to allAtms list."

  - name: open_atm_directions
    screen: atm-locator
    trigger: on_click
    description: "PSU taps an ATM card — launches native maps deep-link with lat/lon."

  # ── PFM Actions ──────────────────────────────────────────────────────────────

  - name: select_pfm_period
    screen: pfm-dashboard
    trigger: on_click
    description: "PSU selects a spending period (7d / 30d / 3m) — recomputes category aggregations."

  - name: tap_pfm_spending_category
    screen: pfm-dashboard
    trigger: on_click
    description: "PSU taps a top-category row → navigates to spending-by-category."

  - name: select_spending_period
    screen: spending-by-category
    trigger: on_click
    description: "PSU selects this month / last month / 3 months — in-memory re-aggregation."

  - name: tap_category_row
    screen: spending-by-category
    trigger: on_click
    description: "PSU taps a category row → navigates to transactions filtered by that category."

  - name: save_budget
    screen: budgets
    trigger: on_click
    description: "PSU saves a new or updated monthly budget — upserted to DataStore."

  - name: delete_budget
    screen: budgets
    trigger: on_click
    description: "PSU deletes a budget card — removed from DataStore."

  - name: budget_over_threshold
    screen: budgets
    trigger: auto
    description: "Budget card renders in error colour because spend >= budget amount for the month."

  - name: tap_subscription_merchant
    screen: recurring-subscriptions
    trigger: on_click
    description: "PSU taps a detected subscription → opens merchant-filtered transaction history."

  # ── Settings / Profile Actions ────────────────────────────────────────────────

  - name: update_theme
    screen: settings
    trigger: on_click
    description: "PSU selects Light / Dark / System theme from appearance settings."

  - name: toggle_biometric_lock
    screen: settings
    trigger: on_click
    description: "PSU enables or disables biometric lock — written to DataStore."

  - name: clear_local_data
    screen: settings
    trigger: on_click
    description: "PSU confirms clear local data — deletes all cached account and transaction rows."

  - name: sign_out_initiated
    screen: profile
    trigger: on_click
    description: "PSU taps Sign Out → confirmation dialog shown."

  - name: sign_out_confirmed
    screen: profile
    trigger: on_click
    description: "PSU confirms sign-out → tokens cleared from EncryptedSharedPreferences / Keychain."
