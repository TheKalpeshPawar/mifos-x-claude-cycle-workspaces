# TAG_REGISTRY.yaml — mifos-x-open-banking analytics tag catalog
# Regenerated 2026-07-28 by /gap-analysis-project (reverse sync from source).
# Source: screens/*/ui.yaml state_model.actions.members (which now mirror the
#         sealed *Action interfaces in source) + the nav-host callbacks.
# Consumer: analytics event tracking layer (core/analytics — MifosAnalyticsEvents).
# Schema: tags[]{name, screen, trigger, description}
#
# The prior catalogue held 76 tags across 25 screens, including events for
# atm-locator, budgets, pfm-dashboard, profile, recurring-subscriptions and
# spending-by-category — none of which ship — plus per-step onboarding events for
# a pager that was never built. Every action tag below corresponds 1:1 to a
# dispatchable member of a sealed *Action interface in source.
#
# 2026-08-02 (/idea-sync): the field-officer and PFM idea-layers were deleted, so no
# tag can be reinstated for them. atm-locator is the exception — it is specified and
# planned, so its events will be added here when the screen is implemented.

schema_version: "1.0"
project: "mifos-x-open-banking"
generated: "2026-07-28"
generated_by: "/gap-analysis-project (reverse sync)"
total_tags: 86
# Count corrected 2026-07-30 (/idea-sync). The field read 65 while the file held 68 —
# a pre-existing drift of 3 from the 2026-07-28 reverse sync, not introduced here.
# Updated 2026-07-31 (/idea-sync): 68 source-backed + 18 specification-only = 86, after adding
# the 4 send-money manual-entry / reference actions the 07-31 design-validate fixes introduced.
tag_provenance:
  source_backed: 68      # 1:1 with a sealed *Action member in source
  specified_only: 18     # send-money / payment-consent / payment-status — no feature module yet

tags:

  # ══ Screen view events — one per shipped destination (20) ══════════════════

  - { name: screen_view_user_onboarding, screen: user-onboarding,   trigger: screen_enter, description: "PSU lands on the static intro screen (IntroRoute, start of the auth graph)." }
  - { name: screen_view_login,           screen: login,             trigger: screen_enter, description: "PSU reaches the consent-staging screen (LoginRoute, or LoginRenewRoute on re-consent)." }
  - { name: screen_view_consent_callback, screen: consent-callback, trigger: screen_enter, description: "Return leg from the bank's app-to-app authorisation." }
  - { name: screen_view_home,            screen: home,              trigger: screen_enter, description: "PSU lands on the home dashboard (bottom-nav tab 0, NavHost start destination)." }
  - { name: screen_view_accounts,        screen: accounts,          trigger: screen_enter, description: "Authorised account list with type-filter chips (bottom-nav tab 1)." }
  - { name: screen_view_account_detail,  screen: account-detail,    trigger: screen_enter, description: "Single account with balances and the capability-gated Explore chip row." }
  - { name: screen_view_account_holder,  screen: account-holder,    trigger: screen_enter, description: "Connected party identity, gated by ReadParty." }
  - { name: screen_view_transactions,    screen: transactions,      trigger: screen_enter, description: "Transaction history with filter, search, date range and pagination." }
  - { name: screen_view_transaction_detail, screen: transaction-detail, trigger: screen_enter, description: "Single transaction detail." }
  - { name: screen_view_beneficiaries,   screen: beneficiaries,     trigger: screen_enter, description: "Saved payees for the account." }
  - { name: screen_view_standing_orders, screen: standing-orders,   trigger: screen_enter, description: "Recurring outbound orders for the account." }
  - { name: screen_view_direct_debits,   screen: direct-debits,     trigger: screen_enter, description: "Direct-debit mandates for the account." }
  - { name: screen_view_scheduled_payments, screen: scheduled-payments, trigger: screen_enter, description: "Future-dated one-off payments for the account." }
  - { name: screen_view_statements,      screen: statements,        trigger: screen_enter, description: "Statement list for the account." }
  - { name: screen_view_statement_detail, screen: statement-detail, trigger: screen_enter, description: "Single statement with its transactions." }
  - { name: screen_view_product,         screen: product,           trigger: screen_enter, description: "Account product terms — fees, rates and features." }
  - { name: screen_view_consent_list,    screen: consent-list,      trigger: screen_enter, description: "Active consents with status and expiry." }
  - { name: screen_view_consent_detail,  screen: consent-detail,    trigger: screen_enter, description: "Granted permissions, expiry, revoke and reconfirm." }
  - { name: screen_view_settings,        screen: settings,          trigger: screen_enter, description: "Settings hub (bottom-nav tab 3, More)." }
  - { name: screen_view_licences,        screen: licences,          trigger: screen_enter, description: "Bundled open-source licence readout." }

  # ══ Action events — 1:1 with sealed *Action members in source (39) ═════════

  # login
  - { name: login_load_permissions_config, screen: login, trigger: "LoginAction.LoadPermissionsConfig", description: "Reads the permission set and computes the consent expiry label." }
  - { name: login_start_oauth,             screen: login, trigger: "LoginAction.StartOAuth",            description: "Creates the consent, builds the FAPI URL and launches the app-to-app redirect." }
  - { name: login_cancel,                  screen: login, trigger: "LoginAction.Cancel",                description: "Aborts the in-flight authorisation." }
  - { name: login_retry,                   screen: login, trigger: "LoginAction.Retry",                 description: "Re-attempts after an authorisation error." }

  # consent-callback
  - { name: consent_callback_process,      screen: consent-callback, trigger: "ConsentCallbackAction.ProcessCallback",   description: "Validates state/nonce and exchanges the authorisation code." }
  - { name: consent_callback_poll,         screen: consent-callback, trigger: "ConsentCallbackAction.PollConsentStatus", description: "Re-checks consent status while awaiting authorisation." }
  - { name: consent_callback_nav_retry,    screen: consent-callback, trigger: "ConsentCallbackAction.NavigateRetry",     description: "Restarts the authorisation attempt." }
  - { name: consent_callback_nav_login,    screen: consent-callback, trigger: "ConsentCallbackAction.NavigateLogin",     description: "Abandons and returns to login." }

  # home
  - { name: home_select_account,           screen: home, trigger: "HomeAction.SelectAccount",           description: "Persists the selected account and re-drives the stream." }
  - { name: home_open_account_selector,    screen: home, trigger: "HomeAction.OpenAccountSelector",     description: "Shows the account selector bottom sheet." }
  - { name: home_dismiss_account_selector, screen: home, trigger: "HomeAction.DismissAccountSelector",  description: "Hides the account selector bottom sheet." }
  - { name: home_retry_load,               screen: home, trigger: "HomeAction.RetryLoad",               description: "Refreshes the home stream after a failure." }

  # accounts
  - { name: accounts_filter,               screen: accounts, trigger: "AccountsAction.FilterAccounts",  description: "Client-side chip filter by canonical account subtype. No API round-trip." }
  - { name: accounts_retry_load,           screen: accounts, trigger: "AccountsAction.RetryLoad",       description: "Re-runs the accounts + balances combined stream." }

  # account-detail / account-holder
  - { name: account_detail_retry_load,     screen: account-detail, trigger: "AccountDetailAction.RetryLoad", description: "Refreshes the account + balances stream." }
  - { name: account_detail_chip_tapped,    screen: account-detail, trigger: "onNavigateToChip",             description: "Explore chip tapped. Carries the AccountDetailChip value — Transactions, Statements, StandingOrders, DirectDebits, ScheduledPayments, Beneficiaries, AtmLocator, Product or Party." }
  - { name: account_holder_retry_load,     screen: account-holder, trigger: "AccountHolderAction.RetryLoad", description: "Refreshes the party stream. Suppressed for non-retriable kinds." }

  # transactions
  - { name: transactions_load,             screen: transactions, trigger: "TransactionsAction.LoadTransactions",      description: "Initial transaction load for the account." }
  - { name: transactions_retry_load,       screen: transactions, trigger: "TransactionsAction.RetryLoad",             description: "Retries after a failure." }
  - { name: transactions_load_more,        screen: transactions, trigger: "TransactionsAction.LoadMore",              description: "Fetches the next page via the OBIE Links.Next cursor." }
  - { name: transactions_filter,           screen: transactions, trigger: "TransactionsAction.FilterTransactions",    description: "Applies a credit/debit filter." }
  - { name: transactions_search,           screen: transactions, trigger: "TransactionsAction.SearchTransactions",    description: "Applies a free-text query." }
  - { name: transactions_open_date_picker, screen: transactions, trigger: "TransactionsAction.OpenDateRangePicker",   description: "Opens the date-range picker." }
  - { name: transactions_set_date_range,   screen: transactions, trigger: "TransactionsAction.SetDateRange",          description: "Applies a date range." }
  - { name: transactions_dismiss_date_picker, screen: transactions, trigger: "TransactionsAction.DismissDateRangePicker", description: "Closes the picker without applying." }
  - { name: transactions_clear_filters,    screen: transactions, trigger: "TransactionsAction.ClearFilters",          description: "Resets filter, query and date range." }
  - { name: transaction_detail_retry_load, screen: transaction-detail, trigger: "TransactionDetailAction.RetryLoad",  description: "Refreshes the transaction-detail stream." }
  - { name: transaction_detail_copy_reference, screen: transaction-detail, trigger: "TransactionDetailAction.CopyReference", description: "Copies the transaction reference to the clipboard." }

  # payments-context read surfaces
  - { name: beneficiaries_retry_load,      screen: beneficiaries,      trigger: "BeneficiariesAction.RetryLoad",  description: "Refreshes the beneficiaries stream." }
  - { name: beneficiaries_search,          screen: beneficiaries,      trigger: "BeneficiariesAction.Search",     description: "Client-side filter by payee name or reference." }
  - { name: standing_orders_retry_load,    screen: standing-orders,    trigger: "StandingOrdersAction.RetryLoad", description: "Refreshes the standing-orders stream. Also fired by pull-to-refresh." }
  - { name: direct_debits_retry_load,      screen: direct-debits,      trigger: "DirectDebitsAction.RetryLoad",   description: "Refreshes the direct-debits stream. Suppressed for ConsentRevoked." }
  - { name: scheduled_payments_retry_load, screen: scheduled-payments, trigger: "ScheduledPaymentsAction.RetryLoad", description: "Refreshes the scheduled-payments stream." }

  # statements
  - { name: statements_retry_load,         screen: statements,       trigger: "StatementsAction.RetryLoad",        description: "Refreshes the statements stream." }
  - { name: statements_download,           screen: statements,       trigger: "StatementsAction.DownloadStatement", description: "Per-row statement file download, tracked in the downloadState map keyed by statementId." }
  - { name: statement_detail_retry_load,   screen: statement-detail, trigger: "StatementDetailAction.RetryLoad",   description: "Refreshes the statement-detail stream." }
  - { name: statement_detail_download_pdf, screen: statement-detail, trigger: "StatementDetailAction.DownloadPdf", description: "Fetches the statement file and hands it to StatementFileHandler." }

  # product
  - { name: product_load,                  screen: product, trigger: "ProductAction.ProductLoad", description: "Loads product terms on mount." }
  - { name: product_retry_load,            screen: product, trigger: "ProductAction.RetryLoad",   description: "Re-dispatches ProductLoad after an error." }

  # consents
  - { name: consent_list_retry_load,       screen: consent-list,   trigger: "ConsentListAction.RetryLoad",            description: "Refreshes the consent stream." }
  - { name: consent_detail_retry_load,     screen: consent-detail, trigger: "ConsentDetailAction.RetryLoad",          description: "Refreshes the consent-detail stream." }
  - { name: consent_detail_confirm_revoke, screen: consent-detail, trigger: "ConsentDetailAction.ConfirmRevoke",      description: "Opens the revoke confirmation dialog. No API call." }
  - { name: consent_detail_dismiss_revoke, screen: consent-detail, trigger: "ConsentDetailAction.DismissRevokeConfirm", description: "Closes the revoke dialog without revoking." }
  - { name: consent_detail_execute_revoke, screen: consent-detail, trigger: "ConsentDetailAction.ExecuteRevoke",      description: "Revokes the consent and signs the user out via AppLogout. A full logout, not a back-navigation." }

  # settings
  - { name: settings_select_theme,         screen: settings, trigger: "SettingsAction.SelectTheme",      description: "Persists DarkThemeConfig through UserDataRepository. The only persisted preference on this screen." }
  - { name: settings_toggle_theme_menu,    screen: settings, trigger: "SettingsAction.ToggleThemeMenu",  description: "Expands the theme dropdown. Local UI state only." }
  - { name: settings_dismiss_theme_menu,   screen: settings, trigger: "SettingsAction.DismissThemeMenu", description: "Collapses the theme dropdown." }
  - { name: settings_retry_load,           screen: settings, trigger: "SettingsAction.RetryLoad",        description: "Re-reads the user-data preference stream." }

  # ── PISP — payment initiation (added 2026-07-30, /idea-sync) ──────────────
  # SPECIFICATION-ONLY. These three screens have no feature module yet (P3), so
  # unlike every tag above, these do NOT correspond to a dispatchable member of a
  # sealed *Action interface in source. They are derived from ui.yaml#state_model
  # and become source-backed when P3 lands. Do not read them as shipped events.

  # send-money
  - { name: send_money_select_debtor,      screen: send-money, trigger: "SendMoneyAction.SelectDebtorAccount",     description: "Chooses the funding account from the PSU's own authorised accounts. In-memory, no API call.", status: specified }
  - { name: send_money_select_creditor,    screen: send-money, trigger: "SendMoneyAction.SelectCreditor",          description: "Chooses the payee — a saved beneficiary or a validated manual sort code + account number.", status: specified }
  - { name: send_money_enter_amount,       screen: send-money, trigger: "SendMoneyAction.EnterAmount",             description: "Captures amount in minor units plus optional reference; validates against available balance.", status: specified }
  - { name: send_money_review,             screen: send-money, trigger: "SendMoneyAction.ReviewPayment",           description: "Advances to the review step. Nothing is staged at the bank yet.", status: specified }
  - { name: send_money_stage_consent,      screen: send-money, trigger: "SendMoneyAction.ConfirmAndStageConsent",  description: "POSTs the domestic-payment-consent with detached JWS + idempotency key. First write the app makes.", status: specified }
  - { name: send_money_submit,             screen: send-money, trigger: "SendMoneyAction.SubmitPayment",           description: "POSTs the payment after authorisation, reusing the same idempotency key. Money moves here.", status: specified }
  - { name: send_money_retry_submit,       screen: send-money, trigger: "SendMoneyAction.RetrySubmit",             description: "Retries a failed submit with the ORIGINAL idempotency key — cannot create a duplicate payment.", status: specified }
  - { name: send_money_cancel,             screen: send-money, trigger: "SendMoneyAction.CancelPayment",           description: "Abandons the payment before any consent is staged.", status: specified }
  - { name: send_money_back_step,          screen: send-money, trigger: "SendMoneyAction.BackStep",                description: "Returns to the amount step after a limit rejection, preserving debtor and creditor.", status: specified }
  # Added 2026-07-31 (/idea-sync STEP 4). These four on_click actions exist in
  # screens/send-money/ui.yaml (lines 237, 269, 295, 350) but had no tag — three of them are the
  # manual-creditor-entry affordance added by the 07-31 design-validate fix pass, which grew the
  # screen's component set without a matching registry pass. This is exactly the drift the
  # mtime staleness flag was pointing at, so the artifact was stale in content, not only in mtime.
  - { name: send_money_show_manual_entry,  screen: send-money, trigger: "SendMoneyAction.ShowManualCreditorEntry", description: "Reveals the manual sort code + account number fields when the PSU has no saved payee, or wants a new one.", status: specified }
  - { name: send_money_manual_sort_code,   screen: send-money, trigger: "SendMoneyAction.UpdateManualSortCode",    description: "Per-keystroke update of the manual sort code field; drives the inline fieldErrors.sortCode validation.", status: specified }
  - { name: send_money_manual_account_no,  screen: send-money, trigger: "SendMoneyAction.UpdateManualAccountNumber", description: "Per-keystroke update of the manual account number field; drives fieldErrors.accountNumber.", status: specified }
  - { name: send_money_enter_reference,    screen: send-money, trigger: "SendMoneyAction.EnterReference",          description: "Captures the optional payment reference, validated separately from amount via fieldErrors.reference.", status: specified }

  # payment-consent
  - { name: payment_consent_check_again,   screen: payment-consent, trigger: "PaymentConsentAction.CheckAgain",         description: "Re-polls the payment consent status during the checking window.", status: specified }
  - { name: payment_consent_restart,       screen: payment-consent, trigger: "PaymentConsentAction.RetryAuthorisation", description: "Emits RestartAuthorisation so send-money stages a fresh consent and relaunches.", status: specified }
  - { name: payment_consent_abandon,       screen: payment-consent, trigger: "PaymentConsentAction.AbandonPayment",     description: "Drops the payment. The staged consent is left unauthorised and expires on its own.", status: specified }

  # payment-status
  - { name: payment_status_refresh,        screen: payment-status, trigger: "PaymentStatusAction.RefreshStatus",   description: "Manual re-read of the payment resource. Offered only while the disposition is in_progress.", status: specified }
  - { name: payment_status_new_payment,    screen: payment-status, trigger: "PaymentStatusAction.StartNewPayment", description: "Opens send-money with a cleared form after a Rejected status. Never resubmits the rejected instruction.", status: specified }

# Screens with no action tags, by design:
#   licences         — stateless composable, no ViewModel, no actions
#   user-onboarding  — stateless IntroScreen; its single continue button is a
#                      nav-host callback (onContinue -> LoginRoute), not an action
