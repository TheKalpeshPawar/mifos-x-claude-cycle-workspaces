# app-shell.yaml — Flavor-aware app navigation shells
# Defines per-flavor bottom navigation, top app bar, and global containers

schema_version: "1.0"
generated_at: "2026-05-28"
# 2026-05-28: consumer.bottom_nav normalized to 5-item pattern (Home/Accounts/Pay/Cards/More)
# to match consumer screen overrides — resolves REG-001 app-shell divergence.

# ─── Consumer Flavor Shell ───────────────────────────────────────────────────
consumer:
  name: "Consumer Banking App"
  bottom_nav:
    visible: true
    items:
      - label: "Home"
        icon: "home"
        target: "home"
        badge: false
      - label: "Accounts"
        icon: "account_balance"
        target: "accounts"
        badge: false
      - label: "Pay"
        icon: "send"
        target: "send-money"
        badge: false
      - label: "Insights"
        icon: "insights"
        target: "pfm-dashboard"
        badge: false
      - label: "More"
        icon: "more_horiz"
        target: "settings"
        badge: false
  top_app_bar:
    visible: true
    elevation: true
    title_variant: "headline-md"
    actions:
      - icon: "notifications"
        target: "notifications"
        badge: true
      - icon: "account_circle"
        target: "profile"
        badge: false
  fab:
    visible: true
    icon: "add"
    action: "quick_action_menu"
  colors:
    primary: "#4C662B"
    secondary: "#386663"
    background: "#F9FAEF"
    surface: "#FFFFFF"

# ─── Shared Elements ─────────────────────────────────────────────────────────
shared:
  snackbar:
    visible: true
    position: "bottom"
  dialog:
    overlay_opacity: 0.5
    elevation: 24
  safe_area:
    top: true
    bottom: true
    left: true
    right: true
