# Mifos X Open Banking — Stitch Design Prompts

> Design prompt package for Stitch AI image generation.
> Generated 2026-05-22 (empty-slate scaffold — prompts are stubs until screens are enriched).

## Status

Stitch is not yet enabled for this project (`stitch.enabled` not declared in PROJECT_CONFIG.yaml).
This file is a placeholder scaffold. Prompts will be generated by `/idea export --stitch` once:

1. Screens are enriched (`/idea-enrich`)
2. Stitch API key is configured (`.env.stitch`)
3. Design tokens are finalized

## Global Prompt Context

When Stitch is enabled, this file provides shared context injected into every screen prompt:

```
App: Mifos X Open Banking
Design system: Material Design 3
Brand color: #1800B1 (deep purple)
Background (light): #FCF8FF
Background (dark): #13131B
Typography body: Inter
Typography display: Space Grotesk
Platform: Android (primary) / iOS / Desktop / Web
Style: Clean fintech — high information density, clear data hierarchy, no decorative elements
```

## Per-Screen Prompt Structure

> To be populated by `/idea export --stitch` once screens reach `designed` status.

```
screens/
  home/stitch/
    content.md     # stitch prompt for home default state
    loading.md     # stitch prompt for home loading state
    empty.md       # stitch prompt for home empty state
  profile/stitch/
    ...
  settings/stitch/
    ...
```
