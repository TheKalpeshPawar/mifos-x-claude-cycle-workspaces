# Changelog

- [idea-sync] 2026-08-03 — **Coherence repair pass; idea-layer now parses end-to-end for the first time in this project's recorded history.** Four YAML files were unparseable and each was breaking a real consumer, silently. `archetypes.yaml` was a two-document stream (`---` header, `---`, body) while `tools/project-dashboard/src/adapters/vocab-adapter.ts:93` reads it with single-document `parseYaml` and on throw pushes to `parseErrors` and returns `[]` — so the dashboard reported zero archetypes for a file holding 13. **The root cause was the framework template** `layers/idea/templates/prototype-render/ARCHETYPES.yaml`, which has the identical shape, so every project scaffolded from it inherits a file no consumer can read; fixed at the template, not just the copy. `APPROVE_LOG.yaml` had five `bulk_summary`…`bulk_summary_5` mapping headers written INLINE between batches of log entries — each a mapping immediately followed by sequence items at the same indent, which is not valid YAML; the summaries were moved to a proper `bulk_summary:` sequence at the end so `entries:` runs unbroken. **That recovered 49 approve records** which had been stranded under `bulk_summary` and were never part of `entries[]` even conceptually — the log went from 42 readable entries to 124. A stray `context:` key orphaned by the same defect was folded back into the run it described. `PIPELINE_STATE.yaml` carried **three** `metrics:` keys plus a duplicate `detail_ref:` in one `capabilities.design-validate` mapping; last-key-wins meant the live 15:40 full-corpus run (score 15) was shadowed by a stale 11:04 one (score 33.5) — renamed to `consolidated_1255_metrics` / `full_run_metrics` so the live block is what resolves. `DESIGN_VALIDATION.yaml` had an entry authored as a flow mapping `{ id: FW-7, item: >- … }` wrapping a block scalar — invalid, and introduced by that same day's earlier `/design-validate` run; reshaped to block-mapping form. **Screen dedupe:** `licenses` and `licences` were the same OSS-licence screen. Evidence decided it rather than preference — source ships `LicencesRoute` (real composable in `feature/settings`) while `LicensesRoute` is a `PlaceholderScreen`, and `licences` is schema v4.0 with proper siblings and live `strings.licences.*` keys whereas `licenses` was a 50KB legacy v3.1 monolith. Merged into `licences`; `flows/app-main.yaml`, `journeys/consumer-profile-settings.yaml` and the about/settings exports were rewired. Roster 47 → 46. **`product` and `products` are NOT duplicates** — an earlier report of mine said they were, wrongly: `product` is per-account product terms (shipped, `ProductRoute(accountId)`, has tests) and `products` is the catalogue (specified, still a placeholder in source). Both retained. **Flow coherence:** 20 of 47 screens carried `flow_ref:` pointing at seven journey IDs that exist nowhere (`account-browsing`, `recurring-and-statements`, `onboarding-consent`, `consent-management`, `home-dashboard`, `settings-and-profile`, `transaction-review`) — legacy names from the 2026-07-28 reverse sync, confirmed pre-existing at HEAD — and 9 screens belonged to no flow at all. Every `flow_ref` was repointed at the real flow it belongs to, and each orphan placed by its own declared `entry_points` rather than by guesswork (`account-holder`, `product`, `scheduled-payments`, `statements` all enter from account-detail chips; `statement-detail` from statements; `consent-list` from the More menu; `consent-detail` from consent-list; `consent-callback` and `user-onboarding` are bootstrap/auth, so app-main). **46/46 screens now belong to a flow.** Also corrected a stale note in `account-detail/flow.yaml` asserting the atm-locator screen had been deleted — it had not, and is now in scope. Verification: 534/534 YAML parse · all `navigates_to`/`flow_ids`/`journey_ids` resolve · all v4.0 `transitions`/`entry_points`/`flow_ref` resolve (a schema variant my previous pass's checker did not cover — the gap that let the `notifications` → `kyc-review` dead link through) · no live reference to any deleted feature · no orphaned DTOs.

- [idea-sync] 2026-08-02 — **Field-officer persona and PFM removed; this is now a consumer-only app.** Deleted 15 features / 730 files: the 12 field-officer screens (`fo-dashboard`, `customer-search`, `customer-detail`, `customer-profile`, `customer-onboarding`, `corporate-onboarding`, `kyc-review`, `account-applications`, `application-detail`, `customer-messages`, `meetings`, `agent-registration`) plus `pfm-dashboard`, `pfm-settings` and `business-insights` — each across `screens/`, `exports/`, `mockups/`, `app-flow/screens/` and the `_cache` views. Also removed `flows/field-officer.yaml`, the 3 `fo-*` journeys, and 7 API groups (`customers`, `kyc`, `account_applications`, `customer_messages`, `meetings`, `field_officer`, `pfm`) with their spec files. **`atm-locator` was explicitly NOT removed** — it is planned work, and since the idea-layer is what `/implement` builds from, deleting the spec would have destroyed the input; it moved `out_of_scope` → `in_scope` in `idea-plan.yaml`, keeping its screen, exports, mockups, the `atm` API group and its 7 DTOs (`AtmLocation`, `Branch`, `BranchRouting`, `GeoLocation`, `PostalAddress`, `LobbyHours`, `DriveUpHours`). **DTO pruning was evidence-driven, not name-driven:** a first reachability pass produced false positives because `Customer` and `Agent` matched prose ("Strong **Customer** Authentication", "Customer Experience Guidelines") rather than type references; re-running against each DTO's declared `used_by` gave the real answer — 12 deleted (`AccountApplication`, `AccountInfo`, `Agent`, `CreditRating`, `Customer`, `CustomerAccountLink`, `CustomerMessage`, `FaceImage`, `KycDocument`, `Meeting`, `MeetingInvitee`, `PersonalDataField`), and **`Transaction` and `AmountOfMoney` were NOT deleted despite scanning as orphans** — their `used_by` named only the deleted `pfm-dashboard` while the DTOs are genuinely consumed by `transactions`/`transaction-detail` and `standing-order-create`; both were repointed. Registry now 55 registered == 55 on disk (also registered `CardAccountRef`, which existed on disk but had never been indexed — a pre-existing gap). `consumer-insights-utilities` was **edited, not deleted**: only its opening `pfm-dashboard` step and `pfm_engagement_rate` metric were removed, preserving its FX / ATM / products / notifications coverage. The **dual-persona framing is retired everywhere**: `IDEA.md` (title, vision, personas, product flavors, module structure, `VARIANT_DEPENDENCY_EXCLUDES`, both navigation graphs), `FEATURES.md` (rewritten against the real 47-screen roster), `ROADMAP.md`, `REQUIREMENTS.md` (FR-030…FR-040 withdrawn; FR-024 withdrawn with PFM), `PROJECT_CONFIG.yaml` (`product_flavors` block removed), both `LAYER_STATUS.md` files, and every legal document — `TERMS_CONDITIONS.md`, `PRIVACY_POLICY.md`, `APP_PRIVACY.yaml`, `DATA_SAFETY.yaml` and the four generated `legal/deploy/` artifacts, which mattered because they are publishable and would otherwise have told users the app has an agent-banking persona and collects third-party KYC data, address, phone and identity-document photos it no longer touches. `journeys/INDEX.md` was **regenerated rather than patched** — it described a 9-journey roster that had not existed on disk for some time (pre-existing drift, unrelated to this removal). `STITCH_STATUS.yaml` pruned 1603 → 787 lines, 13 tracked features all resolving to real screens, valid YAML, `summary.total_features` matching. Verification: every `navigates_to`, `flow_id`, `journey_id` and flow-screen reference resolves; every `spec_file` in `api_manifest.yaml` exists; no orphaned DTOs remain. **Left deliberately unchanged:** append-only history (`verify-logs/`, `sync-logs/`, `PIPELINE_STATE` run records, prior CHANGELOG entries) — those are an accurate record of what the project used to contain; and the source repo, where `AtmLocatorRoute` and `AccountDetailChip.AtmLocator` stay because atm-locator is being built. **Two pre-existing defects surfaced, not fixed:** duplicate screen directories `licences`/`licenses` and `product`/`products`.

- [idea-sync] 2026-07-30 (second pass) — **partial**, but the gate moved: STEP 4.VERIFY now passes **4 of 7** artifact groups, up from 3, because `exports/` landed (23 features) and `_cache/data-flow-views/` was regenerated. The three still absent — `mockups/`, `screens/*/preview/`, `screens/*/prompts/` — are exactly the set the 2026-07-28 reverse sync deferred, with the stitch decision explicitly left open, so what remains is a **user decision, not a defect**. STEP 1 caught one regression, self-inflicted this session: removing `home`'s `quick_actions_card` had orphaned **9 string keys** (`home.quick_actions.accessibility` + `home.action.{pay,transactions,statements,consents}.{label,accessibility}`), leaving the catalogue at 554 declared vs 545 referenced. Removed — parity restored to **545 == 545, 0 missing, 0 orphans**. Orphaned keys are not harmless: they read as coverage in an i18n audit and invite a translator to localise strings no screen will ever render. `APP_FLOW.mmd` was also stale — `user-onboarding` had dropped 6 phantom sub-screen transitions and `transactions` its `pfm` entry point during the stale-artifact fix — regenerated at **80 unique edges** (was 86), 0 stale-feature endpoints, 0 unreachable screens. Enrichment skipped: 0 features carry `content_stale`. Ten checks pass — i18n parity · export freshness (23/23 sidecar hashes match their live `ui.yaml`) · dangling refs (graph-derived) · orphaned FRs · roster consistency · REG-001 32/32 against the merged 3-tier registry · RULE-DATA-FLOW-001 F5 · content_stale · YAML parse · G-ACTION-CONTRACT 0 violations.

- [idea-feature-export] 2026-07-30 — `--all`, run in **5 user-requested batches** so a session-limit interruption would cost at most one batch; every batch wrote its artifacts, `.meta.yaml` sidecars, `EXPORT_MATRIX` cells and capability state before the next began. **23/23 features · 69 files · 4,888 lines** of SPEC.md + API.md. Scope was SPEC + API only — `mockups/` belongs to the separate, still-deferred `/idea-feature-mockup`. Timestamps live in the sidecars, not the artifacts, so re-export diffs stay deterministic. **Three features exported with a recorded gate exception** rather than being skipped: `licences` and `user-onboarding` fail E1 (no ViewModel) and E3 (no API), `settings` fails E3 (client-only) — Step 1.5 says "SKIP, run /idea enrich first", but enrichment cannot add a ViewModel that is absent from *source*, so the gate would never clear, and a skipped feature is indistinguishable from an unexported one in the matrix. **Eleven stale artifacts corrected at source**, not merely documented: `home` lost a 119-line `quick_actions_card` and had its `account_switcher` chip_row reshaped into the `AccountSelectorSheet` it actually is, plus a corrected description, a `state_model` rewritten verbatim from `HomeViewModel.kt:81-88` (it still declared `spendThisMonth`/`topCategoryName`/`activeConsentId`), five dead `Navigate*` vm_actions removed and TC-HOME-016 dropped; `user-onboarding` lost a three-step-pager description and six phantom sub-screen transitions; `accounts` claimed bottom-nav order 0 when Home is 0; `settings` listed biometric lock, session timeout, notifications, a Profile row and Clear Local Data, none of which ship; and **`product` carried a three-way 404 contradiction** — `data-flow.yaml` mapped 404→empty *citing api.yaml as its authority*, while `api.yaml` said error and TC-PROD-006 asserted error — resolved to Empty in all three, because a bank holding no product record is not a failure a customer can act on. **Four gaps recorded rather than invented:** all 14 `account-detail` test scenarios carry empty descriptions; `direct-debits` and `standing-orders` have no test coverage for their `Unsupported` state; and `scheduled-payments` has a capability-gated store but no `Unsupported` UiState, so a `U000` falls through the retryable error path and a deep-link visitor sees a Retry that can never succeed. Verification: all 23 have SPEC+API+sidecar, every sidecar hash matches its live `ui.yaml`, no artifact below the template minimum, zero filler tokens, every `api.yaml` path appears in its `API.md`, YAML parse clean. Also repaired **two duplicate capability keys** in `PIPELINE_STATE.yaml` (`idea-feature-export`, `idea-sync`) where YAML's last-key-wins had let historical records silently shadow the live ones.

- [idea-data-flow] 2026-07-30 — Verified all 23 `data-flow.yaml` files and regenerated the `_cache/data-flow-views/` tree, which the 2026-07-28 reverse sync deleted and named this command as the regenerator for. **Did not rewrite the 23 files**: they carry hand-authored content (20 written against source by the reverse sync, 3 by `/idea-evolve`), and regenerating them into the canonical `entries[]` shape would have destroyed reverse-synced source truth plus the `cache_rationale` / `truthfulness_invariant` / `token_invariant` blocks. RULE-DATA-FLOW-001: **F1 23/23 · F3 23/23 · F4 23/23 · F5 19/23 → 23/23** after remediation; **F2 0/23** (see below). **F5 fix** registered the app's first five WRITE contracts — `OBWriteDomesticConsent4`, `OBWriteDomesticConsentResponse5`, `OBWriteDomestic2`, `OBWriteDomesticResponse5`, `OBWriteFundsConfirmationResponse1` — referenced by the PISP screens but registered nowhere; every prior DTO is read-only AIS. Also corrected `screens/account-holder/api.yaml` `PartyResponse` → `OBReadParty2` (source-side name where 23 of 24 refs use the OBIE wire name), and stale `used_by` entries in `OBReadParty2`/`OBReadParty3` pointing at `party` (renamed 2026-07-28) and `profile` (deleted) — stale refs the earlier sweeps missed because they never scanned `dtos/`. Cache views rebuilt: **23 per-feature · 3 per-flow · 17 per-dto · 0 per-table** (the project owns no server-side tables — server-layer holds client contracts for the external HSBC API only). `EXPORT_MATRIX.yaml` gains its first non-empty cells since the 2026-07-28 reset (27 fresh). **Two findings left unremediated on purpose:** (1) all 23 files fail `data-flow.sibling.schema.json` — which requires `{sibling, schema_version, feature_id, entries}` with `additionalProperties:false` — but that schema is the declared target of the v3.1-JSON→v4.0-YAML migration and `/implement` reads `$defs.persistence_contract` from it, so a 23-file reshape belongs to `/idea-migrate-schema`, not to a data-flow regeneration; (2) only 9 of 23 `docs.yaml` declare `flow_ref`, so per-flow views cover 3 of 9 flows — assigning the other 14 is a flow-modelling judgment owned by `/idea-flows`.

- [idea-sync] 2026-07-30 — Full pipeline; **partial** (STEP 4.VERIFY artifact-existence gate fails by design — exports/mockups/previews have been absent project-wide since the 2026-07-28 reverse sync and remain deferred). Scope was the 3 content-stale PISP features only: the other 20 are approved (19) or implemented (1) and hold approve_hash values from the 2026-07-14 human gate, so full-corpus enrichment would have rewritten approved content and invalidated them. **i18n was the real gap** — 80 string keys were referenced by send-money/payment-consent/payment-status and declared nowhere (catalogue 471 vs 551 referenced). Authored 83 keys (554 now declared == 554 referenced, 0 missing, 0 orphans), with copy written against DESIGN.md 1.1.0: no in-progress payment described as "sent", the confirm CTA carries the amount ("Send %1$s"), no celebratory settlement language. **Second stale-reference pass** caught a `pfm` entry_point in transactions/flow.yaml that the earlier sweep missed — that sweep resolved `target:`/`navigates_to:` but not `entry_points[].source` or `transitions[].from/to`; it is now graph-derived across every edge kind. Artifacts: `APP_FLOW.mmd` regenerated (absent since 2026-07-28 — 23 nodes, 86 edges, 0 stale endpoints, P3 nodes dashed); `_list.json` 20 → 23 with an explicit idea-layer-ahead-of-source block so the PISP entries are not misread as shipped; `TAG_REGISTRY.yaml` +14 PISP tags flagged `status: specified` (no source Action exists yet) and the declared count corrected 65 → 82, a pre-existing drift of 3; `EXPORT_MATRIX.yaml` denominator 20 → 23, cells still empty by design; `LAYER_STATUS.md` rewritten. The 3 features promoted draft → enriched, score 88 — not higher, because derived artifacts are deferred and nothing is verified against source. Verification: 241 YAML files parse, i18n parity exact, 0 dangling/stale references, 0 orphaned FRs, roster consistent, tag count consistent, G-ACTION-CONTRACT 0 violations across 23 ui.yaml. Approval remains a human gate.

- [design-system] 2026-07-30 — "Open Banking — Trust Blue" 1.0.0 → **1.1.0**; `design-tokens.yaml` 2.0.0 → 2.1.0. Extended the system for money **movement**: every prior version described a strictly read-only AISP app, so it had no form validation, no payment-state vocabulary and no irreversible-action contract. Added `semantic.money` (formalised credit/debit/neutral), `semantic.payment_disposition` (in_progress · settled · rejected — in_progress deliberately `secondary`, not `primary`, because primary is already the credit colour and would read as "money arrived"), `form.*` (field outline states, amount field, on-change-after-first-blur validation) and `irreversible_action`. Eight components added (`amount_field`, `field_error`, `status_chip`, `review_card`, `step_indicator`, `payment_summary_row`, `state_submitting`, `state_success`); three PFM components removed (`category_chip`, `chart_donut`, `progress_budget` — their screens were deleted 2026-07-28). Token changes are **additive only**: no colour role, type scale, radius or spacing value changed. Three prose corrections: §Overview's "never moves money" (now AISP+PISP), the bottom-nav rail (documented as "Accounts · Transactions · Consents · Settings"; actual rail has been Home · Accounts · Pay · More since the 2026-07-28 reverse sync — a pre-existing defect), and the tertiary role's stale PFM reservation. Colour is never the sole signal — every disposition carries icon + label (WCAG 1.4.1), which is also what makes the "never render in-progress as sent" rule auditable. Verification: 15 WCAG pairs computed (not asserted), 0 failures, range 4.24–10.90:1; DESIGN.md↔tokens coherence consistent on all 4 runtime-checked pairs; registry integrity clean. New `state/DESIGN_SYSTEM_STATE.yaml`. Not done: DESIGN.md lint, Stitch re-upload (no API key — `tokens_hash` flagged stale), preview regeneration.

- [reverse-sync] 2026-07-28 — Rebuilt the idea-layer from the shipped source (`/gap-analysis-project`, reverse direction; source never written to). Roster 25 → 20 screens: removed 6 that no feature module implements (atm-locator, budgets, pfm-dashboard, profile, recurring-subscriptions, spending-by-category), renamed `party` → `account-holder` to match `feature/account-holder`, and added `licences`, which ships in source but had no idea-layer representation. Every surviving screen's states, actions, DI, routes and navigation were rewritten against the sealed `*Action` / `*UiState` interfaces and nav-host callbacks in source. App-shell tab 2 corrected from Transactions to Pay. String catalogue rebalanced to 471 declared == 471 referenced (0 orphans). All derived artifacts removed: preview HTML, stitch mockups and prompts, exports/, mockups/, _cache/, dashboard/, APP_FLOW.mmd. Ledgers reset rather than carried forward. Verification: G-ILC fail=0 warn=0, G-ACTION-CONTRACT 0 violations, per-feature source-drift 0 findings across 20 features.

- [design-system] 2026-06-29 — Generated "Open Banking — Trust Blue" M3 design system from the exact Material Theme Builder export (seed #266489): DESIGN.md (@google/design.md spec), design-tokens.yaml (29 light + 29 dark roles verbatim), COMPONENTS.md, components/_index.yaml. Minimalist-ui, accessibility-first, theme auto.
- [design-system-stitch] 2026-06-29 — DESIGN.md uploaded to Stitch (asset_id=8085591672064527850, design_md_sha=a3958d1ca6f3)
- 2026-08-02 [design] design-system — claude — 1.2.0 / tokens 2.2.0: added the named spacing steps + icon, border, opacity and touch_targets families and promoted radius to top level, closing design-validate F-004 (49 unresolvable token references)
- 2026-08-02 [design] design-system — claude — 1.3.0 / tokens 2.3.0: added semantic.status mapping warning onto tertiary and success onto primary, with four measured contrast pairs, rather than adding amber and green tonal families
- [design-system-stitch] 2026-08-07 — DESIGN.md uploaded to Stitch (asset_id=2047482829824847747, design_md_sha=09905e9e2d99)
