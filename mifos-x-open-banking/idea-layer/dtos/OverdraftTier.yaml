name: OverdraftTier
version: "1.0.0"
source:
  origin: project
  api_path: "GET /obie/open-banking/v4.0/aisp/accounts/{AccountId}/product"
  api_ref: API-001
  # Version corrected 2026-08-06 — was v3.1; observed calls are all v4.0.
  source_binding: "core/model/src/commonMain/kotlin/org/mifosx/openbanking/core/model/banking/ProductTerms.kt"
description: "One overdraft rate band within a product's terms. The rate is a decimal string, displayed verbatim."
fields:
  - name: overdraftType
    type: String
    required: true
    pii: false
    description: "Overdraft classification this band applies to — Committed, OnDemand and so on"
    primary_key: false
  - name: ear
    type: String
    required: true
    pii: false
    description: "Effective annual rate for the band, as a decimal string"
    primary_key: false
used_by:
  - feature: product
    api_id: product
    role: response
enums: []
