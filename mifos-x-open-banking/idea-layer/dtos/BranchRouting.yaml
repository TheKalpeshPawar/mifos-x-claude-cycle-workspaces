name: BranchRouting
version: "1.0.0"
source:
  origin: project
  api_path: "UNRESOLVED — Open Data branch endpoint; host not established (see Branch.yaml)"
  host_status: unresolved
  api_ref: null
  spec: "UK Open Banking Open Data — outside the OBIE consent surface"
description: "Routing information for a bank branch, identifying the routing scheme (e.g. UK.OBIE.SortCode, UK.OBIE.IBAN, BIC) and the corresponding address or identifier value."
fields:
  - name: scheme
    type: String
    required: true
    pii: false
    description: "Routing scheme identifier (e.g. 'UK.OBIE.SortCode', 'UK.OBIE.IBAN', 'BIC')"
    primary_key: false
  - name: address
    type: String
    required: true
    pii: false
    description: "The routing address or code value for the given scheme"
    primary_key: false
used_by:
  - feature: atm-locator
    api_id: get_branches
enums: []
