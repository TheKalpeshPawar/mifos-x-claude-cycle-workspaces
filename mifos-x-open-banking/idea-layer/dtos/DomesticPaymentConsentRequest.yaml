name: DomesticPaymentConsentRequest
version: "1.0.0"
source:
  origin: project
  api_path: "POST /obie/open-banking/v4.0/pisp/domestic-payment-consents"
  api_ref: API-003
  spec: "OBIE Read/Write v4.0"
  # Version corrected 2026-08-06 (/idea-sync): was v3.1. Every observed call in the PISP-tests
  # corpus is v4.0, and the two generations differ materially — v4.0 re-coded every UK.OBIE.*
  # error string to a short code (U003/U004/U009/U014/U027).
  source_binding: "core/network/src/commonMain/kotlin/org/mifosx/openbanking/core/network/model/pisp/domesticPayment/request/DomesticPaymentConsentRequest.kt"
description: "The consent a PSU authorises before a domestic payment can be submitted. Registered from the network wire type: send-money is specified-not-built (PROJECT_CONFIG#feature_specified_not_built), so core/network carries this model but no feature module consumes it yet."
fields:
  - name: Data
    type: Object
    required: false
    pii: true
    description: "Initiation block — debtor and creditor accounts, instructed amount and remittance information"
    primary_key: false
  - name: Risk
    type: Object
    required: false
    pii: false
    description: "OBIE Risk block describing the payment context the ASPSP scores against"
    primary_key: false
used_by:
  # Repointed 2026-08-06 — send-money was superseded by the seven per-type payment screens.
  - feature: pay-domestic-single
    api_id: stage_domestic_payment_consent
    role: request
enums: []
