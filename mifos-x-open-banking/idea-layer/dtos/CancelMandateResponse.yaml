name: CancelMandateResponse
version: "1.0.0"
source:
  origin: project
  api_path: "DELETE /obp/v7.0.0/banks/{bankId}/accounts/{accountId}/standing-orders/{standingOrderId}"
description: "Response from the DELETE mandate endpoint confirming cancellation outcome."
fields:
  - name: success
    type: Boolean
    required: true
    pii: false
    description: "True if the mandate was cancelled successfully"
    primary_key: false
  - name: message
    type: String
    required: false
    pii: false
    description: "Confirmation copy on success, or error detail on failure"
    primary_key: false
used_by:
  - api_id: obp_cancel_mandate
    feature: direct-debit-detail
    role: response
