# Figma Prompts — Securing Payment (Payment Authorise Handoff)

> These prompts are structured design briefs for Figma generation / Stitch. One section per declared screen state. Derive all colours, typography, and sizing from the M3 earth-green design system tokens documented in SPEC.md.

---

## State: preparing

**Prompt:** Design a full-screen mobile takeover (390px wide, no top bar, no bottom nav) for a payment-authorise handoff screen. Background #F9FAEF. Vertical column layout, centre-aligned both axes, 24dp padding.

**Top section:** A Material Icons "account_balance" icon, 80dp, colour #4C662B, centred. Below it, a headline-small heading "Getting your payment ready to authorise" (Outfit 24sp, #1A1C16, centre). Below that, body-medium body text (Outfit 14sp, #44483D, centre, multi-line): "You'll authorise this payment directly with HSBC. Your username, password and security codes are never entered in this app — and the payment is only sent once you've approved it."

**Summary card:** White (#FFFFFF) filled card, 16dp corner radius, 1dp border colour #CDEDA3, 16dp padding, full-width. Inside: "£250.00" in display-small (Outfit 36sp, bold 700, #4C662B, centre). Below: "To Jordan Avery" in body-large (Outfit 16sp, #1A1C16, centre). Below: "Authorising securely with HSBC Open Banking" in body-small (Outfit 12sp, #44483D, centre).

**Progress:** Below the card, a 32dp circular indeterminate progress indicator, colour #4C662B, centred.

**Footer note:** "Encrypted connection · You can return here anytime" in body-small (Outfit 12sp, #74796D, centre).

**No other interactive elements.** No top app bar. No bottom nav.

---

## State: redirecting

**Prompt:** Same full-screen takeover as `preparing` with these changes:

1. Heading changes to "Taking you to HSBC to authorise this payment" (same Outfit/headline_small, #1A1C16, centre).
2. The 32dp circular spinner is **replaced** by a filled primary button spanning the full content width: label "Continue to HSBC", #4C662B fill, #FFFFFF text (Outfit/label_large 14sp/500), 8dp corner radius, 16dp padding. This button is the manual-launch fallback for the HSBC authorise URL.
3. The summary card (#FFFFFF, 16dp radius, 1dp #CDEDA3 border) still shows £250.00 / To Jordan Avery / Authorising securely with HSBC Open Banking.
4. Security note "Encrypted connection · You can return here anytime" (Outfit/body_small, #74796D) remains below the button.

Everything else (background, bank icon, security explainer) is identical to the `preparing` state.

---

## State: error

**Prompt:** Full-screen mobile takeover (390px, no bars, #F9FAEF background) for a payment-authorise failure.

**Top section:** "account_balance" icon 80dp #4C662B centred. Heading "Couldn't start authorisation" (Outfit/headline_small 24sp, #1A1C16, centre).

**Error card:** Filled card, background #FFDAD6, 8dp corner radius, 16dp padding, full-width. Inside: a row with a "error" Material icon (20dp, colour #BA1A1A) leading the copy "We couldn't reach HSBC to authorise your payment. No money has left your account. Please check your connection and try again." (Outfit/body_small 12sp, colour #410002). The card has an accessibility role of "alert".

**Primary action:** Filled button below the error card, full content width: label "Try again", #4C662B fill, #FFFFFF text (Outfit/label_large 14sp/500), 8dp corner radius, 16dp padding.

**Secondary action:** Text button below the primary: label "Cancel", #386663 text (Outfit/label_large 14sp/500), min-height 44dp, centre-aligned.

**No spinner. No summary card. No security explainer.** Security note not shown in error state.

---

_Generated by /idea export | 2026-06-14_
