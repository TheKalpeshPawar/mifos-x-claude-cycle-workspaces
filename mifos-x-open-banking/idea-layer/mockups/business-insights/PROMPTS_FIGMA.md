# Figma Prompts — Business Insights

> Structured design briefs for Figma generation / Stitch. One section per declared screen state. Derive all colours, typography, and sizing from the M3 earth-green design system tokens documented in exports/business-insights/SPEC.md.

---

## State: populated

**Prompt:** Design a scrollable mobile dashboard screen (390px wide) for business cash-flow analytics. M3 TopAppBar with title "Business Insights" and leading `arrow_back` icon, background #F9FAEF, no bottom nav.

**Account selector row:** An M3 assist chip labelled "TechStart — Business Current GBP" with a trailing `expand_more` icon. Below it, a horizontal-scrolling row of filter chips: Week / Month / Quarter / Year / Custom. Below the chip row, a period label "June 2026" in Outfit/body_medium (14sp), colour #5C6057.

**Cash-flow card:** surface_container_low card, 16dp corner radius, 16dp padding, 20dp horizontal margin. Inside: a 3-column equal-distribution row. Column 1 label "Money In" + value "£75000.00". Column 2 label "Money Out" + value "-£26300.00". Column 3 label "Net" + value "£48700.00" (coloured #4C662B). All values in Outfit/title_small (14sp/500), bold.

**Expenses section heading:** "Expenses by Category" in Outfit/title_small (14sp/500), semibold, 20dp left padding, role heading level 2.

**Expense donut card:** surface_container_low card, 16dp radius, 16dp padding, 20dp horizontal margin. Inside: a donut chart centred at top, then a legend list with 5 rows (each: coloured dot + category name left-aligned, signed amount right-aligned): (1) Payroll & Contractors -£15516.01, (2) Tax -£8000.00, (3) Rent & Facilities -£2500.00, (4) Software & Subscriptions -£450.00, (5) Insurance -£350.00. No Income row in the donut — expenses only.

**Top counterparties heading + card:** "Top Counterparties" heading (same style as expenses title). Below it, a surface_container_low card (16dp radius, 20dp margin) with one row: "Mifos-X-Open-Bank" left-aligned; "1 payment" and "-£15000.00" right-aligned.

---

## State: loading

**Prompt:** Same screen structure as `populated` but with the data cards replaced by skeleton placeholders. Account selector chip, period chip row, and period label ("June 2026") remain visible. Below them: three skeleton cards, each 120dp tall, surface_container_low fill, 16dp radius, 20dp horizontal margin. Apply a shimmer animation (200ms cycle); reduced-motion fallback = static fill. No donut, no legend, no counterparty rows.

---

## State: empty (no business accounts)

**Prompt:** Mobile screen (390px) with M3 TopAppBar ("Business Insights", arrow_back, #F9FAEF). No account selector, no period selector. Centred layout: `storefront` icon (48dp, colour #44483D), then heading "No business accounts" (Outfit/title_small, #1A1C16, centre), then detail text "Business insights appear once a business account is connected to your profile." (Outfit/body_medium, #44483D, centre, 32dp horizontal padding). No buttons.

---

## State: no_activity (account selected, zero transactions)

**Prompt:** Same as `populated` shell but with all data cards replaced. Account selector chip, period chip row, and period label remain. Below them: `storefront` icon (48dp, #44483D, centred), heading "No activity in this period" (Outfit/title_small, #1A1C16, centre), detail "Cash flow for this business account will appear once it has transactions in the selected period." (Outfit/body_medium, #44483D, centre).

---

## State: error (fetch failed)

**Prompt:** Mobile screen with M3 TopAppBar (same style). Centred layout in the content area: body-large text "Could not load business insights" (#1A1C16, centre), body-medium text "Check your connection and try again." (#44483D, centre), then a full-width filled button "Retry" (#4C662B fill, #FFFFFF text, Outfit/label_large 14sp/500, 16dp padding, 8dp corner radius).

---

_Generated by /idea export | 2026-06-14_
