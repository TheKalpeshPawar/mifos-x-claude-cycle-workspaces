# MOCKUP — Splash Screen

| Field   | Value  |
|---------|--------|
| Feature | splash |
| Flavor  | shared |

---

## Screen Layout

The screen is a full-viewport centered column (`alignment: center`, both axes) on a surface background (#FCF8FF). There is no scroll; the content is vertically and horizontally centered at all times.

Top to bottom component hierarchy:

1. `splash_logo` — Mifos X brand mark (120×120 dp)
2. `splash_app_name` — Application name in display_medium type
3. `splash_tagline` — Supporting tagline in body_large
4. `splash_loading_indicator` — Circular progress spinner

---

## Components

### splash_logo
- **Description:** Vector logo asset `mifos_logo`, rendered as a 120×120 dp image with tint overlay
- **Position:** Horizontally centered, vertically centered with siblings
- **Style tokens:**
  - Width/height: 120 dp
  - Tint: #1800B1 (Mifos deep purple)
  - Background: transparent (inherits surface)
- **Accessibility:** role=image, label="Mifos X application logo"

### splash_app_name
- **Description:** Static text "Mifos X Open Banking"
- **Position:** Below logo; padding-top = spacing.lg (24 dp)
- **Style tokens:**
  - Typography: Inter/display_medium (57 sp, weight 400)
  - Color: #1800B1
- **Accessibility:** role=heading, label="Mifos X Open Banking"

### splash_tagline
- **Description:** Static text "Banking for Everyone"
- **Position:** Below app name; padding-top = spacing.sm (8 dp)
- **Style tokens:**
  - Typography: Manrope/body_large (16 sp, weight 400)
  - Color: #008B8B (accent teal)
- **Accessibility:** role=text, label="Banking for Everyone"

### splash_loading_indicator
- **Description:** Animated circular progress indicator (indeterminate)
- **Position:** Below tagline; padding-top = spacing.xl (32 dp)
- **Style tokens:**
  - Size: 40 dp
  - Color: #1800B1
- **Accessibility:** role=progressbar, label="Loading, please wait"

---

## Interaction Patterns

- **No user-initiated interactions.** The screen is fully display-only.
- Auto-navigation fires after 2 000 ms if `hasValidToken == false` → pushes Login.
- Auto-navigation fires immediately (0 ms) if `hasValidToken == true` → pushes Home (CONSUMER) or FO Dashboard (FIELD_OFFICER).
- The loading spinner is always animated during both `loading` and `navigating` states to signal progress.
- Tap events are swallowed (no interactive tap targets on this screen).

---

## Content Data

| Component          | Content Value            |
|--------------------|--------------------------|
| splash_app_name    | "Mifos X Open Banking"   |
| splash_tagline     | "Banking for Everyone"   |
| splash_logo        | Asset: mifos_logo        |

---

## Design Notes

**Color usage:**
- Primary #1800B1 applied to logo tint, app name text, and loading spinner — establishes strong brand identity at first paint.
- Accent teal #008B8B used only for tagline — creates visual hierarchy without competing with the primary brand hue.
- Surface background #FCF8FF (warm white) avoids harsh contrast against the deep purple, consistent with the MD3 surface role.

**Typography:**
- `Inter/display_medium` (57 sp) for the app name gives the opening screen a bold typographic statement.
- `Manrope/body_large` (16 sp) for the tagline maintains legibility at a smaller scale.
- No bold weight overrides — the weight pairing (display vs body) creates the hierarchy.

**Spacing:**
- All spacing uses the 8 dp grid: spacing.sm=8, spacing.lg=24, spacing.xl=32.
- No horizontal padding on text components — centering alignment handles horizontal positioning.

**Accessibility:**
- The circular progress indicator carries a meaningful `label="Loading, please wait"` for screen readers.
- App name is marked as `role=heading` so TalkBack announces it as the page title.
- No minimum touch target requirements apply (no interactive elements).

**Animation:**
- Loading indicator is indeterminate — no duration or percentage values displayed.
- Transition from splash to login/home uses the standard shared-axis Z (material forward) navigation animation.

---

_Generated by /idea export | 2026-05-25_
