id: transaction-review
name: transaction-review
description: "Transaction history flow — view transactions for an account with date and credit/debit filters, search, and open a single transaction for full detail. Category tags are applied to each transaction to feed the PFM feature."
type: happy
feature_ref: transactions
release_phase: P0

screens:
  - transactions
  - transaction-detail

entry_screen: transactions

connections:
  - { from: transactions, to: transaction-detail, type: push, label: "Tap transaction row" }
  - { from: transactions, to: account-detail, type: push, label: "Back button (cross-flow → account-browsing)" }
  - { from: transaction-detail, to: transactions, type: push, label: "Back button" }
