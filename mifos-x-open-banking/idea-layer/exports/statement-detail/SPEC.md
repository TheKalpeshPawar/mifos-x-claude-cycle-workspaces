<!-- source: screens/statement-detail/ui.yaml -->
<!-- source_hash: regenerated-2026-07-16 -->
<!-- generated: 2026-07-16T00:00:00Z -->

# Statement Detail — Implementation Specification

> **Source of Truth**: `idea-layer/screens/statement-detail/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 1.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Statement Detail | StatementDetailViewModel | loading, content, empty, error | Parallel fetch via ktorfit: GET statements/{StatementId} (OBStatement2), GET statements/{StatementId}/transactions (OBReadTransaction6), and GET statements/{StatementId}/file (application/pdf). Statement amounts, fees, and interest render from OBStatement2. PDF streams via ktor-client-download and hands off to the platform share/save sheet. A separate downloadState (Idle\|Downloading\|Downloaded\|DownloadError) drives inline progress indicator and snackbar feedback without blocking the main content state. Empty state fires when statementTransactions returns an empty array — valid OBIE edge case for new accounts. |

---

## State Model

### StatementDetailViewModel

**State class**: `StatementDetailState`

| Field | Type | Notes |
|-------|------|-------|
| uiState | StatementDetailUiState | Screen-level sealed state |
| downloadState | DownloadState | Secondary sealed state driving the PDF download flow independently of uiState. Members: `Idle \| Downloading \| Downloaded(uri: URI) \| DownloadError(message: String)` |

**ScreenState sealed class**: `StatementDetailUiState`

| Member | Payload | Notes |
|--------|---------|-------|
| Loading | — | Parallel GET /statements/{id} + GET /statements/{id}/transactions in flight |
| Content | statement: OBStatement2, statementTransactions: List\<OBTransaction6\> | Both fetches succeeded; statementTransactions is non-empty |
| Empty | statement: OBStatement2 | statementTransactions returned an empty array — valid OBIE edge case for new or inactive accounts; period header and balances still render |
| Error | message: String, errorCode: StatementDetailErrorCode | Any 4xx or network failure from either parallel fetch |

**Error types** (`StatementDetailErrorCode`):

| Error Class | HTTP Status | Condition |
|-------------|-------------|-----------|
| StatementNotFound | 404 | StatementId not found for this account |
| ConsentMissingReadStatements | 403 | ReadStatementsDetail not in consent scope |
| SessionExpired | 401 | Access token expired; re-authentication required |
| StatementFileNotFound | 404 on /file endpoint | PDF not yet generated by HSBC |
| NetworkError | — | Device offline or AIS endpoint unreachable |

**Actions**:

| Action | Signature | Notes |
|--------|-----------|-------|
| statementDetailLoad | `suspend fun statementDetailLoad(accountId: String, statementId: String)` | Parallel GET /statements/{id} + GET /statements/{id}/transactions |
| downloadStatementPdf | `suspend fun downloadStatementPdf(accountId: String, statementId: String)` | GET /statements/{id}/file (Accept: application/pdf) via ktor-client-download |
| retryLoad | `suspend fun retryLoad(accountId: String, statementId: String)` | Delegates to statementDetailLoad |

**DI**:

| Dependency | Role |
|------------|------|
| StatementsRepository | GET /statements/{id} + GET /statements/{id}/transactions via ktorfit |
| StatementFileDownloader | GET /statements/{id}/file via ktor-client-download |

### State Transitions

| From | Trigger | To State |
|------|---------|----------|
| — | Screen mount | loading |
| loading | Both fetches succeed + statementTransactions non-empty | content |
| loading | Both fetches succeed + statementTransactions empty | empty |
| loading | HTTP 401 / 403 / 404 / network error | error |
| error | Retry tapped | loading |
| content / empty | Download PDF button tapped | downloadState: Idle → Downloading → Downloaded or DownloadError |
| content | Transaction row tapped | → transaction-detail (transactionId, accountId) |

---

## Component Inventory

### State: loading

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| (no id) | progress_indicator (circular) | loading | Full-screen loading indicator while parallel fetch of OBStatement2 + OBReadTransaction6 is in flight |

### State: content, empty

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| statement_header_card | card (elevation 2) | content, empty | Period header card showing statement identity fields from OBStatement2 |
| ↳ statement_reference | text (labelMedium, secondary color) | — | Human-readable statement reference (e.g. MAY-2026-STMT); from OBStatement2.StatementReference |
| ↳ statement_period | text (titleMedium) | — | Statement period range in ISO-8601; formatted by ViewModel to "dd MMM yyyy – dd MMM yyyy" from StartDateTime + EndDateTime |
| ↳ statement_type | text (bodySmall, on-surface-variant) | — | OBStatement2 Type field (e.g. RegularPeriodic) |
| ↳ statement_created | text (labelSmall, on-surface-variant) | — | Localized "Created" prefix followed by statement generation timestamp from HSBC AIS (CreationDateTime) |
| balances_header | section_header | content, empty | Section divider for OBStatement2.StatementAmount[] (OpeningBalance, ClosingBalance) |
| balances_list | list | content, empty | Renders all OBStatementAmount2 entries (Type, CreditDebitIndicator, Amount) |
| ↳ balance_row | list_item | — | Single balance entry; trailing content signed from CreditDebitIndicator (Credit = tertiary/green, Debit = error/red); trailing style: titleMedium |
| fees_header | section_header | content, empty | Section divider for OBStatement2.StatementFee[]; `visible_when: statement.StatementFee.length > 0` |
| fees_list | list | content, empty | Renders OBStatementFee1 entries (Description, Amount); `visible_when: statement.StatementFee.length > 0` |
| ↳ fee_row | list_item | — | Single fee entry (e.g. Monthly maintenance fee £0.00); trailing: Amount.Amount + Currency (bodyMedium) |
| interest_header | section_header | content, empty | Section divider for OBStatement2.StatementInterest[]; `visible_when: statement.StatementInterest.length > 0` |
| interest_list | list | content, empty | Renders OBStatementInterest1 entries (Description, Amount); `visible_when: statement.StatementInterest.length > 0` |
| ↳ interest_row | list_item | — | Single interest entry (e.g. In-credit interest £0.21); trailing color from CreditDebitIndicator (tertiary / error) |
| transactions_header | section_header | content, empty | Section divider for statement transactions sourced from OBReadTransaction6 |
| statement_txns_list | list (vertical scroll) | content | Scrollable list of OBTransaction6 entries within the statement period; items_source: `{statementTransactions}` |
| ↳ statement_txn_row | list_item | content | Tappable OBTransaction6 row: overline=BookingDateTime, supporting=TransactionInformation, trailing=signed amount (Credit=tertiary, Debit=error, titleSmall). Navigates to transaction-detail on tap |
| empty_txns_state | empty_state | empty | Shown when statementTransactions returns empty array — valid for new or inactive accounts in the period. Icon: receipt_long |
| download_pdf_button | button (outlined, icon: download) | content, empty | Triggers download_statement_pdf; `enabled_when: downloadState != Downloading` |
| download_progress | progress_indicator (linear) | content, empty | Indeterminate linear progress beneath Download PDF button while ktor streams the PDF binary. `visible_when: downloadState == Downloading` |
| download_result_snackbar | snackbar | content, empty | Auto-dismiss (4000ms) confirmation or error after download completes. `visible_when: downloadState == Downloaded \|\| downloadState == DownloadError` |

### State: error

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| error_state | empty_state (variant: error) | error | Full-screen error shown on 401/403/404/network error. Icon: error_outline; body: `{error.message}` |
| ↳ retry_button | button (filled) | — | Re-triggers retry_load which delegates to statementDetailLoad |

---

## Behaviour / Action Contracts

| Component ID | Action | Effect | Library Refs | Description |
|--------------|--------|--------|--------------|-------------|
| statement_txn_row | navigate_transaction_detail | navigate | — | Navigates to transaction-detail screen for the selected OBTransaction6 entry within the statement period, passing TransactionId and AccountId as route arguments. |
| download_pdf_button | download_statement_pdf | persist_file | ktor-client-download, hsbc-obie-ais-v4.0:statements-file | Calls GET statements/{StatementId}/file (Accept: application/pdf) via ktor-client-download, streams the PDF binary, and hands off to the platform share/save sheet; drives DownloadState from Idle through Downloading to Downloaded or DownloadError. |
| retry_button | retry_load | call_api | ktorfit, hsbc-obie-ais-v4.0:statements | Re-triggers the parallel fetch of OBStatement2 and OBReadTransaction6 after a 401, 403, 404, or network failure, returning the screen to the Loading state. |

---

## API Dependencies

| ID | Method | Path | Permission | Response DTO | Notes |
|----|--------|------|------------|--------------|-------|
| statement-detail | GET | /accounts/{AccountId}/statements/{StatementId} | ReadStatementsDetail | OBReadStatement2 | Returns StatementAmount, StatementFee, StatementInterest arrays; fetched in parallel with statement-txns |
| statement-txns | GET | /accounts/{AccountId}/statements/{StatementId}/transactions | ReadTransactions | OBReadTransaction6 | Requires ReadTransactions in addition to ReadStatementsDetail; empty array is a valid OBIE response |
| statement-file | GET | /accounts/{AccountId}/statements/{StatementId}/file | ReadStatements | null (binary PDF) | application/pdf binary stream; not JSON — no DTO mapping; triggered by download_pdf_button only |

---

## Navigation

- **Entry**: statements → tap statement row (params: accountId, statementId)
- **Row tap (transaction)**: → transaction-detail (params: transactionId, accountId)
- **Back**: top_app_bar leading icon → statements
- **Flow ref**: recurring-and-statements

---

## App-Shell

| Property | Value |
|----------|-------|
| bottom_navigation_visible | true |
| top_app_bar_visible | true |
| top_app_bar_title | `{statement.StatementReference}` (fallback: `{strings.stmt_detail.screen_title}`) |
| top_app_bar_leading | back |
| FAB visible | false |

---

## Testing (9 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-STMTD-001 | Period header renders StatementReference, period range, type, and creation date from OBStatement2 | critical |
| TC-STMTD-002 | Balances section shows OpeningBalance and ClosingBalance with correct sign and color from CreditDebitIndicator | critical |
| TC-STMTD-003 | Fees section hidden when StatementFee[] is empty; renders correct rows when populated | critical |
| TC-STMTD-004 | Interest section hidden when StatementInterest[] is empty; renders correct rows when populated | critical |
| TC-STMTD-005 | Transactions list renders OBTransaction6 rows; tap navigates to transaction-detail with correct transactionId and accountId | critical |
| TC-STMTD-006 | Empty state (empty_txns_state) shown when statementTransactions returns []; period header and balances still visible | critical |
| TC-STMTD-007 | Download PDF button disabled during Downloading; linear progress visible; success snackbar on Downloaded | normal |
| TC-STMTD-008 | DownloadError triggers error snackbar message; button re-enabled | normal |
| TC-STMTD-009 | 401/403/404/network error shows error_state with retry_button; retry returns to loading then content | normal |
