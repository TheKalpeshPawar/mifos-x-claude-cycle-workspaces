<!-- source: screens/accounts/ui.yaml -->
<!-- source_hash: regenerated-2026-07-14 -->
<!-- generated: 2026-07-14T12:00:00Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 1.0.0 -->
<!-- prior_version: — -->
<!-- note: docs.yaml#feature_id not declared; using screen_id=accounts as identifier -->

# Accounts -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/accounts/`
> **Auto-generated by**: `/idea export` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0 | **Status**: designed (quality: 94)

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Accounts | AccountsViewModel | Loading, Content, ConsentExpiring, Empty, Error | Authorised accounts list with balance preview per account. Primary bottom-nav anchor (tab order 1). Supports multi-currency cards (GlobalMoney/GlobalWallet). Client-side AccountSubType filter chips. Consent expiry warning banner when < 30 days remain. Entry point to account-detail. |

## State Model

### AccountsViewModel

- **State**: `accounts: List<AccountWithBalance>`, `filteredAccounts: List<AccountWithBalance>`, `activeFilter: AccountSubType?`, `totalBalanceFormatted: String`, `totalBalanceAccountCount: Int`, `activeConsentId: String`, `consentDaysRemaining: Int`, `isConsentExpiring: Boolean`, `uiState: UiState`
- **ScreenState**: `Loading`, `Content`, `ConsentExpiring`, `Empty`, `Error(cause)`
- **Errors**: `401 → session expired (navigate to login)`, `403 → consent revoked (navigate to consent-list)`, `429 → rate limited (retry CTA)`, `503 → unavailable (retry CTA)`, `network_error → no connectivity (retry CTA)`
- **Events**: `NavigateAccountDetail(accountId: String)`, `NavigateConsentDetail(consentId: String)`, `NavigateLogin`, `NavigateConsentList`
- **Actions**: `LoadAccountsAndBalances()`, `RetryLoad()`, `FilterAccounts(subtype: String?)`, `NavigateAccountDetail(accountId: String)`, `NavigateReconfirmConsent(consentId: String)`
- **DI**: `ktorfit` (AisApiService — accounts + per-AccountId balances), `localConsentCache` (ExpirationDateTime read), `kotlin.coroutines:async/awaitAll` (parallel balance fan-out)

## API Endpoints (2)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| getAccounts | GET | (none) | OBReadAccount6 (Data.Account[]) | 401, 403, 429, 503 | — |
| getAccountBalances | GET | AccountId(String) | OBReadBalance1 (Data.Balance[]) | 401, 403, 404 | — |

## Dependencies (Tier 1)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| consent-callback | upstream | true | OAuth callback lands user on Accounts after successful consent; accounts screen is the post-consent landing |
| login | downstream | true | 401 error routes to login for re-authentication |
| consent-list | downstream | false | 403 consent-revoked error navigates here for re-authorisation |
| account-detail | downstream | true | Account card tap passes AccountId to account-detail |
| consent-detail | downstream | false | ConsentExpiring banner routes here for 90-day OBIE reconfirmation |

## Navigation

- **Route**: `AccountsRoute` (no params — bottom-nav primary destination)
- **From**: `app_shell_bottom_nav` (Accounts tab, order 1), `consent-callback` (OAuth success), `back_stack_pop` (from account-detail)
- **To**: `account-detail(accountId)`, `login` (on 401), `consent-detail(consentId)` (on consent_expiring), `consent-list` (on 403)

## App-Shell

| Property | Value |
|----------|-------|
| bottom_navigation_visible | true |
| top_app_bar_visible | true |
| top_app_bar_title | `{strings.accounts.screen.title}` |
| top_app_bar_leading | auto (none — bottom-nav root) |
| fab_visible | false |
| safe_area_top | respect |
| safe_area_bottom | respect |
| notch_handling | extend-and-pad |

## Flow Logic (3 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Empty accounts check | `Data.Account[].isEmpty()` | empty | continue to balance fan-out | OBIE response with empty array triggers empty state immediately |
| Consent expiry check | `consentDaysRemaining < 30` | consent_expiring | content | ChronoUnit.DAYS.between(Instant.now(), ExpirationDateTime) from localConsentCache |
| Balance partial failure | `balances[AccountId] returns 403` | mark balance_unavailable; keep card | normal balance render | Per-account error isolation: other cards unaffected by one ReadBalances permission gap |

## Testing (10 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-ACCTS-001 | Accounts list loads and renders balance preview for each account | high |
| TC-ACCTS-002 | Loading state shows shimmer skeleton cards during fetch | high |
| TC-ACCTS-003 | Error state with Retry button on 401 token expired | high |
| TC-ACCTS-004 | Empty state when no accounts returned | high |
| TC-ACCTS-005 | Tap account card navigates to account-detail with accountId param | high |
| TC-ACCTS-006 | GlobalMoney and GlobalWallet multi-currency cards render correctly | high |
| TC-ACCTS-007 | Consent expiry banner shown when consent expires within 30 days | high |
| TC-ACCTS-008 | CreditCard account displays 'Balance owed' badge in error colour | high |
| TC-ACCTS-009 | Account type filter chip narrows visible account cards | high |
| TC-ACCTS-010 | Partial balance failure — account card shown without balance when ReadBalances absent | high |
