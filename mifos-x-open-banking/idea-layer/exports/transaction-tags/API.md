# API Reference — Transaction Tags & Notes

| Field | Value |
|---|---|
| Feature | transaction-tags |
| Base URL | https://apisandbox.openbankproject.com |
| Auth Scheme | DirectLogin (header: `DirectLogin token=<token>`) |

---

## GET /obp/v1.2.1/banks/{bankId}/accounts/{accountId}/owner/transactions/{transactionId}/metadata/tags

**Tag:** Transaction-Metadata
**Purpose:** Fetch all tags previously added to a specific transaction, displayed as chips on screen load.

### Path Parameters

| Parameter | Type | Description |
|---|---|---|
| bankId | String | OBP bank identifier |
| accountId | String | Account containing the transaction |
| transactionId | String | Specific transaction identifier |

### Response Fields

| Field | Type | Description |
|---|---|---|
| tags | List\<TransactionTag\> | Array of tag objects |
| id | String | Unique tag identifier |
| value | String | Tag label text (e.g. "Groceries") |
| date | String | ISO 8601 timestamp when tag was added |
| user | User | User object who added the tag |

### Error Codes

| Code | Meaning |
|---|---|
| 401 | USER_NOT_LOGGED_IN |
| 403 | INSUFFICIENT_AUTHORISATION |
| 404 | TRANSACTION_NOT_FOUND |

---

## POST /obp/v1.2.1/banks/{bankId}/accounts/{accountId}/owner/transactions/{transactionId}/metadata/tags

**Tag:** Transaction-Metadata
**Purpose:** Add a new tag to a transaction. Called when the user taps "Add" after typing a tag.

### Path Parameters

Same as GET endpoint above.

### Request Body

| Field | Type | Required | Description |
|---|---|---|---|
| value | String | Yes | Tag label text (e.g. "Organic") |

### Response Fields

| Field | Type | Description |
|---|---|---|
| id | String | Newly created tag identifier |
| value | String | The tag label |
| date | String | ISO 8601 creation timestamp |

### Error Codes

| Code | Meaning |
|---|---|
| 400 | INVALID_TAG_VALUE |
| 401 | USER_NOT_LOGGED_IN |

---

## POST /obp/v1.2.1/banks/{bankId}/accounts/{accountId}/owner/transactions/{transactionId}/metadata/comments

**Tag:** Transaction-Metadata
**Purpose:** Add a private note/comment to a transaction. Called when the user saves the notes text area content.

### Path Parameters

Same as GET endpoint above.

### Request Body

| Field | Type | Required | Description |
|---|---|---|---|
| value | String | Yes | Comment/note text |

### Response Fields

| Field | Type | Description |
|---|---|---|
| id | String | Newly created comment identifier |
| value | String | The note content |
| date | String | ISO 8601 creation timestamp |

### Error Codes

| Code | Meaning |
|---|---|
| 400 | INVALID_COMMENT_VALUE |
| 401 | USER_NOT_LOGGED_IN |

---

## POST /obp/v1.2.1/banks/{bankId}/accounts/{accountId}/owner/transactions/{transactionId}/metadata/images

**Tag:** Transaction-Metadata
**Purpose:** Attach a receipt image URL to a transaction after the image has been uploaded to cloud storage.

### Path Parameters

Same as GET endpoint above.

### Request Body

| Field | Type | Required | Description |
|---|---|---|---|
| label | String | Yes | Image description (e.g. "Receipt") |
| URL | String | Yes | Public URL of the uploaded image |

### Response Fields

| Field | Type | Description |
|---|---|---|
| id | String | Newly created image record identifier |
| label | String | Image description |
| URL | String | Public image URL |
| date | String | ISO 8601 creation timestamp |

### Error Codes

| Code | Meaning |
|---|---|
| 400 | INVALID_IMAGE_URL |
| 401 | USER_NOT_LOGGED_IN |

---

*Generated by /idea export | 2026-05-25*
