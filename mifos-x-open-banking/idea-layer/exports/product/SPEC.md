<!-- source: screens/product/ui.yaml -->
<!-- source_hash: generated-2026-07-16 -->
<!-- generated: 2026-07-16T00:00:00Z -->
<!-- generated_from_feature_version: 2.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->

# Product Terms — Implementation Specification

> **Source of Truth**: `idea-layer/screens/product/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 2.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Product Terms | ProductViewModel | loading, content, empty, error | Calls GET /accounts/{AccountId}/product (OBIE OBReadProduct2) to retrieve the PCA or BCA product definition associated with the account. Parses OBProduct2.PCA for credit interest tier bands (AER, ApplicationFrequency, TierValueMinimum/BandLimit range), overdraft tier bands (EAR, OverdraftType), monthly maximum charge, and features list. Handles empty response (account types without OBProduct2 entries, e.g. GlobalMoney, Savings, CreditCard) as a distinct empty state separate from error. Requires ReadProducts permission in the active consent. Read-only display with no mutations. |

---

## App-Shell

| Attribute | Value |
|-----------|-------|
| Bottom navigation | visible |
| Top app bar | visible — title: `{strings.product_terms_title}`, leading: back |
| FAB | hidden |

---

## State Model

### ProductViewModel

**State class:** `ProductState`

| Field | Type | Notes |
|-------|------|-------|
| `accountId` | `String` | OBIE AccountId passed from account-detail via navigation |
| `product` | `OBProduct2?` | null until Content state; PCA or BCA entry from OBReadProduct2 |
| `uiState` | `ProductUiState` | Current sealed-class state |

**Sealed class:** `ProductUiState`

| Member | Notes |
|--------|-------|
| `Loading` | GET /accounts/{id}/product in flight |
| `Content` | product: OBProduct2 — non-null PCA or BCA entry |
| `Empty` | OBReadProduct2.Data empty or PCA/BCA null (GlobalMoney / Savings / CreditCard account types) |
| `Error` | HTTP 401 / 403 / 404 / network failure from GET /accounts/{id}/product |

**Error types:** `TokenExpiredError` (HTTP 401 — re-authentication required) · `ConsentScopeError` (HTTP 403 — ReadProducts not in active consent) · `ProductNotFoundError` (HTTP 404 — no OBProduct2 record for this AccountId) · `NetworkError` (device offline or AIS endpoint unreachable)

**Actions:**

| Action | Signature | Notes |
|--------|-----------|-------|
| `productLoad` | `suspend fun productLoad(accountId: String)` | GET /accounts/{accountId}/product → OBReadProduct2; dispatched on screen mount |
| `retryLoad` | `suspend fun retryLoad(accountId: String)` | Delegates to productLoad(accountId) after error state |

**DI:** `ProductRepository` (wraps ktorfit GET /accounts/{AccountId}/product) · `NavigationController` (KMP navigation abstraction for back-stack pop)

---

## Components

| ID | Type | State Binding | Data-Driven | Purpose |
|----|------|--------------|-------------|---------|
| (unnamed) | progress_indicator (variant=circular) | loading | false | Shown while OBReadProduct2 fetch is in flight from GET /accounts/{id}/product |
| `product_header_card` | card (elevation=2) | content | true | Product identity card: OBProduct2 ProductName, ProductType enum, and ProductId |
| ↳ `product_type_label` | text (labelMedium, secondary, role=overline, i18n=skip) | content | true | OBProduct2 ProductType enum (e.g. PCA = PersonalCurrentAccount, BCA = BusinessCurrentAccount) |
| ↳ `product_name` | text (headlineMedium, role=title, i18n=skip) | content | true | Human-readable product name from OBProduct2 (e.g. "HSBC Advance Account") |
| ↳ `product_id` | text (labelSmall, on-surface-variant, role=caption) | content | true | Product ID label referencing `{product.ProductId}` |
| `fees_header` | section_header | content | false | "Fees" section divider for PCA ProductDetails fee fields |
| `monthly_max_charge_row` | list_item | content | true | OBPCAProductDetails1 MonthlyMaximumCharge; £0.00 for fee-free accounts; i18n trailing: `{product.PCA.ProductDetails.MonthlyMaximumCharge}` |
| `credit_interest_header` | section_header | content | false | "Credit Interest" section divider for OBCreditInterest1 TierBandSet[] entries |
| `credit_interest_list` | list (items_source=`{product.PCA.CreditInterest.TierBandSet}`) | content | true | Iterates OBTierBandSet1 groups; each group has a TierBandMethod and one or more TierBand entries |
| ↳ `tier_band_list` | list (items_source=`{item.TierBand}`) | content | true | Iterates OBTierBand1 entries within each TierBandSet |
| ↳↳ `tier_band_row` | list_item | content | true | Single AER tier. supporting_text: "Up to {item.BandLimit} · paid {item.ApplicationFrequency}"; trailing (titleMedium): "{item.AER}% AER". Template vars: TierValueMinimum, BandLimit, AER, ApplicationFrequency |
| `overdraft_header` | section_header | content | false | "Overdraft" section divider for OBOverdraft1 OverdraftTierBandSet[] entries |
| `overdraft_list` | list (items_source=`{product.PCA.Overdraft.OverdraftTierBandSet}`) | content | true | Iterates OBOverdraftTierbandSet1 groups for arranged and unarranged overdraft |
| ↳ `overdraft_tier_list` | list (items_source=`{item.OverdraftTierBand}`) | content | true | Iterates OBOverdraftTierBand1 entries; OverdraftType distinguishes Arranged vs Unarranged |
| ↳↳ `overdraft_tier_row` | list_item | content | true | Single EAR tier. supporting_text: "{item.OverdraftType} overdraft"; trailing (titleMedium): "{item.EAR}% EAR" |
| `features_header` | section_header | content | false | "Features" section divider for PCA ProductDetails.Features string array |
| `features_list` | list (items_source=`{product.PCA.ProductDetails.Features}`) | content | true | Renders marketing/capability feature strings associated with the PCA product |
| ↳ `feature_row` | list_item (leading_icon=check_circle, leading_icon_color=primary, i18n=skip) | content | true | Single feature string with check_circle icon; supporting_text is dynamic data from OBProduct2 |
| `empty_product_state` | empty_state (variant=informational, icon=info_outline) | empty | false | Shown when OBReadProduct2 Data is empty or account type (GlobalMoney, Savings, CreditCard) has no OBProduct2 entry; informational — not an error; no Retry button |
| `error_state` | empty_state (variant=error, icon=error_outline) | error | true | Shown on 401/403/404/network error; body = dynamic error message from ViewModel |
| ↳ `retry_button` | button (variant=filled) | error | false | Re-triggers retry_load → product_load ViewModel action |

---

## Behaviour / Action Contracts

| Component ID | Action | Effect | Library Refs | Description |
|--------------|--------|--------|--------------|-------------|
| `retry_button` | `retry_load` | `call_api` | ktorfit, hsbc-obie-ais-v4.0:product | Re-triggers product_load ViewModel action after a 401, 403, 404, or network error, re-fetching OBReadProduct2 from GET /accounts/{AccountId}/product. |

---

## State Transitions

| From | Trigger | To |
|------|---------|-----|
| — | Screen mount | loading (productLoad dispatched) |
| loading | 200 + non-null PCA or BCA | content |
| loading | 200 + Data[] empty or PCA/BCA null | empty (informational; no Retry) |
| loading | 401 / 403 / 404 / network error | error |
| error | retry_button tapped | loading (via retryLoad) |

---

## Navigation

| Direction | Source / Target | Trigger |
|-----------|----------------|---------|
| Entry | account-detail → product | Tap Product chip or menu entry in account-detail |
| Exit | product → account-detail | Back navigation (top app bar back icon or system gesture) |

**Flow ref:** account-browsing

---

## Capability Completeness

| Gate | Implementation |
|------|----------------|
| CC5 (permission visibility) | ReadProducts required; 403 error message explicitly states missing scope: "Consent does not include ReadProducts." |
| CC7 (network data policy) | No caching; always-fresh fetch on mount (cache_strategy: none); prevents stale charge information after bank updates |
| CC8 (cache lifecycle) | No local cache; every screen mount re-fetches from OBIE |
| CC9 (error recovery) | retry_button always visible in error state; empty state (GlobalMoney/Savings account types) intentionally has no Retry — informational, not recoverable |

---

## Testing (9 scenarios)

| Scenario | State | Priority |
|----------|-------|----------|
| Content renders product header, fees, credit interest AER tiers, overdraft EAR tiers, and features list | content | critical |
| Loading state shows circular spinner; content hidden | loading | critical |
| Error state on 401 shows "Session expired" message with Retry; empty state not triggered | error | critical |
| Empty state for GlobalMoney account (200 + empty Data[]); info icon; no Retry button | empty | normal |
| 403 PermissionNotGranted shows "Consent does not include ReadProducts" error message | error | normal |
| 404 shows "No product data available for this account" error message with Retry | error | normal |
| BCA path: OBProduct2.BCA non-null; content renders BCA tier data (not PCA path) | content | normal |
| Network error / IOException shows Retry CTA | error | normal |
| Retry button re-triggers productLoad after error state | error | normal |
