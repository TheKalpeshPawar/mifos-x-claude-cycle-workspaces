<!-- source: screens/user-onboarding/ui.yaml -->
<!-- source_hash: regenerated-2026-07-16 -->
<!-- generated: 2026-07-16T00:00:00Z -->

# User Onboarding — Implementation Specification

> **Source of Truth**: `idea-layer/screens/user-onboarding/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 1.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Welcome to Open Banking | UserOnboardingViewModel | loading, error, empty, intro, permissions_overview, consent_explainer, ob_explainer_open | Three-step onboarding pager educating the PSU about AISP consent before initiating the FAPI 1.0 Advanced flow. Step 1 (intro): hero + headline + body copy establishes UK Open Banking as FCA-regulated and password-free; FAPI 1.0 Advanced and FCA-regulated assist chips. Step 2 (permissions_overview): lists all six OBIE ReadX scopes requested in OBReadConsent1 in plain English, followed by a 90-day consent-duration note. Step 3 (consent_explainer): three AIS-only trust guarantees, FCA/OBIE v4.0 legal footer, and the primary CTA that navigates to the login screen where OBReadConsent1 is staged and the FAPI app-to-app redirect begins. The ob_explainer_open overlay state renders a bottom sheet with a three-step plain-English walkthrough of the FAPI consent journey. No API calls on this screen — all content is static educational copy leading to login. |

---

## State Model

### UserOnboardingViewModel

**State class**: `UserOnboardingState`

| Field | Type | Notes |
|-------|------|-------|
| currentStep | Int | 1 = intro, 2 = permissions_overview, 3 = consent_explainer |
| showObExplainerSheet | Boolean | true = ob_explainer_open overlay |

**ScreenState sealed class**: `UserOnboardingUiState`

| Member | Notes |
|--------|-------|
| Loading | Checking DataStore `onboarding_completed` flag before deciding to show the intro flow or skip to login |
| Error | DataStore IOException on init — PSU can retry the DataStore read |
| Empty | onboarding config absent or feature-flagged off — PSU directed to login without the educational flow |
| Intro | Step 1: hero + FAPI/FCA trust badges + Next CTA (currentStep = 1) |
| PermissionsOverview | Step 2: six OBIE ReadX scopes + consent duration note (currentStep = 2) |
| ConsentExplainer | Step 3: AIS-only guarantees + Connect CTA (currentStep = 3) |
| ObExplainerOpen | Overlay: three-step FAPI journey bottom sheet (showObExplainerSheet = true) |

**Error types**:

| Error Class | Condition |
|-------------|-----------|
| DataStoreIOError | IOException reading `onboarding_completed` flag from `androidx.datastore` |

**Actions**:

| Action | Signature | Notes |
|--------|-----------|-------|
| stepNext | `fun stepNext()` | currentStep = min(currentStep + 1, 3); maps 1→Intro, 2→PermissionsOverview, 3→ConsentExplainer |
| stepBack | `fun stepBack()` | currentStep = max(currentStep - 1, 1); no-op guard when already at step 1 |
| openObExplainer | `fun openObExplainer()` | showObExplainerSheet = true |
| closeObExplainer | `fun closeObExplainer()` | showObExplainerSheet = false |
| navigateToLogin | `fun navigateToLogin()` | Navigator.navigate(Route.Login); only callable from consent_explainer state (step 3) |
| retryOnboardingInit | (implicit re-read) | Re-reads `onboarding_completed` flag from DataStore in a coroutine; transitions through loading then to Intro on success |

**DI**:

| Dependency | Role |
|------------|------|
| OnboardingDataStore | `androidx.datastore` — reads/writes `onboarding_completed` flag |
| NavigationController | KMP navigation abstraction for `navigateToLogin` |

### State Transitions

| From | Trigger | To State |
|------|---------|----------|
| — | Screen mount | loading |
| loading | onboarding_completed=false + config present | intro (step 1) |
| loading | onboarding_completed=true | → login (skip onboarding) |
| loading | DataStore IOException | error |
| loading | onboarding config absent or feature-flagged off | empty |
| error | Retry tapped | loading |
| empty | Skip to login tapped | → login |
| intro | Next tapped | permissions_overview (step 2) |
| permissions_overview | Back tapped | intro (step 1) |
| permissions_overview | Next tapped | consent_explainer (step 3) |
| consent_explainer | Back tapped | permissions_overview (step 2) |
| consent_explainer | Connect with HSBC tapped | → login |
| consent_explainer | How Open Banking works tapped | ob_explainer_open |
| ob_explainer_open | Got it / dismiss | consent_explainer |

---

## Component Inventory

### State: loading

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| onboarding_loading_indicator | progress_indicator (circular) | loading | Shown briefly while ViewModel checks DataStore for a prior onboarding_completed flag before deciding to show the intro flow or skip directly to login |

### State: error

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| onboarding_error_state | empty_state (variant: error) | error | Shown when DataStore read of onboarding completion flag throws an I/O exception; icon: error_outline |
| ↳ onboarding_error_retry_button | button (filled) | — | Re-reads the onboarding_completed flag from DataStore in a coroutine; transitions through loading then to intro on success |

### State: empty

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| onboarding_empty_state | empty_state (variant: neutral) | empty | Graceful fallback when onboarding configuration is absent (e.g. remote feature flag disables onboarding); icon: info_outline |
| ↳ onboarding_empty_skip_button | button (filled) | — | Bypasses the onboarding flow and navigates directly to login when onboarding content is unavailable |

### State: intro (Step 1 of 3)

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| step_indicator_intro | stepper (horizontal, total_steps: 3, current_step: 1) | intro | Shows PSU they are on step 1 of 3 |
| hero_illustration | image (asset: ic_open_banking_hero) | intro | Hero illustration communicating trusted, FCA-regulated, secure bank data sharing; alt: `{strings.hero_illustration_description}` |
| intro_headline | text (headlineMedium, on-surface) | intro | Step 1 headline — explains UK Open Banking as secure, regulated account data sharing |
| intro_body | text (bodyMedium, on-surface-variant) | intro | Step 1 supporting body — FCA-regulated, read-only standard, password never shared |
| fapi_security_badge | chip (assist, icon: verified_user) | intro | Trust badge — FAPI 1.0 Advanced security protocol used for token exchange |
| fca_regulated_badge | chip (assist, icon: account_balance) | intro | Trust badge — app is FCA-regulated under Payment Services Regulations 2017 |
| intro_next_button | button (filled) | intro | Advances PSU from intro to permissions_overview (step 2) |

### State: permissions_overview (Step 2 of 3)

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| step_indicator_permissions | stepper (horizontal, total_steps: 3, current_step: 2) | permissions_overview | Shows PSU they are on step 2 of 3 |
| what_we_read_header | section_header | permissions_overview | Introduces the six OBIE ReadX permission categories |
| permissions_list | list (vertical scroll) | permissions_overview | All six OBIE ReadX permission categories that will be included in OBReadConsent1 |
| ↳ perm_accounts | list_item (icon: manage_accounts) | — | ReadAccountsDetail — account names, sort codes, IBANs for authorised accounts |
| ↳ perm_balances | list_item (icon: account_balance) | — | ReadBalances — current, savings, and credit card balances in real time |
| ↳ perm_transactions | list_item (icon: receipt_long) | — | ReadTransactionsDetail — up to 90 days of debits and credits |
| ↳ perm_standing_orders | list_item (icon: autorenew) | — | ReadStandingOrdersDetail — scheduled recurring payment mandates |
| ↳ perm_direct_debits | list_item (icon: subscriptions) | — | ReadDirectDebits — active mandates authorised on the account |
| ↳ perm_statements | list_item (icon: description) | — | ReadStatementsDetail — monthly statement summaries and available PDF references |
| consent_duration_note | text (bodySmall, on-surface-variant) | permissions_overview | Informs PSU that OBIE consent lasts up to 90 days and is revocable at any time |
| permissions_back_button | button (outlined) | permissions_overview | Navigate back to intro step |
| permissions_next_button | button (filled) | permissions_overview | Advances PSU to step 3 — trust guarantees and Connect CTA |

### State: consent_explainer (Step 3 of 3)

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| step_indicator_trust | stepper (horizontal, total_steps: 3, current_step: 3) | consent_explainer | Shows PSU they are on step 3 of 3 |
| what_we_never_do_header | section_header | consent_explainer | Introduces AIS-only trust guarantees |
| reassurance_list | list (vertical scroll) | consent_explainer | Three AIS-only guarantees: no PISP payments, no password exposure, revocable consent |
| ↳ never_payments | list_item (icon: money_off, icon_color: error) | — | AIS-only — no PISP capability, no money movement possible |
| ↳ never_password | list_item (icon: lock, icon_color: primary) | — | FAPI app-to-app redirect — HSBC credentials never leave HSBC's secure portal |
| ↳ never_locked_in | list_item (icon: cancel, icon_color: primary) | — | PSU can DELETE the OBIE consent at any time via Settings → Consents |
| legal_divider | divider | consent_explainer | Visual separator between trust guarantees and FCA legal footer |
| legal_footer | text (labelSmall, on-surface-variant) | consent_explainer | FCA regulation statement and OBIE v4.0 standard attribution |
| connect_hsbc_button | button (filled, icon: open_in_new) | consent_explainer | Primary CTA — navigates to login screen where OBReadConsent1 is staged and FAPI 1.0 Advanced redirect begins |
| trust_back_button | button (outlined) | consent_explainer | Navigate back to permissions_overview step |
| how_ob_works_button | button (text) | consent_explainer | Opens the FAPI consent journey explainer bottom sheet |

### State: ob_explainer_open (overlay on consent_explainer)

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| ob_explainer_sheet | bottom_sheet | ob_explainer_open | Three-step plain-English walkthrough of the FAPI consent journey |
| ↳ ob_explainer_title | text (titleLarge, on-surface) | — | Bottom sheet title |
| ↳ ob_step1 | list_item (icon: how_to_reg) | — | FAPI Step 1 — PSU approves the OBReadConsent1 permission request on HSBC's secure portal |
| ↳ ob_step2 | list_item (icon: login) | — | FAPI Step 2 — PSU authenticates directly on HSBC via SCA; credentials never shared |
| ↳ ob_step3 | list_item (icon: shield) | — | FAPI Step 3 — HSBC returns a short-lived PSU access token via signed FAPI response |
| ↳ ob_explainer_revoke_note | text (bodySmall, on-surface-variant) | — | Revoke consent reminder within the OB explainer sheet |
| ↳ ob_explainer_close_button | button (filled) | — | Closes the OB explainer sheet and returns PSU to consent_explainer step |

---

## Behaviour / Action Contracts

| Component ID | Action | Effect | Library Refs | Description |
|--------------|--------|--------|--------------|-------------|
| onboarding_error_retry_button | retry_onboarding_init | transform_state | kotlinx-coroutines, androidx.datastore | Re-reads the onboarding_completed flag from DataStore in a coroutine; transitions through loading then to intro on success, or remains in error state on failure. |
| onboarding_empty_skip_button | navigate_to_login | navigate | — | Bypasses the onboarding educational flow and navigates directly to the login screen when onboarding content is unavailable or disabled. |
| intro_next_button | step_next | transform_state | kotlinx-coroutines | Increments the ViewModel currentStep counter from 1 to 2, transitioning the screen state from intro to permissions_overview with no network call. |
| permissions_back_button | step_back | transform_state | kotlinx-coroutines | Decrements the ViewModel currentStep counter from 2 to 1, transitioning the screen state from permissions_overview back to intro with no network call. |
| permissions_next_button | step_next | transform_state | kotlinx-coroutines | Increments the ViewModel currentStep counter from 2 to 3, transitioning the screen state from permissions_overview to consent_explainer with no network call. |
| connect_hsbc_button | navigate_to_login | navigate | — | Primary CTA — navigates to the login screen where OBReadConsent1 is staged and the FAPI 1.0 Advanced app-to-app redirect to HSBC begins. |
| trust_back_button | step_back | transform_state | kotlinx-coroutines | Decrements the ViewModel currentStep counter from 3 to 2, transitioning the screen state from consent_explainer back to permissions_overview with no network call. |
| how_ob_works_button | open_ob_explainer | transform_state | kotlinx-coroutines | Sets showObExplainerSheet to true in the ViewModel StateFlow, overlaying the ob_explainer_open bottom sheet on top of the consent_explainer step. |
| ob_explainer_close_button | close_ob_explainer | transform_state | kotlinx-coroutines | Sets showObExplainerSheet to false in the ViewModel StateFlow, dismissing the OB explainer bottom sheet and returning the PSU to the consent_explainer step. |

---

## API Dependencies

This screen makes **no API calls**. All content is static educational copy.

The downstream API call (OBReadConsent1 staging) occurs on the **login** screen, not here. The screen reads/writes solely the `onboarding_completed` flag via `OnboardingDataStore` (androidx.datastore).

---

## Navigation

- **Entry**: app launch with no active consent (first-run or post-revoke)
- **Entry (alternate)**: post_revoke_redirect — consent revoked from consent dashboard; PSU returned to start of onboarding
- **Within screen**: intro → permissions_overview → consent_explainer (stepNext / stepBack)
- **Overlay**: consent_explainer ↔ ob_explainer_open (openObExplainer / closeObExplainer)
- **Exit**: connect_hsbc_button → login (navigateToLogin; only callable from consent_explainer state)
- **Exit (empty state)**: onboarding_empty_skip_button → login
- **Flow ref**: onboarding-consent

---

## App-Shell

| Property | Value |
|----------|-------|
| bottom_navigation_visible | false |
| top_app_bar_visible | false |
| FAB visible | false |

> No chrome on this screen — full-bleed onboarding pager.

---

## Testing (12 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-ONBOARD-001 | Intro step renders hero, headline, body, FAPI chip, FCA chip, and Next button | high |
| TC-ONBOARD-002 | Tapping Next on intro navigates to permissions_overview step (stepper shows step 2/3) | high |
| TC-ONBOARD-003 | Permissions overview renders all six OBIE ReadX categories with correct icons and consent duration note | high |
| TC-ONBOARD-004 | ReadAccountsDetail is the first permission shown (sort codes and IBANs detail) | medium |
| TC-ONBOARD-005 | Tapping Back on permissions_overview returns to intro (stepper shows step 1/3) | medium |
| TC-ONBOARD-006 | Tapping Next on permissions_overview advances to consent_explainer (stepper shows step 3/3) | high |
| TC-ONBOARD-007 | Consent explainer renders three trust guarantees (never_payments, never_password, never_locked_in), legal footer, and Connect CTA | high |
| TC-ONBOARD-008 | Tapping 'Connect with HSBC' navigates to login screen | high |
| TC-ONBOARD-009 | Tapping Back on consent_explainer returns to permissions_overview | medium |
| TC-ONBOARD-010 | Tapping 'How Open Banking works' opens the OB explainer bottom sheet with three FAPI steps | high |
| TC-ONBOARD-011 | Tapping 'Got it' closes the OB explainer sheet and returns to consent_explainer | medium |
| TC-ONBOARD-012 | DataStore IOException shows error state with retry button; retry transitions back through loading to intro | medium |
