<!-- source: screens/cards/ui.yaml -->
<!-- source_hash: sha256:enriched-2026-05-31-cards-v1.1.0-obp-v7.0.0-verified -->
<!-- generated: 2026-05-31T00:00:00Z -->
<!-- generated_from_feature_version: 1.1.0 -->
<!-- generated_from_contract_version: 1.1.0 -->
<!-- prior_version: — -->

# My Cards — Implementation Specification

> **Source of Truth**: `idea-layer/screens/cards/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 1.1.0 | **Contract version**: 1.1.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| My Cards | CardsViewModel | Loading, Content, Empty, Error | Horizontal carousel of user payment cards with per-card transaction list and deferred quick-action row |

---

## Capabilities

### has_ui

Card carousel (`card_carousel`) with horizontal scroll + snap, per-card status chip (Active/Frozen), network logo (Visa/Mastercard), masked PAN, cardholder name, and deferred quick-action row (Freeze / Set Limit / View PIN / Report Lost). Below carousel: `Card Transactions` section listing 5 recent merchant transactions. Bottom: `Order New Card` outlined button (deferred). Top bar: "My Cards" title + notifications icon. Bottom nav: consumer 5-tab shell (Home / Accounts / Pay / Cards / More).

### has_api

Two live OBP endpoints verified against `apisandbox.openbankproject.com` on 2026-05-31. Carousel populated by `GET /obp/v7.0.0/cards` (consumer DirectLogin, 200). Transactions populated by `GET /obp/v4.0.0/banks/{bankId}/accounts/{accountId}/owner/transactions` keyed on `card.account.id`. Five card-control actions (freeze / set-limit / view-pin / report-lost / order) are deferred — no consumer write path available; each button renders with a "coming soon" handler.

---

## State Model

### CardsViewModel

- **State fields**:
  - `cards`: `List<Card>` — default `emptyList()`
  - `selectedCardId`: `String` — default `""`
  - `cardTransactions`: `List<Transaction>` — default `emptyList()`
  - `uiState`: `CardsUiState` — default `Loading`
- **ScreenState**: `Loading`, `Content`, `Empty`, `Error`
- **Errors**:
  - `LOAD_FAILED` — "Unable to load your cards. Please try again." (global, retry=true)
  - `FREEZE_FAILED` — "Card freeze operation failed. Please contact support." (freeze, deferred)
- **Events**: `CardSelected`, `FreezeCardClicked`, `SetLimitClicked`, `ViewPinClicked`, `ReportLostClicked`, `OrderNewCardClicked`, `TransactionClicked`
- **Actions**: `navigate`, `toggle_freeze_card`, `open_set_limit_sheet`, `reveal_pin`, `report_card_lost`, `order_card`
- **DI**: `CardRepository`, `TransactionRepository`, `BiometricAuthUseCase`

---

## API Endpoints (2)

| Function | Method | Path | Params | Response | Errors | Verified |
|----------|--------|------|--------|----------|--------|----------|
| getCards | GET | `/obp/v7.0.0/cards` | (none) | `List<Card>`: bank_id(String), bank_card_number(String), name_on_card(String), card_type(String), networks(List\<String\>), allows(List\<String\>), enabled(Boolean), cancelled(Boolean), on_hot_list(Boolean), account(CardAccountRef) | 401 | 2026-05-31 — 200 |
| getCardTransactions | GET | `/obp/v4.0.0/banks/{bankId}/accounts/{accountId}/owner/transactions` | `bankId(String)`, `accountId(String)` | `List<Transaction>`: transactions(List\<Transaction\>) | 401 | 2026-05-31 — 200, 28 tx |

> **Deferred actions** — `toggle_freeze_card`, `report_card_lost`, `open_set_limit_sheet`, `reveal_pin`, `order_card` have no consumer OBP write path. Buttons render; handlers emit "coming soon" snackbar. See `screens/cards/api.yaml#/deferred_actions` for verified reasons per action.

---

## Dependencies (Tier feature)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| card-detail | navigate | true | Tapping a card in the carousel navigates to card-detail screen |
| transaction-detail | navigate | true | Tapping a transaction row navigates to transaction-detail screen |

---

## Navigation

- **Route**: cards (no params)
- **From**: bottom_nav tab "Cards" (target: `cards`), home screen card shortcut
- **To**: `card-detail` (tap card item), `transaction-detail` (tap transaction row)

---

## App-Shell

Resolved from `design-system/app-shell.yaml` (consumer flavor) + `screens/cards/ui.yaml#/screens[0]/shell`:

| Attribute | Value |
|-----------|-------|
| Bottom nav visible | true — 5 tabs: Home / Accounts / Pay / Cards / More |
| Active tab | Cards (icon: `credit_card`, target: `cards`) |
| Top app bar | visible, title: "My Cards", navigation_icon: null, action: `notifications_outlined` → `open_notifications` |
| FAB | consumer default visible (`add` → `quick_action_menu`); screen override does not suppress |
| Safe-area | top: true, bottom: true, left: true, right: true |

---

## Flow Logic (0 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| (none) | — | — | — | — |

---

## Data Flow

| Trigger | Endpoint | DTOs | Produces State | Error Path |
|---------|----------|------|----------------|------------|
| on_mount | `GET /obp/v7.0.0/cards` | Card, CardAccountRef | content | 401 → navigate_login |
| after_cards_loaded | `GET /obp/v4.0.0/banks/{bankId}/accounts/{accountId}/owner/transactions` (accountId = card.account.id) | Transaction | content | 401 → navigate_login |

---

## Design Tokens (relevant subset)

| Token | Value | Usage |
|-------|-------|-------|
| color.primary | `#4C662B` | Card gradient (Debit Visa), active chip, action icons |
| color.secondary | `#386663` | Business Mastercard card gradient |
| color.error | `#BA1A1A` | Debit amounts text, Report Lost icon |
| color.surface | `#FFFFFF` | Transaction row background |
| color.background | `#F9FAEF` | Screen background |
| color.outline_variant | `#C5C8BA` | Frozen status chip background |
| color.on_surface_variant | `#44483D` | Transaction date text |
| color.primary_container | `#CDEDA3` | Cardholder name text on card surface |
| radius.xl | 24dp | Card carousel item border-radius |
| radius.md | 12dp | Transaction row border-radius, status chip |
| elevation.level4 | 8dp | Card carousel item elevation |
| typography.headline_large | 32sp / Outfit 400 | Screen title "My Cards" |
| typography.title_large | 22sp / Outfit 400 | Masked PAN, "Card Transactions" section header |
| typography.body_large | 16sp / Outfit 400 | Cardholder name on card, debit amount |
| typography.body_small | 12sp / Outfit 400 | Transaction date |
| typography.label_small | 11sp / Outfit 500 | Status chip text (Active / Frozen) |
| motion.short4 | 200ms | Skeleton shimmer duration (loading state) |
| spacing.md | 16dp | Carousel item gap, transaction row horizontal padding |
| spacing.lg | 24dp | Card inner padding |

---

## Testing (5 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-CARDS-001 | Cards load successfully — carousel shows correct masked PAN, cardholder name, network logo, status chip for each card | High |
| TC-CARDS-002 | Card transactions load after card selection — list shows merchant name, debit amount in red, date | High |
| TC-CARDS-003 | Empty state — API returns zero cards → empty-state icon + "No cards yet" + "Order your first Mifos card" message + Order New Card button | High |
| TC-CARDS-004 | Error state — network failure on `/obp/v7.0.0/cards` → "Unable to load cards" error + retry button visible | High |
| TC-CARDS-005 | Deferred actions — Freeze / Set Limit / View PIN / Report Lost / Order New Card render correctly; tapping any shows "coming soon" snackbar | Medium |
