<!-- source: screens/statements/ui.yaml -->
<!-- source_hash: regenerated-2026-07-16 -->
<!-- generated: 2026-07-16T00:00:00Z -->

# Statements — Implementation Specification

> **Source of Truth**: `idea-layer/screens/statements/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 1.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Statements | StatementsViewModel | loading, content, empty, error | Chronological statement list for a selected account. Each row shows a ViewModel-derived period label ("May 2026"), closing balance in GBP (from OBStatement2.StatementAmount[ClosingBalance]), formatted date range, and a PDF/CSV download icon. Tapping the row navigates to statement-detail; tapping the download icon requests GET /statements/{id}/file via platform file handler. |

---

## State Model

### StatementsViewModel

**State class**: `StatementsState`

| Field | Type | Initial | Notes |
|-------|------|---------|-------|
| uiState | StatementsUiState | Loading | Screen-level sealed state |
| downloadState | Map\<String, DownloadState\> | emptyMap() | Per-StatementId download progress; keys are StatementId strings |

**ScreenState sealed class**: `StatementsUiState`

| Member | Payload | Notes |
|--------|---------|-------|
| Loading | — | GET /accounts/{AccountId}/statements in flight |
| Content | statements: List\<StatementRowUiModel\> | Statements sorted descending by StartDateTime |
| Empty | — | OBReadStatement2 Data.Statement[] returned empty |
| Error | message: String | 401 / 403 / 429 / network; ViewModel localises message by HTTP code |

**Error types**:

| Error Class | HTTP Status | Condition |
|-------------|-------------|-----------|
| TokenExpiredError | 401 | Session expired |
| ConsentScopeError | 403 | ReadStatements (ReadStatementsDetail) not in consent scope |
| RateLimitedError | 429 | Too many requests |
| StatementDownloadUnsupportedError | 501 | Download not available for account type (download path only — no screen state change; toast only) |
| NetworkError | — | AIS endpoint unreachable |

**Actions**:

| Action | Signature | Notes |
|--------|-----------|-------|
| statementsLoad | `suspend fun statementsLoad(accountId: String)` | GET /accounts/{accountId}/statements; maps each OBStatement2 → StatementRowUiModel; sorts descending by StartDateTime |
| navigateToStatementDetail | `fun navigateToStatementDetail(statementId: String, accountId: String)` | NavController.navigate(StatementDetailRoute) |
| retryLoad | `suspend fun retryLoad(accountId: String)` | Delegates to statementsLoad |
| downloadStatement | `suspend fun downloadStatement(statementId: String, accountId: String)` | GET /statements/{id}/file → bytes → platform file handler; downloadState[statementId]: InProgress → Idle |

**DI**:

| Dependency | Role |
|------------|------|
| StatementsRepository | GET /accounts/{AccountId}/statements via ktorfit |
| StatementFileRepository | GET /accounts/{AccountId}/statements/{StatementId}/file via ktor-client-download |

### UI Model: StatementRowUiModel

| Field | Type | Derivation |
|-------|------|-----------|
| StatementId | string | OBStatement2.StatementId (verbatim) |
| StatementReference | string | OBStatement2.StatementReference |
| periodLabel | string | Derived from StartDateTime via kotlinx-datetime (e.g. "May 2026") |
| closingBalanceFormatted | string | StatementAmount[Type==ClosingBalance].Amount formatted as "£X,XXX.XX" |
| startDateFormatted | string | OBStatement2.StartDateTime formatted as "1 May 2026" |
| endDateFormatted | string | OBStatement2.EndDateTime formatted as "31 May 2026" |

### State Transitions

| From | Trigger | To State |
|------|---------|----------|
| — | Screen mount | loading |
| loading | OBReadStatement2 success + Data.Statement[] non-empty | content |
| loading | 200 OK + Data.Statement[] empty | empty |
| loading | HTTP 401 / 403 / 429 / network error | error |
| error | Retry tapped | loading |
| content | Statement row tapped | → statement-detail (statementId, accountId) |
| content | Download icon tapped | downloadState[statementId]: InProgress → Idle (success or error); toast on 501 or network |

---

## Component Inventory

### State: loading

- **Layout**: column, padding 16dp
- **Components**:
  - `statements_skeleton` — list variant skeleton, item_count 3; background: surface, cornerRadius: 8dp; a11y: loading label. Shimmer placeholder shown while OBReadStatement2 fetch is in flight.

### State: content

- **Layout**: lazy_column, vertical scroll, padding 0dp; separator: `statement_row_divider`
- **Components**:
  - `statements_list` — items_source `{statements}`, sorted descending by StartDateTime:
    - `statement_row` per item (list_item):
      - headline: `{item.periodLabel}` (e.g. "May 2026")
      - supporting_text: "Closing balance: {item.closingBalanceFormatted}" (e.g. "£2,847.63")
      - trailing_content: `{item.startDateFormatted} – {item.endDateFormatted}` (bodySmall)
      - minHeight: 72dp; ripple: true; background: surface
      - on_click: navigate_statement_detail → statement-detail (statementId, accountId)
      - child: `download_statement_button` — icon_button, icon: file_download, tint: primary, size: 24dp
        - Shows circular_progress spinner when `downloadState[item.StatementId] == InProgress`
        - on_click: download_statement(statementId, accountId) — stops event propagation so row tap does not also fire
  - `statement_row_divider` — visual separator between statement rows

### State: empty

- **Layout**: column, gravity center, padding 32dp
- **Components**:
  - `empty_statements` — icon: description (document); icon_tint: onSurfaceVariant; title: titleMedium; body: bodyMedium. Shown when OBReadStatement2 Data.Statement[] is empty.

### State: error

- **Layout**: column, gravity center, padding 32dp
- **Components**:
  - `error_state` — empty_state variant error; icon: error_outline; icon_tint: error; body: `{error.message}` (ViewModel-localised per HTTP code)
  - `retry_button` — filled button; cornerRadius: 20dp; horizontalPadding: 24dp; triggers retry_load

---

## Behaviour / Action Contracts

| Component ID | Action | Effect | Library Refs | Description |
|--------------|--------|--------|--------------|-------------|
| statement_row | navigate_statement_detail | navigate | — | Opens statement-detail screen for the selected period, passing StatementId and accountId as navigation arguments. |
| download_statement_button | download_statement | persist_file | ktor-client-download, hsbc-obie-ais-v4.0:statements-file | Calls GET /accounts/{AccountId}/statements/{StatementId}/file (Accept: application/pdf, text/csv) and streams the response bytes to the platform file handler, saving the statement PDF or CSV to device storage. |
| retry_button | retry_load | call_api | ktorfit, hsbc-obie-ais-v4.0:statements | Re-issues GET /accounts/{AccountId}/statements after a recoverable 401 or network error, returning the screen to Loading and then Content, Empty, or Error. |

---

## API Dependencies

| ID | Method | Path | Permission | Response DTO | Notes |
|----|--------|------|------------|--------------|-------|
| statements | GET | /accounts/{AccountId}/statements | ReadStatementsDetail | OBReadStatement2 | ReadStatementsDetail (not just ReadStatements) required so StatementAmount (OpeningBalance, ClosingBalance) is included |
| statement_file | GET | /accounts/{AccountId}/statements/{StatementId}/file | ReadStatementsDetail | null (binary PDF/CSV) | Accept: application/pdf, text/csv; HTTP 501 → toast unavailable, no screen state change |

---

## Navigation

- **Entry**: account-detail → tap Statements chip (params: accountId)
- **Row tap**: → statement-detail (params: statementId, accountId)
- **Back**: top_app_bar leading icon → account-detail
- **Dependencies tier 1**: account-detail (parent) · statement-detail (child)
- **Flow ref**: recurring-and-statements

---

## App-Shell

| Property | Value |
|----------|-------|
| bottom_navigation_visible | true |
| top_app_bar_visible | true |
| top_app_bar_title | {strings.statements.screen_title} |
| top_app_bar_leading | back |
| FAB visible | false |

---

## Testing (9 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-ST-001 | Statement list renders period labels and closing balances correctly | critical |
| TC-ST-002 | Skeleton (3 rows) shown during fetch | critical |
| TC-ST-003 | Row tap navigates to statement-detail with correct statementId and accountId | critical |
| TC-ST-004 | Download icon shows circular spinner during in-flight download; reverts on completion | critical |
| TC-ST-005 | Empty state shown when Data.Statement[] is empty | normal |
| TC-ST-006 | 401 error shows localised message and Retry button | normal |
| TC-ST-007 | 403 error shows localised message and Retry button | normal |
| TC-ST-008 | 429 error shows localised message and Retry button | normal |
| TC-ST-009 | 501 on download shows toast only — no screen state change | normal |
