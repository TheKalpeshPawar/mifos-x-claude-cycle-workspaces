<!-- source: screens/pfm-dashboard/ui.yaml -->
<!-- source_hash: generated-2026-07-17 -->
<!-- generated: 2026-07-17T00:00:00Z -->
<!-- generated_from_feature_version: 2.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->

# My Finances — Implementation Specification

> **Source of Truth**: `idea-layer/screens/pfm-dashboard/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 2.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| My Finances | PfmDashboardViewModel | loading, content, empty, error | Client-computed finance overview using kotlinx-datetime period windowing and kotlinx-coroutines across accounts. Net worth sums all balances; monthly spend and income filter by CreditDebitIndicator. Top categories rank AIS debits by MerchantCategory descending. Insights apply spend-vs-prior-month and category-over-budget rules. Period selector re-triggers computation with a new kotlinx-datetime window (7d/30d/3m). All computation is viewModelScope-scoped; no network calls on this screen. |

---

## App-Shell

| Attribute | Value |
|-----------|-------|
| Bottom navigation | visible (root destination — Finances tab, order 3) |
| Top app bar | visible — title: `{strings.pfm_dashboard.title}` |
| FAB | hidden |

---

## State Model

### PfmDashboardViewModel

**State class:** `PfmDashboardState`

| Field | Type | Notes |
|-------|------|-------|
| `selectedPeriod` | `PfmPeriod` | 7d \| 30d \| 3m; re-triggers computation on change |
| `netWorth` | `String` | Formatted GBP sum of all account balances |
| `topCategories` | `List<PfmCategory>` | Ranked AIS debits by MerchantCategory descending |
| `insights` | `List<PfmInsight>` | Rule-based insight cards from computation engine |
| `uiState` | `PfmDashboardUiState` | Current sealed-class state |

**Sealed class:** `PfmDashboardUiState`

| Member | Notes |
|--------|-------|
| `Loading` | PFM coroutines computing from cache; shimmer shown |
| `Content` | netWorth, accountBreakdown, periodIncome, periodSpend, topCategories, insights |
| `Empty` | No transactions in local Room/SQLDelight cache (EC-PFM-001) |
| `Error` | Exception during cache read or aggregation computation (EC-PFM-002) |

**Error types:** `EmptyCacheError` (no transactions in local cache after account sync — EC-PFM-001) · `AggregationError` (exception during Room/SQLDelight read or computation — EC-PFM-002)

**Actions:**

| Action | Signature | Notes |
|--------|-----------|-------|
| `loadFinancesOverview` | `suspend fun loadFinancesOverview(period: PfmPeriod = PfmPeriod.DAYS_30)` | Reads cache; parallel coroutines: balance sum, period aggregation (income/spend), category grouping with %, insight rules |
| `selectPeriod` | `fun selectPeriod(period: PfmPeriod)` | Switches PfmPeriod; re-triggers loadFinancesOverview with new window |
| `reloadFinances` | `suspend fun reloadFinances()` | Retry from error state; re-dispatches loadFinancesOverview(selectedPeriod) |
| `navigateSpendingByCategory` | `fun navigateSpendingByCategory(category: String? = null)` | Push spending-by-category with optional category filter |
| `navigateBudgets` | `fun navigateBudgets()` | Push budgets route |
| `navigateRecurringSubscriptions` | `fun navigateRecurringSubscriptions()` | Push recurring-subscriptions route |

**DI:** `PfmRepository` (wraps Room/SQLDelight pfm_cache.db reads and aggregation queries)

---

## Components

| ID | Type | State Binding | Data-Driven | Purpose |
|----|------|--------------|-------------|---------|
| `loading_shimmer` | shimmer (rows=4, row_heights=[120,80,200,80]) | loading | false | Shimmer skeleton while PFM coroutines compute from cache |
| `error_view` | error_state (icon=warning_amber) | error | false | Error state when computation fails; hosts retry button as a typed child |
| ↳ `pfm_error_retry_button` | button (filled, label=`{strings.pfm_dashboard.error.retry}` "Try again") | error | false | Filled retry button; tap re-dispatches loadFinancesOverview via reloadFinances |
| `period_selector` | segmented_button | content | true | Period scope selector (7d / 30d / 3m); switching re-computes spend/income/categories |
| `net_worth_card` | card (elevation=2) | content | true | Aggregated net worth card; sum equals credit balances minus credit card balance |
| ↳ `net_worth_label` | text (labelMedium, secondary) | content | false | "Net Worth" label |
| ↳ `net_worth_amount` | text (displaySmall, primary) | content | true | Formatted GBP net worth (e.g. "£14,955.45") |
| ↳ `net_worth_sub` | text (bodySmall, on-surface-variant) | content | true | Account count subtitle |
| ↳ `net_worth_breakdown` | stack (vertical, spacing=xs) | content | true | Per-account balance breakdown rows |
| ↳↳ `breakdown_current` | list_item (style=compact) | content | true | Everyday current account balance row |
| ↳↳ `breakdown_savings` | list_item (style=compact) | content | true | ISA saver account balance row |
| ↳↳ `breakdown_credit` | list_item (style=compact, trailing_color=error) | content | true | Credit card outstanding balance row; trailing displayed in error colour |
| `spending_income_header` | text (titleSmall, on-surface-variant) | content | false | "Spending vs Income" section header |
| `spending_income_card` | card (elevation=1) | content | true | Income vs spend bar chart with net cashflow label for selected period |
| ↳ `spending_income_bars` | bar_chart | content | true | Dual-bar: income (primary colour) vs spend (error colour); bar heights proportional to values |
| ↳ `net_cashflow_label` | text (labelMedium, primary) | content | true | Net cashflow label; +/− prefix reflects saved vs overspent |
| `top_categories_header` | text (titleSmall, on-surface-variant) | content | false | "Top Spending Categories" section header |
| `top_categories_list` | stack (vertical, items_source=`{top_categories}`) | content | true | Ranked spending categories with % progress bars; sorted by amount descending |
| ↳ `category_row` | list_item (items_source=item) | content | true | Category row: name, transaction count, GBP amount, spend-% bar; tap navigates filtered |
| ↳↳ `category_spend_bar` | progress_linear (value=`{item.percentage_of_spend}`, max=100, color=primary) | content | true | Spend-% progress bar within each category row |
| `view_all_categories` | text_button | content | false | Navigates to spending-by-category with no category filter |
| `insights_header` | text (titleSmall, on-surface-variant) | content | false | "Insights" section header |
| `insights_list` | stack (vertical, items_source=`{insights}`) | content | true | Rule-based insight cards; at least one insight when transactions present |
| ↳ `insight_card` | card (elevation=1) | content | true | Single insight card: title and body non-empty, grounded in real metric |
| ↳↳ `insight_icon` | icon (size=20, color=primary) | content | true | Insight-category icon (dynamic `{item.icon}`) |
| ↳↳ `insight_title` | text (titleSmall) | content | true | Insight headline; non-empty, no placeholder tokens |
| ↳↳ `insight_body` | text (bodySmall, on-surface-variant) | content | true | Insight detail grounded in actual computed metric |
| `pfm_nav_chips` | chip_group (scroll_direction=horizontal) | content | false | Horizontal chip group for quick PFM navigation |
| ↳ `chip_spending` | chip (icon=pie_chart) | content | false | Shortcut to spending-by-category (no pre-filter) |
| ↳ `chip_budgets` | chip (icon=savings) | content | false | Shortcut to budgets screen |
| ↳ `chip_subscriptions` | chip (icon=autorenew) | content | false | Shortcut to recurring-subscriptions screen |
| `empty_state_view` | empty_state (icon=bar_chart) | empty | false | Shown when no cached transactions exist; no retry button — instructional only |

---

## Behaviour / Action Contracts

| Component ID | Action | Effect | Library Refs | Description |
|--------------|--------|--------|--------------|-------------|
| `pfm_error_retry_button` | `reload_finances` | `transform_state` | `kotlinx-coroutines` | Re-dispatches loadFinancesOverview: re-reads cached transactions and account balances from PfmRepository (Room/SQLDelight) and recomputes net worth, period income/spend, top categories and insights in parallel via kotlinx-coroutines, transitioning error → loading → content. Client-only — no API call. |
| `category_row` | `navigate_spending_by_category` | `navigate` | — | Navigates to spending-by-category screen passing the MerchantCategory name as a route parameter, pre-filtering transaction debits to this category for the selected PFM period. |
| `chip_spending` | `navigate_spending_by_category` | `navigate` | — | Quick-access chip navigating to spending-by-category without a category pre-filter, showing all AIS debit categories ranked by amount for the currently selected PFM period. |
| `chip_budgets` | `navigate_budgets` | `navigate` | — | Navigates to the budgets screen where the PSU can view monthly spend targets and compare actual spending against set budgets for the current period. |
| `chip_subscriptions` | `navigate_recurring_subscriptions` | `navigate` | — | Navigates to the recurring-subscriptions screen displaying detected standing orders and regular subscription debits grouped from the AIS transaction cache. |

Note: `period_selector` (action: `select_period`) and `view_all_categories` (action: `navigate_spending_by_category`) have `on_click` bindings without explicit `action_contract` blocks in the source YAML.

---

## Navigation

| Direction | Source / Target | Trigger | Params |
|-----------|----------------|---------|--------|
| Entry | app_shell_bottom_nav → pfm-dashboard | Finances tab (bottom nav, order 3) | — |
| Entry | accounts-list → pfm-dashboard | My Finances shortcut | — |
| Exit | pfm-dashboard → spending-by-category | category_row tap or chip_spending tap | category: String? (optional) |
| Exit | pfm-dashboard → budgets | chip_budgets tap | — |
| Exit | pfm-dashboard → recurring-subscriptions | chip_subscriptions tap | — |

**Flow ref:** pfm-review (bottom-nav root destination; no back-stack entry from peers)

---

## Local Cache Dependency

This screen makes **no network calls**. All data comes from `pfm_cache.db` (Room/SQLDelight):

- **Populated by**: the `transactions` screen on each AIS fetch
- **Storage**: Room/SQLDelight in app-private internal files directory; GDPR-relevant (device-local only)
- **TTL**: 24 hours; staleness indicator prompts background re-sync without blocking the PSU
- **Clear trigger**: manual (Settings → Clear Local Data) and consent_revoked

---

## Flow Logic (4 decisions)

| Decision | Condition | True Path | False Path | Implementation |
|----------|-----------|-----------|------------|----------------|
| Cache state | No transactions in cache after loadFinancesOverview | emit Empty state | continue to computation | EC-PFM-001 |
| Computation result | Exception during Room/SQLDelight read or aggregation | emit Error state; log via Kermit | emit Content state | EC-PFM-002 |
| Net cashflow sign | periodIncome > periodSpend | show '+£X saved this period' (primary colour) | show '−£X overspent this period' (error colour) | net_cashflow_label vm field |
| Period selection | User taps new period segment | selectPeriod dispatched; reload with new window | selectedPeriod unchanged | selectPeriod action |

---

## Testing (10 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-PFM-001 | Dashboard renders net worth card, spending vs income chart, and top categories in content state | high |
| TC-PFM-002 | Loading state shows shimmer while PFM coroutines compute | high |
| TC-PFM-003 | Empty state shown when no transactions in local cache | high |
| TC-PFM-004 | Error state shown when computation fails, with retry action | high |
| TC-PFM-005 | Tapping a category row navigates to spending-by-category with category param | high |
| TC-PFM-006 | Tapping Budgets chip navigates to budgets screen | high |
| TC-PFM-007 | Tapping Subscriptions chip navigates to recurring-subscriptions | medium |
| TC-PFM-008 | Switching period selector re-computes spending and income for new window | medium |
| TC-PFM-009 | View all categories button navigates to spending-by-category with no filter | medium |
| TC-PFM-010 | Net cashflow label shows overspent sign when spend exceeds income | low |
