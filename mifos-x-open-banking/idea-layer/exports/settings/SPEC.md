<!-- source: screens/settings/ui.yaml -->
<!-- source_hash: a14204542d4a6df97e7084b25067fc3a1057dfe0f584e67c89903809f192cced -->
<!-- generated: 2026-07-14T00:00:00Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 1.0.0 -->
<!-- prior_version: — -->

# Settings -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/settings/ui.yaml`
> **Auto-generated by**: `/idea export` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0 | **Status**: enriched

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Settings | SettingsViewModel | Loading, Content | App settings hub — appearance (theme), security (biometric lock + session timeout), notifications (consent expiry + security alerts), account links (Manage Consents, Profile, Clear Local Data), and About & Legal. Client-only; reads DataStore preferences Flow. |

## App-Shell

| Property | Value |
|----------|-------|
| Bottom Navigation | Visible |
| Top App Bar | Visible — title: `{strings.settings.title}` |
| Top App Bar Leading | None (root destination) |
| FAB | Hidden |

## State Model

### SettingsViewModel

- **State**: settings.theme(`String`) · settings.biometric_lock_enabled(`Boolean`) · settings.session_timeout(`String`) · settings.notify_consent_expiry(`Boolean`) · settings.notify_security_alerts(`Boolean`) · settings.app_version(`String`) · uiState(`ScreenState`)
- **ScreenState**: Loading, Content
- **Errors**: BiometricUnavailable (event, non-fatal), ClearLocalDataFailed (show error snackbar with retry), ExternalUrlFailed (show snackbar, no retry)
- **Events**: ShowClearDataConfirmation, LocalDataCleared, BiometricUnavailable
- **Actions**:
  - `updateTheme(theme: String)` — writes theme key to DataStore atomically; triggers dynamic recomposition
  - `toggleBiometricLock()` — flips biometric_lock_enabled; emits BiometricUnavailable if no enrolled biometrics
  - `updateSessionTimeout(duration: String)` — persists from whitelist [2 min, 5 min, 10 min, 30 min]
  - `toggleConsentExpiryNotification()` — persists notify_consent_expiry to DataStore
  - `toggleSecurityAlertsNotification()` — persists notify_security_alerts to DataStore
  - `navigateConsentList()` — emits navigation event to consent-list route
  - `navigateProfile()` — emits navigation event to profile route
  - `clearLocalDataConfirm()` — emits ShowClearDataConfirmation; UI opens bottom-sheet
  - `clearLocalData()` — deletes Room/SQLDelight cache rows + resets DataStore cached-at timestamps; emits LocalDataCleared
  - `openExternalUrl(url: String)` — fires platform intent to system browser
  - `openOssLicences()` — emits navigation event to AboutLibraries destination
- **DI**: DataStoreSettingsRepository, LocalCacheRepository (Room/SQLDelight), BiometricManager, BuildConfig

## API Endpoints (0)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| (none — client-only; all settings persisted locally via DataStore) | — | — | — | — | — |

## Dependencies (Tier 1)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| consent-list | navigation-target | false | Navigated to from Manage Consents row |
| profile | navigation-target | false | Navigated to from Profile row |

## Navigation

- **Route**: SettingsRoute()
- **From**: app_shell_bottom_nav (More tab, order 4)
- **To**: consent-list (Tap Manage Consents row), profile (Tap Profile row)

## Flow Logic (3 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| DataStore first emission? | preferences Flow has not yet emitted | Remain in Loading state (shimmer visible) | Transition to Content with live settings values | collectAsStateWithLifecycle on StateFlow |
| Biometric hardware available? | BiometricManager.canAuthenticate() returns BIOMETRIC_SUCCESS | Write biometric_lock_enabled to DataStore | Emit BiometricUnavailable event; show informational dialog; no DataStore write | toggleBiometricLock() guard |
| Clear local data confirmed? | User taps 'Clear' in bottom-sheet | Delete Room/SQLDelight rows → reset DataStore timestamps → emit LocalDataCleared → show success snackbar | Show confirmation bottom-sheet (clearLocalDataConfirm action) | clearLocalData() on confirm |

## Testing (12 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-SET-001 | Loading skeleton renders before DataStore emits first value | high |
| TC-SET-002 | Settings screen renders all sections with current preference values | high |
| TC-SET-003 | Tapping Manage Consents navigates to consent-list | high |
| TC-SET-004 | Tapping Profile navigates to profile | medium |
| TC-SET-005 | Toggling Biometric App Lock persists new value | high |
| TC-SET-006 | Selecting Dark theme applies immediately | high |
| TC-SET-007 | Toggling Consent Expiry Reminders off persists and survives restart | medium |
| TC-SET-008 | Clear Local Data shows confirmation bottom-sheet and clears cache on confirm | medium |
| TC-SET-009 | Biometric toggle gracefully handles unavailable hardware | medium |
| TC-SET-010 | Session timeout picker accepts only whitelisted durations | low |
| TC-SET-011 | Toggling Security Alerts notification off persists and survives restart | low |
| TC-SET-012 | Tapping an About & Legal external link opens the correct URL in system browser | medium |
