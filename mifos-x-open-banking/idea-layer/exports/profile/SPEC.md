<!-- source: screens/profile/ui.yaml -->
<!-- source_hash: regenerated-2026-07-14 -->
<!-- generated: 2026-07-14T12:00:00Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 1.0.0 -->
<!-- prior_version: generated-2026-07-14 -->

# Profile -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/profile/ui.yaml`
> **Auto-generated by**: `/idea export` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0 | **Status**: designed

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Profile | ProfileViewModel | loading, content, content_expiring, confirm_sign_out, error, empty | Connected identity from HSBC AIS party endpoint (name, email, mobile, residential address). Full active-consent summary (status, expiry, granted permissions). Expiry warning banner shown when consent.days_remaining ≤7. Sign-out clears tokens and consent from encrypted storage. Requires ReadParty permission. Empty state shown when OBIE returns an empty Data.Party[] array. |

## App-Shell

| Attribute | Value |
|-----------|-------|
| Bottom navigation | visible |
| Top app bar | visible — title: profile_screen_title, leading: back |
| FAB | hidden |

## State Model

### ProfileViewModel

- **State**: loading — API call in flight; content — `party: OBParty2`, `consent: ConsentSummary(status, expiry_date, consent_id, permissions, days_remaining)`, `party.Initials: String`; content_expiring — same as content + `consent.days_remaining ≤ 7`; confirm_sign_out — dialog shown, `previousState` saved; error — `error.message: String`; empty — `Data.Party[]` returned with zero elements (EC-PROF-006)
- **ScreenState**: loading, content, content_expiring, confirm_sign_out, error, empty
- **Errors**:
  - EC-PROF-001 (HTTP 401 → token refresh → retry; second 401 → executeSignOut)
  - EC-PROF-002 (HTTP 403 ReadParty missing → error with permission message)
  - EC-PROF-003 (HTTP 404 → error with party-not-found message)
  - EC-PROF-004 (Network timeout → error with generic message, Retry)
  - EC-PROF-005 (consent.days_remaining ≤ 7 → content_expiring state)
  - EC-PROF-006 (HTTP 200 with empty Data.Party[] → empty state; empty_state icon=person_off; no retry button — PSU should contact their bank)
- **Events**: navigation events to consent-detail, login
- **Actions**:
  - `loadProfile(): suspend Unit` — GET /accounts/{accountId}/party; reads local consent; derives Initials; computes days_remaining; emits content | content_expiring | empty | error
  - `navigateConsentDetail(): Unit` — emits navigation to consent-detail
  - `navigateConsentRenew(): Unit` — emits navigation to consent-detail with renew=true
  - `confirmSignOut(): Unit` — transitions to confirm_sign_out; saves previousState
  - `dismissSignOutDialog(): Unit` — transitions back to previousState
  - `executeSignOut(): suspend Unit` — clears access token, refresh token, ConsentId from EncryptedSharedPreferences/Keychain; navigates to login
  - `retryLoad(): suspend Unit` — transitions error → loading; re-invokes loadProfile()
- **DI**: PartyApiService (ktorfit), ConsentRepository (local store), SecureStorage (EncryptedSharedPreferences/Keychain), Navigator

## API Endpoints (1)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| party | GET | AccountId(String) | OBReadParty2 | 401, 403, 404, 200+empty | accounts/{AccountId}/party |

## Dependencies (none)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| (none) | — | — | — |

## Navigation

- **Route**: ProfileRoute()
- **From**: settings (Tap Profile row)
- **To**: consent-detail (Manage Consent or Renew Consent tapped), login (sign-out confirmed — tokens cleared)

## Flow Logic (5 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Empty party response | HTTP 200 but `Data.Party[]` is empty | emit empty state (EC-PROF-006; person_off icon; no retry) | proceed to content/content_expiring | loadProfile() post-response check |
| Consent expiry check | consent.days_remaining ≤ 7 after successful party load | emit content_expiring state (show warning banner + Renew Consent CTA) | emit content state | loadProfile post-load check |
| HTTP 401 handling | Token refresh succeeds after first 401 | Retry party call with new token; emit content | Second 401 → executeSignOut() → navigate login | EC-PROF-001 |
| Sign-out confirmation | User taps confirm_sign_out_button | executeSignOut() — clear tokens, navigate login | dismissSignOutDialog() — restore previousState | confirmSignOut / dismissSignOutDialog |
| HTTP 403 | ReadParty scope absent from consent | emit Error('Consent does not include profile access.') | continue with party response | EC-PROF-002 |

## Testing (13 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-PROF-001 | Profile loads and renders identity, consent summary with permissions, and sign-out | high |
| TC-PROF-002 | Loading state shown while fetching party | high |
| TC-PROF-003 | Error state with Retry on 403 — ReadParty permission not granted | high |
| TC-PROF-004 | Sign Out shows confirmation dialog | high |
| TC-PROF-005 | Confirming sign out clears tokens and navigates to login | high |
| TC-PROF-006 | Manage Consent navigates to consent-detail | high |
| TC-PROF-007 | 401 triggers token refresh; on second 401 user is signed out | medium |
| TC-PROF-008 | content_expiring state shows warning banner when consent expires within 7 days | medium |
| TC-PROF-009 | Address row displays residential address from OBIE party payload | medium |
| TC-PROF-010 | 404 from party endpoint shows party-not-found error message | medium |
| TC-PROF-011 | Cancelling sign-out dialog restores previous state without clearing tokens | low |
| TC-PROF-012 | Permissions list renders only granted permissions from cached consent | low |
| TC-PROF-013 | HTTP 200 with empty Data.Party[] renders empty state with person_off icon and no retry button | medium |
