<!-- source: screens/login/ui.yaml -->
<!-- source_hash: generated-2026-07-16 -->
<!-- generated: 2026-07-16T00:00:00Z -->
<!-- generated_from_feature_version: 2.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->

# Connect with HSBC — Implementation Specification

> **Source of Truth**: `idea-layer/screens/login/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 2.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Connect with HSBC | ConsentOnboardingViewModel | content, loading, authorising, error, empty | OAuth/FAPI initiation screen. Stages OBReadConsent1 to hsbc-obie-ais-v4.0:account-access-consents (10 read scopes incl. ReadAccountsDetail, ReadBalances, ReadTransactionsDetail) via ktorfit after obtaining a client_credentials token. Stores ConsentId in local storage, builds a FAPI 1.0 Advanced /authorize URL (response_type=code id_token, scope=openid accounts) with a PS256-signed request object (jose4j) containing ConsentId, nonce, and state. Launches app-to-app redirect to HSBC for SCA and account selection; screen transitions to authorising state once redirect is launched. Consumer returns via the consent-callback deep link. |

---

## App-Shell

| Attribute | Value |
|-----------|-------|
| Bottom navigation | hidden |
| Top app bar | visible — title: `{strings.screen.login.title}`, leading: back |
| FAB | hidden |

---

## State Model

### ConsentOnboardingViewModel

**State class:** `ConsentOnboardingState`

| Field | Type | Notes |
|-------|------|-------|
| `requestedPermissions` | `List<OBPermission>` | 10 OBIE read scopes loaded from OBReadConsent1.Permissions config |
| `consentExpiryLabel` | `String` | VM-formatted ExpirationDateTime, e.g. "Expires 26 Sep 2026" |
| `errorMessage` | `String?` | Set on consent-create failure; null otherwise |

**Sealed class:** `ConsentOnboardingUiState`

| Member | Notes |
|--------|-------|
| `Content` | requestedPermissions: List<OBPermission>, consentExpiryLabel: String |
| `Loading` | consent-create POST in flight |
| `Authorising` | FAPI redirect launched; user authenticating in HSBC |
| `Error` | userMessage: String |
| `Empty` | zero OBIE permissions staged; prevents zero-scope OBReadConsent1 |

**Error types:** `NetworkException` · `HttpException` · `FapiRedirectException`

**Actions:**

| Action | Signature | Notes |
|--------|-----------|-------|
| `loadPermissionsConfig` | `fun loadPermissionsConfig(): Unit` | Reads OBReadConsent1.Permissions; computes consentExpiryLabel = DateFormatter(now + 90.days, 'd MMM yyyy', prefix='Expires ') |
| `startOAuth` | `suspend fun startOAuth(): Unit` | POST consent-create; stores ConsentId + state + nonce; builds FAPI URL (PS256); launches app-to-app redirect |
| `navigateBack` | `fun navigateBack(): Unit` | Pop to user-onboarding; no side-effects |

**DI:** `ConsentService` · `FapiAuthBuilder` · `LocalStorage` · `Navigator`

---

## Components

| ID | Type | State Binding | Data-Driven | Purpose |
|----|------|--------------|-------------|---------|
| `loading_indicator` | progress_indicator (variant=linear) | loading | false | Linear progress indicator shown while the consent-create POST is in flight |
| `loading_label` | text (bodyMedium, on-surface-variant) | loading | false | Status label shown alongside the progress indicator during consent-create |
| `authorising_spinner` | progress_indicator (variant=circular) | authorising | false | Circular spinner shown after FAPI redirect launches while user authenticates in HSBC |
| `authorising_label` | text (bodyMedium, on-surface-variant) | authorising | false | Status message shown while user authenticates in HSBC's app or portal |
| `authorising_hint` | text (bodySmall, on-surface-variant) | authorising | false | Hint telling the user to complete sign-in in HSBC then return; shown below spinner |
| `hsbc_explainer_card` | card (elevation=1) | content | false | Card contextualising the app-to-app redirect: HSBC will ask the PSU to approve permissions and select accounts |
| ↳ `hsbc_logo` | image (asset=ic_hsbc_logo) | content | false | HSBC brand identity confirming which bank the PSU is being redirected to |
| ↳ `ob_regulated_badge` | text (labelSmall, primary, icon_leading=verified_user) | content | false | Inline badge confirming this is a regulated UK Open Banking connection (FCA-authorised TPP) |
| ↳ `explainer_headline` | text (titleMedium, on-surface) | content | false | Headline confirming the app-to-app redirect destination for informed PSU consent |
| ↳ `explainer_body` | text (bodySmall, on-surface-variant) | content | false | Clarifies FAPI redirect behaviour and credential-protection guarantee to the PSU |
| ↳ `card_divider` | divider | content | false | Visual separator between PSU-facing explainer copy and technical security notice |
| ↳ `security_notice` | text (labelSmall, on-surface-variant, icon_leading=lock_outline) | content | false | FAPI/mTLS/PS256 security notice for technically-aware PSUs and Open Banking developers |
| `permissions_header` | section_header | content | false | Section heading introducing the dynamic list of OBIE read permissions staged in OBReadConsent1 |
| `permissions_list` | list (scroll_direction=vertical, items_source=`{requested_permissions}`) | content | true | Dynamic list rendering each OBIE permission with its human-readable label and description (10 rows expected) |
| ↳ `permission_row` | list_item (icon=check_circle_outline, icon_color=primary) | content | true | Individual OBIE permission row — headline_text: `{item.label}`, supporting_text: `{item.description}`; rendered once per entry in requested_permissions |
| `consent_validity_note` | text (bodySmall, on-surface-variant) | content | false | OBIE-mandated disclosure of consent validity period and TransactionFromDateTime window |
| `consent_expiry_display` | text (labelMedium, primary) | content | true | VM-computed formatted expiry label from ExpirationDateTime (e.g. "Expires 26 Sep 2026"); shown below validity note |
| `continue_hsbc_button` | button (variant=filled, icon=open_in_new) | content | false | Primary CTA triggering consent-create POST, FAPI URL build, and app-to-app redirect to HSBC |
| `cancel_button` | button (variant=text) | content | false | Secondary CTA allowing PSU to abandon the flow and return to the onboarding screen |
| `error_state` | empty_state (variant=error, icon=error_outline) | error | true | Shown when consent-create POST fails (network, 400, 500); body shows VM-mapped user-friendly message |
| ↳ `retry_button` | button (variant=filled) | error | false | Retry CTA that re-triggers start_oauth after consent-create failure |
| `login_empty_state` | empty_state (variant=neutral, icon=manage_search) | empty | false | Shown when ViewModel resolves zero OBIE permissions; prevents PSU from initiating a zero-scope consent that HSBC would reject with 400 |
| ↳ `login_empty_back_button` | button (variant=filled) | empty | false | Navigates back to user-onboarding when no OBIE permissions are available to stage |

---

## Behaviour / Action Contracts

| Component ID | Action | Effect | Library Refs | Description |
|--------------|--------|--------|--------------|-------------|
| `continue_hsbc_button` | `start_oauth` | `share_external` | ktorfit, jose4j, private_key_jwt, fapi-1.0-advanced, hsbc-obie-ais-v4.0:account-access-consents | Stages OBReadConsent1 via POST to /account-access-consents (ktorfit), builds a FAPI 1.0 Advanced /authorize URL with PS256-signed request object (jose4j, private_key_jwt), then launches app-to-app redirect to HSBC for SCA and account selection. |
| `cancel_button` | `navigate_back` | `navigate` | — | Navigates back to user-onboarding, abandoning the HSBC consent flow; no consent is staged and no OAuth state is stored when this path is taken. |
| `retry_button` | `start_oauth` | `share_external` | ktorfit, jose4j, private_key_jwt, fapi-1.0-advanced, hsbc-obie-ais-v4.0:account-access-consents | Re-triggers consent-create POST (ktorfit) and FAPI 1.0 Advanced /authorize URL construction (jose4j, private_key_jwt) after a prior failure, then relaunches the HSBC app-to-app redirect for SCA. |
| `login_empty_back_button` | `navigate_back` | `navigate` | — | Navigates back to user-onboarding when no OBIE permissions are available to stage, preventing the PSU from initiating a zero-scope consent that HSBC would reject. |

---

## Navigation

| Direction | Source / Target | Trigger |
|-----------|----------------|---------|
| Entry | user-onboarding → login | User taps "Connect with HSBC" on the onboarding screen |
| Exit | login → consent-callback | HSBC authorisation redirect returns with auth code (OS deep-link; not an in-app NavController transition) |
| Exit | login → user-onboarding | User taps Cancel (content state) or Go Back (empty state) |

---

## Flow Logic (3 decisions)

| Decision | Condition | True Path | False Path | Implementation |
|----------|-----------|-----------|------------|----------------|
| startOAuth result | consent-create POST succeeds + FAPI redirect launched | Transition to authorising state | Transition to error with mapped userMessage | `ConsentOnboardingViewModel.startOAuth()` |
| Error code mapping | NetworkException | userMessage = "Unable to reach HSBC. Check your connection and try again." | Map HttpException/FapiRedirectException code to message | `startOAuth` error_handling block |
| Permissions count | `requested_permissions` list is empty after `loadPermissionsConfig` | Transition to empty state; Continue CTA hidden | Transition to content state | `ConsentOnboardingViewModel.loadPermissionsConfig()` |

---

## Testing (8 scenarios)

| ID | Scenario | State | Priority |
|----|----------|-------|----------|
| TC-LOGIN-001 | Content state renders all 10 OBIE read permissions and consent validity | content | high |
| TC-LOGIN-002 | Loading state shown while consent-create POST is in flight | loading | high |
| TC-LOGIN-003 | Error state shown on consent-create failure with user-friendly message | error | high |
| TC-LOGIN-004 | Cancel navigates back to onboarding | content | medium |
| TC-LOGIN-005 | Successful consent-create stores ConsentId and transitions to authorising state | authorising | high |
| TC-LOGIN-006 | Authorising state displays correct holding UI after FAPI redirect launches | authorising | medium |
| TC-LOGIN-007 | Network error surfaces correct user-friendly message | error | medium |
| TC-LOGIN-008 | Empty state shown and prevents consent-create when no OBIE permissions are configured | empty | medium |
