<!-- source: screens/transaction-detail/ui.yaml -->
<!-- source_hash: — -->
<!-- generated: 2026-07-14T12:00:00Z -->
<!-- generated_from_feature_version: — -->
<!-- generated_from_contract_version: — -->
<!-- prior_version: — -->

# Transaction Detail — Implementation Specification

> **Source of Truth**: `idea-layer/screens/transaction-detail/`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: — | **Contract version**: — | **Status**: designed

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Transaction Detail | TransactionDetailViewModel | loading, content, error | Full detail for a single transaction. Shows colour-coded amount header, merchant name, Booked/Pending status badge, booking/value dates, client-derived Category, conditional MCC row, running balance, copyable reference, and proprietary bank code. Read-only AISP. Retry gated by error.recoverable; non-recoverable errors show Go Back. |

## State Model

### TransactionDetailViewModel

- **State**: `TransactionDetailUiState` — sealed class
  - `Loading`
  - `Content(transaction: TransactionDetailUiModel)`
  - `Error(message: String, recoverable: Boolean)`
- **TransactionDetailUiModel fields** (mapped from OBTransaction6):
  - `TransactionId: String`
  - `Amount.Amount: BigDecimal`
  - `Amount.Currency: String`
  - `CreditDebitIndicator: String` — Credit | Debit
  - `Status: String` — Booked | Pending
  - `BookingDateTime: String`
  - `ValueDateTime: String`
  - `Category: String` — client-derived via MCC lookup
  - `MerchantDetails.MerchantName: String?`
  - `MerchantDetails.MerchantCategoryCode: String?` — conditionally rendered when non-null
  - `Balance.Amount.Amount: BigDecimal`
  - `Balance.CreditDebitIndicator: String`
  - `TransactionInformation: String`
  - `ProprietaryBankTransactionCode.Code: String`
  - `ProprietaryBankTransactionCode.Issuer: String`
- **Errors**:
  - `TokenExpiredError` — HTTP 401; user_message: Session expired. Please log in again. (recoverable=true)
  - `ConsentWithdrawnError` — HTTP 403; user_message: Access to transactions has been withdrawn. (recoverable=false)
  - `TransactionNotFoundError` — transactionId absent in list; (recoverable=false)
  - `NetworkError` — null; user_message: No network connection. Please check your connection and retry. (recoverable=true)
- **Events**: copy_to_clipboard → ClipboardManager (Android) / UIPasteboard (iOS); snackbar confirmation
- **Actions**:
  - `loadTransactionDetail(transactionId: String, accountId: String)` — checks in-memory cache; fetches OBReadTransaction6 on miss; filters by transactionId; maps with MCC lookup
  - `navigateBack()` — pop screen; accountId retained on nav back-stack
  - `retryLoad()` — delegates to loadTransactionDetail; guard: only when Error(recoverable=true)
  - `copyTransactionReference(reference: String)` — ClipboardService expect/actual; one-shot snackbar
- **DI**: ktorfit (rest-client), ClipboardService (expect/actual), TransactionCategoryMapper (pure-Kotlin)

## API Endpoints (1)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| transactions | GET | AccountId(String) | OBReadTransaction6 | 401, 403 | — |

> Note: OBIE v4.0 AIS has no single-transaction endpoint. Client resolves transaction by filtering cached or freshly-fetched OBReadTransaction6 list by transactionId route param.

## Dependencies (Tier 1)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| transactions | parent | true | transactionId + accountId passed as nav route params from transactions list |

## Navigation

- **Route**: TransactionDetailRoute(transactionId: String, accountId: String)
- **From**: transactions (tap transaction row) with params transactionId, accountId
- **To**: transactions (back button; accountId retained on nav back-stack)

## App-Shell

| Property | Value |
|----------|-------|
| bottom_navigation_visible | false |
| top_app_bar_visible | true |
| top_app_bar_title | {strings.transaction_detail_screen_title} |
| top_app_bar_leading | back |
| fab_visible | false |

## Flow Logic (3 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| transactionId resolution | found in cache (TTL 300s) or fetched list? | Content(transaction) | Error(TransactionNotFoundError, recoverable=false) | loadTransactionDetail cache-first + list filter |
| error recovery | error.recoverable == true? | Show Retry button | Show Go Back button (non-recoverable: 403, NotFound) | Error state children visibility binding |
| MCC row visibility | MerchantDetails.MerchantCategoryCode != null? | Show MCC row | MCC row hidden | mcc_row visible conditional binding |

## Testing (9 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-TXNDTL-001 | Transaction detail renders full Tesco Stores debit transaction (Booked) | critical |
| TC-TXNDTL-002 | Credit transaction renders amount in primary colour; MCC row hidden when null | high |
| TC-TXNDTL-003 | Loading state shown while transaction resolves | (normal) |
| TC-TXNDTL-004 | Error state shown when transaction not found in list (non-recoverable) | critical |
| TC-TXNDTL-005 | Back button navigates to transactions screen | (normal) |
| TC-TXNDTL-006 | 401 TokenExpired shows error with Retry button (recoverable) | high |
| TC-TXNDTL-007 | 403 ConsentWithdrawn shows error with Go Back button only (non-recoverable) | high |
| TC-TXNDTL-008 | Copy reference writes TransactionInformation to clipboard | (normal) |
| TC-TXNDTL-009 | Pending transaction shows secondary-container status badge and future ValueDateTime | (medium) |
