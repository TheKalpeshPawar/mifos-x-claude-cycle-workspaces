<!-- source: screens/transaction-detail/ui.yaml -->
<!-- source_hash: regenerated-2026-07-16 -->
<!-- generated: 2026-07-16T00:00:00Z -->

# Transaction Detail — Implementation Specification

> **Source of Truth**: `idea-layer/screens/transaction-detail/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 1.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Transaction Detail | TransactionDetailViewModel | loading, content, error, empty | Resolves a single transaction by transactionId from the in-memory OBReadTransaction6 list fetched via ktorfit on the transactions screen. Surfaces Amount (with currency), MerchantDetails.MerchantName, Status, BookingDateTime, ValueDateTime, client-derived Category, MerchantCategoryCode, Balance.Amount, TransactionInformation (with copy-to-clipboard), and ProprietaryBankTransactionCode for a read-only AISP detail view. Retry gated by error.recoverable flag; non-recoverable errors (403 ConsentWithdrawn, TransactionNotFound) show Go Back instead. |

---

## State Model

### TransactionDetailViewModel

**State class**: `TransactionDetailState`

| Field | Type | Notes |
|-------|------|-------|
| transactionId | String | Route param; key for in-memory lookup |
| accountId | String | Route param; used for cache key and retry fetch |
| uiState | TransactionDetailUiState | Screen-level sealed state |

**ScreenState sealed class**: `TransactionDetailUiState`

| Member | Payload | Notes |
|--------|---------|-------|
| Loading | — | In-memory cache check or OBReadTransaction6 fetch in flight |
| Content | transaction: OBTransaction6 with client-derived Category | Successful resolution of transactionId in result set |
| Error | message: String, recoverable: Boolean | 401 (recoverable), 403 (non-recoverable), network (recoverable) |
| Empty | — | transactionId absent in a successful OBReadTransaction6 response; distinct from error state — no Retry offered |

**Error types**:

| Error Class | HTTP Status | Recoverable | Recovery Action |
|-------------|-------------|-------------|-----------------|
| TokenExpiredError | 401 | true | retry_load |
| ConsentWithdrawnError | 403 | false | navigate_back |
| TransactionNotFoundError | — (client) | false | navigate_back → routes to Empty state |
| NetworkError | — | true | retry_load |

**Note on TransactionNotFoundError**: Raised when the client filter returns zero matches after a successful OBReadTransaction6 fetch. Routes to the `empty` state (not the error state) — the fetch succeeded with a valid response but returned no matching record.

**Actions**:

| Action | Signature | Notes |
|--------|-----------|-------|
| loadTransactionDetail | `suspend fun loadTransactionDetail(transactionId: String, accountId: String)` | Checks in-memory cache (TTL 300s) first; fetches OBReadTransaction6 on miss; filters by transactionId; maps with MCC Category lookup |
| navigateBack | `fun navigateBack()` | Pops screen off nav stack; accountId retained by nav back-stack |
| retryLoad | `fun retryLoad()` | Delegates to loadTransactionDetail; guard: only callable when uiState is Error with recoverable=true |
| copyTransactionReference | `fun copyTransactionReference(reference: String)` | Writes to ClipboardService expect/actual; emits one-shot snackbar confirmation |

**DI**:

| Dependency | Role |
|------------|------|
| TransactionsRepository | GET /accounts/{AccountId}/transactions via ktorfit; filters by transactionId |
| ClipboardService | expect/actual platform clipboard for copyTransactionReference (ClipboardManager Android / UIPasteboard iOS) |
| NavigationController | KMP navigation abstraction for route push/pop |

### State Transitions

| From | Trigger | To State |
|------|---------|----------|
| — | Screen mount | loading |
| loading | transactionId found in cache or fetch result | content |
| loading | transactionId absent in successful 200 response | empty |
| loading | HTTP 401 / network error | error (recoverable=true) |
| loading | HTTP 403 | error (recoverable=false) |
| error (recoverable) | Retry tapped | loading |
| error (non-recoverable) | Go Back tapped | → transactions |
| empty | Go Back tapped | → transactions |
| content | Back button tapped | → transactions |

---

## Component Inventory

### State: loading

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| (no id) | progress_indicator (circular) | loading | Visual feedback while OBReadTransaction6 list is fetched and filtered by transactionId |

### State: all (navigation — always visible)

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| back_button | icon_button (arrow_back) | — | Pops transaction-detail from the navigation stack |

### State: content

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| amount_header | text (displaySmall) | content | Large colour-coded amount: primary for credit (`+£{Amount}`), error for debit (`-£{Amount}`) |
| transaction_currency_meta | text (labelMedium, on-surface-variant) | content | ISO 4217 currency code (e.g. GBP) displayed below the amount |
| merchant_name | text (headlineMedium) | content | Merchant name from OBTransaction6.MerchantDetails.MerchantName; fallback to TransactionInformation for non-card credits and bank transfers |
| status_badge | chip (assist variant) | content | OBIE Status enum badge: Booked = primary-container, Pending = secondary-container |
| header_separator | divider | content | Visual separator between header block and detail card |
| detail_card | card (elevation 1) | content | Grouped detail card of all supplementary OBTransaction6 fields |
| ↳ detail_card_header | text (titleSmall, on-surface-variant) | — | Section title for the detail card |
| ↳ booking_date_row | list_item | — | ISO 8601 BookingDateTime (when the transaction cleared) |
| ↳ value_date_row | list_item | — | ISO 8601 ValueDateTime (effective value date; may differ from BookingDateTime for pending transactions) |
| ↳ category_row | list_item | — | Client-derived PFM category from ProprietaryBankTransactionCode.Code MCC lookup (e.g. MCC 5411 → Groceries) |
| ↳ mcc_row | list_item | — | ISO 18245 Merchant Category Code from MerchantDetails.MerchantCategoryCode; `visible: {transaction.MerchantDetails.MerchantCategoryCode != null}` — hidden for direct credits and bank transfers |
| ↳ balance_after_row | list_item | — | Running balance after transaction from OBTransactionCashBalance; sign derived from Balance.CreditDebitIndicator |
| ↳ reference_row | list_item | — | Free-text reference from OBTransaction6.TransactionInformation; trailing copy-to-clipboard icon triggers copyTransactionReference |

### State: error

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| error_state | empty_state (variant: error) | error | Full-screen error surface; icon: error_outline; body: `{error.message}` |
| ↳ retry_button | button (filled) | — | Re-fetches OBReadTransaction6 when error.recoverable=true; `visible: {error.recoverable}` |
| ↳ go_back_button | button (outlined) | — | Pops screen when error.recoverable=false (403 ConsentWithdrawn or TransactionNotFound); `visible: {!error.recoverable}` |

### State: empty

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| transaction_empty_state | empty_state (variant: neutral) | empty | Shown when transactionId does not match any entry in the OBReadTransaction6 result set (transaction outside consent window or removed since the list was fetched); icon: receipt_long |
| ↳ transaction_empty_back_button | button (outlined) | — | Returns PSU to the transactions list when the selected transactionId yields no detail record |

---

## Behaviour / Action Contracts

| Component ID | Action | Effect | Library Refs | Description |
|--------------|--------|--------|--------------|-------------|
| back_button | navigate_back | navigate | — | Pops the transaction-detail screen from the navigation stack, returning the PSU to the transactions list. |
| retry_button | retry_load | call_api | ktorfit, hsbc-obie-ais-v4.0:transactions | Re-fetches GET /accounts/{AccountId}/transactions when error.recoverable is true (401 token expiry or network error) and filters the result by transactionId to resolve the detail record. |
| go_back_button | navigate_back | navigate | — | Pops the transaction-detail screen when a non-recoverable error occurs (403 ConsentWithdrawn or TransactionNotFound); returns the PSU to the transactions list. |
| reference_row | copy_to_clipboard | copy_clipboard | platform-clipboard-manager | Copies the TransactionInformation reference string to the system clipboard via platform ClipboardManager and emits a snackbar confirmation to the PSU. |
| transaction_empty_back_button | navigate_back | navigate | — | Returns the PSU to the transactions list when the selected transactionId yields no matching record in the OBReadTransaction6 response. |

---

## API Dependencies

| ID | Method | Path | Permission | Response DTO | Notes |
|----|--------|------|------------|--------------|-------|
| transactions | GET | /accounts/{AccountId}/transactions | ReadTransactionsDetail | OBReadTransaction6 | No single-transaction endpoint in OBIE v4.0 AIS. Client filters cached (TTL 300s) or freshly-fetched list by transactionId route param. |

---

## Navigation

- **Entry**: transactions → tap transaction row (params: transactionId, accountId)
- **Entry (alternate)**: statement-detail → tap statement_txn_row (params: transactionId, accountId)
- **Back button / Go Back**: → transactions (accountId retained by nav back-stack)
- **Flow ref**: transaction-review

---

## App-Shell

| Property | Value |
|----------|-------|
| bottom_navigation_visible | false |
| top_app_bar_visible | true |
| top_app_bar_title | {strings.transaction_detail_screen_title} |
| top_app_bar_leading | back |
| FAB visible | false |

---

## Testing (11 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-TXNDTL-001 | Transaction detail renders full Tesco Stores debit transaction (Booked, MCC 5411) with correct amount color, category, and balance | critical |
| TC-TXNDTL-002 | Credit transaction renders amount in primary colour; MCC row hidden when MerchantCategoryCode is null | high |
| TC-TXNDTL-003 | Loading state shown while transaction resolves from cache or fetch | high |
| TC-TXNDTL-004 | 401 TokenExpired shows error state with Retry button (recoverable=true) | critical |
| TC-TXNDTL-005 | 403 ConsentWithdrawn shows error state with Go Back button only (recoverable=false) | critical |
| TC-TXNDTL-006 | TransactionNotFound (transactionId absent in 200 response) shows empty state with Go Back button — not error state | critical |
| TC-TXNDTL-007 | Tapping copy-to-clipboard icon on reference_row writes TransactionInformation to system clipboard and shows snackbar | high |
| TC-TXNDTL-008 | Pending transaction shows secondary-container status badge | medium |
| TC-TXNDTL-009 | Back button navigates to transactions screen; accountId retained in nav back-stack | high |
| TC-TXNDTL-010 | Empty state renders when transactionId yields no match after a fresh OBReadTransaction6 fetch | high |
| TC-TXNDTL-011 | Network error shows error state with Retry button (recoverable=true); retry re-fetches and resolves | medium |
