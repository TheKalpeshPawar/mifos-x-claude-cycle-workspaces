<!-- source: screens/budgets/ui.yaml -->
<!-- source_hash: computed-on-write-2026-07-14 -->
<!-- generated: 2026-07-14T00:00:00Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 1.0.0 -->
<!-- prior_version: — -->
<!-- note: docs.yaml#feature_id not declared; using screen_id=budgets as identifier -->
<!-- note: api.yaml declares endpoints: [] — client-only feature (DataStore + kotlinx-serialization); no external endpoints -->

# Budgets -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/budgets/`
> **Auto-generated by**: `/idea export` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0 | **Status**: designed (quality: 94)

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Budgets | BudgetsViewModel | Loading, Content, Empty, Error | Set and track monthly spending budgets per category. Form with category dropdown and amount field (validates numeric > 0). Budget cards with linear progress bar (error colour when over-budget). Delete affordance per card. Client-only: budgets persisted via kotlinx-serialization to DataStore; spend values from current-month AIS transaction cache. |

## State Model

### BudgetsViewModel

- **State**: `budgets: List<BudgetEntry(category: String, used: Double, limit: Double, fraction: Double, progress_color: Color, is_over_budget: Boolean, alert_text: String, remaining_label: String, percent_label: String)>`, `form: BudgetForm(category: String, amount: String, amountError: String?)`, `current_month_label: String`, `uiState: BudgetsUiState`
- **ScreenState**: `Loading`, `Content(budgets)`, `Empty`, `Error`
- **Errors**: `EC-BUD-001 (amount empty → 'Amount is required')`, `EC-BUD-002 (non-numeric → 'Enter a valid amount')`, `EC-BUD-003 (≤ 0 → 'Budget must be greater than £0')`, `EC-BUD-004 (DataStore IOException on mount → error state + retry)`, `EC-BUD-005 (DataStore write fail → snackbar 'Couldn't save budget. Tap to retry.')`
- **Events**: `NavigateSpendingByCategory(category: String)`, `ShowSnackbar(message: String)`
- **Actions**: `retryLoad()`, `selectBudgetCategory(category: String)`, `updateBudgetAmountField(amount: String)`, `saveBudget()`, `deleteBudget(category: String)`, `navigateSpendingByCategory(category: String)`
- **DI**: `kotlinx-serialization` (DataStore persistence), `kotlinx-datetime` (current-month boundary computation for AIS transaction cache aggregation)

## API Endpoints (none)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| (none) | — | — | — | — | — |

Client-only feature (gap_strategy: client_only). Spend values derived from in-app AIS transaction cache; budgets persisted to DataStore. No external API calls.

## Dependencies (Tier 1)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| pfm-dashboard | upstream | true | Entry point — Tap 'Budgets' chip on PFM dashboard |
| spending-by-category | downstream | true | 'View transactions' button on budget card navigates with category param |
| transactions | indirect | false | AIS transaction cache populated by transactions feature; budgets reads cached data without calling transactions endpoint directly |

## Navigation

- **Route**: `BudgetsRoute` (no params)
- **From**: `pfm-dashboard` (Tap Budgets chip — flow_ref: pfm-review)
- **To**: `pfm-dashboard` (system back / top_app_bar back), `spending-by-category(category)` (View transactions button on budget card)

## App-Shell

| Property | Value |
|----------|-------|
| bottom_navigation_visible | true |
| top_app_bar_visible | true |
| top_app_bar_title | `{strings.budgets.screen_title}` |
| top_app_bar_leading | back |
| fab_visible | false |
| safe_area_top | respect |
| safe_area_bottom | respect |
| notch_handling | extend-and-pad |

## Flow Logic (4 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| DataStore read result | `budgets.isEmpty()` | empty | content | After `budgets_load` on mount; empty triggers empty state (form still visible above) |
| Amount form validation | `amount.isBlank()` | required error (EC-BUD-001) | continue to numeric check | Inline field-level error; no DataStore write |
| Numeric validation | `amount.toDoubleOrNull() == null` | numeric error (EC-BUD-002) | continue to min check | Client-side; no DataStore write |
| Min value check | `amount.toDouble() <= 0` | min error (EC-BUD-003) | persist budget (upsert by category) | Duplicate category overwrites existing limit |

## Testing (13 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-BUD-001 | Loading state shown on mount | high |
| TC-BUD-002 | Content state — budgets render with correct progress and colours | high |
| TC-BUD-003 | Empty state when no budgets set | high |
| TC-BUD-004 | Error state when DataStore read fails on mount | high |
| TC-BUD-005 | Retry after error re-triggers budgets_load | high |
| TC-BUD-006 | Saving budget with valid inputs adds a new entry | high |
| TC-BUD-007 | Saving a budget for an existing category overwrites previous limit | high |
| TC-BUD-008 | Deleting a budget removes it from the list | medium |
| TC-BUD-009 | Deleting last budget transitions to empty state | medium |
| TC-BUD-010 | Amount field validation — empty amount shows required error | medium |
| TC-BUD-011 | Amount field validation — non-numeric value shows error | medium |
| TC-BUD-012 | Amount field validation — zero or negative value shows error | low |
| TC-BUD-013 | Tapping budget category chip navigates to spending-by-category | low |
