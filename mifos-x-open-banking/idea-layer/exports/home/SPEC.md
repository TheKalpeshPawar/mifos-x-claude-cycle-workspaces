<!-- source: screens/home/ui.yaml -->
<!-- source_hash: 66c8a95765ac08cf659947bd96cf36fcad9dd366c305ca94719196dcf4ee4475 -->
<!-- generated: 2026-07-14T00:00:00Z -->
<!-- generated_from_feature_version: 1.0 -->
<!-- generated_from_contract_version: 1.0 -->
<!-- prior_version: — -->

# Home Dashboard — Implementation Specification

> **Source of Truth**: `idea-layer/screens/home/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 1.0 | **Contract version**: 1.0 | **Status**: designed

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Home | HomeViewModel | loading, content, empty, error | Landing dashboard (Home tab, bottom-nav order 0). Account-switcher chip strip, hero balance card (InterimAvailable), quick-action row (Pay disabled/PISP future; Transactions; Statements; Manage consents), recent 5 transactions preview, spending snapshot card. Three parallel OBIE AIS calls: GET /accounts + parallel GET /balances + GET /transactions. |

## State Model

### HomeViewModel : BaseViewModel\<HomeState, HomeEvent, HomeAction\>

- **State**: allAccounts(List\<OBAccount6\>), selectedAccountId(String), selectedAccount(OBAccount6?), selectedAccountBalance(OBCashBalance3?), availableBalance(OBCashBalance3?), recentTransactions(List\<OBTransaction6\>), spendThisMonth(String), topCategoryName(String), activeConsentId(String), uiState(UiState)
- **ScreenState**: Loading, Content, Empty(reason: no_accounts|no_consent), Error(code: EC-HOME-001..006, message: String, recoverable: Boolean)
- **Errors**: EC-HOME-001: 401 token expired (recoverable=true, navigate to login); EC-HOME-002: 403 ReadAccountsDetail absent (recoverable=false); EC-HOME-003: 429 rate-limit (recoverable=true, exponential back-off); EC-HOME-004: empty Data.Account[] (empty state reason=no_accounts); EC-HOME-005: no active consent (empty state reason=no_consent); EC-HOME-006: network error (recoverable=true)
- **Events**: NavEvent.ToAccountDetail(accountId), NavEvent.ToTransactions(accountId), NavEvent.ToStatements(accountId), NavEvent.ToSpendingByCategory(), NavEvent.ToConsentList(), NavEvent.ToLogin()
- **Actions**: LoadHome(); SelectAccount(accountId: String) — persists to DataStore + re-fetches balance + transactions; RetryLoad(); NavigateToAccountDetail(accountId: String); NavigateToTransactions(accountId: String); NavigateToStatements(accountId: String); NavigateToSpending(); NavigateToConsentList()
- **DI**: AisApiService (via Ktorfit), DataStore, PfmRoomRepository, Navigator, CoroutineScope

## API Endpoints (3)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| getAccounts | GET | (none) | OBReadAccount6 (Data.Account[]) | 401, 403, 429, 503 | — |
| getBalances | GET | AccountId(String) | OBReadBalance1 (Data.Balance[]) | 401, 403, 404 | — |
| getTransactions | GET | AccountId(String), fromBookingDateTime(String ISO-8601) | OBReadTransaction6 (Data.Transaction[], top 5 Booked) | 401, 403, 429 | — |

## Parallelism Contract

```
coroutineScope {
  val balanceDef = async { apiService.getBalances(selectedAccountId) }
  val txDef = async { apiService.getTransactions(selectedAccountId, limit = 5) }
  val balance = balanceDef.await()
  val txList = txDef.await()
}
```

Balance type preference: InterimAvailable → InterimBooked → OpeningBooked. CreditCard + CreditDebitIndicator=Debit renders balance in error colour.

## Dependencies (Tier 1)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| account-detail | child | true | Hero card tap nav — accountId param |
| transactions | child | true | Quick action + View all nav — accountId param |
| statements | child | true | Quick action nav — accountId param |
| consent-list | child | true | Manage consents quick action nav |
| spending-by-category | child | false | Spending snapshot card tap nav |
| transaction-detail | child | false | Recent tx row tap — transactionId + accountId params |
| login | successor | true | 401 error + empty state CTA |

## Navigation

- **Route**: Home()
- **From**: app_shell_bottom_nav (Home tab, order 0), post-login (FAPI callback → home), consent-callback (successful OAuth)
- **To**: account-detail(accountId), transactions(accountId), statements(accountId), consent-list, spending-by-category, transaction-detail(transactionId, accountId), login (401 / empty CTA)

## App-Shell

| Property | Value |
|----------|-------|
| bottom_navigation_visible | true |
| top_app_bar_visible | false |
| FAB visible | false |
| safe-area | respect top + bottom, extend-and-pad notch |

## Flow Logic (5 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Accounts empty? | Data.Account[].isEmpty() | Empty state reason=no_accounts; CTA → login | Proceed to load balance + transactions | LoadHome() |
| Balance type? | InterimAvailable present? | Use InterimAvailable as hero balance | Fall back to InterimBooked → OpeningBooked | getBalances() balance derivation |
| CreditCard debit balance? | AccountType=CreditCard && CreditDebitIndicator=Debit | Render hero balance in error colour | Neutral on-surface-variant colour | ViewModel amountColor derivation |
| Transaction debit or credit? | CreditDebitIndicator=Debit | Amount renders in error colour prefixed '- £' | Amount renders in primary colour prefixed '+ £' | OBTransaction6.amountColor derivation |
| Error recoverable? | error.recoverable == true | Show Retry CTA in error state | Hide Retry CTA (e.g. 403 non-recoverable) | error state visible_when binding |

## Testing (16 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-HOME-001 | Home screen loads content state for Priya's default selected account | high |
| TC-HOME-002 | Loading state shows shimmer skeletons during initial fetch | high |
| TC-HOME-003 | Account switcher re-loads content for a different account | high |
| TC-HOME-004 | Hero balance card tap navigates to account-detail | high |
| TC-HOME-005 | Transactions quick action navigates to transactions screen | high |
| TC-HOME-006 | 'View all' link navigates to transactions screen | high |
| TC-HOME-007 | Manage consents quick action navigates to consent-list | high |
| TC-HOME-008 | Spending snapshot card navigates to spending-by-category | high |
| TC-HOME-009 | Pay quick action is disabled (PISP not implemented) | high |
| TC-HOME-010 | Error state renders with retry CTA on 401 token expired | high |
| TC-HOME-011 | Error state — non-recoverable 403 hides Retry button | high |
| TC-HOME-012 | Empty state renders with CTA when no accounts returned | high |
| TC-HOME-013 | Tap recent transaction row navigates to transaction-detail | high |
| TC-HOME-014 | CreditCard account shows balance owed styling in hero card | medium |
| TC-HOME-015 | Recent transactions debit amounts show in error colour, credits in primary | medium |
| TC-HOME-016 | Statements quick action navigates to statements screen | low |
