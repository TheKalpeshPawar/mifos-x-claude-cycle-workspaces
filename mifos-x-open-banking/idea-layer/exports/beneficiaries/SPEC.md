<!-- source: screens/beneficiaries/ui.yaml -->
<!-- source_hash: computed-on-write-2026-07-14 -->
<!-- generated: 2026-07-14T00:00:00Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 1.0.0 -->
<!-- prior_version: — -->
<!-- note: docs.yaml#feature_id not declared; using screen_id=beneficiaries as identifier -->

# Beneficiaries -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/beneficiaries/`
> **Auto-generated by**: `/idea export` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0 | **Status**: designed (quality: 91)

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Beneficiaries | BeneficiariesViewModel | Loading, Content, Empty, Error | Read-only list of saved payees for an account from OBReadBeneficiary5. Shows avatar initials, creditor name, scheme-type label (Sort Code / IBAN / Paym / Card / Account), account identifier, and payment reference. Client-side search by name or reference. Typed error recovery: Retry for 401/429/network, View Consents for 403. |

## State Model

### BeneficiariesViewModel

- **State**: `accountId: String`, `beneficiaries: List<OBBeneficiary5>`, `beneficiariesFiltered: List<OBBeneficiary5>`, `searchQuery: String`, `error: ErrorDetail(type: ErrorType, message: String)`, `uiState: BeneficiariesUiState`
- **ScreenState**: `Loading`, `Content(beneficiariesFiltered)`, `Empty`, `Error(type, message)`
- **Errors**: `TokenExpired(401, retry=true)`, `ConsentRevoked(403, retry=false — View Consents CTA)`, `RateLimited(429, retry=true — exponential back-off)`, `NetworkError(IOException/timeout, retry=true)`, `EmptyResult (Data.Beneficiary[] empty — transitions to empty state)`
- **Events**: `NavigateBack(accountId: String)`, `NavigateConsentList`
- **Actions**: `LoadBeneficiaries(accountId: String)`, `FilterBeneficiaries(query: String)`, `RetryLoad()`, `NavigateToConsents()`, `NavigateBack()`
- **DI**: `ktorfit` (AIS endpoint — `AisApiService.getBeneficiaries(accountId)`)

### Scheme Label Mapping (ViewModel helper)

| OBIE SchemeName | Display Label |
|-----------------|--------------|
| UK.OBIE.SortCodeAccountNumber | Sort Code |
| UK.OBIE.IBAN | IBAN |
| UK.OBIE.Paym | Paym |
| UK.OBIE.PAN | Card |
| (default) | Account |

## API Endpoints (1)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| getBeneficiaries | GET | AccountId(String) | OBReadBeneficiary5 (Data.Beneficiary[]) | 401, 403, 429, 500 | — |

## Dependencies (Tier 1)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| account-detail | upstream | true | AccountId route param passed from account-detail Beneficiaries chip |
| consent-list | downstream | false | Navigation target on 403 ConsentRevoked error via View Consents CTA |

## Navigation

- **Route**: `BeneficiariesRoute(accountId: String)`
- **From**: `account-detail` (Tap Beneficiaries chip — flow_ref: recurring-and-statements)
- **To**: `account-detail(accountId)` (back — top_app_bar leading), `consent-list` (View Consents on 403 ConsentRevoked)

## App-Shell

| Property | Value |
|----------|-------|
| bottom_navigation_visible | true |
| top_app_bar_visible | true |
| top_app_bar_title | `{strings.beneficiaries.screen_title}` |
| top_app_bar_leading | back |
| fab_visible | false |
| safe_area_top | respect |
| safe_area_bottom | respect |
| notch_handling | extend-and-pad |

## Flow Logic (2 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Empty beneficiaries list | `Data.Beneficiary[].isEmpty()` | empty | content | OBIE response with empty array → empty state (no payees registered) |
| Error type routing | `error.type == ConsentRevoked` | show View Consents CTA only | show Retry CTA only | `isRetryable` filter: `{TokenExpired, RateLimited, NetworkError}` → Retry; `ConsentRevoked` → View Consents |

## Testing (9 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-BEN-001 | Beneficiaries list loads and renders all five saved payees | p0 |
| TC-BEN-002 | Loading state shown during beneficiaries fetch | p0 |
| TC-BEN-003 | Empty state when no beneficiaries registered for the account | p1 |
| TC-BEN-004 | Error state with Retry button on 401 token expired | p0 |
| TC-BEN-005 | Back button navigates to account-detail | p1 |
| TC-BEN-006 | Search bar filters beneficiaries by creditor name | p1 |
| TC-BEN-007 | Search bar yields no-results empty state within content state | p2 |
| TC-BEN-008 | Error state with View Consents button on 403 consent revoked | p1 |
| TC-BEN-009 | Avatar initials derived from creditor name for visual identity | p2 |
