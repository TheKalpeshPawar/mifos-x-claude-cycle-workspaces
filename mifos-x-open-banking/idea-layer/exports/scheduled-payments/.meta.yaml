feature: scheduled-payments
generated_at: '2026-07-30T10:00:00Z'
generated_by: /idea-feature-export (refresh-stale, single feature)
batch: null            # not a batch run \u2014 [R] refresh of the one feature invalidated this session
source_hash: 0f33ec921191
source_hash_kind: raw-file-sha256-12
source_hash_caveat: >-
  RAW sha256 of screens/scheduled-payments/ui.yaml, truncated to 12. The runtime's
  hash contract (_shared/idea-export-detail.md "Hash-stamping contract") specifies a
  STRIPPED content_hash \u2014 ui.yaml with the exports/, enrichment_history/ and
  status_history/ blocks plus the leading `# source_hash:` line removed \u2014 so this value
  is NOT contract-conformant and must not be compared against one that is. The prior
  value (bde6567a0e34) was inherited and its derivation is unknown; it was not
  recomputed here. Flagged rather than silently overwritten, because a wrong hash under
  a right-looking key breaks the export-freshness check without failing loudly.
artifacts:
  spec_md:
    path: exports/scheduled-payments/SPEC.md
    sha256: cf176da7aa4f46ca
    lines: 96
  api_md:
    path: exports/scheduled-payments/API.md
    sha256: dd3be7924a71a025    # unchanged \u2014 api.yaml was not touched
    lines: 89
note: Timestamps live here, not in the artifacts, so re-export diffs stay deterministic.
resolved_gap: >-
  RESOLVED IN THE IDEA-LAYER 2026-07-30 (was `known_gap`): the feature now declares the
  `unsupported` state + unsupported_scheduled_payments component that its two gated
  siblings declare. Re-exported because that ui.yaml edit invalidated this SPEC.
open_gap: >-
  SOURCE STILL UNFIXED. ScheduledPaymentsUiState ships four states and
  classifyScheduledPaymentsError() still routes U000 to NetworkError (retriable), so the
  dead-end Retry persists on device until /implement lands. This SPEC describes declared
  intent, not shipped behavior \u2014 see SPEC.md section 2.
