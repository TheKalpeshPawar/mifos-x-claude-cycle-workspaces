<!-- source: screens/scheduled-payments/ui.yaml -->
<!-- source_hash: b0b99be73e1ceffb357ad1e04d9924fa58f0866227668fd040ff810958fbf58e -->
<!-- generated: 2026-07-14T00:00:00Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 1.0.0 -->
<!-- prior_version: — -->

# Scheduled Payments -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/scheduled-payments/ui.yaml`
> **Auto-generated by**: `/idea export` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0 | **Status**: designed

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Scheduled Payments | ScheduledPaymentsViewModel | Loading, Content, Empty, Error | Read-only list of future-dated one-off scheduled payments sourced from OBReadScheduledPayment3. Shows payee name, instructed amount (GBP), formatted scheduled date (EEE d MMM yyyy), ScheduledType chip (Execution / Arrival), CreditorAccount.Identification (sort code + account number), and payment reference. |

## App-Shell

| Property | Value |
|----------|-------|
| Bottom Navigation | Visible |
| Top App Bar | Visible — title: `{strings.sp_screen_title}` |
| Top App Bar Leading | Back |
| FAB | Hidden |

## State Model

### ScheduledPaymentsViewModel

- **State**: scheduledPayments(`List<ScheduledPaymentUiModel>`) · uiState(`ScreenState`) · accountId(`String`)
- **ScreenState**: Loading, Content(scheduledPayments), Empty, Error(message)
- **Errors**: TokenExpired (retry=true, code=401), ConsentRevoked (retry=true, code=403), RateLimited (retry=true, code=429), NetworkError (retry=true)
- **Events**: NavigationEvent.Back
- **Actions**:
  - `loadScheduledPayments(accountId: String)` — GET /accounts/{AccountId}/scheduled-payments; maps ScheduledType enum to label + icon + a11y string; formats ScheduledPaymentDateTime as EEE d MMM yyyy
  - `retryLoad()` — re-invokes loadScheduledPayments with same accountId
  - `navigateBack()` — emits NavigationEvent.Back to NavController
- **DI**: AisApiService (Ktorfit), AccountId (nav arg)

## API Endpoints (1)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| loadScheduledPayments | GET /accounts/{AccountId}/scheduled-payments | AccountId(String) | OBReadScheduledPayment3 | 401, 403, 429 | scheduled_payments |

## Dependencies (Tier 1)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| account-detail | navigation-source | true | AccountId passed as nav param from account-detail Scheduled chip |
| login | auth | true | Valid PSU access token with ReadScheduledPaymentsDetail permission |

## Navigation

- **Route**: ScheduledPaymentsRoute(accountId: String)
- **From**: account-detail (Tap Scheduled chip, passes accountId)
- **To**: account-detail (Back button)

## Flow Logic (3 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| API response empty? | Data.ScheduledPayment[] has 0 entries | Transition to Empty state | Map entries to UiModel list; transition to Content | loadScheduledPayments() post-response check |
| HTTP error code? | 401 / 403 / 429 / IOException | Transition to Error(typedMessage); show Retry | Proceed to Content or Empty | when-block on HTTP status in Ktorfit interceptor |
| ScheduledType=Execution? | item.ScheduledType == Execution | label='Execution date', icon='calendar_today', a11y='Money leaves your account on {date}' | label='Arrival date', icon='arrow_downward', a11y='Funds arrive at {payee} on {date}' | toUiModel() sealed-when mapping |

## Testing (8 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-SP-001 | Scheduled payments list loads and renders all pending payments | p0 |
| TC-SP-002 | Loading state shown during scheduled payments fetch | p0 |
| TC-SP-003 | Empty state when no scheduled payments pending | p1 |
| TC-SP-004 | Error state with Retry button on 401 token expired | p0 |
| TC-SP-005 | Back button navigates to account-detail | p1 |
| TC-SP-006 | Error state shows consent-revoked message on 403 | p1 |
| TC-SP-007 | ScheduledType=Arrival renders 'Arrival date' chip with arrow_downward icon | p1 |
| TC-SP-008 | CreditorAccount.Identification (sort code + account number) displayed in each card | p2 |
