<!-- source: screens/transactions/ui.yaml -->
<!-- source_hash: regenerated-2026-07-16 -->
<!-- generated: 2026-07-16T00:00:00Z -->

# Transactions — Implementation Specification

> **Source of Truth**: `idea-layer/screens/transactions/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 1.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Transactions | TransactionsViewModel | loading, content, empty, error | Full transaction history for a single authorised account. Fetches OBReadTransaction6 via ktorfit; transparently pages via Links.Next cursor. Applies client-side CreditDebitIndicator filtering, kotlinx-datetime LocalDate grouping by BookingDateTime, and case-insensitive search over TransactionInformation and MerchantDetails.MerchantName. Derives PFM Category from ProprietaryBankTransactionCode.Code or MCC via an MCC lookup map. Computes period totals from the filtered result set. Distinguishes Pending vs Booked Status to render a badge on unsettled transactions. |

---

## State Model

### TransactionsViewModel

**State class**: `TransactionsState`

| Field | Type | Notes |
|-------|------|-------|
| accountId | String | Route param |
| uiState | TransactionsUiState | Screen-level sealed state |
| transactions | List\<OBTransaction6\> | Full in-memory accumulated list (all pages fetched so far) |
| activeFilter | TransactionFilter | All / Credit / Debit |
| searchQuery | String | Case-insensitive substring match against TransactionInformation + MerchantDetails.MerchantName |
| hasNextPage | Boolean | true when Links.Next is non-null in the last response |
| isPaginating | Boolean | true while a next-page fetch is in flight |

**ScreenState sealed class**: `TransactionsUiState`

| Member | Payload | Notes |
|--------|---------|-------|
| Loading | — | First-page GET /accounts/{AccountId}/transactions in flight |
| Content | transactionsByDate: Map\<LocalDate, List\<OBTransaction6\>\>, period totals | Transactions grouped by BookingDateTime via kotlinx-datetime; period totals computed from filtered set |
| Empty | — | Active filter, search, or date range yields zero matching rows |
| Error | message: String, recoverable: Boolean | 401 (recoverable), 403 (non-recoverable), 429 (recoverable), network (recoverable) |

**Error types**:

| Error Class | HTTP Status | Recoverable | Recovery Action |
|-------------|-------------|-------------|-----------------|
| TokenExpiredError | 401 | true | retry_load |
| ConsentWithdrawnError | 403 | false | navigate_back |
| RateLimitError | 429 | true | retry_load |
| NetworkError | — | true | retry_load |

**Actions**:

| Action | Signature | Notes |
|--------|-----------|-------|
| loadTransactions | `suspend fun loadTransactions(accountId: String)` | GET /accounts/{accountId}/transactions; groups by BookingDateTime LocalDate; derives Category via MCC lookup |
| filterTransactions | `fun filterTransactions(filter: TransactionFilter)` | Client-side CreditDebitIndicator filter; recomputes period totals; may emit empty state |
| openDateRangePicker | `fun openDateRangePicker()` | Sets `showDateRangePicker = true`; shows system date range picker dialog |
| searchTransactions | `fun searchTransactions(query: String)` | Case-insensitive match on TransactionInformation and MerchantDetails.MerchantName |
| navigateTransactionDetail | `fun navigateTransactionDetail(transactionId: String, accountId: String)` | Pushes transaction-detail route with transactionId + accountId params |
| clearFilters | `fun clearFilters()` | Resets activeFilter=All, searchQuery='', dateRange=null; transitions to content if transactions remain |
| retryLoad | `fun retryLoad()` | Resets nextLink=null, hasNextPage=false, isPaginating=false; delegates to loadTransactions |
| loadMoreTransactions | `suspend fun loadMoreTransactions()` | GET nextLink (OBIE Links.Next absolute URL) via ktorfit; appends OBTransaction6 rows; updates hasNextPage and nextLink; recomputes period totals and filteredList |
| setDateRange | `fun setDateRange(from: LocalDate, to: LocalDate)` | Sets dateRange; resets pagination cursor; re-fetches with fromBookingDateTime + toBookingDateTime bounds |

**DI**:

| Dependency | Role |
|------------|------|
| TransactionsRepository | GET /accounts/{AccountId}/transactions via ktorfit; OBIE Links.Next pagination |
| NavigationController | KMP navigation abstraction for route push/pop |

### State Transitions

| From | Trigger | To State |
|------|---------|----------|
| — | Screen mount | loading |
| loading | 200 OK + Data.Transaction[] non-empty | content |
| loading | 200 OK + Data.Transaction[] empty | empty |
| loading | HTTP 401 / 429 / network error | error (recoverable=true) |
| loading | HTTP 403 | error (recoverable=false) |
| error (recoverable) | Retry tapped | loading |
| content | Filter chip tapped | content (filtered) or empty |
| content / empty | Search query entered | content (filtered) or empty |
| content | Date range confirmed | loading (re-fetch with bounds) |
| content | Load more tapped | content (appended) |
| content | Clear filters tapped (from empty) | content |
| content | Transaction row tapped | → transaction-detail (transactionId, accountId) |

---

## Component Inventory

### State: loading

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| loading_spinner | progress_indicator (circular) | loading | Full-screen visual feedback while GET /accounts/{AccountId}/transactions first page is in flight |

### State: content

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| period_summary | stat_block | content | Period totals computed client-side from filtered result set: `total_credit` (primary color) + `total_debit` (error color) |
| filter_chips | chip_row (horizontal scroll) | content, empty | Client-side filter controls applied to in-memory result set |
| ↳ filter_all | chip (filter, selected by default) | — | Resets CreditDebitIndicator filter to show all transactions |
| ↳ filter_money_in | chip (filter, icon: arrow_downward) | — | Filters to CreditDebitIndicator=Credit rows only |
| ↳ filter_money_out | chip (filter, icon: arrow_upward) | — | Filters to CreditDebitIndicator=Debit rows only |
| ↳ filter_date_range | chip (filter, icon: date_range) | — | Opens date-range picker bottom sheet; on confirm resets pagination cursor and re-fetches with date bounds |
| search_field | text_field | content, empty | Case-insensitive substring search over TransactionInformation and MerchantDetails.MerchantName; leading_icon: search; trailing_icon: clear; validation: none |
| transactions_list | list (vertical scroll) | content | Date-grouped list of OBTransaction6 rows after client-side filter and search; items_source: `{transactions_by_date}` |
| ↳ date_group_header | section_header | — | BookingDateTime date header; formatted as "EEE d MMM yyyy" by ViewModel via kotlinx-datetime LocalDate grouping |
| ↳ transaction_row | list_item | — | Tappable transaction row: leading icon (arrow_downward credit / arrow_upward debit), merchant name (tx_merchant), PFM category tag (tx_category_tag), colour-coded amount (Credit=primary, Debit=error, titleMedium). Navigates to transaction-detail on tap |
| ↳↳ tx_merchant | text (bodyMedium) | — | Transaction merchant or narrative from OBTransaction6.TransactionInformation |
| ↳↳ tx_category_tag | chip (assist, labelSmall) | — | Client-derived PFM category from ProprietaryBankTransactionCode.Code / MCC lookup |
| ↳↳ tx_pending_badge | badge (warning) | — | Warning badge shown only when OBTransaction6.Status=Pending; `visible_when: {item.Status == 'Pending'}` |
| load_more_button | button (text) | content | Triggers next-page fetch using Links.Next cursor; `visible_when: {has_next_page && !is_paginating}` |
| pagination_loader | progress_indicator (linear) | content | Linear progress at list bottom while next-page fetch is in flight; `visible_when: {is_paginating}` |

### State: empty

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| empty_transactions | empty_state | empty | Shown when active filter, search, or date range yields zero matching OBTransaction6 rows; icon: receipt_long |
| ↳ clear_filters_button | button (outlined) | — | Resets CreditDebitIndicator filter, date range, and search query; transitions to content if transactions remain |

### State: error

| Component ID | Type | State Binding | Purpose |
|--------------|------|---------------|---------|
| error_state | empty_state (variant: error) | error | Contextual error surface; body `{error.message}` resolved by ViewModel from i18n strings by HTTP status |
| ↳ retry_button | button (filled) | — | Re-issues GET /accounts/{AccountId}/transactions; `visible_when: {error.recoverable}` |

---

## Behaviour / Action Contracts

| Component ID | Action | Effect | Library Refs | Description |
|--------------|--------|--------|--------------|-------------|
| filter_all | filter_transactions | transform_state | kotlinx-coroutines | Resets the CreditDebitIndicator filter to show all transactions, recomputes period totals from the full in-memory OBReadTransaction6 list, and updates the UI state without re-fetching. |
| filter_money_in | filter_transactions | transform_state | kotlinx-coroutines | Filters the in-memory OBReadTransaction6 list to CreditDebitIndicator=Credit rows only and recomputes period totals without issuing any network request. |
| filter_money_out | filter_transactions | transform_state | kotlinx-coroutines | Filters the in-memory OBReadTransaction6 list to CreditDebitIndicator=Debit rows only and recomputes period totals without issuing any network request. |
| filter_date_range | open_date_range_picker | transform_state | kotlinx-coroutines | Opens the date-range picker bottom sheet as a local UI state transition; on confirm resets the pagination cursor and re-fetches with fromBookingDateTime and toBookingDateTime bounds. |
| search_field | search_transactions | transform_state | kotlinx-coroutines | Applies a case-insensitive substring search over TransactionInformation and MerchantDetails.MerchantName against the in-memory OBReadTransaction6 list without re-fetching. |
| transaction_row | navigate_transaction_detail | navigate | — | Pushes the transaction-detail route with the tapped OBTransaction6 TransactionId and AccountId as route params. |
| load_more_button | load_more_transactions | call_api | ktorfit, hsbc-obie-ais-v4.0:transactions | Fetches the next transactions page using the Links.Next cursor stored in next_link, appends returned OBTransaction6 rows to the in-memory list, and updates has_next_page and next_link from the new response. |
| clear_filters_button | clear_filters | transform_state | kotlinx-coroutines | Resets the CreditDebitIndicator filter, date range, and search query to defaults, transitioning the screen from empty back to content if transactions remain in the in-memory list. |
| retry_button | retry_load | call_api | ktorfit, hsbc-obie-ais-v4.0:transactions | Re-issues GET /accounts/{AccountId}/transactions from page one after a recoverable error (401 or network failure), resetting the pagination cursor before the fetch. |

---

## API Dependencies

| ID | Method | Path | Permission | Response DTO | Notes |
|----|--------|------|------------|--------------|-------|
| transactions | GET | /accounts/{AccountId}/transactions | ReadTransactionsDetail | OBReadTransaction6 | Optional query params: fromBookingDateTime, toBookingDateTime (ISO-8601). Links.Next pagination cursor stored in next_link state. Credit/debit filter and text search applied client-side. |

---

## Navigation

- **Entry**: account-detail → tap Transactions chip (params: accountId)
- **Entry (alternate)**: pfm → drill-down from PFM category (params: accountId, category)
- **Row tap**: → transaction-detail (params: transactionId, accountId)
- **Back**: top_app_bar leading icon → account-detail
- **Flow ref**: transaction-review

---

## App-Shell

| Property | Value |
|----------|-------|
| bottom_navigation_visible | true |
| top_app_bar_visible | true |
| top_app_bar_title | {strings.transactions.title} |
| top_app_bar_leading | back |
| FAB visible | false |

---

## Testing (12 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-TXN-001 | Transaction list loads and renders 10 transactions grouped by date with period totals | critical |
| TC-TXN-002 | Loading state shown while transactions fetch is in flight | critical |
| TC-TXN-003 | Error state with Retry button on 401 token expired | critical |
| TC-TXN-004 | Error state without Retry button on 403 consent withdrawn | critical |
| TC-TXN-005 | Empty state when no transactions match active filter; Clear filters button transitions back to content | critical |
| TC-TXN-006 | Money in filter chip shows only Credit transactions; period totals updated | high |
| TC-TXN-007 | Money out filter chip shows only Debit transactions; period totals updated | high |
| TC-TXN-008 | Search field filters transactions by TransactionInformation substring (case-insensitive) | high |
| TC-TXN-009 | Tapping a transaction row navigates to transaction-detail with correct transactionId and accountId | high |
| TC-TXN-010 | Pending transaction shows warning 'Pending' badge; booked rows show no badge | high |
| TC-TXN-011 | Load more button appears when Links.Next present; tapping it appends next page; button disappears on last page | medium |
| TC-TXN-012 | Date range chip re-fetches with ISO-8601 fromBookingDateTime/toBookingDateTime bounds; pagination cursor reset | medium |
