<!-- source: screens/transactions/ui.yaml -->
<!-- source_hash: regenerated-2026-07-14 -->
<!-- generated: 2026-07-14T12:00:00Z -->
<!-- generated_from_feature_version: — -->
<!-- generated_from_contract_version: — -->
<!-- prior_version: — -->

# Transactions — Implementation Specification

> **Source of Truth**: `idea-layer/screens/transactions/`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: — | **Contract version**: — | **Status**: designed

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Transactions | TransactionsViewModel | loading, content, empty, error | Full transaction history for a single authorised account. Supports credit/debit filter chips, date-range chip with server-side re-fetch, and free-text search. Transactions grouped by booking date. Period summary strip shows totals. OBIE Links.Next pagination via Load more affordance. |

## State Model

### TransactionsViewModel

- **State**: `TransactionsUiState` — sealed class
  - `Loading`
  - `Content(transactionsByDate: Map<LocalDate, List<OBTransaction6>>)`
  - `Empty`
  - `Error(message: String, recoverable: Boolean)`
- **Additional state fields**:
  - `activeFilter: TransactionFilter` — All | Credit | Debit
  - `searchQuery: String`
  - `dateRange: DateRange?`
  - `nextLink: String?` — OBIE Links.Next absolute URL
  - `hasNextPage: Boolean`
  - `isPaginating: Boolean`
  - `period.totalCredit: BigDecimal` — sum of Credit amounts in filtered set
  - `period.totalDebit: BigDecimal` — sum of Debit amounts in filtered set
- **Errors**:
  - `TokenExpiredError` — HTTP 401; user_message: Session expired. Please log in again. (recoverable=true)
  - `ConsentWithdrawnError` — HTTP 403; user_message: Access to transactions has been withdrawn. (recoverable=false)
  - `RateLimitError` — HTTP 429; user_message: Too many requests. Please wait a moment and retry. (recoverable=true)
  - `NetworkError` — null; user_message: No network connection. Please check your connection and retry. (recoverable=true)
- **Events**: navigation to transaction-detail; date range picker open/close
- **Actions**:
  - `loadTransactions(accountId: String)` — GET /accounts/{AccountId}/transactions; groups by BookingDateTime LocalDate; derives Category via MCC lookup
  - `filterTransactions(filter: TransactionFilter)` — client-side filter; recomputes period totals; may emit empty state
  - `openDateRangePicker()` — shows system date range picker dialog
  - `searchTransactions(query: String)` — case-insensitive match on TransactionInformation and MerchantDetails.MerchantName
  - `navigateTransactionDetail(transactionId: String, accountId: String)` — push transaction-detail route
  - `clearFilters()` — resets filter=All, searchQuery='', dateRange=null
  - `retryLoad()` — resets pagination state; delegates to loadTransactions
  - `loadMoreTransactions()` — GET nextLink; appends to transactions list; recomputes totals and filteredList
  - `setDateRange(from: LocalDate, to: LocalDate)` — resets pagination cursor; re-fetches with date bounds
- **DI**: ktorfit (rest-client), kotlinx-datetime (datetime), TransactionCategoryMapper (pure-Kotlin)

## API Endpoints (1)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| transactions | GET | AccountId(String), fromBookingDateTime(String?), toBookingDateTime(String?) | OBReadTransaction6: Data.Transaction[], Links.Next | 401, 403, 429 | — |

## Dependencies (Tier 1)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| account-detail | parent | true | accountId passed as nav route param from account-detail Transactions chip |
| pfm | optional-entry | false | drill-down from PFM category passes accountId and pre-filters by category |

## Navigation

- **Route**: TransactionsRoute(accountId: String)
- **From**: account-detail (Transactions chip), pfm (drill-down from PFM category with accountId, category)
- **To**: account-detail (back button), transaction-detail (tap transaction row with transactionId, accountId)

## App-Shell

| Property | Value |
|----------|-------|
| bottom_navigation_visible | true |
| top_app_bar_visible | true |
| top_app_bar_title | {strings.transactions.title} |
| top_app_bar_leading | back |
| fab_visible | false |

## Flow Logic (5 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| initial load result | Data.Transaction[] non-empty? | Content(grouped by date) | Empty state | loadTransactions groups result |
| filter/search result | filteredList non-empty? | Content (filtered) | Empty state (with Clear filters button) | filterTransactions / searchTransactions |
| date range change | setDateRange called? | Reset cursor + re-fetch with fromBookingDateTime/toBookingDateTime | Client-side filter only | setDateRange delegates to loadTransactions with bounds |
| pagination | hasNextPage && !isPaginating? | Show Load more button | Hide Load more button | has_next_page state binding |
| error recovery | error.recoverable? | Show Retry button | Hide Retry button (403 non-recoverable) | error state children visibility |

## Testing (12 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| TC-TXN-001 | Transaction list loads and renders 10 transactions grouped by date | critical |
| TC-TXN-002 | Loading state shown while transactions fetch is in flight | (normal) |
| TC-TXN-003 | Error state with Retry button on 401 token expired | critical |
| TC-TXN-004 | Error state without Retry button on 403 consent withdrawn | critical |
| TC-TXN-005 | Empty state when no transactions match active filter | critical |
| TC-TXN-006 | Money in filter shows only Credit transactions | high |
| TC-TXN-007 | Money out filter shows only Debit transactions | high |
| TC-TXN-008 | Search filters transactions by TransactionInformation text | high |
| TC-TXN-009 | Tapping a transaction row navigates to transaction-detail with correct params | high |
| TC-TXN-010 | Pending transaction shows 'Pending' badge; booked rows show no badge | high |
| TC-TXN-011 | Load more button appears and appends next page when Links.Next present | medium |
| TC-TXN-012 | Date range filter re-fetches with ISO-8601 bounds; resets pagination cursor | medium |
