<!-- source: screens/transactions/api.yaml -->
<!-- source_hash: api-transactions-1ep-2026-07-14 -->
<!-- generated: 2026-07-14T12:00:00Z -->

# Transactions — API Reference

> **Source of Truth**: `idea-layer/screens/transactions/api.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0

## Endpoints (1)

| Function | Method | Table | Auth | Params | Response | Cache |
|----------|--------|-------|------|--------|----------|-------|
| transactions | GET | — | Yes | AccountId(String), fromBookingDateTime(String? ISO-8601), toBookingDateTime(String? ISO-8601) | OBReadTransaction6: Data.Transaction[], Links.Next, Links.Self, Links.Last, Meta.TotalPages | in-memory per accountId |

## Error Handling

All endpoints follow standard error mapping:
- 401 → Auth error — `TokenExpiredError(recoverable=true)` → Retry button shown
- 403 → Consent withdrawn — `ConsentWithdrawnError(recoverable=false)` → no Retry
- 429 → Rate limit exceeded — `RateLimitError(recoverable=true)` → Retry with back-off hint
- null (network) → `NetworkError(recoverable=true)` → Retry button shown

## Full Contracts

See `idea-layer/screens/transactions/api.yaml` → `endpoints[]` for complete request/response schemas.

### Path Templates

- `GET /accounts/{AccountId}/transactions` — Returns OBReadTransaction6 (permission: ReadTransactionsDetail).
  - Optional query params: `fromBookingDateTime` (ISO-8601), `toBookingDateTime` (ISO-8601) for date-range filter.
  - Response includes `Links.Next` (absolute URL for next page, null on last page). Page size is HSBC-controlled (~100 transactions/page).
  - Credit/debit filtering and text search applied client-side against accumulated in-memory list.
  - Date-range changes reset pagination cursor (nextLink → null) and trigger fresh first-page fetch.
