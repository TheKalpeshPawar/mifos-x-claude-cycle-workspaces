<!-- source: screens/party/ui.yaml -->
<!-- source_hash: regenerated-2026-07-14 -->
<!-- generated: 2026-07-14T21:00:00Z -->

# Account Holder — Implementation Specification

> **Source of Truth**: `idea-layer/screens/party/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0 | **Status**: designed

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Account Holder | PartyViewModel | loading, content, error, empty, consent_required | PSU identity screen gated by ReadParty permission. Calls GET /accounts/{AccountId}/party (OBReadParty2) and GET /accounts/{AccountId}/parties (OBReadParty3) in parallel via ktorfit, merges results by PartyId. Displays name, legal name, party type, contact info (email, mobile, phone), and OBPostalAddress8 address cards. |

---

## State Model

### PartyViewModel

- **State fields**: `uiState: PartyUiState = PartyUiState.Loading` · `accountId: String = ""`
- **ScreenState**: `PartyUiState.Loading` · `PartyUiState.Content(party: OBParty2)` · `PartyUiState.Error(message: String, errorCode: PartyErrorCode)` · `PartyUiState.Empty` · `PartyUiState.ConsentRequired`
- **Errors**: `PartyErrorCode.AUTH` (HTTP 401 — token expired; Retry CTA) · `PartyErrorCode.NETWORK` (device offline or AIS unreachable; Retry CTA)
- **Actions**: `partyLoad(accountId: String)` — parallel GET /party + GET /parties, merge by PartyId · `retryLoad(accountId: String)` — delegates to partyLoad · `navigateToConsent()` — routes to consent-list
- **DI**: `partyRepository: PartyRepository (Koin single)` · `ktorfit: Ktorfit (AIS http client, Koin single)`

### State Transitions

| From | Trigger | To State |
|------|---------|----------|
| — | Screen mount | loading |
| loading | Parallel API success + data present | content |
| loading | 200 OK but no party records | empty |
| loading | HTTP 401 or network error | error |
| loading | HTTP 403 PermissionNotGranted | consent_required |
| error | Retry tapped | loading |
| consent_required | Re-authorise tapped | → consent-list (navigation) |

---

## Screen Inventory

### State: loading

- **Layout**: column, center-aligned, padding 24dp
- **Components**:
  - `loading_indicator` — circular ProgressIndicator; a11y: "Loading account holder details"

### State: content

- **Layout**: scrollable column, 16dp item spacing
- **Components**:
  - `party_header_card` (elevation 2):
    - `party_type_label` — text labelMedium, secondary color, value: `{party.PartyType}` (enum: Sole/JointSole/JointMortgagor)
    - `party_name` — text headlineMedium, value: `{party.Name}`
    - `party_full_legal_name` — text bodyMedium, on-surface-variant, value: `{party.FullLegalName}`
  - `contact_header` — section_header "Contact"
  - `email_row` — list_item; leading icon: email; visible when `party.EmailAddress` non-empty; trailing: `{party.EmailAddress}` bodyMedium
  - `mobile_row` — list_item; leading icon: phone_android; visible when `party.Mobile` non-empty; trailing: `{party.Mobile}` bodyMedium
  - `phone_row` — list_item; leading icon: phone; visible when `party.Phone` non-empty; trailing: `{party.Phone}` bodyMedium
  - `address_header` — section_header "Address"
  - `address_list` — iterates `party.Address[]` (OBPostalAddress8); each item is `address_card` (elevation 1):
    - `address_type` — text labelSmall, secondary, value: `{item.AddressType}` (Residential/Business/POBox/Correspondence)
    - `address_line_1` — text bodyMedium, value: `{item.BuildingNumber} {item.StreetName}`
    - `address_line_2` — text bodyMedium, value: `{item.AddressLine[0]}`; visible when present
    - `address_town_postcode` — text bodyMedium, value: `{item.TownName}, {item.PostCode}`
    - `address_country` — text bodySmall, on-surface-variant, value: `{item.Country}` (ISO 3166-1 alpha-2)
    - `address_full_card` — screen-reader-only concatenated full address string (TalkBack/VoiceOver)

### State: error

- **Layout**: column, center-aligned, padding 24dp
- **Components**:
  - `error_state` — empty_state variant error; icon: error_outline; title: error title i18n; body: `{error.message}` (401: token expired / network: offline message)
  - `retry_button` — filled button; triggers `retry_load` action
- **Note**: HTTP 403 routes to consent_required state, NOT here

### State: empty

- **Layout**: column, center-aligned, padding 24dp
- **Components**:
  - `empty_state_view` — empty_state variant info; icon: person_off; no action button
- **Trigger**: 404 response OR 200 OK with no party records

### State: consent_required

- **Layout**: column, center-aligned, padding 24dp
- **Components**:
  - `consent_required_view` — empty_state variant warning; icon: lock_person; title + body from strings
  - `reauthorise_button` — filled button; navigates to consent-list (Manage consents) screen for ReadParty re-grant
- **Trigger**: HTTP 403 PermissionNotGranted from /party or /parties

---

## API Dependencies

| Endpoint | Method | Path | Permission | Response DTO | Data Path |
|----------|--------|------|------------|--------------|-----------|
| party | GET | /accounts/{AccountId}/party | ReadParty | OBReadParty2 | Data.Party (OBParty2) |
| parties | GET | /accounts/{AccountId}/parties | ReadParty | OBReadParty3 | Data.Party[] (OBParty2[]) |

Both endpoints are called **in parallel** on screen mount. Results are merged by `PartyId`; pick-first on conflict.

---

## Navigation

- **Entry**: account-detail → tap "Account Holder" chip (requires ReadParty in active consent)
- **Back**: top_app_bar leading icon → account-detail
- **Consent flow**: consent_required state → reauthorise_button → consent-list screen
- **Flow ref**: account-browsing

---

## Design Tokens

| Usage | Token | Value |
|-------|-------|-------|
| Screen background | `background` | #F7F9FF |
| Card container (header) | `surfaceContainerHighest`, elevation 2 | #E0E3E8 |
| Card container (address) | `surfaceContainerHigh`, elevation 1 | #E5E8ED |
| Name heading | headlineMedium | Roboto 28sp/36, weight 400 |
| Legal name | bodyMedium + on-surface-variant | Roboto 14sp/20, #41474D |
| Party type overline | labelMedium + secondary | Roboto 12sp/500, #50606E |
| Address type overline | labelSmall + secondary | Roboto 11sp/500, #50606E |
| Body text | bodyMedium | Roboto 14sp/20 |
| Address country | bodySmall + on-surface-variant | Roboto 12sp, #41474D |
| Section headers | titleSmall | Roboto 14sp/500 |
| Filled button | primary bg, on-primary text | bg #266489, text #FFFFFF |
| Min touch target | — | 48dp |
| Screen padding | — | 24dp (states) |
| Content item spacing | — | 16dp |
