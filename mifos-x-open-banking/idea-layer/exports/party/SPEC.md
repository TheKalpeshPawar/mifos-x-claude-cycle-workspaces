<!-- source: screens/party/ui.yaml -->
<!-- source_hash: generated-2026-07-16 -->
<!-- generated: 2026-07-16T00:00:00Z -->
<!-- generated_from_feature_version: 2.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->

# Account Holder — Implementation Specification

> **Source of Truth**: `idea-layer/screens/party/ui.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Feature version**: 2.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| Account Holder | PartyViewModel | loading, content, error, empty, consent_required | PSU identity screen gated by ReadParty permission. Calls GET /accounts/{AccountId}/party (OBReadParty2) and GET /accounts/{AccountId}/parties (OBReadParty3) in parallel via ktorfit, merges results by PartyId. Displays name, legal name, party type, contact info (email, mobile, phone), and OBPostalAddress8 address cards. A 403 PermissionNotGranted transitions to consent_required state. |

---

## App-Shell

| Attribute | Value |
|-----------|-------|
| Bottom navigation | visible |
| Top app bar | visible — title: `{strings.party.screen_title}`, leading: back |
| FAB | hidden |

---

## State Model

### PartyViewModel

**State class:** `PartyState`

| Field | Type | Notes |
|-------|------|-------|
| `uiState` | `PartyUiState` | Current sealed-class state |
| `accountId` | `String` | OBIE AccountId passed as navigation argument from account-detail |

**Sealed class:** `PartyUiState`

| Member | Notes |
|--------|-------|
| `Loading` | Parallel GET /party + GET /parties in flight |
| `Content` | party: OBParty2 (merged from /party + /parties by PartyId) |
| `Error` | message: String, errorCode: PartyErrorCode |
| `Empty` | 200 response but no party records returned from either endpoint |
| `ConsentRequired` | 403 PermissionNotGranted — ReadParty not in consent scope |

**Error types:** `PartyErrorCode.AUTH` (HTTP 401 — access token expired; Retry CTA) · `PartyErrorCode.NETWORK` (device offline or AIS endpoint unreachable; Retry CTA)

**Actions:**

| Action | Signature | Notes |
|--------|-----------|-------|
| `partyLoad` | `suspend fun partyLoad(accountId: String)` | Parallel GET /party + GET /parties; merge by PartyId (pick_first on conflict) |
| `retryLoad` | `suspend fun retryLoad(accountId: String)` | Delegates to partyLoad(accountId) |
| `navigateToConsent` | `fun navigateToConsent()` | Routes to consent-list for ReadParty re-grant |

**DI:** `PartyRepository` (Koin single; parallel GET /party + GET /parties via ktorfit) · `Ktorfit` (AIS http client; Koin single)

---

## Components

| ID | Type | State Binding | Data-Driven | Purpose |
|----|------|--------------|-------------|---------|
| `loading_indicator` | progress_indicator (variant=circular) | loading | false | Shown while parallel GET /party + GET /parties calls are in flight |
| `party_header_card` | card (elevation=2) | content | true | Identity header card: OBParty2 PartyType, Name, FullLegalName |
| ↳ `party_type_label` | text (labelMedium, secondary, role=overline) | content | true | OBParty2 PartyType enum value (Sole, JointSole, JointMortgagor) |
| ↳ `party_name` | text (headlineMedium, role=title) | content | true | OBParty2 Name — display name of the PSU |
| ↳ `party_full_legal_name` | text (bodyMedium, on-surface-variant, role=subtitle) | content | true | OBParty2 FullLegalName — legal name as registered with the bank |
| `contact_header` | section_header | content | false | Section divider for OBParty2 contact fields (email, phone, mobile) |
| `email_row` | list_item (leading_icon=email) | content | true | OBParty2 EmailAddress; trailing: `{party.EmailAddress}` bodyMedium; visible when non-empty |
| `mobile_row` | list_item (leading_icon=phone_android) | content | true | OBParty2 Mobile; E.164 format from HSBC; visible when non-empty |
| `phone_row` | list_item (leading_icon=phone) | content | true | OBParty2 Phone; visible when non-empty |
| `address_header` | section_header | content | false | Section divider for OBParty2 Address[] array (may contain multiple types) |
| `address_list` | list (items_source=`{party.Address}`) | content | true | Iterates OBPostalAddress8 entries (Residential, Business, etc.) |
| ↳ `address_card` | card (elevation=1) | content | true | Single OBPostalAddress8 displayed as a card; supports multiple address types |
| ↳↳ `address_type` | text (labelSmall, secondary, role=overline) | content | true | OBPostalAddress8 AddressType (Residential, Business, POBox, Correspondence) |
| ↳↳ `address_line_1` | text (bodyMedium) | content | true | `{item.BuildingNumber} {item.StreetName}` |
| ↳↳ `address_line_2` | text (bodyMedium) | content | true | `{item.AddressLine[0]}`; visible when AddressLine is non-empty |
| ↳↳ `address_town_postcode` | text (bodyMedium) | content | true | `{item.TownName}, {item.PostCode}` |
| ↳↳ `address_country` | text (bodySmall, on-surface-variant) | content | true | ISO 3166 2-letter country code |
| ↳↳ `address_full_card` | text (bodySmall, screen_reader_only) | content | true | Concatenated full address string for TalkBack/VoiceOver; invisible on screen |
| `error_state` | empty_state (variant=error, icon=error_outline) | error | true | Shown on 401 token-expiry or network error; body: `{error.message}`; note: 403 routes to consent_required, not here |
| ↳ `retry_button` | button (variant=filled) | error | false | Re-triggers retry_load → party_load ViewModel action |
| `empty_state_view` | empty_state (variant=info, icon=person_off) | empty | false | Shown when /party and /parties respond 200 but return no party records |
| `consent_required_view` | empty_state (variant=warning, icon=lock_person) | consent_required | false | Shown when 403 PermissionNotGranted returned; ReadParty not in consent scope |
| ↳ `reauthorise_button` | button (variant=filled) | consent_required | false | Navigates user to the consent-list (Manage consents) screen to re-grant ReadParty permission |

---

## Behaviour / Action Contracts

| Component ID | Action | Effect | Library Refs | Description |
|--------------|--------|--------|--------------|-------------|
| `retry_button` | `retry_load` | `call_api` | ktorfit, hsbc-obie-ais-v4.0:party | Re-triggers the parallel GET /party and GET /parties fetch after a 401 or network error, returning the screen to Loading and then Content or Empty. |
| `reauthorise_button` | `navigate` | `navigate` | — | Routes the user to the consent-list (Manage consents) screen so they can re-grant ReadParty permission to resolve the 403 PermissionNotGranted response from the party endpoint. |

---

## State Transitions

| From | Trigger | To |
|------|---------|-----|
| — | Screen mount | loading |
| loading | Parallel API success + data present | content |
| loading | 200 OK but no party records (Data.Party absent and Data.Party[] empty) | empty |
| loading | HTTP 401 or network error | error |
| loading | HTTP 403 PermissionNotGranted | consent_required |
| error | retry_button tapped | loading (via retryLoad) |
| consent_required | reauthorise_button tapped | → consent-list (navigation) |

---

## Navigation

| Direction | Source / Target | Trigger |
|-----------|----------------|---------|
| Entry | account-detail → party | Tap "Account Holder" chip (requires ReadParty in active consent) |
| Exit | party → account-detail | Back navigation (top_app_bar leading icon or system gesture) |
| Exit | party → consent-list | reauthorise_button tapped in consent_required state |

**Flow ref:** account-browsing

---

## Testing (6 scenarios)

| ID | State | Priority |
|----|-------|----------|
| Content renders merged PartyId with name, legal name, party type, contact, and addresses | content | critical |
| Loading state shows circular spinner; content hidden | loading | critical |
| Error state (401) shows Retry CTA with token-expired message | error | critical |
| 403 PermissionNotGranted transitions to consent_required; Retry hidden; Re-authorise CTA visible | consent_required | critical |
| Empty state (200 + no party records) shows person_off icon; no Retry button | empty | normal |
| Merge strategy: overlapping PartyId from /party and /parties uses pick_first | content | normal |
