# Account Holder — API Reference

> **Source of Truth**: `idea-layer/screens/party/api.yaml`
> **Auto-generated by**: `/idea export` — Do not edit manually.
> **Source hash**: regenerated-2026-07-14

---

## Endpoints (2)

| ID | Method | Path | Auth | Permission | Response DTO | Data Path |
|----|--------|------|------|------------|--------------|-----------|
| party | GET | /accounts/{AccountId}/party | OAuth2 FAPI 1.0 | ReadParty | OBReadParty2 | Data.Party (OBParty2) |
| parties | GET | /accounts/{AccountId}/parties | OAuth2 FAPI 1.0 | ReadParty | OBReadParty3 | Data.Party[] (OBParty2[]) |

Both endpoints called **in parallel** on screen mount. Results merged by `PartyId`; pick-first strategy on conflict.

---

## GET /accounts/{AccountId}/party

### Path Parameters

| Param | Type | Required | Description |
|-------|------|----------|-------------|
| AccountId | string | yes | OBIE AccountId obtained from GET /accounts; must be within active consent scope |

### Response: OBReadParty2 (HTTP 200)

**Data path**: `OBReadParty2.Data.Party`

| Field | Type | Required | Notes |
|-------|------|----------|-------|
| PartyId | string | yes | Unique identifier; used for deduplication merge with /parties response |
| Name | string | yes | Display name of the PSU |
| FullLegalName | string | no | Full registered legal name |
| PartyType | OBExternalPartyType1Code | yes | Sole \| JointSole \| JointMortgagor |
| EmailAddress | string | no | Displayed when non-empty |
| Phone | string | no | Displayed when non-empty |
| Mobile | string | no | E.164 format; displayed when non-empty |
| Address[] | OBPostalAddress8 | no | 0–N addresses; each: AddressType, BuildingNumber, StreetName, AddressLine[], TownName, PostCode, Country |

### Error Mapping

| HTTP Status | Condition | UI State |
|-------------|-----------|----------|
| 401 | Access token expired | `PartyUiState.Error(message, PartyErrorCode.AUTH)` — Retry CTA |
| 403 | PermissionNotGranted — ReadParty absent from consent | `PartyUiState.ConsentRequired` — Re-authorise CTA → consent-list |
| 404 | No OBParty2 record for AccountId | `PartyUiState.Empty` |
| 200 + Data.Party absent | Empty response body | `PartyUiState.Empty` |
| 500 | Server error | `PartyUiState.Error(message, PartyErrorCode.NETWORK)` — Retry CTA |
| IOException / timeout | Device offline or AIS unreachable | `PartyUiState.Error(e.localizedMessage, PartyErrorCode.NETWORK)` — Retry CTA |

---

## GET /accounts/{AccountId}/parties

### Path Parameters

| Param | Type | Required | Description |
|-------|------|----------|-------------|
| AccountId | string | yes | OBIE AccountId; same as above |

### Response: OBReadParty3 (HTTP 200)

**Data path**: `OBReadParty3.Data.Party[]`

Same field set as OBReadParty2.Data.Party. May include additional joint account holders not returned by /party.

### Error Mapping

Same mapping as GET /accounts/{AccountId}/party above. For parallel invocation, the first 403 received transitions to `ConsentRequired`; the first 401 or network error transitions to `Error`.

---

## Merge Strategy

```
partyId → OBParty2 mapping:
  1. Collect all PartyId values from /party.Data.Party (0 or 1 records)
  2. Collect all PartyId values from /parties.Data.Party[] (0–N records)
  3. Union by PartyId; on duplicate PartyId, keep first occurrence (pick_first)
  4. If union is empty → PartyUiState.Empty
  5. If union is non-empty → PartyUiState.Content(party: merged OBParty2)
```
