# ============================================================
# CAPABILITY_STATE.yaml — Project-Side Idea Capability Tracker
# ============================================================
# Generated by: /idea init --force (Phase 1 backfill)
# Rebuilt: 2026-05-27 (forced rebuild of previous schema-2.0 file)
# Read by:      /idea dashboard (Layer Sanity row + Capabilities grid)
#               PROJECT_MIGRATION (drift detection between framework
#               registry version and per-capability artifact_template_version_at_run)
# Spec:         core/registries/IDEA_CAPABILITY_REGISTRY.yaml (framework-side)
# Plan:         PLAN-fw-260506-idea-capability-registry
#
# DO NOT hand-edit unless you know what you're doing — this is
# auto-maintained. To force re-init: delete the file and run /idea init.
# ============================================================

schema_version: "1.0.0"
project: "mifos-x/mifos-x-open-banking"
generated: "2026-05-27"
generator: "/idea init (Phase 1 backfill)"
framework_version_at_init: "2.86.0"
init_mode: "backfill"

# ============================================================
# Capabilities used (rows added by detection or per-run writers)
# ============================================================

capabilities_used:
  enrich:
    last_run: "inferred-2026-05-27"
    framework_version_at_run: "<2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: true
    detection_rule: "Glob idea-layer/screens/*.yaml >= 1 file"
    artifact_count: 39

  sync:
    last_run: "inferred-2026-05-27"
    framework_version_at_run: "<2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: true
    detection_rule: "idea-layer/flows/*.yaml >= 1 OR APP_FLOW.mmd exists OR state/sync-logs/ exists"
    artifact_count: 3

  analytics:
    last_run: "inferred-2026-05-27"
    framework_version_at_run: "<2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: true
    detection_rule: "TAG_REGISTRY.yaml exists AND events array >= 1 entry"
    artifact_count: 24

  plan:
    last_run: "inferred-2026-05-27"
    framework_version_at_run: "<2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: true
    detection_rule: "idea-plan.yaml exists AND quality field is set"
    artifact_count: 1

  verify:
    last_run: "inferred-2026-05-27"
    framework_version_at_run: "<2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: true
    detection_rule: "state/PIPELINE_STATE.yaml exists AND capabilities.verify block is non-empty"
    artifact_count: 1

  train:
    last_run: "inferred-2026-05-27"
    framework_version_at_run: "<2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: true
    detection_rule: "training/ exists AND contains >= 1 file"
    artifact_count: 2

# ============================================================
# Init metadata (audit trail)
# ============================================================

init:
  ran_at: "2026-05-27"
  detected_count: 6
  not_detected_count: 9
  skipped_due_to_error: []

# ============================================================
# Phase 3+ fields (placeholder — populated when feature ships)
# ============================================================

drift_check:
  last_run: null
  drifted_capabilities: []

per_capability_writes_enabled: false
