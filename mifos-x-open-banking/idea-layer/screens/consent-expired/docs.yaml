sibling: docs
schema_version: "4.0"
feature_id: consent-expired
name: "Reconnect Your Account"
feature: consent-expired
flavor: consumer
status: enriched
quality_score: 100
contract_version: "1.1.0"
capabilities:
  - has_ui
  - has_api
status_history:
  - to: draft
    at: "2026-06-11"
    by: "/idea add (OBIE migration)"
    note: "Renewal screen for the OBIE account-access-consent journey. Shown when a previously-authorised consent has expired (Status EXPD) or been revoked (RJCT/CANC). Reachable from splash on cold start (stored ConsentId resolves to non-AUTH) and from consent-manager on revoke. May call GET /aisp/account-access-consents/{ConsentId} on mount to confirm status. 'Reconnect' CTA navigates to consent-request to renew."
  - to: enriched
    at: "2026-06-12"
    by: "/idea enrich"
    note: "Enriched to 100/100 — API-linked reference line, ConsentExpiredError hierarchy, route params (consentId, reason), CTA contentDescription, testing + documentation blocks (FR-005)."
screens:
  - id: consent-expired
    name: "Reconnect Your Account"
    viewmodel: "ConsentExpiredViewModel"
    archetype: error
    shell:
      top_bar: false
      bottom_nav: false
dependencies:
  tier: feature
  features:
    - consent-request
    - consent-intro
