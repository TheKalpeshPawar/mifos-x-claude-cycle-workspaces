screen_id: licences

# Client-only screen — no external API surface.
# The licence catalogue is a build-time asset in the feature/settings module.
endpoints: []

client_only:
  reason: "Static bundled content. LicencesScreen reads files/mpl_licence.txt from the module's Compose resources via produceState; there is no network call, no repository and no ViewModel."
