# TEST SPEC — Licences

| Field      | Value                                    |
|------------|------------------------------------------|
| Feature    | licences                                 |
| Source     | `screens/licences/tests.yaml`            |
| Scenarios  | 3                                        |
| Priorities | critical 1 · normal 2                    |
| States     | content 3                                |

> Generated from declared scenarios. Each scenario below is a binding target for one
> `@Test` in `feature/settings`'s `commonTest` source set.

---

## Coverage

| State   | Scenarios | Notes |
|---------|-----------|-------|
| content | 3         | The screen models no loading, error or empty state — see TC-LICENCES-003. |

Single-state coverage is correct here rather than a gap: `licences` declares
`state_model: {}` ("no ViewModel, no actions and no error surface"), so there are no other
states to exercise.

---

## TC-LICENCES-001 — Licences screen renders the bundled MPL text

**Priority:** critical · **State:** content

- **Given** `feature/settings` Compose resource `files/mpl_licence.txt` is present in the build
- **When** User navigates from settings → About → Licences
- **Then**
  - `settings:licences:screen` is displayed
  - The intro paragraph renders in `titleMedium`
  - `settings:licences:list` renders the decoded licence text in a monospace `bodySmall` block
  - The column scrolls vertically

---

## TC-LICENCES-002 — Back navigation returns to settings

**Priority:** normal · **State:** content

- **Given** Licences screen is displayed
- **When** User taps the `KptScaffold` navigation icon
- **Then** `onBack` fires `popBackStack()`, returning to the settings screen

---

## TC-LICENCES-003 — Body is empty before the resource read resolves

**Priority:** normal · **State:** content

- **Given** `produceState` has not yet completed `Res.readBytes`
- **When** The screen first composes
- **Then**
  - `settings:licences:list` renders the empty-string initial value
  - No spinner or loading state is shown — source models none

This scenario pins a deliberate absence. The screen shows an empty body rather than a spinner
during the resource read, and a future change that adds a loading state would break this test —
which is the point.

---

## Traceability

| Scenario | Binds to |
|----------|----------|
| TC-LICENCES-001 | `settings:licences:screen`, `settings:licences:list` |
| TC-LICENCES-002 | `KptScaffold` navigation icon → `popBackStack()` |
| TC-LICENCES-003 | `produceState` initial value |

**Note:** `licences` is one of two features that fail the `/idea-feature-export` E1 gate
(`state_model: {}`), so it has no SPEC.md. Its scenarios are still exportable — test coverage
does not require a ViewModel.

---

_Generated by /idea-feature-test-export | 2026-08-03_
