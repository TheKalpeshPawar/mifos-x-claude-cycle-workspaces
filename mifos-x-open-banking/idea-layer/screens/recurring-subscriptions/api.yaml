screen_id: recurring-subscriptions

# Client-only — no external API (gap_strategy: client_only).
# PFM analytics computed in-app from AIS transaction data.
# Recurring patterns are detected from transaction history cached
# by the transactions feature; no additional bank endpoint is called.
endpoints: []
