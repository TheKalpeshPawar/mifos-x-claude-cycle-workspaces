sibling: demo-data
schema_version: "2.1.0"
feature_id: privacy-policy

screen_id: privacy-policy
_content_policy: "RULE-PROTO-CONTENT-001 — zero placeholders. Static-content screen; see _no_dto below."
_migrated_from: "schema 4.0 demo_entries shape, 2026-08-02 via /idea-data"

_no_dto: >-
  Consumes NO endpoint — api.yaml declares none and the policy text is a bundled static asset.
  The entries below are screen METADATA, not fetched content, so DD3/DD5 have nothing to check.
  Same class as terms-of-service and payment-result.

document_metadata:
  - id: pp_metadata
    title: "Privacy Policy"
    content_source: static_asset
    asset_path: "composeResources/files/privacy-policy.html"
    version: "1.0"
    last_updated: "2026-05-28"
    effective_from: "2026-06-01"
    scroll_position: 0
    is_loading: false

# Regulatory disclosure the screen renders as a labelled block beneath the policy text.
# Real entities, not filler — this app is an FCA-regulated-context AISP/PISP reference build
# and the controller/processor split is a GDPR Article 13 requirement, not decoration.
data_governance:
  - id: pp_governance
    gdpr_compliant: true
    data_controller: "Mifos Initiative"
    data_controller_contact: "privacy@mifos.org"
    data_processor: "HSBC UK Bank plc"
    lawful_basis: "Consent (UK GDPR Art. 6(1)(a)) — PSU-granted account-information consent"
    retention_note: "Account data is held only for the life of the consent and is purged on revocation."
    subject_rights: ["access", "rectification", "erasure", "portability", "withdraw-consent"]

states:
  - { scope: content, document: pp_metadata, governance: pp_governance }

_states_note: >-
  content-only, and intentionally so — a bundled asset has no loading or error path. Listed in
  DESIGN_VALIDATION D-3 alongside terms-of-service; this file records that the single state is
  by design.

last_generate:
  timestamp: "2026-08-02T17:12:00Z"
  command: "/idea-data"
  dto_versions: {}
