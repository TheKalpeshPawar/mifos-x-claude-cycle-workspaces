sibling: api
schema_version: "4.0"
feature_id: privacy-policy
name: "Privacy Policy"
feature: shared
flavor: shared
status: approved

# Privacy Policy content is bundled as a static asset — no network API required.
# This shell is present to satisfy the 4-file screen contract.
endpoints: []

note: "Static legal content served from bundled app asset (privacy_policy.html). No backend API calls."
