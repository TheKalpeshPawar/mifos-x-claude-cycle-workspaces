sibling: docs
schema_version: '4.0'
feature_id: auth-callback
name: Completing Connection
feature: account-access-consent
flavor: consumer
status: enriched
quality_score: 96
contract_version: 1.1.0
capabilities:
  - has_ui
  - has_api
status_history:
  - status: draft
    at: "2026-06-11"
    by: "/idea-add (manual draft authoring)"
    note: "Deep-link callback takeover authored for the OBIE redirect return. Exchanges the authorization code for an access token, then routes by consent context — home for AIS, payment-result for payment; consent-declined / payment-declined on error. SHARED between account-access-consent and payment-initiation flows. Awaiting /idea-enrich."
  - status: enriched
    at: "2026-06-12"
    by: "/idea-enrich (auth-callback)"
    note: "Enriched the shared OBIE redirect-return takeover: split processing into exchanging + verifying phases (CallbackPhase), made the consentContext-keyed dual routing explicit (AIS→home / consent-declined; PAYMENT→payment-result / payment-declined), bound the three OBIE endpoints with full contracts, added per-state layouts, a11y, MVI state_model, and a demo-data.yaml with realistic Bearer-token / consent-AUTH / payment-submit samples for both branches."
screens:
  - id: auth-callback
    name: Completing Connection
    archetype: status
    viewmodel: AuthCallbackViewModel
    states_handled: [Exchanging, Verifying, Error]
    shell:
      top_bar: false
      bottom_nav: false
dependencies:
  tier: feature
  features:
    - shared-core
    - obp-auth
    - account-access-consent
    - payment-initiation
