sibling: docs
schema_version: "4.0"
feature_id: user-onboarding

feature: consent-onboarding
contract_version: "2.0.0"
screen_id: user-onboarding
score: 95
status: approved
approve_hash: "sha256:4f97618b7301d0170c67664beb220cc4aca53c7a0a3849351192e03e0e35131f"
approved_at: "2026-07-14T00:00:00Z"
approved_by: "/idea approve"
approve_predicate_version: "1.0.0"
approve_contract_version: "2.0.0"
cluster: consent
acceptance_refs:
  - FR-001
  - FR-002
scaffolded_by: /idea-heal
archetype: onboarding
# flow_ref backfilled 2026-08-02 (/idea-sync) from flows/*.yaml#screens[] membership.
flow_ref: onboarding-consent
# ── Description corrected 2026-07-30 (/idea-feature-export stale-artifact fix) ──
# The prior text described a THREE-STEP pager (intro -> permissions_overview ->
# consent_explainer) plus an ob_explainer bottom sheet. Source ships ONE stateless screen:
# states [intro], IntroScreen.kt, viewmodel null, and TC-ONB-003 asserts no state and no I/O.
# Unbuilt scope from before the 2026-07-28 reverse sync, not a missing implementation.
description: >-
  Single-screen first-run education before the PSU connects an HSBC account. Establishes UK Open
  Banking as FCA-regulated and FAPI 1.0 Advanced-secured via a hero illustration, headline, body
  copy and trust chips, then hands off to login. Stateless: no ViewModel, no I/O — the continue
  button is a nav-host callback, not a dispatched action. Ships inside feature/login as
  onboarding/IntroScreen.kt; IntroRoute is the startDestination of AuthGraphRoute. Skipped on the
  re-consent path, which goes to LoginRenewRoute inside the authenticated host.
has_ui: true
has_server: false
# No has_api: zero operations — a static intro inside feature/login.
# has_system_integration: hands off to the bank authorisation redirect (deep_link).
capabilities: [has_ui, has_flow, has_static_content, has_system_integration]
# V1. V2/V3 N/A — zero device permissions app-wide (see login/docs.yaml).
system_capabilities:
  - kind: deep_link
    detail: "Terminal edge hands off to LoginRoute, which stages the consent and starts the bank redirect."
metadata:
  category: nav_only
  implementation_ready: true
  impl_ready_at: '2026-06-29T12:00:00Z'
  impl_ready_schema: v2
contracts: []
libraries: []
# vm_actions EMPTIED 2026-08-01 (/idea-sync, from /idea-verify XS-3).
#
# This block still declared five actions — StepNext, StepBack, OpenObExplainer,
# CloseObExplainer, NavigateToLogin — driving a three-step pager and a bottom sheet that
# do not exist. Three of them named states this screen does not declare:
#   StepNext        -> "1→intro, 2→permissions_overview, 3→consent_explainer"
#   NavigateToLogin -> "Only callable from consent_explainer state (step 3)"
#   Open/CloseObExplainer -> showObExplainerSheet
# ui.yaml:66 declares states: [intro]. ui.yaml:34-40 records every one of these actions,
# both extra states and the sheet, as removed in the 2026-07-28 reverse sync because source
# never built them.
#
# The contradiction was INTERNAL to this file: the `description` above was corrected on
# 2026-07-30 to read "Stateless: no ViewModel, no I/O — the continue button is a nav-host
# callback, not a dispatched action", and these five actions survived directly beneath that
# correction. A codegen consumer reading vm_actions would have emitted a ViewModel for a
# screen the spec says has none.
#
# Empty is the correct value, not a placeholder: ui.yaml sets `state_model: {}` and
# `implementation.viewmodel: null`, and TC-ONB-003 asserts "No ViewModel is resolved — the
# composable takes only onContinue and a Modifier".
vm_actions: []
nav_callbacks:
  # The screen's only outward edge. Host-owned, NOT a dispatched action — which is precisely
  # why it does not appear in vm_actions above.
  - "onContinue -> navController.navigate(LoginRoute)"
error_cases: []
test_fixtures:
  source: screens/user-onboarding/tests.yaml
  # Corrected 2026-08-01 (/idea-sync, from /idea-verify XS-1): was scenario_count 12 with
  # priority_distribution high:7/medium:5. tests.yaml holds 4 scenarios (TC-ONB-001..004) and
  # uses the critical/normal vocabulary, not high/medium — the buckets were as stale as the
  # total. The 12 counted the pre-07-28 pager suite that was reverse-synced away.
  scenario_count: 4
  priority_distribution:
    critical: 2
    normal: 2
status_history:
  - from: enriched
    to: designed
    at: '2026-07-01T11:09:01.393Z'
    by: /idea-export-stitch
  - from: designed
    to: approved
    at: "2026-07-14T00:00:00Z"
    by: "/idea approve"
    contract_version: "2.0.0"
    bypass_flags: []
    reason: ""
