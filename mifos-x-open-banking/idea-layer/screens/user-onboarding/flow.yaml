# ── Phantom transitions REMOVED 2026-07-30 (/idea-feature-export stale-artifact fix) ──
# 6 transitions referenced user-onboarding[intro], [permissions_overview],
# [consent_explainer] and [ob_explainer_open] — sub-screens that do not exist. Source ships
# one stateless screen (states: [intro]). They were the flow half of the three-step-pager
# prose corrected in docs.yaml this pass. Only the real edge to login remains.
feature: consent-onboarding
flow_ref: onboarding-consent
entry_points:
- source: app_launch_no_consent
  label: "App launched with no active consent \u2014 first-run or post-revoke"
- source: post_revoke_redirect
  label: "Consent revoked from consent dashboard \u2014 PSU returned to start of onboarding"
transitions:
- from: user-onboarding
  to: login
  trigger: Tap 'Connect with HSBC' on step 3 (consent_explainer)
  action: navigate_to_login
  requires_state: consent_explainer
