sibling: flow
schema_version: "4.0"
feature_id: cards
flow_ids:
  - consumer-banking
navigates_to:
  - card-detail
flow_logic: []
# transaction-detail removed: the card-transactions list never shipped, so the
# Cards screen has no nav path to transaction-detail (shipped onCardClick → card-detail only).
journey_ids:
  - consumer-cards-financing
