type: transactions
title: "Transactions"
flavor: consumer
components:
  - type: filter_chip_group
    id: transaction_filters
    options: ["All", "Debit", "Credit", "Pending"]
  - type: transaction_list
    id: transactions_list
    on_item_click:
      target: transaction_detail
states:
  - loading
  - content
  - empty
navigation:
  - target: transaction_detail
    label: "transaction_detail"
