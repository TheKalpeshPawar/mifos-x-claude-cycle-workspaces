sibling: flow
schema_version: "4.0"
feature_id: home
flow_ids:
  - consumer-banking
# Reconciled to shipped homeGraph callbacks (HomeDestination.kt) on 2026-06-11: the hero
# tile grid wires exactly these destinations. onAccounts is invoked only by the Empty
# state's "Set Up Account" CTA. Transactions/send-money/beneficiaries/cards/notifications/
# profile/settings are NOT reachable from Home in the shipped build (they live on other tabs).
navigates_to:
  - accounts            # Empty-state "Set Up Account" CTA (onAccounts)
  - standing-orders
  - business-insights
  - products
  - fx-rates
  - atm-locator         # only when showFindAtm == true
  - pfm-dashboard       # "Insights" tile (onInsights)
flow_logic: []
journey_ids:
  - consumer-authentication
  - consumer-accounts-payments
