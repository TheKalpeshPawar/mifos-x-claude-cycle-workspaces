sibling: api
schema_version: "4.0"
feature_id: payment-result
name: Payment Sent
feature: payment-result
flavor: consumer
status: enriched

# Stateless, args-driven success screen — rendered entirely from navigation
# arguments passed by the confirm / SCA screens. No ViewModel, no network call.
endpoints: []

note: "PaymentResultScreen is a stateless composable. The booked transaction's amount, beneficiary, source label, transaction id, charge and status are all carried in as nav args. No OBP API is invoked here."
