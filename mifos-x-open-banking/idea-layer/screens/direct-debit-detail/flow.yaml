sibling: flow
schema_version: "4.0"
feature_id: direct-debit-detail
flow_ids:
  - consumer-banking
navigates_to:
  - direct-debits
  - transactions
flow_logic:
  - from: direct-debit-detail
    to: transactions
    trigger: ddd_view_all
    note: "'View all' opens the funding account's full transaction history (onViewAllPayments)."
  - from: direct-debit-detail
    to: direct-debits
    trigger: ddd_empty_back
    note: "Empty-state Back CTA returns to the mandate list when the mandate no longer resolves. No Edit nav — direct debits are merchant-initiated (view + local cancel only)."
journey_ids:
  - consumer-cards-financing
