sibling: flow
schema_version: "4.0"
feature_id: send-money-confirm
flow_ids:
  - consumer-banking
navigates_to:
  - authorise-handoff
  - send-money-amount
flow_logic:
  - on: confirm_submit
    nav: authorise-handoff
    label: "Confirm & Send — POST the kind's payment-consent (AWAU) + hand off to HSBC to authorise (consentContext = kind)"
  - on: edit_click
    nav: send-money-amount
    label: "Edit payment"
journey_ids:
  - consumer-accounts-payments
