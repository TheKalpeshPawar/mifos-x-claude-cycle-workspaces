sibling: demo-data
schema_version: "2.1.0"
feature_id: terms-of-service

screen_id: terms-of-service
_content_policy: "RULE-PROTO-CONTENT-001 — zero placeholders. Static-content screen; see _no_dto below."
_migrated_from: "schema 4.0 demo_entries shape, 2026-08-02 via /idea-data"

_no_dto: >-
  This screen consumes NO endpoint — api.yaml declares none, and the legal text is a static
  asset bundled with the app. There is no DTO to populate, so RULE-DEMO-DATA-001 DD3/DD5 have
  nothing to check here. The entries below are screen METADATA (what the chrome renders around
  the text), not fetched content. Same class as payment-result, which declares
  demo_data_required: false because it too consumes nothing.
  The 4.0 field `termsContent: "static_asset"` is preserved as a sentinel, not a placeholder —
  it names the bundling strategy rather than standing in for missing content.

document_metadata:
  - id: tos_metadata
    title: "Terms of Service"
    content_source: static_asset
    asset_path: "composeResources/files/terms-of-service.html"
    version: "1.0"
    last_updated: "2026-05-28"
    effective_from: "2026-06-01"
    scroll_position: 0
    is_loading: false

states:
  - { scope: content, document: tos_metadata }

_states_note: >-
  content is the ONLY state this screen has, and that is correct rather than a gap: a bundled
  asset cannot fail to load over the network, cannot be empty, and has nothing to fetch. See
  D-3 in DESIGN_VALIDATION — this screen is listed there as content-only, and this file is the
  evidence that the listing is intended rather than an omission. The open question D-3 raises
  is why it ships an api.yaml at all.

last_generate:
  timestamp: "2026-08-02T17:12:00Z"
  command: "/idea-data"
  dto_versions: {}
