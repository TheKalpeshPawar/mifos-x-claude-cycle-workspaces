sibling: api
schema_version: "4.0"
feature_id: terms-of-service
name: "Terms of Service"
feature: shared
flavor: shared
status: approved

# Terms of Service content is bundled as a static asset — no network API required.
# This shell is present to satisfy the 4-file screen contract.
endpoints: []

note: "Static legal content served from bundled app asset (terms_of_service.html). No backend API calls."
