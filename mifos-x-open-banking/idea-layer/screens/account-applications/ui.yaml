type: account_applications
title: "Account Applications"
flavor: fieldOfficer
components:
  - type: filter_chip_group
    id: status_filters
    options: ["All", "Pending", "Approved", "Rejected"]
  - type: application_list
    id: applications_list
    on_item_click:
      target: application_detail
states:
  - loading
  - content
  - empty
navigation:
  - target: application_detail
    label: "application_detail"
