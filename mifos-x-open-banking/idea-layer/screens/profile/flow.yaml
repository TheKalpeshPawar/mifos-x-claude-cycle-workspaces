feature: profile
flow_ref: settings-and-profile

entry_points:
  - source: settings
    label: "Tap Profile row in Settings"

transitions:
  - from: profile
    to: consent-detail
    trigger: "Tap Manage Consent button"
  - from: profile
    to: login
    trigger: "Sign-out confirmed — clears tokens + consent, redirects to login"
