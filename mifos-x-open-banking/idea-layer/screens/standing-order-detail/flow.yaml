sibling: flow
schema_version: "4.0"
feature_id: standing-order-detail
flow_ids:
  - consumer-banking
navigates_to:
  - transaction-detail
flow_logic:
  - from: standing-order-detail
    to: transaction-detail
    trigger: sod_history_row
    passes: [bankId, accountId, transactionId]
    note: "Recent Executions rows drill into the underlying transaction. No Edit nav (top-bar Edit removed — OBP cannot edit standing orders)."
journey_ids:
  - consumer-cards-financing
