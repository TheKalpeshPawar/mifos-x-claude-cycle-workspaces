sibling: docs
schema_version: "4.0"
feature_id: products
name: Products
feature: products
flavor: consumer
status: enriched
# quality_score: docs.yaml is the OWNER of this value. The idea-matrix extracts it as
# yaml_number_avg across every *.yaml in this directory, so a divergent copy in a sibling
# silently averages into a number neither file intends (products reported 93 = (91+95)/2
# until 2026-08-07). ui.yaml mirrors this value; no other sibling declares one.
quality_score: 88
contract_version: "2.0.0"
approve_hash: "sha256:82356e6be5442003eb3e017613c62747e39b05861728d07f062915f726250f45"
approved_at: "2026-08-05T05:15:00Z"
approved_by: "/idea approve"
approve_predicate_version: "1.0.0"
approve_contract_version: "2.0.0"
capabilities:
  - has_ui
  - has_api
status_history:
  - status: draft
    timestamp: "2026-05-25T00:00:00Z"
  - status: enriched
    timestamp: "2026-05-25T00:00:00Z"
  - status: designed
    at: "2026-05-25T00:00:00Z"
    by: "/idea export"
    exports: [spec_md, api_md, mockup_md]
  - status: enriched
    at: "2026-05-25T12:00:00Z"
    by: "/idea-enrich"
    note: "Added 4 real OBP products (Instant Access Savings, Fixed Rate Bond, Personal Loan, Platinum Credit Card), Mortgages category tab, application-detail nav target, loading/empty/error states, upgraded API to OBP v5.0.0"
  - from: designed
    to: approved
    at: "2026-05-25T13:45:00Z"
    by: "/idea approve"
    contract_version: "1.1.0"
    bypass_flags: []
  - from: approved
    to: approved
    at: "2026-06-11T14:45:00Z"
    by: "/idea approve"
    contract_version: "1.1.0"
    bypass_flags: []
    note: "Approved after source→spec reconciliation (spec matches shipped Kotlin) + /idea verify PASS. Write-back shape (no approve_hash), consistent with project pattern. Previews for reconciled screens may lag the YAML pending a separate re-render."
  - from: enriched
    to: approved
    at: "2026-08-05T05:15:00Z"
    by: "/idea approve"
    contract_version: "2.0.0"
    bypass_flags: []
    reason: "--all-ready bulk approval; predicate C1-C6 green. First approval carrying a verifiable approve_hash."
  - from: approved
    to: enriched
    at: "2026-08-07"
    by: "/idea-sync"
    quality_score: 88
    previous_quality_score: 91
    note: >-
      Non-api siblings rewritten against the api.yaml contract restored on 2026-08-07 —
      HSBC UK Open Data Product Finder, four families, two brands, NO auth. ui.yaml,
      data-flow.yaml, demo-data.yaml, tests.yaml and flow.yaml had all still described the
      deleted OBP bank-wide catalogue: a per-bank fan-out over bankId, a client-side
      Personal/Business audience split over BUSINESS_MARKERS keywords, and a client-side
      category badge. demo-data.yaml additionally carried five authored Kenyan products in
      KES under a Central Bank of Kenya licence, in OBP snake_case, from a field-officer
      persona this app no longer has. Replaced wholesale with records captured verbatim
      from live api.hsbc.com responses, wire defects preserved.
      Reversals worth naming: the 2026-08-06 conclusion that the outbound product URL "has
      no equivalent at all" is FALSE (it is CoreProduct.ProductURL, nested inside the
      marketing state); the `unauthenticated` / session-expired state is UNREACHABLE and
      has been deleted (this API takes no credential — no 401, no 403 on any path); and
      three of four families would be silently emptied by a shared on-sale filter, because
      BCA is false at brand level and CCC and SMEL omit OnSaleIndicator entirely.
      Dropped back to `enriched`: the contract changed under an approval granted against a
      different model, and this rewrite has not been through the human gate or re-rendered.
    verified: >-
      Every factual claim in the rewritten siblings traces to a live HTTP 200 captured or
      re-probed 2026-08-07. Segment behaviour, the empty-response sentinels and the BCA
      payload measurements were re-probed live during this pass.
      NOT verified: no preview re-render, no mockup regeneration, and no source
      reconciliation — the shipped feature/products Kotlin still implements the 2026-06 OBP
      per-bank model and is now the drift.
exports:
  spec_md:
    path: exports/products/SPEC.md
    at: "2026-05-30T00:00:00Z"
  api_md:
    path: exports/products/API.md
    at: "2026-05-30T00:00:00Z"
  mockup_md:
    path: exports/products/MOCKUP.md
    at: "2026-05-30T00:00:00Z"
screens:
  - id: products
    name: Products
    archetype: index_list
    route: /products
    scroll: vertical
dependencies:
  tier: feature
  features: []
  # EMPTIED 2026-08-07. This screen reads a PUBLIC catalogue from api.hsbc.com with no
  # credential, so it depends on no other feature's data and is reachable before login.
  # Previously declared, and why each is gone:
  #   application-detail — dropped 2026-08-02; a field-officer screen, and there is no
  #                        apply journey. flow.yaml already declared navigates_to empty.
  #   accounts           — the 2026-06 OBP model read the PSU's accounts to discover which
  #                        banks to fan out over, and the 2026-08-06 AIS model read them to
  #                        drive a per-account loop. Neither model survives; this screen
  #                        touches nothing belonging to the PSU.
  #   send-money         — no call path to it ever existed from this screen. Residue.
  #   home               — home holds the entry tile, so home depends on products, not the
  #                        reverse. Declaring it here inverted the edge.
