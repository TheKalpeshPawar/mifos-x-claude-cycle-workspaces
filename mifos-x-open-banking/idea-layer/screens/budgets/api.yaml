screen_id: budgets

# Client-only — no external API (gap_strategy: client_only).
# PFM analytics computed in-app from AIS transaction data.
# Budgets are persisted to local DataStore; spend values read from
# the in-app transaction cache.
endpoints: []
