feature: statements
flow_ref: recurring-and-statements

entry_points:
  - source: account-detail
    label: "Statements chip in account-detail Explore row"

transitions:
  - from: statements
    to: statement-detail
    trigger: "Tap statement row"
    params: [statementId, accountId]
  - from: statements
    to: account-detail
    trigger: "Back navigation"
