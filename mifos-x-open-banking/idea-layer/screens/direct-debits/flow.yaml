sibling: flow
schema_version: "4.0"
feature_id: direct-debits
flow_ids:
  - consumer-banking
navigates_to:
  - direct-debit-detail
  - accounts
flow_logic:
  - from: direct-debits
    to: direct-debit-detail
    trigger: mandate_card
    passes: [bankId, accountId, mandateId]
    note: "Tapping a mandate card opens its detail. There is no create flow — direct debits are merchant-initiated (no FAB)."
  - from: direct-debits
    to: accounts
    trigger: nav_back
    note: "Top-bar back navigation."
journey_ids:
  - consumer-cards-financing
