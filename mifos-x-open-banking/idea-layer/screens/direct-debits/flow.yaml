feature: direct-debits
flow_ref: recurring-and-statements

entry_points:
  - source: account-detail
    label: "Tap Direct Debits chip"
    params: [accountId]

transitions:
  - from: direct-debits
    to: account-detail
    trigger: "Back button"
    params: [accountId]
