feature: pfm
flow_ref: pfm-review

entry_points:
  - source: pfm-dashboard
    label: "Tap 'By Category' chip on PFM dashboard"
  - source: pfm-dashboard
    label: "Tap a top-category row on PFM dashboard"
    params: [category]

transitions:
  - from: spending-by-category
    to: transactions
    trigger: "Tap a category row — drill into category transactions"
    params: [category]
