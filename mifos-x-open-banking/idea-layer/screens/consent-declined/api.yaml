id: consent-declined
feature: consent-declined
contract_version: "1.1.0"
flavor: consumer
operations: []
note: "Terminal UI-only screen. No outbound OBIE operation originates here — the declined outcome (consent Status RJCT or CANC, or an OAuth error param) is resolved upstream in auth-callback and passed in as nav args. The 'Try again' CTA navigates to consent-request, which re-issues POST /obie/open-banking/v4.0/aisp/account-access-consents. Listed for completeness/orphan-route coherence."
dependencies:
  tier: feature
  features: [obie-consent]
