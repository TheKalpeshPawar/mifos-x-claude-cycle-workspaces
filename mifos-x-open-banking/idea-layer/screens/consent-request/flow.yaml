sibling: flow
schema_version: "4.0"
feature_id: consent-request
flow_ids:
  - account-access-consent
navigates_to:
  - bank-authorize-handoff
  - consent-intro
flow_logic:
  - on: grant_success
    nav: bank-authorize-handoff
    label: "Grant — POST account-access-consents (AWAU) + build signed authorise request"
  - on: cancel_click
    nav: consent-intro
    label: "Back"
journey_ids:
  - consumer-account-access-consent
