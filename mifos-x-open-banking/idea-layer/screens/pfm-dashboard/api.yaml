screen_id: pfm-dashboard

# Client-only — no external API (gap_strategy: client_only).
# PFM analytics computed in-app from AIS transaction data.
# Transactions are cached from the transactions feature; this screen
# reads the in-memory/local cache only.
endpoints: []
