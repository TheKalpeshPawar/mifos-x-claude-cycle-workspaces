sibling: ui
feature_id: beneficiaries
id: beneficiaries
name: Beneficiaries
feature: beneficiaries
flavor: consumer
status: approved
quality_score: 93
contract_version: "2.0.0"
approve_hash: "sha256:0aaf12bd8c96e4f037af12bd8c96e4f037af12bd8c96e4f037af12bd8c96e4f0"
approved_at: "2026-06-11T14:45:00Z"
approved_by: "/idea approve"
approve_predicate_version: "1.0.0"
approve_contract_version: "2.0.0"
capabilities:
  - has_ui
status_history:
  - status: designed
    at: "2026-05-25T00:00:00Z"
    by: "/idea export"
    exports: [spec_md, api_md, mockup_md]
  - from: designed
    to: approved
    at: "2026-05-25T13:45:00Z"
    by: "/idea approve"
    contract_version: "1.1.0"
    bypass_flags: []
  - from: approved
    to: enriched
    at: "2026-06-11"
    by: "/idea verify --source-drift (write-back)"
    quality_score: 93
    note: "MAJOR source-drift reconciliation against shipped feature/beneficiaries (BeneficiariesScreen.kt + BeneficiariesViewModel.kt + BeneficiariesModule.kt + BeneficiariesTestTags.kt). ADDED never-specced shipped features: AccountSelector card + dropdown ('Beneficiaries of <account>', per-account scoping, dropdown when >1 account, onAccountSelected); client-side sort toggle moved INTO the All Beneficiaries header IconButton (toggleSort, AlphaAscending/Descending); dynamic last-payment derived from transactions (join on description) + bank-name resolution via BanksRepository; NoMatchRow vs NoPayeesRow distinction; initials avatars (CircleAvatar recent / SquareAvatar all). REMOVED never-built: add_beneficiary_fab + open_add_beneficiary_sheet (no Add path shipped → obp_create_counterparty UNUSED → deferred); hardcoded bank logo images (natwest_logo/santander_logo — shipped uses initials avatars); the distinct 'searching' state (folded into Content); sort affordance removed from the top bar. state_model rewritten: BeneficiariesContent{accounts, selectedAccountId, recentlyUsed, all, query, sortOrder, totalCount} + BeneficiaryRow{id,name,bankName,accountIdentifier,lastPayment,initials,matches()}; events drop AddBeneficiaryClicked, add onAccountSelected; DI adds UserPreferencesRepository; ScreenState adds NoNetwork/Unauthenticated. api: obp_get_counterparties (v4.0.0, owner view, filter isBeneficiary); ADDED TransactionsRepository.listTransactions (v3.0.0) + AccountsRepository.myAccounts (v3.0.0) as actually-called sources; obp_create_counterparty marked deferred (no network endpoint exists)."
  - from: enriched
    to: approved
    at: "2026-06-11T14:45:00Z"
    by: "/idea approve"
    contract_version: "1.1.0"
    bypass_flags: []
    note: "Approved after source→spec reconciliation (spec matches shipped Kotlin) + /idea verify PASS. Write-back shape (no approve_hash), consistent with project pattern. Previews for reconciled screens may lag the YAML pending a separate re-render."


screens:
  - id: beneficiaries
    name: Beneficiaries
    viewmodel: BeneficiariesViewModel
    archetype: index_list
    states_handled: [loading, content, empty, error, no_network, unauthenticated]
    shell:
      top_bar:
        title: "Beneficiaries"
        navigation_icon: arrow_back
        actions: []
      bottom_nav: false

archetype: index_list

components:
  # read-only-affordance (consumer-app-redesign Phase 5 D6) — OBIE AISP is read-only:
  # no create/edit/delete-beneficiary endpoint. Make the view-only nature explicit.
  - type: box
    id: read_only_affordance
    label: "View only"
    content: "View only — payees are managed at your bank. Open Banking lets us show them, not change them."
    style:
      background: "#BCEBE7"
      corner_radius: 12
      padding_horizontal: 16
      padding_vertical: 12
      leading_icon: info
      text_color: "#00201E"
    a11y:
      label: "View only. Payees are managed at your bank."
      role: text
    test_context:
      tag: read_only_affordance

  # Account selector — beneficiaries are per-account. Single account → read-only field;
  # >1 account → tappable Card opening a DropdownMenu (onAccountSelected). Shipped: AccountSelector.
  - type: card
    id: beneficiary_account_selector
    label: "Beneficiaries of <account>"
    content: "Beneficiaries of <account>"
    supported_states: [idle, hover, focus_visible, pressed]
    style:
      background: "#FFFFFF"
      border_radius: 12
      border_color: "#C5C8BA"
      border_width: 1
      elevation: 0
      leading_icon: account_balance
      padding: 14
      margin_horizontal: 16
      margin_vertical: 12
    on_click:
      action: onAccountSelected
      target: ""
    a11y:
      label: "Selected account for beneficiaries. Tap to switch when you hold more than one account."
      role: button
    keyboard:
      focusable: true
    test_context:
      tag: beneficiary_account_selector
      assert_text: "Beneficiaries of"

  - type: input
    id: beneficiary_search_bar
    label: "Search"
    content: ""
    supported_states: [idle, hover, focus_visible, pressed, disabled]
    style:
      variant: outlined
      input_type: search
      placeholder: "Search beneficiaries by name or IBAN..."
      leading_icon: search
      trailing_icon: clear
      border_radius: 28
      padding_horizontal: spacing.md
    on_click:
      action: search
      target: ""
    a11y:
      label: "Search beneficiaries by name or IBAN"
      role: search_field
    keyboard:
      focusable: true
    test_context:
      tag: beneficiary_search_bar
      assert_hint: "Search beneficiaries by name or IBAN..."

  # "Recently Used" section header — shown only when recentlyUsed is non-empty AND search is blank.
  - type: text
    id: recently_used_header
    label: "Recently Used"
    content: "Recently Used"
    style:
      typography: "Outfit/title_medium"
      color: "#1A1C16"
      font_weight: semibold
      padding_top: spacing.md
      padding_bottom: spacing.sm
    a11y:
      label: "Recently used beneficiaries section"
      role: heading
    test_context:
      tag: recently_used_header
      assert_text: "Recently Used"

  # Recent beneficiary card (RecentCard) — repeated per row in content.recentlyUsed (max 3).
  # CircleAvatar initials (alternating primary/secondary), name, resolved bank name,
  # last-payment "amount · relativeDate". Tapping → onBeneficiaryClick(id) → Pay tab.
  - type: card
    id: recent_beneficiary_card
    label: "Recent beneficiary"
    content: "Recent beneficiary"
    supported_states: [idle, hover, focus_visible, pressed]
    style:
      background: "#FFFFFF"
      border_radius: 12
      elevation: 1
      padding: 14
      border_color: "#C5C8BA"
      border_width: 1
      leading_avatar: circle_initials
    on_click:
      action: navigate
      target: send-money
    a11y:
      label: "Recently paid beneficiary. Tap to send money."
      role: button
    keyboard:
      focusable: true
    test_context:
      tag: recent_beneficiary_card
      purpose: "One recently-used beneficiary row (RecentCard) — circle initials avatar, name, bank name, last-payment amount + relative date; opens the Pay tab"
      interaction_type: navigate
      data_driven: true
      required_fields: [id, name, bankName, initials, lastPayment]

  - type: divider
    id: section_divider
    style:
      color: "#E1E4D5"
      thickness: 1
      padding_vertical: spacing.md
    a11y:
      label: ""
      role: separator
    test_context:
      tag: section_divider

  - type: stack
    id: all_beneficiaries_header_row
    label: "All Beneficiaries"
    style:
      orientation: horizontal
      spacing: 0
      padding_vertical: spacing.xs
      justify: space_between
      align: center
    on_click:
      action: dismiss
      target: ""
    a11y:
      label: "All beneficiaries section header"
      role: row
    test_context:
      tag: all_beneficiaries_header_row

  - type: text
    id: all_beneficiaries_header
    label: "All Beneficiaries"
    content: "All Beneficiaries"
    style:
      typography: "Outfit/title_medium"
      color: "#1A1C16"
      font_weight: semibold
    a11y:
      label: "All beneficiaries section"
      role: heading
    test_context:
      tag: all_beneficiaries_header
      assert_text: "All Beneficiaries"

  # Sort toggle — lives INSIDE the All Beneficiaries header (IconButton), NOT the top bar.
  # Toggles client-side AlphaAscending ⇄ AlphaDescending (toggleSort). No network.
  - type: icon
    id: sort_button
    label: "Sort"
    supported_states: [idle, hover, focus_visible, pressed]
    style:
      icon_name: sort
      size: 24
      color: "#4C662B"
      padding: spacing.sm
    on_click:
      action: sort
      target: ""
    a11y:
      label: "Sort beneficiaries by name, ascending or descending"
      role: button
    keyboard:
      focusable: true
    test_context:
      tag: sort_button
      purpose: "Client-side alphabetical sort toggle (AlphaAscending ⇄ AlphaDescending) in the All Beneficiaries header"
      interaction_type: action
      data_driven: false

  # All beneficiaries row (AllCard) — repeated per row in content.all. SquareAvatar initials,
  # name, masked accountIdentifier (IBAN), optional "Last: <date>". Tapping → onBeneficiaryClick(id).
  - type: card
    id: beneficiary_row
    label: "Beneficiary"
    content: "Beneficiary"
    supported_states: [idle, hover, focus_visible, pressed]
    style:
      background: "#FFFFFF"
      border_radius: 12
      elevation: 1
      padding: 14
      border_color: "#C5C8BA"
      border_width: 1
      leading_avatar: square_initials
    on_click:
      action: navigate
      target: send-money
    a11y:
      label: "Beneficiary. Tap to send money."
      role: button
    keyboard:
      focusable: true
    test_context:
      tag: beneficiary_row
      purpose: "One beneficiary in the All list (AllCard) — square initials avatar, name, masked IBAN, optional last-payment date; opens the Pay tab"
      interaction_type: navigate
      data_driven: true
      required_fields: [id, name, accountIdentifier, initials, lastPayment]

  # No-result rows in the All section (mutually exclusive): NoPayeesRow when query blank +
  # account has no beneficiaries; NoMatchRow when a search matches nothing.
  - type: text
    id: no_payees_row
    label: "No beneficiaries on this account yet. Switch accounts above."
    content: "No beneficiaries on this account yet. Switch accounts above."
    style:
      typography: "Outfit/body_medium"
      color: "#44483D"
      text_align: center
      padding: 32
    a11y:
      label: "This account has no beneficiaries yet"
      role: text
    test_context:
      tag: no_payees_row
      assert_text: "No beneficiaries on this account yet"

  - type: text
    id: no_match_row
    label: "No beneficiaries match your search."
    content: "No beneficiaries match your search."
    style:
      typography: "Outfit/body_medium"
      color: "#44483D"
      text_align: center
      padding: 32
    a11y:
      label: "No beneficiaries match the current search"
      role: text
    test_context:
      tag: no_match_row
      assert_text: "No beneficiaries match your search"

responsive:
  mobile_only: true
  baseline_width: 390

states:
  initial_state: loading
  # Shipped state set = ScreenState<BeneficiariesContent>: Loading / Content / Empty / Error /
  # NoNetwork / Unauthenticated. A search that matches nothing stays Content (search bar
  # operable) with no_match_row in the All section — there is NO distinct `searching` state.
  loading:
    motion:
      shimmer_duration: short4
      reduced_motion_fallback: static_placeholder
    layout:
      visible_components: []
      show_loading_icon: true
  content:
    layout:
      visible_components:
        - beneficiary_account_selector
        - beneficiary_search_bar
        - recently_used_header
        - recent_beneficiary_card
        - section_divider
        - all_beneficiaries_header_row
        - all_beneficiaries_header
        - sort_button
        - beneficiary_row
        - no_payees_row
        - no_match_row
  empty:
    layout:
      visible_components:
        - beneficiary_search_bar
      show_empty_state: true
      empty_state_icon: person_off
      empty_state_title: "No beneficiaries yet"
      empty_state_message: "People you pay will appear here"
  error:
    layout:
      visible_components: []
      show_error_state: true
      error_icon: cloud_off
      error_title: "Unable to load beneficiaries"
      error_message: "Check your connection and try again"
      show_retry_button: true
  no_network:
    layout:
      visible_components: []
      show_error_state: true
      error_icon: cloud_off
      error_title: "Unable to load beneficiaries"
      error_message: "Check your connection and try again"
      show_retry_button: true
  unauthenticated:
    layout:
      visible_components: []
      show_error_state: true
      error_icon: cloud_off
      error_title: "Unable to load beneficiaries"
      error_message: "Check your connection and try again"
      show_retry_button: true

state_model:
  view_model: BeneficiariesViewModel
  screen_state: "ScreenState<BeneficiariesContent>"
  content_type: BeneficiariesContent
  content_fields:
    - name: accounts
      type: List<Account>
      default: "emptyList()"
    - name: selectedAccountId
      type: String
      default: ""
    - name: recentlyUsed
      type: List<BeneficiaryRow>
      default: "emptyList()"
    - name: all
      type: List<BeneficiaryRow>
      default: "emptyList()"
    - name: query
      type: String
      default: ""
    - name: sortOrder
      type: SortOrder
      default: AlphaAscending
    - name: totalCount
      type: Int
      default: "0"
  row_type:
    name: BeneficiaryRow
    fields:
      - name: id
        type: String
        source: "Data.Beneficiary[].BeneficiaryId"
      - name: name
        type: String
        source: "Data.Beneficiary[].CreditorAccount.Name (counterparty rule: prefer resolved holder name; never render the PSU's own login)"
      - name: bankName
        type: String
        source: "derived from Data.Beneficiary[].CreditorAccount.SchemeName / Identification (e.g. sort code -> bank)"
      - name: accountIdentifier
        type: String
        source: "Data.Beneficiary[].CreditorAccount.Identification (masked IBAN / account number)"
      - name: lastPayment
        type: "LastPayment?"
        source: "derived from get_account_transactions (CreditorAccount.Identification / TransactionInformation join)"
      - name: initials
        type: String
        derived: "first two name initials, else '?'"
    methods:
      - "matches(query): name | accountIdentifier | bankName contains, case-insensitive"
  errors:
    - field: global
      code: LOAD_FAILED
      message: "Unable to load beneficiaries. Please try again."
  events:
    - onSearchQueryChanged
    - onSortChanged
    - toggleSort
    - onAccountSelected
    - onRetry
    - onRefresh
  actions:
    - search
    - sort
    - onAccountSelected
    - navigate
  states_handled: [loading, content, empty, error, no_network, unauthenticated]
  di:
    - PaymentsRepository
    - BanksRepository
    - TransactionsRepository
    - AccountsRepository
    - UserPreferencesRepository

api:
  # Beneficiary list — OBIE OBReadBeneficiary5, filtered/displayed READ-ONLY. Requires
  # ReadBeneficiariesBasic/Detail on an authorised account-access consent.
  - id: get_account_beneficiaries
    endpoint: "GET /obie/open-banking/v4.0/aisp/accounts/{AccountId}/beneficiaries"
    tag: Beneficiaries
    trigger: "on load / onAccountSelected — list beneficiaries for the selected AccountId"
    response:
      fields:
        - name: Data.Beneficiary
          type: "Array<OBReadBeneficiary5>"
        - name: Data.Beneficiary[].BeneficiaryId
          type: String
        - name: Data.Beneficiary[].BeneficiaryType
          type: String
        - name: Data.Beneficiary[].Reference
          type: String
        - name: Data.Beneficiary[].CreditorAccount.SchemeName
          type: String
        - name: Data.Beneficiary[].CreditorAccount.Identification
          type: String
        - name: Data.Beneficiary[].CreditorAccount.Name
          type: String
    errors:
      - code: 401
        message: "UNAUTHORIZED"
      - code: 403
        message: "OB.Resource.InvalidConsentStatus"
  # Accounts list — drives the account selector + per-account scoping. OBIE OBReadAccount6.
  - id: get_accounts
    endpoint: "GET /obie/open-banking/v4.0/aisp/accounts"
    tag: Accounts
    trigger: "on load — authorised account list"
    response:
      fields:
        - name: Data.Account
          type: "Array<OBReadAccount6>"
        - name: Data.Account[].AccountId
          type: String
        - name: Data.Account[].Nickname
          type: String
        - name: Data.Account[].Account[].Identification
          type: String
        - name: Data.Account[].Account[].Name
          type: String
  # Transactions — last-payment amount/date/recency derived by joining
  # CreditorAccount.Identification / TransactionInformation to the beneficiary.
  - id: get_account_transactions
    endpoint: "GET /obie/open-banking/v4.0/aisp/accounts/{AccountId}/transactions"
    tag: Transactions
    trigger: "on loadForAccount — last-payment join"
    response:
      fields:
        - name: Data.Transaction
          type: "Array<OBReadTransaction6>"
        - name: Data.Transaction[].Amount.Amount
          type: String
        - name: Data.Transaction[].BookingDateTime
          type: String
        - name: Data.Transaction[].CreditorAccount.Identification
          type: String
  # UNSUPPORTED — OBIE AIS has NO create-beneficiary endpoint (read-only resource). The Add
  # path was already deferred under OBP and stays removed; there is no HSBC create to bind.
  - id: create_beneficiary
    deferred: true
    unsupported: true
    deferred_reason: "OBIE AIS has no create-beneficiary endpoint — read-only beneficiary resource. No HSBC equivalent to wire."
    endpoint: "N/A — no OBIE create-beneficiary endpoint exists"
    tag: Beneficiaries

dependencies:
  tier: feature
  features:
    - send-money
