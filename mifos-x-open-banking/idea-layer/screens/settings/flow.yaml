feature: settings
flow_ref: settings-and-profile

entry_points:
  - source: app_shell_bottom_nav
    label: "More tab (order 4)"

transitions:
  - from: settings
    to: consent-list
    trigger: "Tap Manage Consents row"
  - from: settings
    to: profile
    trigger: "Tap Profile row"
