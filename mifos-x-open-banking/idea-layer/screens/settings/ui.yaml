# Screens for feature: settings
# Generated by /idea-plan --from-source on 2026-05-20
# status: partial — settings screen functional (theme toggle), notification screen is stub

feature: settings
status: partial
_source: source_analysis
_source_files:
  - feature/settings/src/commonMain/kotlin/org/mifos/feature/settings/SettingsScreen.kt
  - feature/settings/src/commonMain/kotlin/org/mifos/feature/settings/notification/NotificationScreen.kt

screens:
  - name: Settings
    archetype: settings
    purpose: "App settings — theme customization (light/dark toggle), navigation to subscreens"
    composition:
      - KptScaffold
      - Column
      - OutlinedCard        # one card per setting category
      - Icon                # leading icon per card
      - Text                # card label
      - IconButton          # chevron / open dialog

    state_model:
      class: null
      kind: stateless-with-dialog
      states: [Success]
      action_count: 0
      event_count: 0
      notes: "SettingsScreen is stateless. Theme dialog state managed in AppViewModel (app-scope)."

    data_dependencies:
      - service: AppViewModel
        ops: [updateAppTheme]
        scope: app

    requirements_covered: [FR-006]

  - name: Notification
    archetype: settings
    purpose: "Notification preferences — currently a placeholder scaffold"
    composition:
      - KptScaffold
      - Column
      - Text

    state_model:
      class: null
      kind: stateless
      states: [Success]
      action_count: 0
      event_count: 0
      notes: "Stub — needs NotificationViewModel + preferences model"

    data_dependencies: []

    requirements_covered: []

navigation:
  entry: Settings
  edges:
    - from: Settings
      to: Notification
      trigger: tap "Notifications" card

next_steps:
  - "Implement NotificationViewModel + UserNotificationPreferences model"
  - "Add more settings: language, account, privacy, about"
  - "Migrate theme toggle from AppViewModel to SettingsViewModel for separation of concerns"
