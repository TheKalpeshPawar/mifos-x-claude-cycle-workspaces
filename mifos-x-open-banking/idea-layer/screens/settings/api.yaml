sibling: api
schema_version: "4.0"
feature_id: settings

screen_id: settings

# Client-only — no external API (gap_strategy: client_only).
# All settings are persisted locally (DataStore / SharedPreferences).
endpoints: []
