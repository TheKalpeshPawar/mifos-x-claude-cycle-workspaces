# Screens for shared root: splash
# Generated by /idea-plan --from-source on 2026-05-20
# Splash is not tied to a feature module — lives in cmp-shared / RootNav

feature: null
status: enriched
_source: source_analysis
_source_files:
  - cmp-shared/.../RootNavViewModel.kt
  - cmp-android/.../AndroidSplashScreen.kt   # platform-specific impl

screens:
  - name: Splash
    archetype: onboarding
    purpose: "Initial app launch — handles bootstrap (theme load, user check) then navigates to TasksList"
    composition:
      - AndroidSplashScreen   # platform: android (System SplashScreen API)
      - Text                  # fallback: app name

    state_model:
      class: RootNavViewModel
      kind: bootstrap
      states: [Loading]
      action_count: 1
      event_count: 0
      key_actions:
        - bootstrap   # loads theme prefs, determines initial destination

    data_dependencies:
      - service: UserDataRepository
        ops: [getThemeMode, getAppPreferences]

    requirements_covered: []   # implicit bootstrap, no FR

navigation:
  entry: Splash
  edges:
    - from: Splash
      to: TasksList
      trigger: bootstrap complete (Loading → Success)
