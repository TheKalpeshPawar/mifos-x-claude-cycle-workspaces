sibling: flow
schema_version: "4.0"
feature_id: transaction-detail

feature: transactions
flow_ref: consumer-banking

entry_points:
  - source: transactions
    label: "Tap transaction row"
    params: [transactionId, accountId]

transitions:
  - from: transaction-detail
    to: transactions
    trigger: "Back button"
    params: [accountId]
