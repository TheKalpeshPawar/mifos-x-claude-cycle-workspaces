sibling: flow
schema_version: "4.0"
feature_id: product

feature: product
flow_ref: consumer-banking

entry_points:
  - source: account-detail
    label: "Product chip or menu entry in account-detail"

transitions:
  - from: product
    to: account-detail
    trigger: "Back navigation"
