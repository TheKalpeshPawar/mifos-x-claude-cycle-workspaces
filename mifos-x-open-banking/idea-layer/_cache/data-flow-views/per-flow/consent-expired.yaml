# Auto-generated by /idea generate-data-flow — 2026-07-14
# Flow: consent-expired | Screens: accounts (primary), login (cross-flow exit)
schema_version: "1.0"
generated_at: "2026-07-14T00:00:00Z"
flow_id: consent-expired
type: error
description: "Error/edge-case flow — consent expired/revoked detected on accounts AIS call; cross-flows to onboarding-consent"
release_phase: P0
note: "No dedicated screens; surfaces as in-screen banner/error state on accounts, then re-directs to login"
screens:
  - accounts  # surfaces error/consent_expiring state
  - login     # cross-flow re-consent entry point

endpoints_union:
  # Same endpoints as accounts screen; flow triggered by 403 UK.OBIE.Resource.ConsentMismatch response
  - {id: accounts-list, screen: accounts, method: GET, path: "/accounts", dto: OBReadAccount6}

dtos_union:
  - OBReadAccount6

error_coverage_summary:
  accounts: "403 → navigate_reconfirm_consent (consent_expiring state) → login (cross-flow re-consent)"
