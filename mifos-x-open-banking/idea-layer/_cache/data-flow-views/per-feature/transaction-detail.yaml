# Auto-generated by /idea generate-data-flow — 2026-07-14
# Source: idea-layer/screens/transaction-detail/data-flow.yaml
schema_version: "1.0"
generated_at: "2026-07-14T00:00:00Z"
feature: transaction-detail
screen_id: transaction-detail
api_kind: external
flow: transaction-review

params:
  - name: transactionId
    type: string
    required: true
    source: route
  - name: accountId
    type: string
    required: true
    source: route

cache_strategy:
  type: cache-first
  cache_key: "transactions_{accountId}"
  cache_ttl_seconds: 300
  note: "Reuses transactions list from transactions screen; no network call if cache hit"

endpoints:
  - id: transactions
    method: GET
    path: "/accounts/{AccountId}/transactions"
    dto: OBReadTransaction6
    permission: ReadTransactionsDetail
    note: "Called only on cache-miss or explicit retry"

dtos:
  - name: OBReadTransaction6
    role: response
    api_id: transactions

triggers:
  - id: transaction_detail_load
    type: on_mount
    endpoints: [transactions]
    client_filter: {field: TransactionId, value: "{params.transactionId}"}
    states_produced: [loading, content, empty, error]
    error_coverage:
      has_typed_routing: false
      note: "api.yaml has no typed errors; error.recoverable drives retry vs back-nav"
  - id: retry_load
    type: on_user_action
    endpoints: [transactions]
    guard: "{error.recoverable == true}"
  - id: copy_to_clipboard
    type: on_user_action
    client_side: true
    side_effect: "ClipboardManager.setPrimaryClip(transaction.TransactionInformation)"

error_coverage_summary:
  fully_typed: false
  note: "No typed errors in api.yaml; error.recoverable differentiates retry vs navigate_back"
