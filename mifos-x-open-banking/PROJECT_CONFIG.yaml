# PROJECT_CONFIG.yaml — Machine-readable project configuration (schema v2)
# Generated by /project-import → /idea-plan --from-source on 2026-05-20
# Source: https://github.com/openMF/mifos-x-open-banking.git (depth=1 shallow clone)

schema_version: 2

# ─── Project Identity ────────────────────────────────────────────────────────
project:
  name: mifos-x-open-banking
  type: kmp
  status: initialized
  created: 2026-05-20

# ─── Tech Stack (detected from gradle/libs.versions.toml + source scan) ──────
tech:
  language: kotlin                 # Kotlin 2.1.20
  ui_framework: compose-multiplatform   # Compose Multiplatform 1.8.2
  architecture: stream-first       # Migrated from MVI to BaseViewModel + ScreenDataStream + Store5
  di: koin                         # Koin 4.1.0 + koin-annotations 2.1.0 + kotlinInject 0.7.2
  network: ktor-ktorfit            # Ktor 3.2.0 + Ktorfit 2.5.2
  database: room-kmp               # Room 2.7.2 + sqlite-bundled 2.5.2

# ─── Backend ────────────────────────────────────────────────────────────────
# Firebase detected via firebaseBom 33.16.0 (crashlytics + performance plugins active).
# Backend integration is observability-only at this point; no auth/firestore yet.
# Production firebase_project_id pending — set when adopting prod environment.
backend:
  provider: firebase
  environments:
    local:
      auto_generated: true
    production:
      firebase_project_id: TBD     # populate after /server env validate

# ─── Platforms (detected from settings.gradle.kts include() blocks) ──────────
platforms: [android, ios, desktop, web]

# ─── Layers ──────────────────────────────────────────────────────────────────
layers:
  plan:
    enabled: true
  design-spec:
    enabled: true
    design_tool: figma
  server:
    enabled: true                  # firebase wired; can be deepened with /server init
  client:
    enabled: true
  feature:
    enabled: true
  infrastructure:
    enabled: true
  platform:
    enabled: true                  # expect/actual confirmed (cmp-android, cmp-ios, cmp-desktop, cmp-web)
  testing:
    enabled: true
    frameworks: [kotlin-test]

# ─── Source Metadata ─────────────────────────────────────────────────────────
source:
  repo_url: https://github.com/openMF/mifos-x-open-banking.git    # canonical upstream
  fork_url: git@github.com:TheKalpeshPawar/mifos-x-open-banking.git # user's working remote (origin in local clone)
  default_branch: dev              # upstream default
  current_branch: feat/sync-workspace-with-template-post-sync  # user's active local branch (as of 2026-05-22)
  local_clone_at: workspaces/mifos-x/mifos-x-open-banking/source/mifos-x-open-banking
  local_clone_kind: symlink        # symlink → /home/kalpesh/OpenSource/Mifos/mifos-x-open-banking
  symlink_target: /home/kalpesh/OpenSource/Mifos/mifos-x-open-banking
  shallow: false                   # full git history available
  origin: user_external            # user-managed external clone (NOT framework-cloned)
  package_name: org.mifos          # extracted from feature module package
  modules:
    apps: [cmp-android, cmp-desktop, cmp-ios, cmp-web]
    shared: [cmp-shared, cmp-navigation]
    core: [core:analytics, core:common, core:data, core:database, core:datastore, core:designsystem, core:domain, core:model, core:network, core:ui]
    core_base: [core-base:analytics, core-base:common, core-base:database, core-base:designsystem, core-base:network]
    feature: [feature:home, feature:profile, feature:settings, feature:crypto, feature:currency-rates, feature:emi-calculator]
    # NOTE (2026-05-22): crypto, currency-rates, emi-calculator are template residue
    # from kmp-project-template. They will be removed from source.gradle and source tree.
    # Active product scope = [home, profile, settings] only.
    feature_pending_removal: [feature:crypto, feature:currency-rates, feature:emi-calculator]
