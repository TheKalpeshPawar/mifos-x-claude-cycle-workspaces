# PROJECT_CONFIG.yaml — Machine-readable project configuration (schema v2)
# Generated by /project-add (lightweight scaffold) on 2026-05-22
# Source: ~/OpenSource/Mifos/android-client (symlinked; openMF Mifos Mobile staff/field-officer app)

schema_version: 2

# ─── Project Identity ────────────────────────────────────────────────────────
project:
  name: android-client
  type: kmp
  subtype: kmp-app                 # consumer app forked from kmp-project-template
  status: planning                 # lightweight scaffold; idea-layer is a stub
  created: 2026-05-22

# ─── Tech Stack (detected from gradle/libs.versions.toml + source scan) ──────
tech:
  language: kotlin                 # Kotlin 2.2.21
  ui_framework: compose-multiplatform
  architecture: mvi                # template-derived MVI pattern (BaseViewModel + Effect.kt)
  di: koin                         # Koin 4.1.1
  network: ktor-ktorfit            # Ktorfit 2.7.1
  database: room-kmp               # Room 2.8.4 + sqlite-bundled 2.6.2

# ─── Backend ────────────────────────────────────────────────────────────────
# Mifos Fineract REST API (canonical Mifos backend). Configured per project; production
# instance defined in the source-tree config (migrated 2a8e90a5).
backend:
  provider: fineract
  environments:
    local:
      auto_generated: false
    production:
      api_base_url: TBD            # populate from source-tree config

# ─── Platforms (detected from settings.gradle.kts include() blocks) ──────────
# cmp-android is primary; cmp-desktop/ios/web are scaffolded via template.
platforms: [android, desktop, ios, web]

# ─── Layers ──────────────────────────────────────────────────────────────────
layers:
  plan:
    enabled: true
  design-spec:
    enabled: true
    design_tool: figma
  server:
    enabled: true                  # Fineract REST consumer
  client:
    enabled: true
  feature:
    enabled: true
  infrastructure:
    enabled: true
  platform:
    enabled: true
  testing:
    enabled: true
    frameworks: [kotlin-test]

# ─── Source Metadata ─────────────────────────────────────────────────────────
source:
  repo_url: https://github.com/openMF/android-client.git              # canonical upstream
  fork_url: https://github.com/TheKalpeshPawar/android-client.git     # user's working remote (origin)
  default_branch: development
  current_branch: development
  local_clone_at: workspaces/mifos-x/android-client/source/android-client
  local_clone_kind: symlink
  symlink_target: /home/kalpesh/OpenSource/Mifos/android-client
  shallow: false
  origin: user_external            # user-managed external clone
  package_name: com.mifos.mifosxdroid    # androidPackageNamespace from libs.versions.toml
  template_base: openMF/kmp-project-template          # consumer of kmp-project-template
  modules:
    apps: [cmp-android, cmp-desktop, cmp-ios, cmp-web]
    shared: [cmp-shared, cmp-navigation]
    core: [core:common, core:data, core:database, core:datastore, core:designsystem, core:domain, core:model, core:network, core:ui]
    core_base: [core-base:analytics, core-base:common, core-base:database, core-base:designsystem, core-base:network, core-base:platform, core-base:ui]
    feature: [feature:about, feature:activate, feature:auth, feature:center, feature:checker-inbox-task, feature:client, feature:collectionSheet, feature:data-table, feature:document, feature:groups, feature:loan, feature:note, feature:offline, feature:passcode, feature:path-tracking, feature:recurringDeposit, feature:report, feature:savings, feature:search, feature:search-record, feature:settings]
  feature_count: 21

# ─── Versions (key dependencies) ────────────────────────────────────────────
versions:
  kotlin: "2.2.21"
  ktorfit: "2.7.1"
  koin: "4.1.1"
  room: "2.8.4"
  sqlite_bundled: "2.6.2"
  firebase_bom: "34.7.0"

# ─── Architecture pattern (W6) ───────────────────────────────────────────────
kmp_arch: "stream-first@latest"     # default recommended; PATTERN_REGISTRY drives code-gen
