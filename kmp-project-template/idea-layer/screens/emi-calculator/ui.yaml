# Screens for feature: emi-calculator
# Generated by /idea-plan --from-source on 2026-05-22
# status: draft — extracted from source

feature: emi-calculator
status: draft
_source: source_analysis
_source_files:
  - feature/emi-calculator/src/commonMain/kotlin/org/mifos/feature/emicalculator/ui/EmiCalculatorScreen.kt
  - feature/emi-calculator/src/commonMain/kotlin/org/mifos/feature/emicalculator/ui/EmiCalculatorViewModel.kt
  - feature/emi-calculator/src/commonMain/kotlin/org/mifos/feature/emicalculator/calculateEmi.kt

screens:
  - name: EmiCalculator
    archetype: form
    purpose: "Pure-Kotlin EMI calculator — principal + annual rate % + tenure months → monthly EMI / total payment / total interest."
    composition:
      - KptScaffold              # top bar
      - OutlinedTextField        # principal (number)
      - OutlinedTextField        # annual rate % (number, decimals)
      - OutlinedTextField        # tenure months (integer)
      - Card                     # results display (monthly EMI / total / interest)
      - Text                     # formatted currency

    state_model:
      class: EmiCalculatorViewModel
      kind: MVI                   # extends BaseViewModel<EmiState, Nothing, EmiAction>
      states: [content]            # form auto-computes; no loading state
      action_count: 3
      event_count: 0
      key_actions:
        - updatePrincipal
        - updateRate
        - updateTenure

    data_dependencies: []         # pure local computation

    requirements_covered: [FR-005]

navigation:
  entry: EmiCalculator
  edges: []                       # leaf screen

deeplinks: []
