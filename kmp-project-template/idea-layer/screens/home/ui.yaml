# Screens for feature: home
# Generated by /idea-plan --from-source on 2026-05-22
# status: draft — extracted from source

feature: home
status: draft
_source: source_analysis
_source_files:
  - feature/home/src/commonMain/kotlin/org/mifos/feature/home/HomeScreen.kt

screens:
  - name: Home
    archetype: dashboard
    purpose: "Hub landing screen — 4 feature cards for navigation to demos (Crypto, Currency Rates, EMI Calculator, …)."
    composition:
      - KptScaffold         # top bar + settings icon
      - LazyVerticalGrid    # 2-column grid of feature cards
      - Card                # per-feature card (icon + label + description)
      - Icon                # Material symbol per card

    state_model:
      class: null            # stateless — no ViewModel
      kind: stateless
      states: [content]
      action_count: 0
      event_count: 0

    data_dependencies: []    # navigation-only; no repository

    requirements_covered: [FR-001]

navigation:
  entry: Home
  edges:
    - from: Home
      to: CryptoWatchlist
      trigger: tap Crypto card
    - from: Home
      to: CurrencyRates
      trigger: tap Currency Rates card
    - from: Home
      to: EmiCalculator
      trigger: tap EMI Calculator card
    - from: Home
      to: Settings
      trigger: tap top-bar settings icon

deeplinks: []
